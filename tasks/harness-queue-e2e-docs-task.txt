BUILD + TEST (code + docs; LongHorizon-Harness; slices 5-6 left over from run ad2ec433 / PR #4): work ONLY in /home/harness/work/LongHorizon-Harness on branch feat/queue-e2e-and-docs (run `git fetch origin && git checkout -B feat/queue-e2e-and-docs origin/feat/queue-api-and-ux-speed` - base on that branch, or origin/main if PR #4 is already merged; say which). Use the workspace .venv-dev (never pip into /home/harness/venv). Slice 5: e2e/happy_path.sh + e2e/happy_path.py + Makefile target e2e-happy + pytest marker e2e: start a LOCAL lh-harness instance on a free port with a temp runs_root and a fake agent (no real model calls, never CT110), POST /api/queue with a trivial task, wait for queue.launched, resolve the completion gate via /approvals/{id}/resolve, assert the run reaches completed; cover the inter-tick double-launch race (two entries for one workspace -> one launched, one skipped with reason) and the base_check wiring. Slice 6: docs/ux-performance.md with p50/p95 before (old full-rebuild path, measured by checking out the parent of the slice-4 commit into a temp dir or by a flag) and after (scripts/bench_snapshot.py), CHANGELOG entry, docs/release-checklist-ct110.md (abort running runs -> deploy-harness.sh -> resume; the exact config.toml block for CT110 incl. the queue directory under runs_root; the launcher on the overseer PC is retired after the first service-side launch is observed). Sweep stray __pycache__ dirs from the tree. Run the FULL test suite at the end and report the count. HARD RULES: explicit-path commits, no push, hermetic tests, no secrets. Auditor: verify happy_path never targets CT110 (grep for 192.168.21.168 / harness.lan) and that the suite count is reported.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
