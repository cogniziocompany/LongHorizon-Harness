BUILD + TEST (config + docs; PR B of the "E2E model registry" plan; UAT-first through the lane: deploy-uat-litellm on CT204 then a FULL e2e run-all green before prod): purge the retired local models from the LiteLLM gateway config and write the team/key repoint runbook. Planning session 3e71c218; full plan in tasks/e2e-model-registry-2026-09-08.md of LongHorizon-Harness - READ IT FIRST via `git show origin/main:tasks/e2e-model-registry-2026-09-08.md` in /home/harness/work/LongHorizon-Harness after `git fetch origin` (never write there). Decisions already made by Paxton: retire every qwen3.5:* local tag and qwen3.6 (+qwen3.6:*); keep gpt-oss:20b, functiongemma, deepseek-ocr, glm-ocr, embeds, reranker; qwen3.5:397b-cloud is a cloud relay - KEEP. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/litellm-retire-qwen35 (run `git fetch origin && git checkout -B feat/litellm-retire-qwen35 origin/main` first - base on origin/feat/e2e-model-registry instead if that PR is not merged yet, and say so); plain git; commit by EXPLICIT PATH; no push; NO live calls (the overseer runs the team/key repoint and the /model/delete cleanup from the runbook); do not edit .github/workflows/; do not change the pinned image digest; docker-compose.yml untouched.
READ FIRST: infrastructure/litellm-config.yaml (model_list entries qwen3.6 ~:900, qwen3.5:27b ~:1002, qwen3.5:9b ~:1028/1037, qwen3.5:9b--64k/--128k/--max-effort ~:1048-1105, qwen3.5:4b--64k ~:1115/1124; fallback chains qwen3.5:9b ~:250/:279 and qwen3.6 ~:262/:273; tier comment ~:875; ptait07/corsair header ~:994; the line ~2245 "local lane policy is qwen3.8 and newer only"), design docs/teams-permissions-oss-implementation.md:122-127 (team/model table), infrastructure/README.md:260-278 (key examples), the azure-inference adapter key used by e2e suite 18, e2e/suites/13 header policies.
SLICES: (1) config purge: delete the retired model_list entries and fallback chains, fix the comments, keep qwen3.5:397b-cloud; YAML must parse; a unit check (node or python) asserting no model_name matches the retired globs and no fallback chain references one; verify `git diff --stat` touches only litellm-config.yaml (+ the check script). Commit: chore(litellm): retire qwen3.5 local tags and qwen3.6. (2) runbook docs/litellm-retire-qwen35.md: (a) pre-flight read of the live LiteLLM DB: teams and keys whose models list names a retired tag (SQL or /team/list + /key/list with the master key env name), (b) repoint order: team first, then key (/team/update, /key/update) to qwen3.8, (c) after deploy, `store_model_in_db: true` means DB-added rows linger: GET /model/info for retired names and /model/delete them, (d) verification = the new e2e drift test "no retired model in /v1/models" plus suite 13 Phase 0, (e) rollback = revert the PR (the lane redeploys) and re-add teams' models. Update design docs/teams-permissions-oss-implementation.md and infrastructure/README.md examples to qwen3.8. Commit: docs(litellm): qwen3.5/3.6 retirement runbook + team repoint. (3) CHANGELOG + the litellm-config header comment listing the local floor. Commit: docs(litellm): local floor qwen3.8.
HARD RULES: UAT first (the lane applies CT204, then the FULL run-all must be green before prod - the overseer enforces); nothing deleted that is not in the retired list; no image/compose/workflow changes; no secrets; explicit-path commits; no push. Completion = commits, diff stat, the check-script output, and the exact operator commands (pct exec 204 then 202; env names only). Auditor: no git fetch; verify only the retired entries were removed, qwen3.5:397b-cloud kept, YAML parses, and the runbook orders team before key.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

## PLANNED FUTURE PHASE (Paxton, 2026-09-09) - MONTHLY AUTOMATED MODEL HYGIENE JOB
DO NOT BUILD THIS IN THIS RUN. This section exists so the retirement you are doing by hand now is designed to be run by a script later; where a choice makes the manual step easier to automate at no extra cost, take it, and say in your report which choices you made for that reason. Record this section in the plan document so the follow-up task can be written from it.

INTENT. What is being done by hand today - work out which models are dead, repoint their callers, remove them from the config, the e2e suites and the old process code - becomes a scheduled job that runs MONTHLY across all known related repos and infra, so the estate never again carries a year of dead model names discovered only when an acceptance gate fails for every caller.

DEFAULT ORCHESTRATING MODEL. The job runs unattended, so its model must be route-verified, off the contended local GPU, and cheap:
 - PRIMARY: `qwen3.8-27b:synthetic-anthropic` - verified 2026-09-09 on `/v1/messages` streaming and non-streaming, 0.7s to first token, and it emits NO thinking block (it carries `reasoning_effort: none`), which is what keeps Claude Code's streaming path clean.
 - FALLBACK: `kimi-k3:synthetic-anthropic`, then `glm-5.3-flash:cloud`.
 - EXPLICITLY NOT the local span (`qwen3.8` / `qwen3.8-nothink` on ptait01 `:11442`): it runs `OLLAMA_NUM_PARALLEL=2` and a single eight-token request measured 240 SECONDS while harness runs were active. A monthly job must never contend with the harness for executor slots.
 - The job must probe its model with a 1-3 second emit check before starting real work, using the HARNESS's own virtual key rather than the master key - a master-key probe passes models the harness cannot reach, which on 2026-09-09 let two runs die instantly on `403 key not allowed to access model`.

SCOPE OF "ALL KNOWN RELATED REPOS AND INFRA" - enumerate from a committed manifest, never from a hardcoded list in the script:
 repos cognizioware-mcp-tools, cognizioware-powerplatform, mcp-cognizioware, cognizioware-qa, cognizioware-hydra-rsi, LongHorizon-Harness; infra the prod and UAT LiteLLM routers (CT202/CT204), the n8n workflows (committed JSON and the live cloud instance), the e2e suites, the eval configs, the Ops Control Center doctor targets, and the virtual key allow-lists.

WHAT THE JOB DOES EACH MONTH:
 1. Collect usage per model_name for the trailing 30 days from the router's own data (`/spend/logs`, `/global/activity/model`) plus Langfuse, and record the retention window it actually got - a gap is "unknown", never "unused".
 2. Cross-reference every model_name against n8n workflows, repo literals, e2e suites, virtual key allow-lists, `router_settings.fallbacks` and `context_window_fallbacks`. Membership of a chain or an allow-list COUNTS AS USAGE - a fallback target has no direct traffic by design, and deleting one turns a recoverable failure into a dead run.
 3. Skip any model registered inside the carve-out window (new models have zero usage because they are new).
 4. Never collapse the deliberate two-route pairs: each Synthetic upstream is registered twice on purpose, `openai/`-prefixed for `/v1/chat/completions` and `custom_openai/`-prefixed `:synthetic-anthropic` for `/v1/messages`.
 5. OPEN A PULL REQUEST, DO NOT ACT. The job's output is a branch plus a PR containing the audit table, the proposed repoints and the proposed removals, and an EXCEPTIONS AND QUESTIONS section for Paxton. It must never delete a model, edit a live router, or touch an n8n workflow on its own - the deletions go through `POST /model/delete` run by a human from the generated runbook, because the database is authoritative over the YAML and a 200 from that API is not proof the write landed.
 6. Post the summary where an operator will see it (the Ops Control Center row and the fleet window), naming the specific models rather than showing a bare colour.

GUARDRAILS THE JOB MUST CARRY: read-only against every live system; a hard cap on how many models one run may propose retiring, so a bad usage query cannot propose emptying the catalogue; a dry-run mode that is the default; and an assertion that every model named in a fallback chain still exists after the proposed change is applied.
