VERIFY-AND-PIN (docs + manifest + package pins only). n8n → code (LangGraph JS) orchestration, Phase A. Authoritative spec, read FIRST: design/orchestrator/code-first-orchestrator-handoff.md (v2) §12 Phase A; design/orchestrator/orchestration-migration-audit-2026-09-05.md (G2, G3, G7, G10, R7); design/handoffs/n8n-code-orchestration-20260905/trms-delivery-contract.json (#inspectedSources). Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform-phasea (branch feat/orchestrator-phase-a-verify-pin from origin/develop @ b252273); never cd into or run git against any other checkout; plain git commands only; commit by EXPLICIT PATH (never git add -A; CRLF phantoms); no push. Read-only against n8n, Dataverse, Langfuse and the gateway; no workflow edits, activations or imports; the ONLY write-side action allowed is calling the two pre-minted temporary LiteLLM keys (below). No runtime import of new packages. `node` may need `source ~/.nvm/nvm.sh`.

Access facts (verified by the overseer 2026-09-05 23:45 PT): SSH from this worktree shell is NOT available (deployments/ssh/config has no CT103 alias and pve151 fails host-key verification here) — do NOT add host keys or SSH config. Use the n8n REST API instead: base URL, API key, LiteLLM base URL/key and the two temporary residency keys are in the untracked file `.phasea.env` at the worktree root (KEY=VALUE lines; `set -a; source .phasea.env; set +a`). NEVER commit, print, or copy `.phasea.env` or any value from it into a document; refer to keys by alias (`phasea-residency-default`, `phasea-residency-nofallback`, `lh-harness`, `cognizioware-qa`). n8n API: `GET $N8N_BASE_URL/api/v1/workflows?limit=250` (56 workflows, 40 active as of tonight), `GET /api/v1/workflows/{id}` (full JSON export), `GET /api/v1/executions?status=waiting&workflowId=…&limit=…`, `GET /api/v1/credentials` returns names/types only if permitted (record names, never data); n8n version: try `GET $N8N_BASE_URL/rest/settings` (versionCli) or `GET /healthz`; image digest is UNVERIFIED without SSH — write "UNVERIFIED (no SSH from the run; overseer to fill)" rather than guessing. The residency keys are restricted to the local model `qwen3.8`; `phasea-residency-nofallback` was generated with `disable_fallbacks: true`; the router has global `fallbacks:` (litellm-config.yaml ~line 188) and `default_fallbacks`. Router model ids that exist tonight: general-chat, glm-5.3-flash:cloud, glm-5.3:cloud, glm-5.3:pool, deepseek-v4-pro:cloud, deepseek-v3.2:cloud, kimi-k2.6:cloud, kimi-k2.7-code:cloud, kimi-k2.7-code:pool (there is NO "deepseek pro4 flash" model — record that).

Paxton's decisions D1–D15 (2026-09-05, normalized by the overseer; put them verbatim-in-spirit into baseline-manifest.json `decisions` and cite them wherever a disposition is applied):
D1 DRD precedence: treat `Task_2_2_DRD_v1_3_Updates.md` as the normative DELTA/overlay from v1.2 → v1.3 (v1.2 body + the Updates file = governing v1.3).
D2 Walkthrough reference: always the latest human document — `TRMS_Solution_Walkthrough_v1_2.docx` is the frozen reference; state the rule "latest human doc wins" in the manifest.
D3 ACS email: the ACS outbound-email change does NOT supersede the FRD/DRD requirements; model it as a BROWNFIELD MODIFICATION for the eval; the eval must use a test relay / fake-configured endpoint instead of a real ACS endpoint (record the fake-relay design as a Phase B item; if you have a better isolation idea, write it as an alternative, not a decision).
D4–D8 (US-06 legacy activity import, US-22 EnvisionConnect bulk import, hearing T-3 reminder, US-09 campaign prerequisite + missing QA fixtures, inaccurate historical completion claims e.g. Case auto-creation): ALL "reference gap" — never reduce the normative denominator. NEW REQUIREMENT from Paxton attached to these: a FORESIGHT GAP ANALYSIS / proactive technical-debt reduction becomes part of the eval — after the user creates their first process/customization, the system looks ahead and determines what will be missing (the TRMS auto case creation was missed because it was assumed to use the out-of-the-box case and was never finished); at the end of the whole process, what foresight was not needed or may be useful is handed back into the docs, specs, demo and walkthrough docs. Phase A records this as a named eval component ("foresight-gap-analysis") in the manifest and the audit's gap table with a Phase B/G design pointer; do not build it here.
D9 Missing screenshot slots 3.3-F, 5.4-A, 5.4-B: capture new references during Phase G1 (Paxton: "yes you do this").
D10 Acceptance rule: define the PRIORITY items on the "user submits request → revenue" nightly path; acceptance = all priority items pass at the industry-standard threshold — record the proposed default 99% for priority (critical-path) checks and 95% for non-critical, both marked "proposed default, confirm", with a one-paragraph rationale citing common SLO practice (no fabricated citations; mark UNVERIFIED where you cannot cite).
D11 Delivery format: written docs + chaptered recordings (default accepted).
D12 Auto-approval after first manual acceptance: accept-and-continue unless a change is pending (default accepted); Phase A still enables no auto-approval.
D13 Pilot model binding: switch to the `general-chat` alias; the alias must resolve to glm-5.3-flash:cloud primary, deepseek-v4-pro:cloud backup, then kimi-k2.6:cloud / kimi-k2.7-code:cloud — record what `general-chat` resolves to TODAY on the router (from /v1/model/info or /model_group/info if readable with the qa key; else UNVERIFIED) and write the required alias change as a Phase B config item for cognizioware-mcp-tools (NOT changed by this run).
D14 Residency: cloud allowed for the pilot tenant; the G7 fallback test result still decides the key config for any local-only tenant.
D15 Temporary keys: allowed; the overseer already minted the two keys above and deletes them after the run — you MUST NOT mint, modify or delete keys.

Slices, one per round:
(1) Live n8n inventory → design/orchestrator/baseline-2026-09-06.md: n8n version (or UNVERIFIED), workflow list (id, name, active, updatedAt) grouped into this repo's families (design-build-sprint, bmad-design, sprint-build, eval-micro-run, support/*) versus the ~50 out-of-scope workflows recorded only as a count + owner list; export the in-scope workflow JSON to design/orchestrator/n8n-exports/<id>.json with SHA-256 per file in the doc; waiting executions count per in-scope workflow; credential NAMES/types only. Commit: docs(orchestrator): phase A n8n baseline inventory.
(2) Gateway checks appended to the baseline doc: /v1/models snapshot (ids only), health of kimi-k2.7-code:cloud and general-chat (one minimal chat completion each with the qa key, record latency/status), LiteLLM version (from /health/readiness or headers, else UNVERIFIED), per-key access-group visibility in the pattern of e2e/suites/17-mcp-server-filtering for dataverse-read, dataverse-write, xrm-metadata, xrm-appmodule, xrm-sitemap, qa-playwright, rag-lookup (tools/list via the MCP gateway with x-mcp-servers / access-group headers; record which groups exist vs are missing on the router), and the RESIDENCY TEST: with phasea-residency-default request model qwen3.8 and then a cloud model id — record whether the router served/fell back to a cloud deployment (inspect the response `model`/headers `x-litellm-model-id`); repeat with phasea-residency-nofallback; conclude whether global fallbacks can leak a local-only key to cloud and which key config prevents it. Commit: docs(orchestrator): phase A gateway ACL + residency results.
(3) TRMS baseline manifest → design/orchestrator/baseline-manifest.json from trms-delivery-contract.json#inspectedSources: re-hash every listed file (paths under /mnt/c/... as given; if a file is missing, record `missing: true` — do not fail the whole manifest, but list drifted/missing hashes prominently in the baseline doc), plus `decisions` D1–D15 as above, `dispositions`, `missingSlots`, `interviewChoices`, `evalComponents` (incl. foresight-gap-analysis), `pilotModelBinding`. Commit: docs(orchestrator): phase A TRMS baseline manifest.
(4) Package pins: add exact versions to packages/backend/package.json (or a new packages/orchestrator/package.json stub if the handoff §12 says so): @langchain/langgraph 1.4.14, @langchain/langgraph-checkpoint-postgres 1.0.5, @langchain/core 1.2.9, @langchain/openai (current 1.x exact), @langchain/mcp-adapters 1.1.4, @langfuse/langchain 5.11.0; bump @langfuse/otel and @langfuse/tracing to the same 5.11.x; `npm install` must resolve with zod ^3.24 on Node 22 and Express 5 (quote the resolution output; if the registry is unreachable, report NOT RUN); `npm run build` in packages/backend stays green; no runtime import. Commit: chore(orchestrator): phase A package pins.
(5) Exit: `node tests/ci/workflow-guards.mjs` passes; `git diff --stat origin/develop..HEAD` touches only design/**, packages/**/package.json, package-lock.json; write the Phase B gate summary at the end of the baseline doc (lockfile resolved? fallback result? D10–D12 recorded? general-chat alias change needed?).

HARD RULES: no secrets or key values in any file or output; never commit .phasea.env or n8n credential data; no .github/** changes; no runtime code; no key minting/deletion; explicit-path commits; no push. Completion = commit hashes, diff stat, the residency conclusion in one paragraph, the list of UNVERIFIED items, and the Phase B gate summary. Auditor: no git fetch; grep the diff for `sk-`, `eyJ` (JWT), `perm:` and `X-N8N-API-KEY` values (must be absent); confirm .phasea.env is untracked and not in any commit; confirm decisions D1–D15 appear in the manifest.
