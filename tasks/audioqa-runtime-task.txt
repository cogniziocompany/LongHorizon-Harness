FIX + TEST (repo only; the lane deploys): the audioqa-mcp container crash-loops on CT202 with `python: can't open file '/app/server.py'` after the image finally builds (Dockerfile now pins torch/torchaudio CPU wheels and treats UTMOS prefetch as best-effort). Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch fix/audioqa-runtime (run `git fetch origin && git checkout -B fix/audioqa-runtime origin/main` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no docker daemon here (Dockerfile changes must be reasoned from the file). Files: infrastructure/docker/audioqa-mcp/{Dockerfile,server.py or src/,requirements.txt,prefetch.py}, infrastructure/docker-compose.yml (audioqa-mcp service: command/working_dir/volumes; CRLF file - change only intended lines, check `git diff --stat`), e2e/suites/3*-audioqa*.test.js if present.
DELIVER: (1) the runtime stage of the Dockerfile COPYs the server module to the path the CMD/compose command expects (or fix the command) so `python /app/server.py` (or the module form) starts; add a HEALTHCHECK hitting the MCP endpoint on :3111; (2) an e2e suite `39-audioqa-mcp-health.test.js` (soft-skip off-LAN) asserting tools/list on http://192.168.21.161:3111/mcp returns the 4 audioqa tools with AUDIOQA_MCP_TOKEN (env name only); (3) a note in the audioqa README on how the image is laid out. Commit each. Auditor: no git fetch; verify the CMD path matches a COPY destination and the compose command; no secrets. Completion = commit hashes, diff stat, the exact runtime path fixed.
