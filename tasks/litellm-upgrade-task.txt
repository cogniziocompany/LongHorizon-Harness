BUILD + TEST (repo only; the overseer performs the schema migration, snapshots and rollout): upgrade the CT202/CT204 LiteLLM gateway from 1.93.0 to v1.100.0 and adopt the v1.100.0 MCP-gateway features. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/litellm-1100 (run `git fetch origin && git checkout -B feat/litellm-1100 origin/main` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no docker here; never call CT202/CT204 live. infrastructure/docker-compose.yml is CRLF - change only the intended lines and verify with `git diff --stat`. Do NOT lower e2e/baseline.json aggregateMinTools (45) or any e2e threshold.

READ FIRST: .github/workflows/deploy-mcp-tools.yml (jobs e2e-tests, deploy-uat-litellm ~lines 293-305, deploy ~536-542 where the digest is derived), infrastructure/docker-compose.yml lines 20-45 (litellm-router: digest pin + comment history), infrastructure/litellm-config.yaml (general_settings, litellm_settings.callbacks -> hooks.thinking_normalizer.thinking_normalizer_instance, mcp_servers, mcp_aliases), infrastructure/deploy-mcp-tools.ps1 (~line 299), infrastructure/README.md (~line 71), infrastructure/scripts/safe-recreate.sh, e2e/suites/01-litellm-health.test.js, 22-aggregate-baseline.test.js, 34-litellm-thinking-roundtrip.test.js, infrastructure/docker/litellm/hooks/thinking_normalizer.py (+ tests), design docs/ptait01-maintenance-restart-runbook.md, .claude/skills/mcp-gateway-ops (runbook).

CONTEXT (plan by Paxton's session f5a28320, 2026-09-06): the pin exists because the mutable :main-latest tag caused two schema-drift outages (one bricked the admin API and made scoped-key enforcement FAIL OPEN); a live-only pin was reverted by a redeploy, so the pin lives in the repo. CT202 runs with DISABLE_SCHEMA_UPDATE=True (live-only env, not in compose) so LiteLLM never self-migrates; the overseer runs the Prisma migrate-diff and applies only additive DDL. Target image (resolved 2026-09-06): ghcr.io/berriai/litellm@sha256:c8756e7b9a61fe45df2ccb5b781d388c3b2f3a21ef9e4956630caef20f9f03aa (tag v1.100.0, multi-arch index; if a v1.100.0-stable tag exists when you read this, say so but keep this digest - the overseer re-resolves). Out of scope: access-group budgets, custom auto-router tiers.

SLICES (one per round; each = files + tests + explicit-path commit):
(1) Phase 0 - make the UAT lane able to test an image bump. In deploy-uat-litellm: add a "Sync compose + pull pinned image" step BEFORE the restart that derives the digest exactly like the prod job (`LITELLM_IMAGE=$(grep -oE 'ghcr\.io/berriai/litellm@sha256:[0-9a-f]{64}' infrastructure/docker-compose.yml | head -1)`, fail if empty), scp + pct push infrastructure/docker-compose.yml to $DEST_DIR/docker-compose.yml on CT204 and `pct exec 204 -- docker pull "$LITELLM_IMAGE"`; replace the restart (which wrongly passes the container_name to `docker compose restart`) with `docker compose --env-file mcp-tools.env up -d --no-deps --force-recreate litellm-router`; add a post-recreate assertion: poll http://192.168.21.162:4000/health/liveliness until 200 (max 3 min), then GET /health with the master key (env name LITELLM_UAT_MASTER_KEY if the workflow has it, else read it on the box from mcp-tools.env inside the pct exec) and fail on 5xx or on "prisma"/"schema" errors in `docker logs --since 5m`. Fix the stale :main-latest references in infrastructure/deploy-mcp-tools.ps1 (~299: derive the digest from docker-compose.yml) and infrastructure/README.md (~71). Commit: ci(uat): sync compose + pull the pinned image, recreate with env-file, assert health.
(2) Compose: bump the digest to the v1.100.0 sha above and APPEND to the existing comment block (keep the 2026-07-10 history): "upgraded 1.93.0 -> v1.100.0 on 2026-09-07; schema migrated additively by the overseer (prisma migrate diff -> reviewed SQL); verified on CT204 first". Mirror `DISABLE_SCHEMA_UPDATE: "${DISABLE_SCHEMA_UPDATE:-True}"` under litellm-router.environment (today it exists only on the live box - the same live-only drift pattern that reverted the 2026-06-28 pin). Commit: build(litellm): pin v1.100.0; mirror DISABLE_SCHEMA_UPDATE into compose.
(3) Config for the v1.100.0 MCP-gateway features in infrastructure/litellm-config.yaml: (a) RFC 7662 /introspect + RS256-signed MCP session tokens per the v1.100.0 release notes - the private key ONLY as an env reference (os.environ/LITELLM_MCP_SESSION_PRIVATE_KEY; the public key os.environ/LITELLM_MCP_SESSION_PUBLIC_KEY), documented as a runbook entry name in "design docs/mcp-placeholder-services-audit-and-plan.md" (names only); (b) toolset enforcement on teams/orgs/users expressed ONLY via mcp access groups (never server IDs - IDs regenerate on config change and ID-scoped keys go deny-all); document "update the team before the key". Do not remove or rename existing mcp_servers, aliases or access_groups. Verify the hook registration survives: read the v1.100.0 CustomLogger / async_pre_call_hook signature from the LiteLLM changelog or source if reachable (git clone --depth 1 --branch v1.100.0 https://github.com/BerriAI/litellm into /tmp if network allows; else mark UNVERIFIED) and adapt infrastructure/docker/litellm/hooks/thinking_normalizer.py only if the signature changed (keep unit tests green). Commit: feat(litellm): v1.100.0 MCP session introspection + RS256 tokens; access-group toolsets; hook compatibility note.
(4) e2e: add a soft version assertion to e2e/suites/01-litellm-health.test.js (read the running version from /health/readiness or the equivalent and compare with the version recorded next to the digest in docker-compose.yml comment via a small parser; soft-skip off-LAN); add a `24-litellm-scoped-key-enforcement` style check if one does not exist: with a key scoped to one access group, tools/list must NOT include a server outside the group and a tools/call to it must be denied (needs a QA-scoped key env name LITELLM_QA_SCOPED_KEY; soft-skip when unset). Keep aggregateMinTools at 45. Commit: test(e2e): litellm version assertion + scoped-key enforcement check.
(5) Docs: the mcp-gateway-ops skill/runbook gets an "Upgrade procedure" section (snapshot CT204/CT202 with `pct snapshot`, pg_dump, extract schema.prisma from the image, `prisma migrate diff --script`, classify DDL, apply additive DDL in a transaction, keep DISABLE_SCHEMA_UPDATE, rollback by reverting the digest + restoring the dump), and infrastructure/README.md pin section updated. Commit: docs(litellm): upgrade runbook.
Verification you can do here: YAML parses; unit tests for the hook pass; e2e suites soft-skip off-LAN; `grep -c main-latest` across infrastructure/ and .github/ is 0. HARD RULES: no secrets; no live calls; no thresholds lowered; explicit-path commits; no push. Completion = commit hashes, `git diff --stat origin/main..HEAD`, the list of env NAMES the operator must set (LITELLM_MCP_SESSION_PRIVATE_KEY, LITELLM_MCP_SESSION_PUBLIC_KEY, DISABLE_SCHEMA_UPDATE), and the exact verification checklist for CT204 then CT202 (admin API /key/list + /team/list 200; scoped key denied outside its group; /introspect active true/false; aggregate tools >= 45; no prisma/schema errors; uptime climbing). Auditor: no git fetch; confirm the digest is the one above, aggregateMinTools unchanged, no :main-latest left, compose CRLF preserved, no key material in files.
