FIX + TEST (repo only; the lane deploys): two defects in the cognizioware-mcp-tools deploy lane found on 2026-09-07. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch fix/lane-config-reload-and-health-suite (run `git fetch origin && git checkout -B fix/lane-config-reload-and-health-suite origin/main` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls; no thresholds lowered.
(1) .github/workflows/deploy-mcp-tools.yml, job "Deploy to CT202": the "Sync infrastructure files" step copies litellm-config.yaml to CT202 but nothing restarts the router when ONLY the config changed (compose sees no container change). Lane 34162318474 deployed the qwen3.8-nothink route to disk while the running router kept the old model list; the overseer had to restart by hand. Fix: compute sha256 of litellm-config.yaml before and after the sync (on the box) and, if it changed, run `docker compose --env-file mcp-tools.env up -d --no-deps --force-recreate litellm-router` (never bare `docker compose up`, never `down -v`), then poll /health/readiness until `"db":"connected"` (max 3 min) and assert the new model appears in GET /v1/models when the diff added a model_name (parse the yaml diff for `model_name:` additions). Do the same in the UAT job if it has the same gap. Commit: ci(deploy): recreate litellm-router when litellm-config.yaml changes.
(2) e2e/suites/01-litellm-health.test.js: the beforeAll calls bare GET /health, which runs a completion against every deployment (paid Perplexity, Ollama Cloud quota, GPU load) and exceeds the 15 s hook timeout -> the Post-deploy Smoke Test fails every lane (lane 34162318474: 4 failed). Replace with /health/liveliness (200) + /health/readiness (status healthy, db connected); read the running version from the readiness body if present, else from `GET /` or the `x-litellm-version` header, else soft-skip the version assertion with a clear message; keep the compose-comment version parser. Never call bare /health anywhere in e2e; add a lint-style test that greps the suites for `:4000/health"` or `/health'` without /liveliness|/readiness|?model= and fails if found. Commit: test(e2e): health suite uses liveliness/readiness; no bare /health sweeps.
HARD RULES: no secrets; explicit-path commits; keep aggregateMinTools 45. Completion = commit hashes, diff stat, and the exact assertions the smoke step now makes. Auditor: no git fetch; confirm no bare /health remains under e2e/ and the recreate uses --env-file mcp-tools.env.
