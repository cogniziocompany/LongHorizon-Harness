BUILD + TEST (code; mcp-cognizioware): show an operator, in the billing admin UI, WHICH users have no matching billing details - instead of finding out when someone is blocked in the product. Paxton's direction, 2026-09-08. Work ONLY in /home/harness/work/mcp-cognizioware on branch feat/admin-billing-detail-flag (run `git fetch origin && git checkout -B feat/admin-billing-detail-flag origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NO live Stripe calls in tests (stub/fake only); env NAMES only.

WHY, FROM TODAY: powerplatform's session setup calls `GET /api/user-org?email=...` on this service and refuses the user when it does not resolve. On 2026-09-08 an operator spent an hour on a user who was blocked at that check, believing the account was missing from billing. It was not - the caller was sending the wrong partner key and every lookup 401'd. Separately, three real test accounts genuinely had no `user_organizations` row and had to be created one at a time through `POST /api/v1/customers/ensure`. In both cases there was NOWHERE an operator could look to answer the simple question: which users do we have billing details for, and which are we going to block?

DO NOT BUILD A PARALLEL SURFACE - EXTEND WHAT EXISTS. Paxton's instruction, 2026-09-08: if the existing thing fits, do not duplicate the same solution. Verified before this task was written:
 - `src/McpCognizioware.BillingService/Controllers/OnboardingAdminController.cs` already serves `api/v1/admin/onboarding` with a LIST endpoint returning `StripeCustomerId`, `StripeSubscriptionId`, `CreatedAtUtc`, `UpdatedAtUtc`, a per-record detail endpoint carrying Stripe customer/subscription plus each provisioning step's status, and a retry action.
 - The admin UI is a single page: `src/McpCognizioware.BillingService/wwwroot/admin/index.html`.
So the flag belongs ON THAT EXISTING LIST AND PAGE. Add a new endpoint or a new page ONLY if the existing one genuinely cannot carry it, and if you conclude that, SAY WHY in the completion report before doing it.

THE SUBTLETY THAT MATTERS - READ THIS BEFORE DESIGNING. The existing admin list is built around ONBOARDING records. The thing that actually blocks a user in the product is a different lookup: powerplatform calls `GET /api/user-org?email=...`, which resolves against `user_organizations`. A user can plausibly have one without the other. On 2026-09-08 three accounts were refused by the product because they had no `user_organizations` row, while a fourth was refused only because the caller's partner key was wrong and the row existed all along.
So the flag must reflect THE STATE THE PRODUCT GATE ACTUALLY USES, not merely onboarding progress. Establish how the two relate in this schema before you build - do not assume they are the same set - and state plainly in the report what you found. If a user can exist in one and not the other, the view must make that visible rather than quietly showing only onboarding rows.

WHAT TO SHOW - four states, distinguishable at a glance in the list, each saying WHICH part is missing rather than a bare warning triangle:
 - no `user_organizations` row at all (the product WILL refuse this user)
 - a row exists but carries no Stripe customer id
 - a Stripe customer id exists but there is no active subscription
 - complete
Show the email, what is missing, and when the row was created. Match the existing page's style, layout and fetch patterns exactly; introduce no framework, no new design language, no restyling.

BACKEND: prefer extending the existing admin controller's response over adding a new endpoint. Whatever you do must be READ-ONLY - this task adds no way to create or modify a customer; `POST /api/v1/customers/ensure` already exists for that and is out of scope. Page or bound the result so a large tenant cannot render an unbounded list. Never return a Stripe secret, a partner key or any credential; a Stripe CUSTOMER id is fine to show, a key is not.

DO NOT: change what any gate decides; add a way to create or edit customers from this view; duplicate the guest kill-switch admin work in PR #91, which is held on a missing EF migration (task 50) - if your change touches the same files, say so plainly in the report so the two can be sequenced rather than colliding.

TESTS (this repo's hermetic pattern; exclude the DataIntegrity tests, which dial a LAN database and can never pass in CI): each of the four states renders its own distinguishable flag; the endpoint is read-only and returns no credential; the response is bounded; an empty result renders as "no users" rather than an error. Report the count and state the baseline first - it was 376 hermetic tests passing on this repo earlier today.

HARD RULES: read-only; no credentials in the repo, in fixtures or in any response - env NAMES only; match the existing admin UI's style rather than restyling it; explicit-path commits; no push. Completion = commits, diff stat, test counts, the four flag states with the exact text an operator sees for each, whether you extended the existing endpoint or added one and why, how `user_organizations` relates to onboarding records in this schema, and a one-line statement of what this view would have shown for the three accounts that were missing on 2026-09-08. Auditor: read-only - verify NO parallel admin page or duplicate list endpoint was created where the existing one could carry the change, that the endpoint cannot write, that no key or secret can reach the response or the page, that the four states are genuinely distinguishable rather than one generic warning, and that the result is bounded.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
