DESIGN + BUILD (docs, workflow and package changes; deploy is the overseer's). Cognizioware: (A) move ALL regular CI/CD, e2e, eval and QA execution off developer PCs onto the company's Proxmox compute servers, and (B) make cognizioware-qa a service/sub-package that other repos' apps and UIs (Power Platform app, Hydra, mcp-cognizioware admin) can call with a prepared payload to trigger a QA run and get results back. Work ONLY in this workspace /home/harness/work/cognizioware-qa (branch feat/qa-service-and-ci-offload from origin/master; the repo is already cloned on this CT110 node — run `git fetch origin && git checkout -B feat/qa-service-and-ci-offload origin/master` first); plain git only; commit by EXPLICIT PATH; no push. Sibling repos are available read-only under /home/harness/work/ (cognizioware-mcp-tools, mcp-cognizioware, westhivecapital-hivemind) — read them, never modify them; changes those repos need are written as PATCH FILES + task texts under design/ci-offload/patches/ for the overseer to apply.

Paxton's requirements (2026-09-06, verbatim in spirit): "all e2e should not be on a single dev box … we should only be running active local dev testing on there and then exception cases; regular CI/CD pipeline should be offloaded to our PVE servers (e.g. cognizioware-ptait07)"; "auto approve anything to get it off this PC and into the actual compute servers correctly"; "come up with a clever way to have cognizioware-qa as a sub-package deployable or accessible from UIs of the other repos using it; having the payload to trigger the QA run with payload prepared is a must-have use case"; "another clever way / industry practices on how to have it easily callable from other repos' apps and instance runs on the servers in our infra"; "Hydra needs to integrate the QA, the web UX, and pass over details to test, if known".

Verified infra facts (overseer, 2026-09-06 09:00–09:20 PT): the three GitHub self-hosted runners that ran on the dev PC ptait09 (`ptait09` labels self-hosted,Windows,X64,lan-deploy for cognizioware-powerplatform; `ptait09-qa` self-hosted,Windows,X64,qa-host for cognizioware-qa; `ptait09-mcp` for mcp-cognizioware) have been DEREGISTERED; the QA backend (docker: cognizioware-qa-backend-1 :8400 + postgres) still runs on ptait09 and must move. cognizioware-mcp-tools already uses a Linux LXC runner `gh-runner-lan` (CT203 on ptait01, labels self-hosted,Linux,X64,lan-cognizioware) — the reference pattern. The overseer is creating CT210 `ci-runners` on ptait07 (192.168.21.170, Debian 12, 8c/16GB/80GB, nesting, Docker, Node 22, pwsh 7.5, user `runner`) to host: one runner per repo (labels: powerplatform → self-hosted,Linux,X64,lan-deploy; qa → self-hosted,Linux,X64,qa-host; mcp-cognizioware → self-hosted,Linux,X64,lan-mcp) and the QA backend compose (localhost:8400 from the runner's point of view). Hosts: ptait07 (192.168.21.138; CT100 pp-dev-uat .163, CT101 ruflo, CT201 ops-it-services), ptait01 (192.168.21.110; CT100 billing lanes, CT202 LiteLLM/mcp-tools, CT203 gh-runner-lan, CT204 uat LiteLLM, CT208 auth broker — this host was at load 334 today from LiteLLM zombie processes; do not add load there), corsairai300 (192.168.21.151; CT105 powerplatform prod, CT110 lh-harness = this node, Hydra orchestrator in /opt/cognizioware-hydra, hydra.cognizioware.com). Windows-only assumptions in current workflows that must be removed: pp eval-gate.yml/promote.yml use `shell: pwsh` and `"C:\Program Files\Git\bin\bash.exe"`; qa-gate.yml ce job installs "Playwright Chromium (Windows host, no --with-deps)"; CE login uses `TRMS_AUTH=password` with CE_USERNAME/CE_PASSWORD secrets (now set); qa-gate posts commit statuses `cognizioware-qa/<target>-<env>` with a `score=… passed=… failed=… suites=…` description that promote.yml polls (now freshness-bound on the deployed sha).

Deliverables, one slice per round:
(1) design/ci-offload/00-policy-and-topology.md — the policy ("dev boxes run only active local dev tests and explicit exception cases; every scheduled, PR, promotion, e2e, eval and QA execution runs on PVE-hosted runners"), the runner topology (CT210 on ptait07 as the primary Linux runner host, CT203 as the second; labels per repo; one ephemeral-style runner service per repo with `--ephemeral`-safe cleanup; docker-in-LXC with nesting; concurrency: runner groups/labels so evals never queue behind deploys), capacity and isolation rules (QA backend + Postgres as a compose stack on CT210 under /opt/cognizioware-qa; Playwright via `npx playwright install --with-deps chromium` or the official mcr.microsoft.com/playwright image), secrets handling (registration tokens are one-hour, never stored; runner env from /etc/ci-runners/*.env, chmod 600), and the exception path for "run this on my PC" (a `runs-on` matrix input defaulting to the server labels). Cite the practices you rely on with URLs (ephemeral runners "cattle not pets", nesting=1 for docker in LXC, GitHub's recommendation to keep self-hosted runners on private repos only).
(2) design/ci-offload/patches/: exact patch files (unified diff) for cognizioware-powerplatform `.github/workflows/eval-gate.yml`, `promote.yml`, `deploy-*.yml` and for mcp-cognizioware's workflows: replace Windows-only shells/paths with cross-platform steps (`shell: bash`; pwsh steps rewritten or kept only where pwsh on Linux is fine), `runs-on: [self-hosted, Linux, lan-deploy]`, Playwright install with deps, and no `C:\` paths; plus a `runs-on` override input for the exception path. Each patch has a matching task text (.task.txt) so the overseer can apply it as its own managed run. Also `scripts/ci-runners/install-runner.sh` (registers a repo runner as a systemd service given REPO, NAME, LABELS and a one-hour token from `gh api -X POST repos/<r>/actions/runners/registration-token`), `scripts/ci-runners/qa-backend.compose.yml` (backend + postgres, port 8400, restart unless-stopped, healthcheck) and a `Makefile`/`README` for CT210.
(3) QA as a service, the must-have: a versioned trigger contract `qa-run-request.v1` (JSON schema under packages/contracts/: target litellm|billing|ce|chat-tier1|chat-tier2|hydra, env dev|uat|prod, profile, base_url, env_id, caller_repo, caller_sha, caller_ref, requested_by (human email or agent id), correlation_id, callback {kind: github_status|webhook|hydra, url|context}, options {trms_e2e_dir, filters, timeout_minutes}) and result contract `qa-run-result.v1` (score line fields, per-suite results, artifacts URLs, run URL, started/finished, verdict, failure classification real|test-debt|drift). Implement THREE entry points that all accept the same payload: (a) `POST /api/qa-runs` on the existing QA backend (packages/backend; persists the request, dispatches `qa-gate.yml` via `workflow_dispatch` with a GitHub App/PAT stored server-side, returns run id, and exposes `GET /api/qa-runs/{id}` + a webhook callback on completion — the qa-gate workflow reports back with `POST /api/qa-runs/{id}/result` using QA_API_KEY); (b) a reusable workflow `.github/workflows/qa-run.yml` (`workflow_call`) that other repos call directly with the same inputs, so promotion pipelines need no polling; (c) a small npm sub-package `@cognizioware-qa/client` (packages/client: `buildQaRequest()`, `triggerQaRun()`, `waitForQaRun()`, typed to the contracts; publishable to GitHub Packages and usable as a git submodule/workspace) plus an MCP tool spec `qa_run_trigger` / `qa_run_status` for the LiteLLM MCP gateway (spec + handler in packages/backend, registered later by the overseer in cognizioware-mcp-tools). Every entry point validates the payload against the schema and rejects unknown targets.
(4) UI integration: "Run QA" affordances with prepared payloads — (a) in the QA frontend (packages/frontend): a form pre-filled from an environment record; (b) a drop-in web component/snippet `qa-run-button` (packages/client/web) that other apps (Power Platform admin page, mcp-cognizioware admin SPA, Hydra Reporting/Ship Plan) embed with a prepared payload and the callback pointing at the caller; (c) for Hydra specifically: design/ci-offload/30-hydra-integration.md — how Hydra's Ship Plan step (RP-02) and the reporting slice (RP-02a) call `POST /api/qa-runs` with details it already knows (repo, sha, env, base_url, env_id) and receive results into the run drawer, and how the web-UX handback's Playwright spec (cognizioware-powerplatform e2e/playwright/nav-archive.spec.js) becomes a QA target `web-ux` that runs on the server runner with the MSAL secrets already in cognizioware-qa. Tests: schema validation, API happy path + auth, reusable workflow guard (`node tests/ci/workflow-guards.mjs` if present).
(5) Industry-practice survey (short, cited): design/ci-offload/40-callability-options.md comparing (1) reusable workflow + workflow_dispatch/repository_dispatch with a GitHub App token, (2) an internal QA REST/MCP service with webhook callbacks (what you built), (3) a test-orchestration platform (Testkube/Testsigma-style) vs our Proxmox/LXC reality, (4) GitHub Checks API vs commit statuses for gate results, and (5) ephemeral autoscaled runners (LXD/Proxmox scripts) — with a recommendation and what to adopt now vs later. Then design/ci-offload/HANDBACK.md: what was built, the patches list, the CT210 install runbook (exact commands for the overseer), the migration checklist (deregister PC runners ✔ done, register CT210 runners, move QA backend + its Postgres data, re-run the powerplatform prod eval gate, delete the PC's runner services), and open decisions.
Commits: docs(ci): offload policy + topology · feat(ci): runner install + qa backend compose + workflow patches · feat(qa): qa-run contracts, API, reusable workflow, client + MCP spec · feat(qa): run-QA UI + Hydra integration design · docs(ci): callability survey + handback.

HARD RULES: no secrets or tokens in code/docs (env names only); never modify sibling checkouts; no push; commit by explicit path; the QA backend API must reject unknown targets and require QA_API_KEY; the reusable workflow must not hard-code runner hostnames (labels only). Completion = commit hashes, test output, the patches list, the CT210 runbook, and the migration checklist with status. Auditor: no git fetch; verify no Windows paths remain in the patched workflows, no secrets in the diff, and that all three entry points validate the same schema.
