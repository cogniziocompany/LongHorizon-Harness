BUILD (config + docs; the overseer deploys via the runner-v2 lane): make chat.easybutt0n.ai (Open WebUI "Fleet Chat") the user entry point for queuing lh-harness tasks. Work ONLY in /home/harness/work/ptait09-easybutt0n-ai on branch feat/fleet-chat-lhharness-tool (run `git fetch origin && git checkout -B feat/fleet-chat-lhharness-tool origin/easybutt0n-runner-v2` first); plain git; commit by EXPLICIT PATH; no push; no live calls; no secrets (env names only).
CONTEXT: the chat Tools menu today lists cognizioware, ptait09, ptait-desk03, htpc01 (openwebui/mcpo.config.json). The harness will expose MCP tools harness_enqueue_task / harness_list_queue / harness_run_status / harness_resolve_gate through the LiteLLM MCP gateway (alias lhharness, access group fleet-runners or a new harness-ops group; the chat uses its own virtual key, env name OPENWEBUI_GATEWAY_KEY). The tool must be ON by default for the Fleet Chat workspace.
DELIVER: (1) openwebui/mcpo.config.json + .example: add the lhharness tool server pointing at the gateway MCP endpoint (URL + header env names), enabled by default; (2) a default knowledge/skill document for the chat (Open WebUI Knowledge or the system prompt file under openwebui/prompts/fleet-tasks.md, whichever mechanism the repo already uses for defaults - reuse it and say which): how to write a scoped task (repo, branch, deliverables, hard rules, trio: kimi for dev, qwen for QA), what the gates mean, how to check status, the rules (prod through lanes only; never resume a cancelled run without knowing who cancelled; ASCII in gate answers), and the link pattern to the harness UI http://192.168.21.168:8799/runs/<run_id> (until Hydra hosts the view); (3) docs/FLEET-CHAT-LHHARNESS.md: user flow + operator steps (create the gateway virtual key with the access group, set the env, redeploy the chat via the lane); (4) e2e check e2e/fleet-chat-tools.mjs (soft-skip off-LAN): the chat tool list contains lhharness and tools/list through the gateway returns the four harness tools. Completion = commits, diff stat, operator env NAMES. Auditor: no git fetch; no key material; the tool defaults ON.
