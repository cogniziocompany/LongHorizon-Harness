FIX + TEST (code; the overseer merges and re-dispatches the promotion): the powerplatform primary eval gate now fails cases INSTANTLY with `build: QA_FAIL_MAX (attempts N, 0s, exec <id>)` -> "dead-build signature (QA_FAIL_MAX in 0s) => fail regardless of stale artifacts" -> outcome=fail, without running a build. Evidence: promotion 34198528281 (sha cbd0e88, dev gate, 2026-09-08 07:20Z): micro-form-spacer, multi-mini-app, multi-table-and-data-import and multi-webhook-crud all failed this way within seconds (eval_runs a9a1d6d7, d551ec02, 9dd53aa3, ...), SUITE 12/16; the same sha passed dev 16/16 forty minutes earlier in run 34191344068, whose UAT gate then failed multi-webhook-crud with a real 752 s attempt (exec 154030) followed by a 0-second duplicate exec (154048) read as dead build although the webhook->CRUD step succeeded (eval_runs 709d3b0a); the 845bc89 promotion (34170161587) failed UAT twice the same way on multi-table-and-data-import. Hypotheses to test in order: (1) the eval runner reuses a session/build whose QA attempt counter is already at max from a previous run (state persisted in the dev/uat app DB or in eval artifacts), so a new promotion gets an immediate QA_FAIL_MAX; (2) the runner reads a stale exec record (different exec id, 0 s) after a retry and classifies it as the result; (3) the app's build worker is unhealthy after back-to-back redeploys. Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch fix/eval-gate-dead-build (run `git fetch origin && git checkout -B fix/eval-gate-dead-build origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live calls to the lanes/Dataverse/Stripe (you MAY read the dev app's public health endpoints read-only if needed: http://192.168.21.163:3000/api/health); env names only.
READ FIRST: e2e/evals/run.mjs (attempt loop, QA_FAIL_MAX / NO_RESPONSE detection, exec id handling, the "dead-build signature" rule, how sessions/builds are created per case), the eval_runs logging, packages/backend build/QA endpoints (where QA attempts are counted and reset; what returns QA_FAIL_MAX), the retry/classify logic from PR #86 and the smoke/timeout inputs from PR #90, docs/deploy/eval-gate-hybrid-options.md, and the logs quoted above.
SLICES: (1) reproduce with a unit test: a case whose first exec returns QA_FAIL_MAX at 0 s must be classified infra/unknown and retried with a FRESH session/build (never reuse an exec/session that is at max attempts); dedupe results by exec id and ignore 0-second reads that follow a real attempt; (2) make the runner create a fresh session per case per run (or reset the per-case QA attempt state through the existing API) and record `freshSession=true` in eval_runs; (3) the gate report distinguishes "dead-build (app state)" from "fail" and exits 3 (blocked) for the former per the infra-aware contract; (4) docs: eval runbook section "instant QA_FAIL_MAX" with the reset procedure the overseer runs on dev/uat if state is stuck (names only). Explicit-path commits, unit tests green, actionlint if available. Completion = commits, diff stat, the operator reset procedure, and the exact promotion dispatch to try (`-f ref=cbd0e88... -f is_production=true` from develop). Auditor: no git fetch; verify no threshold lowered, 0-second reads never count as results, fresh session per case.
