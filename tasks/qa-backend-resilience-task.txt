FIX (repo only; the overseer applies on CT210 via the lane or a documented step): the cognizioware-qa backend container on CT210 (repo-backend-1, host ports 4000 and 8400 -> 4000) exited with code 255 on 2026-09-07 ~11:40 UTC (ptait07/CT210 restart window) and stayed down for ~6 h because its restart policy is not unless-stopped; every QA gate (web-ux, ce, billing, litellm) then fails at "Wait for QA backend to be healthy" with connection refused on localhost:8400 - which reads like a product failure. Work ONLY in /home/harness/work/cognizioware-qa on branch fix/backend-restart-policy (run `git fetch origin && git checkout -B fix/backend-restart-policy origin/master` first); plain git; commit by EXPLICIT PATH; no push; no live calls.
DELIVER: (1) the compose/deployment definition the backend runs from on CT210 (find it: docker-compose*.yml / deployments/ / docs; if the container is not compose-managed, say so and add a compose file + a documented one-time migration step): restart: unless-stopped, a HEALTHCHECK on /health, and a named volume for its data; (2) qa-gate.yml "Wait for QA backend to be healthy": when the backend is down after the wait, fail the gate as infra-blocked (status `error`, "blocked by infrastructure: QA backend unreachable on qa-host") using the new infra-aware semantics, not `failure`; (3) a runner-side step (or documented systemd unit) that starts the backend if it is not running before the wait (docker start by name, compose up -d) - bounded and idempotent; (4) docs/ops/qa-backend.md (where it runs, how it restarts, how to check). Completion = commits, diff stat, the exact one-time command for CT210. Auditor: no git fetch; verify the policy and healthcheck are present and the gate reports infra, not failure.

ALSO (found 2026-09-07 18:50 PT): the `web-ux` job's "Wait for QA backend to be healthy" step polls `${QA_API_URL:-http://localhost:8400}/health`, but the backend serves `/api/health` (`/health` is 404 - the `ce` job already uses `/api/health`). Every web-ux dispatch fails at that step even with the backend up (runs 34170962602, 34171112859). Fix the path in the web-ux job (and any other job with the same typo), and make the wait print the URL + last HTTP code on failure.
