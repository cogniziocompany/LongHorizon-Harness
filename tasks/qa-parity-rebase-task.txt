BUILD + TEST (git integration only): rebase/merge the cognizioware-qa branch feat/admin-config-parity-gate (PR #18, task 11 output, cut from the old ci/qa-gate-billing base) onto current origin/master and resolve the four conflicts so PR #18 becomes mergeable. Work ONLY in /home/harness/work/cognizioware-qa: `git fetch origin && git checkout feat/admin-config-parity-gate && git merge origin/master` (merge, not rebase, so the pushed history stays intact). Conflicts: .github/workflows/qa-gate.yml (both sides added jobs/inputs - keep ALL master jobs and inputs (web-ux target, runner flow gate, tee log, junit reporter, infra classifier, captureToken/tagLog wiring) AND the new admin job + [tag:] handling; never drop a master job), .gitignore (union), scripts/gate-report.mjs (keep master's --source junit/infra-aware behaviour AND the [tag:...] prefix parsing from the branch; run its tests), scripts/validate-workflows.sh (union of checks; must pass on the merged workflow). After resolving: `node --test` / vitest in admin-config-acceptance/ green, `scripts/validate-workflows.sh` green, `git diff --stat origin/master..HEAD` reviewed (no accidental deletions of master files), single merge commit, then `git push` YOURSELF to update the PR branch and STOP (your run holds a scoped GH_TOKEN,
push + PR only, since 2026-09-14 16:21 PT); do NOT merge. NO live calls; no secrets; do not touch e2e specs. Auditor: verify no master job or input was removed from qa-gate.yml (diff the job list origin/master vs HEAD) and that validate-workflows.sh passes.

 - EXCEPTION - if your diff touches `.github/workflows/`, the scoped token REFUSES workflow-file
   pushes by permission. Commit, then STOP and say the branch needs an overseer push; do not retry.
