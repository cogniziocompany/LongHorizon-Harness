BUILD + TEST (code + docs; cognizioware-mcp-tools): the OPERATOR-VISIBLE half of the config-authority work. Task 57 was cut back to three slices (authority inventory, YAML annotations, drift check) because it could not fit its budget; this task carries the deferred half. Work ONLY in /home/harness/work/cognizioware-mcp-tools-b on branch feat/config-drift-surfaces (run `git fetch origin && git checkout -B feat/config-drift-surfaces origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO writes to any live database, router or box; env NAMES only.

DEPENDS ON TASK 57, which produces `docs/config-authority.md` (which config keys are FILE-, DATABASE- or MERGE-authoritative) and a read-only drift-check script. If 57's branch is not merged yet, branch from it and SAY SO; if its outputs do not exist, build against the contract described here and state clearly what you assumed.

WHY THIS EXISTS: on 2026-09-08 the LiteLLM router's settings were found to live in Postgres, not the committed YAML (`general_settings.store_model_in_db: true`). An operator edited the file, restarted, verified the file was correct INSIDE the container, and nothing changed - the fix only worked when applied through the API. Separately CT202's root disk reached 100%, killed the production router for an hour and destroyed 23 harness runs, and NOTHING on any operator surface showed it coming. Both were invisible until something broke.

SLICES (one commit each, explicit paths):
 (1) DOCTOR ROW - CONFIG DRIFT. The Ops Control Center in this repo is the operator surface: `infrastructure/docker/ops-control-center/`. Verified 2026-09-08 it exposes exactly SEVEN routes - /, overview, catalog, e2e, certs, admin-centres, audit. THERE IS NO /doctor ROUTE: the doctor is `doctor/ops_doctor.py` reading `doctor/targets.json`, and its results RENDER ON /overview (Components/Pages/Overview.razor, which carries the "Run doctor" button). Adding a targets.json row is necessary but NOT sufficient - confirm the row actually appears on /overview and say how you confirmed it. Green when file and database agree on every DB-backed key; on divergence, name the specific keys and both values, not a bare red light. Read an existing target and match its schema, cadence and failure-text style. READ-ONLY; never print a key, token or database URL.
 (2) DOCTOR ROWS - DISK. targets.json has 45 entries and NOT ONE checks disk; ops_doctor.py never calls df or statvfs. Add disk capacity rows for CT202, CT204, CT210 and the corsairai300 host, WARN at 80% and CRITICAL at 90%, showing the actual free figure rather than a bare colour so a person can see it trending. Confirm they render on /overview. Report what each reads today so the first run is the baseline.
 (3) HARNESS HEALTH ROW - one row, NOT a second wallboard. The harness is invisible in ops today; its state lives only in Hydra and fleet.easybutt0n.ai. fleet.easybutt0n.ai owns run detail BY DESIGN and duplicating it here is the mistake this estate keeps making - DO NOT build a run list. Add ONE row answering the infrastructure question: does the harness API on CT110 answer, is the node registered online, how many runs are active, how deep is the queue, how many failed in the last hour. Bearer by env NAME, read-only. Name the numbers on failure. In the report, state where an operator should look for WHICH question - infrastructure health in ops, run detail in the fleet window, device and control in Hydra - so the three surfaces stay distinct.
 (4) E2E SUITE. Follow the numbering and structure of a neighbouring suite in this repo's e2e directory (READ ONE FIRST and match its shape exactly). Assert, read-only against the live gateway: that every key the inventory marks DB-backed agrees between the committed YAML and the live LiteLLM_Config rows; that the drift check exits non-zero on divergence and zero on agreement; and - the assertion that would have caught 2026-09-08 - that router_settings.cooldown_time and allowed_fails in the DATABASE match committed intent, so a future inert file edit fails a suite instead of killing runs. Also assert general_settings and router_settings do not disagree with each other on any shared key, because today they do. NO WRITES.
 (5) LANE WIRING. Run the drift check in the deploy lane. It must NOT hard-fail on day one - divergence exists right now and blocking every deploy on it would be its own outage. Emit a WARNING with the diff in the step summary and leave one clearly-marked switch to promote it to blocking later. State which divergences exist today.
 (6) RECONCILE PROCEDURE, dry-run by default, for pushing committed YAML values into the database through the supported API when a human decides the file should win. `POST /config/update` MERGES rather than replaces (verified 2026-09-08: updating two keys left all 21 fallback chains and 5 context-window fallbacks intact), so document that, and warn that a naive full-object PUT would destroy the fallback chains. Take a backup of the affected row FIRST and VERIFY THE BACKUP IS NON-EMPTY before changing anything - an attempt that day produced a zero-byte backup through a quoting error and nobody noticed. And record the hard-won rule: A 200 FROM THAT API IS NOT PROOF THE WRITE LANDED - it returned 200 twice while silently discarding `allowed_fails_policy`, because that key is not in the pinned version's schema. Always read the row back and diff.
 (7) TESTS for anything you add: the doctor rows render, the drift check's exit codes, the e2e assertions against fixtures. Report counts, baseline first. No live calls beyond the read-only pattern neighbouring suites already use.

HARD RULES: read-only against every live system - no writes to the database, the router or any box; no credentials, keys or database URLs in the repo or in any output, and redact anything key-shaped in diffs; do NOT build a run wallboard in ops; do not reconcile the live divergences here, only report them; do not restart any service; explicit-path commits; no push. Completion = commits, diff stat, test counts, what each disk row reads today, what the new rows show on /overview right now, and the list of divergences that exist today. Auditor: read-only - verify no write path and no key literal, that the lane warns rather than blocks, that the doctor rows name specifics rather than showing a bare colour, and that nothing here duplicates the fleet run wallboard.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

## ADDED 2026-09-09 (folded in from task 58, which was RETIRED because the fix was simply configured correctly rather than built)
The Synthetic Anthropic-route problem is FIXED AND LIVE - do not re-solve it, and do not treat this as a build task for the routing itself. What is left here is the two READ-ONLY assertions that would have caught it, which belong with your other config-drift e2e work.

Settled mechanism (established by testing candidate fixes, not by re-probing the symptom). LiteLLM picks the upstream API from the model's PROVIDER PREFIX, not from api_base:
  openai/<model>        -> on /v1/messages LiteLLM calls the provider's OpenAI RESPONSES API (/v1/responses); Synthetic does not implement it -> 404
  custom_openai/<model> -> chat-completions; VERIFIED 200 on /v1/messages
  hosted_vllm/<model>   -> chat-completions; VERIFIED 200 on /v1/messages
  openai_like/<model>   -> 400, unmapped in the pinned version
That is why "Synthetic is OpenAI compatible" and "Synthetic 404s for the harness" were BOTH true - different routes, same model. A 200 on /v1/chat/completions is not evidence about /v1/messages.
Live on the prod router now, registered via POST /model/new (so they live in the LiteLLM_Config Postgres rows, NOT the committed YAML - exactly the authority split this task documents): qwen3.8-27b / glm-5.3-flash / kimi-k3 / glm-5.2, each as `<name>:synthetic-anthropic` on `custom_openai/`, api_key os.environ/SYNTHETIC_API_KEY, rpm 15. The original `openai/`-prefixed `:synthetic` entries STAY for non-Anthropic callers - two models per upstream, one per route, each verified on the route it serves (Paxton's instruction, 2026-09-09). Do NOT delete or repoint the openai/ entries.

 (8) E2E - BOTH-ROUTES ASSERTION. Assert read-only that every model expected to serve a harness role answers on the route it must serve: each `:synthetic-anthropic` twin returns 200 on `/v1/messages`, asserted ON `/v1/messages` ITSELF and never inferred from `/v1/chat/completions`. Include the STREAMING path: a non-streaming call to kimi-k3 returns a thinking block with `signature: null` while the streaming path is clean, and Claude Code streams - a streaming thinking delta with a null signature breaks it with "Content block is not a thinking block", which killed four runs in September. Probe with the HARNESS's own virtual key, not the master key: a master-key probe passes models the harness cannot reach, and on 2026-09-09 that let two runs die instantly on `403 key not allowed to access model`. NO WRITES.

 (9) ALIAS-IS-NOT-A-BACKEND ASSERTION. Verified 2026-09-09: `kimi-k3:cloud` is silently SERVED BY `glm-5.3-flash:cloud` because a fallback chain redirects it, so an alias can report one model and deliver another and anything that spreads work across "different models" by name can collapse onto one backend without saying so. Read the model name reported back in the `message_start` event and fail when a requested alias is served by a different deployment than intended. REPORT which aliases fail it today; do NOT change any fallback chain here - surfacing them is the deliverable, reconciling them is a separate decision for Paxton.
