FIX (repo only; the lane deploys Caddy): make https://fleet.easybutt0n.ai serve the Hydra fleet view instead of the dead `fleet-admin:8787` upstream. Facts (2026-09-07 20:50 PT): Caddy on CT202 (infrastructure/docker/caddy/Caddyfile, site block ~line 78) reverse-proxies fleet.easybutt0n.ai to `fleet-admin:8787`; that service was a live-only 2026-08-09 deploy of the device registry from the runner repo (ptait09-easybutt0n-ai/fleet-admin), never in this repo's compose, and it no longer exists on CT202 (no container, no image; today's disk-full recovery pruned stopped containers). Hydra (https://hydra.cognizioware.com, orchestrator on corsairai300 192.168.21.151) is the fleet control plane going forward and its fleet view absorbs the device registry (device list, check-ins, harness nodes). Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch fix/fleet-hostname-to-hydra (run `git fetch origin && git checkout -B fix/fleet-hostname-to-hydra origin/main` first); plain git; commit by EXPLICIT PATH; no push; no live calls. DELIVER: (1) Caddyfile: fleet.easybutt0n.ai reverse_proxy to the Hydra orchestrator (LAN address + port from the hydra repo docs / existing hydra_mcp entry in litellm-config.yaml - cite it; header_up Host preserved; same TLS dns-cloudflare block; keep the LAN split-horizon note) and remove the dead fleet-admin upstream; (2) design docs/handoff-docs/ct202-live-config-drift-and-fleet-admin-deploy.md: append a dated section "fleet-admin retired 2026-09-07 - superseded by Hydra; fleet.easybutt0n.ai now fronts Hydra"; (3) e2e: the public health smoke (deploy-mcp-tools.yml smoke list or e2e suite) includes https://fleet.easybutt0n.ai/ expecting the Hydra login/200, soft-skip off-LAN; (4) note the DNS shape (public A 76.51.14.4 grey-cloud + Pi-hole split-horizon to 192.168.21.161) in the Caddyfile comment. Completion = commit, diff stat, the Caddy reload the lane performs. Auditor: no git fetch; no other site blocks touched.
