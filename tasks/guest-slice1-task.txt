BUILD (code + tests + docs). Cognizioware Guest Launch — SLICE 1 of the authorized implementation: F03/F04 fail-closed authorization/redaction fixes + the Claims365-pattern admin shell, exactly as specified in design/plans/guest-launch/FIRST-SLICE-PROPOSAL.md (read FIRST, sections 1.1–1.11, 2.1–2.5, 3 out-of-scope, 4 engineering order). Supporting spec, read in this order: design/plans/guest-launch/DECISION-REGISTER.md (Paxton resolved it 2026-09-06: every proposed default accepted; D01 baseline, D02 SPA test-mode front door first, D03 Power Pages PAYG licensing are TOP PRIORITY — GO; D11 admin design MEDIUM), design/plans/guest-launch/F03-F04-authorization-defects-2026-09-05.md (the 12 defects with file:line, exploit sketch, fix and proving test), design/plans/cognizioware-guest-launch-handoff.md, -plan.md (§ "Proposed architecture and nonnegotiable safety invariants", § "Claims365-pattern admin center"), -backlog.md (F03, F04, A01, A02 acceptance criteria), and design/plans/README-final-plan-data.md (commercial decisions that override older billing language: corporate BYOK zero surcharge; managed usage cost ×1.20). Work ONLY in this workspace /home/harness/work/cognizioware-powerplatform (branch feat/guest-slice-1 already checked out from origin/develop @ f7bb19c); plain git only; commit by EXPLICIT PATH (never git add -A; never commit packages/backend/dist/**); no push. Node 22 is at /usr/bin/node; `npm ci` in packages/backend and packages/frontend if node_modules are missing. No Dataverse, Stripe, MSAL or n8n credentials exist on this node: everything is proven with unit/integration tests and fakes; never mark a runtime check as passed.

Read-only references available on this node: /home/harness/work/mcp-cognizioware (billing service; do not modify — that is the separate billing slice run), /home/harness/work/cognizioware-qa (QA gate contracts). The Claims365 reference package is NOT on this node — implement 2.x from the proposal's crosswalk descriptions (MainLayout shell, RangePicker, stage cards, Setup checklist) using the existing React admin's inline-style conventions and the slate palette; no CSS framework, no new UI dependencies.

Slices, one per round, following the proposal's engineering order (§4): each ends with `npm run build` + tests green in the touched package and a commit by explicit path.
(1) Order rows 1–2: baseline note update in design/plans/guest-launch/F01-baseline-2026-09-05.md (branch, base sha, run id); migrations `environment_grants` and `proposed_role_changes` (follow the 0NN_*.sql pattern in packages/backend/src/db/migrations; next free number); `requireAuthOrSecret` service-identity refactor (1.6) and billing-check token validation + session ownership (1.1). Tests. Commit: fix(auth): service identity, billing-check validation and session ownership (guest slice 1).
(2) Order row 3: environment grants + target-environment ownership + default-env fallback removal (1.2); step/tool read ownership guards (1.3). Tests. Commit: fix(auth): environment grants and read ownership guards.
(3) Order rows 4–5: internal-test approval entitlement check (1.4), client overage server-side re-check (1.5), n8n callback execution/session binding (1.7), RSI eval/harvest admin-only gating (1.8), leads endpoint hardening (1.9). Tests. Commit: fix(auth): approval entitlement, overage re-check, callback binding, admin gating, leads hardening.
(4) Order row 6: admin config redaction with typed allowlisted descriptors and URL/connection-string redaction across APIs, logs and exports (1.10); role-override proposal workflow / separation of duties (1.11). Tests must show a DATABASE_URL-style value never leaves the API in any form. Commit: fix(admin): typed config redaction and role-change proposals.
(5) Order rows 7–8: `AdminShell` extracted from AdminPage.tsx, expanded navigation to the coverage map (Users, Environments, Sessions, Traces, Evals, RSI Cases, Config, Audit, plus Billing, Dual-write status, Financial variance, Infrastructure, Telemetry, Access, Setup), shared per-user tenant-scoped RangePicker, "unavailable" panels that say unavailable rather than showing healthy mock data (2.1–2.4); Setup checklist panel + backend probe endpoint (2.5). Component tests. Commit: feat(admin): Claims365-pattern shell, navigation, unavailable panels and setup checklist.
(6) Order row 9: integration review; re-check every F03/F04 citation against the final tree; update design/plans/guest-launch/F03-F04-authorization-defects-2026-09-05.md with a "Fixed in slice 1" column (commit sha per defect) and write design/plans/guest-launch/SLICE-1-HANDBACK.md: what changed, migrations, tests + counts, the go/no-go criteria from the proposal §5 with evidence, what is NOT proven at runtime (Dataverse/MSAL/n8n) and how the QA gate (cognizioware-qa target `ce`, profile greenfield, and the web-ux target) will prove it after the dev-lane deploy. `node tests/ci/workflow-guards.mjs` must pass. Commit: docs(guest-launch): slice 1 handback.

HARD RULES (from the handoff's safety invariants and Paxton's decisions): fail closed — no client-side approval can restore paid access or admin privilege; never authorize by email alone; no Stripe, billing, pricing, meter or scheduled-task changes in this slice (billing lives in mcp-cognizioware and is a separate run); no hard deletes; no secrets, keys or connection strings in code/docs/tests (env names only); custom Dataverse only in sample copy; INTERNAL TEST framing on approval/terms UI; no "BMAD" or pricing copy in UI; no .github/** changes; no new npm dependencies; commit by explicit path; no push. Completion = commit hashes, build + test output per package, the migrations list, the defect→commit table, the go/no-go evidence table, and the NOT-PROVEN list. Auditor: no git fetch; verify each of the 12 defects has a test that fails on the old behaviour and passes now (or is explicitly listed as not in slice with the proposal's reason), no secrets in the diff, no dist/** committed, and that unavailable panels never render mock data.
