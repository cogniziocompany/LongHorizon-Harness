BUILD + TEST (code + docs; the overseer files the upstream issue, merges and converges PTAIT09): fix the Windows installer for the Hivemind overseer-session ingest so it has a supported non-admin path and stops flashing a console window every 5 minutes, and fold the hand-made local wrapper into the repo. Planning session 17509e0b-7a53-47d9-90e7-2a4d269a16b9 (plan file lives on the overseer PC; everything you need is below). Work ONLY in /home/harness/work/cognizioware-hydra-rsi on branch fix/overseer-ingest-installer (run `git fetch origin && git checkout -B fix/overseer-ingest-installer origin/main` first - base on origin/main, NOT on any feat/ branch); plain git; commit by EXPLICIT PATH; no push; NO live calls and NO scheduled-task registration (you are on Linux; everything is static analysis plus hermetic tests); env NAMES only, never key values.
ROOT CAUSE (verified by the human planner on PTAIT09; re-check each claim against the source and report anything that differs): scripts/install-overseer-ingest.ps1 registers the principal with `New-ScheduledTaskPrincipal -UserId $env:USERNAME -LogonType S4U -RunLevel Highest` and carries `#Requires -RunAsAdministrator`. S4U does NOT attach a console, so the script's own install does not flash - but because it demands elevation, operators hand-register the task instead, which lands on LogonType InteractiveToken, and an interactive logon running the console binary node.exe is what flashes. `Settings/Hidden` does not help; it only hides the row in the Task Scheduler UI. So the defect is a MISSING SUPPORTED NON-ADMIN INSTALL PATH, not a stray flag.
COLLATERAL DEFECTS in the same file, all live: (a) `-Execute 'node'` resolves through PATH, which a task logon session may not carry, so the task fails file-not-found rather than reporting an ingest error; (b) line ~32 uses the three-argument `Join-Path $repoRoot 'scripts' 'overseer-session-ingest.mjs'`, which is PowerShell 6+ only, so the declared `#Requires -Version 5.1` is false and the script only runs under pwsh 7; (c) the task's ExecutionTimeLimit equals its 5-minute repetition interval, while the ingester caps at 200 posts per pass with a 30 s timeout each, so a cold first pass gets killed; (d) task stdout/stderr goes nowhere on Windows, while scripts/overseer-ingest.cron.example already redirects to ~/.claude/hivemind-overseer-ingest.log on Linux - which is why this defect survived three days silently.
REUSE THE REPO'S OWN CORRECT PATTERN: device-agent/src/install-service.js registers the hydra-agent task properly - it resolves the interpreter as process.execPath and embeds that absolute path in the task XML instead of trusting PATH. install-overseer-ingest.ps1 is the only place left using the bare-executable pattern and the only .ps1 in the tree. Follow install-service.js.
CHANGES (one commit per slice, explicit paths):
 (1) scripts/install-overseer-ingest.ps1 - emit a hidden launcher and point the action at it: at install time write `%LOCALAPPDATA%\cognizioware\overseer-ingest.vbs` next to the env file containing a WScript.Shell.Run call with window style 0 and wait-for-exit True, and register the action as `-Execute 'wscript.exe' -Argument '"<vbs path>"'`. Rationale to keep in a comment: -WindowStyle Hidden is a PowerShell parameter and does nothing for node.exe; wrapping in powershell -WindowStyle Hidden flashes its own console first; a conhost visibility hack is fragile across Windows builds; wscript.exe is a native windowed host present on every Windows install and never allocates a console. This exact wrapper is already proven by hand on PTAIT09 (task ran, LastTaskResult 0, state file advanced, no window) - the point of this slice is to make it a repo-generated artifact so it survives a reinstall. Commit: fix(overseer-ingest): generate a hidden launcher for the scheduled task.
 (2) same file - drop `#Requires -RunAsAdministrator`; detect elevation at runtime and choose the principal: elevated keeps `-LogonType S4U -RunLevel Highest` (runs while logged out), not elevated uses `-LogonType InteractiveToken -RunLevel Limited`, which slice 1 makes safe. Print which mode was chosen and the tradeoff (the interactive principal only fires while the user is logged on, which is acceptable for a workstation transcript ingester). Commit: fix(overseer-ingest): supported non-admin install path.
 (3) same file - replace `-Execute 'node'` with the resolved `(Get-Command node).Source`, throwing a clear error when node is absent; split the three-argument Join-Path into nested two-argument calls so `#Requires -Version 5.1` becomes true; raise ExecutionTimeLimit to 15 minutes (above the 5-minute repetition); have the generated launcher redirect the ingester's stdout and stderr to `~\.claude\hivemind-overseer-ingest.log`, matching the Linux cron example. Commit: fix(overseer-ingest): interpreter path, PS5.1 compatibility, time limit, logging.
 (4) same file - idempotence: -Install overwrites the launcher as well as the task (Register-ScheduledTask -Force); -Uninstall removes the .vbs alongside the task and states explicitly that it leaves the env file and the state file (~\.claude\hivemind-overseer-ingest-state.json) behind on purpose. Commit: fix(overseer-ingest): idempotent install and uninstall.
 (5) tests + docs: add a hermetic test that parses the generated launcher and the task-action arguments from a dry-run of the install function (mock the ScheduledTask cmdlets and the filesystem; no registration) and asserts: the action is wscript.exe with the quoted vbs path, the vbs contains window style 0 and the absolute node path, the log redirect is present, and the time limit exceeds the repetition interval. If PowerShell is unavailable in the workspace, write the test as a node script that reads the .ps1 and asserts on the emitted strings, and SAY SO. Update scripts/overseer-session-ingest.README.md (install block: the non-admin path, the pwsh 7 requirement, the node prerequisite, and the three things that live under %LOCALAPPDATA%\cognizioware\ - the hydra checkout, overseer-ingest.env, overseer-ingest.vbs) and design/HANDOFF.md's install section. Commit: test+docs(overseer-ingest): launcher contract and install paths.
FLAG IN THE COMPLETION REPORT, do not fix here: the stub env file the installer writes defaults MEMORY_URL to a memory-mcp REST port that is not reachable from PTAIT09 (the gateway MCP transport is what works), so it steers a new operator to the broken transport; and nothing refreshes the runtime checkout at %LOCALAPPDATA%\cognizioware\hydra, which sits at 1d83c00 (PR #7) while main is far ahead - recommend one of: a pull step at the top of each run, a second scheduled task, or an explicit recorded pin.
HARD RULES: no live scheduled-task or registry operations; no secrets; do not touch scripts/overseer-session-ingest.mjs behaviour (only its documentation) unless a slice above requires it; explicit-path commits; no push. Completion = commits, diff stat, test output, the exact operator command lines for the elevated and non-elevated installs, and the flagged items above. Auditor: verify the action is wscript.exe rather than node, that no code path still requires elevation, that Join-Path is two-argument everywhere, and that the time limit exceeds the repetition interval.
CONFLICT REVIEW (done by the overseer against the in-flight work, hydra main 0fdc9ca): the run for task 16b (hivemind experience ingest) sits in this same workspace on branch feat/hivemind-experience-ingest and touches scripts/hivemind-ingest.mjs, a new scripts/hivemind-recall-valued.mjs, their tests and fixtures, AND appends ~105 lines to the ROOT README.md. There is no file overlap with the installer work except that root README. THEREFORE: do NOT edit the root README.md in this task - put all documentation in scripts/overseer-session-ingest.README.md and design/HANDOFF.md only. Base on origin/main and ignore feat/hivemind-experience-ingest; the two branches merge independently.
CORRECTION to the plan facts, verified in the source: %LOCALAPPDATA%\cognizioware is only the ENV-FILE directory as far as this script is concerned; the `hydra\` runtime checkout underneath it is operator convention and appears nowhere in the repo. Document that explicitly in the README slice (what the installer creates versus what the operator created by hand).
HAZARD to document in the same slice: the task action embeds the absolute script path computed at install time, so moving or re-cloning the runtime checkout silently breaks the task - node exits non-zero and, until slice 3 lands, the output is discarded, so the failure is invisible. Also note that an auto-update that git-pulls mid-cycle can swap the .mjs under a running node process, which is why slice 3 raises the time limit above the repetition interval; any future updater must run outside the run window. The state cursor ~/.claude/hivemind-overseer-ingest-state.json is keyed by absolute transcript path, so a checkout move does not lose the cursor, but changing --root re-ingests (the server dedupes by content hash).

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
