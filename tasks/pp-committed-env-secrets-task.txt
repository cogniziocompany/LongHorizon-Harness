TASK 225 - cognizioware-powerplatform: committed env files carry live secrets. Untrack, rotate, deliver by name.

MEASURED 2026-09-23 (variable NAMES only; no value was printed anywhere): prod.env, uat.env and dev.env are TRACKED at the
repo root on `develop`. prod.env: 16 secret-named vars with non-placeholder values incl. AZURE_CLIENT_SECRET, DB_PASSWORD,
DATABASE_URL, BILLING_POSTGRES_PASSWORD, LITELLM_API_KEY, BILLING_API_KEY, GITHUB_TOKEN. uat.env and dev.env: 12 each.
In history since 2026-03-11 (uat), 2026-07-14 (prod), 2026-07-16 (dev). Repo is PRIVATE. Same defect class as hydra
(task 216 / hydra PR #30) - follow that PR's pattern.
SCOPE (code): `git rm --cached` the three files, add them to .gitignore, ship `*.env.example` with NAMES only, and make
the app FAIL LOUDLY at startup when a required name is missing (no silent defaults). Inventory which deploy path reads
each file (promote.yml, compose, the CT120/CT105 hosts) so nothing in dev/uat/prod silently loses config on the next deploy.
STOP GATE - ROTATION IS PAXTON'S, NOT THE RUN'S: list every credential that must be rotated, grouped by owner system
(Entra app secret, Postgres roles, LiteLLM keys, billing key, GitHub token), and stop. Do NOT rewrite git history.
Push + PR; DO NOT MERGE - removing a file a deploy reads is a prod-affecting change.
