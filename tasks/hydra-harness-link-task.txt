BUILD + TEST (code; the overseer deploys Hydra on corsairai300 after the harness release): in the Hydra fleet view, link every harness run to the lh-harness web UI dynamically and show the queue. Work ONLY in /home/harness/work/cognizioware-hydra-rsi on branch feat/fleet-view-harness-links (run `git fetch origin && git checkout -B feat/fleet-view-harness-links origin/feat/harness-fleet` first - the fleet plane branch; if it was merged to main use origin/main and say so); plain git; commit by EXPLICIT PATH; no push; no live calls (fixtures).
DELIVER: (1) the fleet API client learns each harness node's public UI base URL from the fleet registry entry (field ui_base_url, e.g. http://192.168.21.168:8799 for CT110) so multiple harness instances each link to their own UI; (2) run rows/cards get "Open in harness UI" -> {ui_base_url}/runs/{run_id} and gate rows deep-link to the gate; (3) a Queue panel reading GET /api/queue from each harness node (pending/launched/done with reasons) and an "Enqueue task" form posting to POST /api/queue (name, task text, workspace, trio, max_rounds, priority) using the fleet API's existing auth; (4) tests with fixture JSON (two harness nodes, one queue) and docs/fleet-view-harness-links.md incl. the registry field. Completion = commits, diff stat, tests. Auditor: no git fetch; URLs come from the registry, never hard-coded; no secrets.
