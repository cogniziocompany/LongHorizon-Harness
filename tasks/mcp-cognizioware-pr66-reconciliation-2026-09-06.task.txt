BUILD-ONLY. Read the authoritative plan FIRST and follow it exactly: /mnt/c/Users/PaxtonTait/.claude/plans/ut-the-tree-does-hazy-fox.md (Windows C:\Users\PaxtonTait\.claude\plans\ut-the-tree-does-hazy-fox.md) — "PR #66 reconciliation + admin UI loading states". Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/mcp-cognizioware-recon (currently branch fix/prod-compose-env from origin/develop @ 8946eea3); never cd into or run git against /mnt/c/Users/PaxtonTait/source/mcp-cognizioware or any other checkout; plain git commands only (never --git-dir/--work-tree). Always diff against origin/develop, never against another checkout. Key measured fact from the plan: PR #66 (codex/prod-billing-env-fix) is a net DELETION of application code (src/: +67/-7214) taken from a stale prod box tree; it must NOT be merged. Deliver the plan's Fix 1a, Fix 1b (decision), and Fix 3 as two branches in this worktree, one slice per round:
(1) Fix 1a on branch fix/prod-compose-env (already checked out): port ONLY the compose/cloudflare hunks from commits 410072d4, eba5c822, ebf2b608, a4c1dffd into infrastructure/docker-compose.prod.yml — Stripe__BasePriceId, Stripe__TokenMeteredPriceId, Stripe__SecretKey, Stripe__PublishableKey, Stripe__AllowTestMode, Stripe__SuccessUrl, Stripe__CancelUrl, Stripe__TrialPeriodDays; the Onboarding__* block (Enabled, ProvisioningEnabled, terms/privacy/DPA versions, AllowedRegions__0, GuestRedirectUrl, PowerPagesOrigin, CaptchaSecret, QaCaptchaBypassSecret); Partner__ApiKey; healthcheck -> curl -fsS -m 8 http://localhost:20251/api/v1/admin/auth-config || exit 1 (the /api/health hang restart-looped prod via autoheal); cloudflared config-file mode with prod.config.yaml + credentials.json mounts. cloudflare/prod.config.yaml ALREADY EXISTS on develop — reconcile with the box version from those commits instead of adding a new file, and explain the differences. Three things must NOT be carried: do not delete develop's EntraId__AdminSpaClientId explanatory comment; use Partner__ApiKey=${PARTNER_API_KEY} (NOT ${LINDY_WEBHOOK_SECRET}; the attribute doc says a dedicated secret); do not adopt the typo'd external network "mcp-cognizioware-networkk" — keep develop's network definition and add a comment that the box must be checked (docker network ls) before switching to an external net. All values as ${VAR} or ${VAR:-default}; no literal secrets. Verify: git diff --stat origin/develop..HEAD -- src/ is EMPTY (config only); docker compose -f infrastructure/docker-compose.prod.yml config -q with a throwaway env must validate. Commit by explicit path: fix(prod-compose): Stripe/Onboarding/Partner env wiring, auth-config healthcheck, cloudflared config-file mode (from the CT100 box lineage).
(2) Fix 1b decision: develop's PartnerBillingController has no GET /api/onboarding(/lookup) and SubscriptionStatusResponse already exists in Models/PartnerBillingModels.cs, so Models/DTOs/PartnerBillingResponses.cs from the prod branch is dead code on develop. Do NOT add it. Write the decision (route-name mismatch /api/onboarding vs /api/onboarding/lookup, shape unverified against cognizioware-powerplatform) into design docs/handoffs/pr66-reconciliation-2026-09-06.md together with the measured facts from the plan (identical StripeService, +1.1M resurrected files, src/ -7214) and the list of what the merge would have deleted. Commit on the same branch by explicit path.
(3) Fix 3 on a second branch: git checkout -b fix/admin-loading-states origin/develop (same worktree). Edit ONLY src/McpCognizioware.BillingService/wwwroot/admin/index.html (single self-contained page; keep it dependency-free, CSS @keyframes only): add .spinner and .busy overlay CSS using the existing tokens (--panel, --accent, --muted); add section#bootView.center with spinner + "Loading…" visible on first paint (not hidden) and add "bootView" to the array in show(); add setBusy(cardEl, on) and wrap the fetches in load(), loadAccounts(), showResources() (replace its bare "Loading…" text with the spinner); add withBusy(btn, fn) that disables the button, swaps the label for a spinner and restores in finally, wired to #save, #reload, #acctReload, .delBtn, .delAcctBtn; in the #save restore path call acCheck() rather than re-enabling blindly so the Access-Control/Global-Admin gate is kept. Do NOT auto-spin inside api(). Keep the existing 🛠 Infra Control Center linkbtn untouched. No new network requests. Verify by opening the file's JS mentally and with node --check on the extracted script if node is available; state that a real browser check (Slow 3G throttle: boot spinner before login view, card overlays, buttons spin and re-enable, #save still gated) is the owner's step. Commit by explicit path: feat(admin): loading states — boot spinner, card busy overlays, busy buttons.
HARD RULES: no changes to Program.cs, StripeService.cs, StripeWebhookController.cs, Migrations/**, .github/**; no secrets or key values anywhere (cloudflare/credentials.json is tracked on develop — do not touch it, but flag it in the handoff doc); commit by EXPLICIT PATH (CRLF phantoms — never git add -A); no push; do not close or comment on PR #66 (overseer does Fix 1c) and do not touch CT100 (overseer does Fix 2). Completion = commit hashes per branch, git diff --stat origin/develop..<branch> for both branches, the compose config validation output, and the Fix 1b decision text. Auditor: no git fetch; confirm fix/prod-compose-env touches no src/** and fix/admin-loading-states touches only the admin index.html.
