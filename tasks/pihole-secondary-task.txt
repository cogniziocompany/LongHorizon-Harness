BUILD (repo only; the overseer executes on the Proxmox host): a SECONDARY Pi-hole DNS for the Cognizioware LAN so DNS no longer dies with ptait01. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/pihole-secondary (run `git fetch origin && git checkout -B feat/pihole-secondary origin/main` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls to any host, no docker here; no secrets (the pihole web password is an env name PIHOLE_WEBPASSWORD only).

CONTEXT (Paxton, 2026-09-07): the only Pi-hole (CT105 on ptait01 192.168.21.110, DNS 192.168.21.3) went down with the host today and the whole LAN lost name resolution (GitHub unreachable, lanes and harness runs died). Paxton had to add Google DNS by hand. Decision: a second Pi-hole as an LXC on corsairai300 (Proxmox 192.168.21.151, template local:vztmpl/debian-12-standard_12.12-1_amd64.tar.zst, existing CTs 105 and 110 there - pick CTID 106), static IP 192.168.21.4/24, gateway 192.168.21.1, hostname pihole2, 1 vCPU, 512 MB, 4 GB disk, onboot=1, unprivileged. Primary stays 192.168.21.3; DHCP (router 192.168.21.1) will hand out BOTH: 192.168.21.3 then 192.168.21.4.


NAMING (Paxton, 2026-09-07): mirror the existing primary EXACTLY. The primary is CT105 on ptait01, hostname `pihole`, answers as `pi.hole`, no extra record for itself; its dns.hosts local records use plain names in the lan.easybutt0n.ai zone (htpc01.lan.easybutt0n.ai, ptait09.lan.easybutt0n.ai, ...) and service names directly under easybutt0n.ai (litellm.easybutt0n.ai -> 192.168.21.161 ...), plus dnsmasq address= entries for powerplatform-dev/-uat. The secondary: CT hostname `pihole2`, answers as `pi.hole` like the primary, and carries the IDENTICAL dns.hosts + dnsmasq record set (the teleporter import and the daily sync guarantee this). Do not invent new names or prefixes; the only new records are `pihole.lan.easybutt0n.ai -> 192.168.21.3` and `pihole2.lan.easybutt0n.ai -> 192.168.21.4`, added to BOTH instances (pi.hole stays the per-instance self alias; it is not a shared name).

DELIVER under infrastructure/pihole-secondary/ (one commit each):
(1) create-ct-corsairai300.sh - idempotent bash run ON the PVE host: pct create 106 with the values above (net0 name=eth0,bridge=vmbr0,ip=192.168.21.4/24,gw=192.168.21.1), features nesting=1, start, wait for network, then inside: apt update, install curl, run the official Pi-hole unattended installer with a setupVars/pihole.toml preseed (interface eth0, upstreams 1.1.1.1 + 8.8.8.8, query logging on, web password from $PIHOLE_WEBPASSWORD env), and print the admin URL. Dry-run by default, --apply to execute.
(2) sync/ - keep the two in sync AUTOMATICALLY with Nebula Sync (github.com/lovelaze/nebula-sync, Pi-hole v6 API): a docker-compose.yml to run on corsairai300 (host docker, restart unless-stopped) with PRIMARY=http://192.168.21.3|$PIHOLE_PRIMARY_PASSWORD, REPLICAS=http://192.168.21.4|$PIHOLE_SECONDARY_PASSWORD, FULL_SYNC=true, RUN_GRAVITY=true, CRON="*/15 * * * *" (env names only, values in a gitignored .env.example template), plus a one-shot `sync-now.sh`. Admin happens ONLY on the primary (http://pihole.lan.easybutt0n.ai/admin); the secondary is a mirror and its admin page is never edited by hand - state that in the README. Also provide sync-from-primary.sh (teleporter export/import) as the manual fallback for the initial seed before Nebula Sync runs the first time; document the v5 vs v6 difference. If the primary turns out to be Pi-hole v5 (check `pihole -v`), say so and fall back to the teleporter cron, because Nebula Sync needs v6 on both.
(3) README.md - runbook: why (today's incident), the DHCP change to make on the router (192.168.21.1: DNS servers 192.168.21.3,192.168.21.4), how clients on this PC/the CTs pick up both, the verification (dig @192.168.21.4 github.com; stop CT105 for 30 s and confirm resolution still works), rollback (pct stop/destroy 106, revert DHCP), and the standing rule "DNS never lives only on an inference host".
(4) e2e/suites/38-lan-dns-redundancy.test.js - soft-skip off-LAN: both 192.168.21.3 and 192.168.21.4 answer an A query for github.com within 2 s; fails if only one answers.
HARD RULES: no secrets, no live calls, explicit-path commits, no push. Completion = the four commits, the exact command lines the overseer runs on corsairai300 and ptait01 in order, and the DHCP setting Paxton must change on the router. Auditor: no git fetch; check idempotency (re-running create with CT 106 present is a no-op), that no password appears in files, and that the test soft-skips off-LAN.
