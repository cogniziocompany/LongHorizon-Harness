RESEARCH + OPTIONS (docs first, then a small implementable slice; the overseer picks the option and schedules the follow-up build): relieve the eval-gate delay in the cognizioware-powerplatform promotion pipeline with a HYBRID approach. Today every promotion (promote.yml, pinned sha, dev -> uat -> prod) runs the full primary eval suite serially per environment (16 cases incl. multistep, each up to 4 infra-aware attempts of up to 1200 s), on one self-hosted LAN runner (ct210-pp; a second runner ct210-pp2 was added 2026-09-07 23:25 PT so deploys no longer queue behind gates), against a dev/uat app that serves one eval at a time, with the CE QA gate (cognizioware-qa) as a secondary gate. Observed 2026-09-07: the restore promotion of 845bc89 took >6 h wall clock across dev + uat gates, twice hit NO_RESPONSE-for-1200s infra errors on multi-table-and-data-import, and blocked the Env->Session v2 and guest-slices promotions behind it. Paxton (2026-09-07 23:35 PT): "is there a hybrid approach to relieve the eval gate delay? research this and come up with options".
Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch docs/eval-gate-hybrid (run `git fetch origin && git checkout -B docs/eval-gate-hybrid origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live calls to the lanes, n8n, Dataverse or Stripe; secrets are env NAMES only.
READ FIRST: .github/workflows/promote.yml and eval-gate.yml (jobs, runners, timeouts, the infra-aware retry/classify/exit-3 logic from PR #86), the eval runner under evals/ (case list, tool-level suite, multistep cases, how a case decides NO_RESPONSE, the eval_runs table logging), scripts/deploy + rollout (how many app instances a lane has), design/plans/guest-launch/E2E-TIMELINE-2026-09-07.md, the eval flakiness notes in the evalfix history (git log --grep=eval), and the QA gate contract in cognizioware-qa (READ only at /home/harness/work/cognizioware-qa: qa-gate.yml, infra-aware statuses). Pull the last 30 days of eval_runs outcomes if a dump/fixture exists in the repo; otherwise reason from the workflow logs quoted here.
DELIVERABLE 1 (round 1-2): docs/deploy/eval-gate-hybrid-options.md - a decision memo. Quantify today's critical path (per-stage minutes, where time goes: model latency, build waits, retries, serial cases, runner contention). Then options, each with expected wall-clock gain, risk to the "gates decide" doctrine, implementation size, and what it would NOT catch: (A) tiered gating - fast smoke subset (e.g. 4 cases, <15 min) as the blocking gate per stage and the full suite running async post-deploy with auto-rollback/auto-promotion-hold on regression; (B) parallel cases - run independent eval cases concurrently against N app instances or N ephemeral dev stacks (compose profile per case), bounded by Ollama/LiteLLM capacity (5 cloud keys, kimi pool) and by Dataverse rate limits; (C) promote-once, verify-per-stage - run the full suite ONCE on dev and, for uat/prod, only an environment-difference subset (config, auth, billing wiring, one multistep) since the artifact is the same pinned image; (D) result reuse/caching - skip cases whose inputs (image sha + case version + env fingerprint) already have a green eval_runs row within N hours, with an explicit "force full" input; (E) infra-error fast path - detect NO_RESPONSE early (progress heartbeat, 300 s no-tokens timeout instead of 1200 s), retry on a different model key/pool immediately, and separate "environment sick" (block, page) from "case flaky" (retry); (F) model-side - route eval cases to the fastest healthy pool (the new 5th key, local 3090 span for QA-only cases) with a per-case budget; (G) scheduling - reserve one runner label for gates and one for deploys (now possible with ct210-pp2), and a nightly full-suite run that pre-validates develop so daytime promotions start from a known-green sha. Recommend a HYBRID (likely A+E+G first, then C or D) with a phased plan and the metric to prove it (median promotion wall-clock, p90, false-green rate). Commit: docs(eval): eval-gate hybrid options memo.
DELIVERABLE 2 (round 3-4): the cheapest slice that is safe to ship now, behind inputs/flags defaulting to today's behaviour: e.g. in eval-gate.yml a `suite` input (full|smoke) with a documented smoke case list and the promote.yml wiring to use smoke for the blocking gate on uat/prod ONLY when the same sha already has a full green dev eval (option A+C), plus the NO_RESPONSE early-timeout knob (option E) as an env var with today's 1200 s default. Unit tests for any new script logic; workflow lint if available. Commit: feat(eval-gate): smoke suite input + early no-response timeout (defaults unchanged).
HARD RULES: defaults byte-identical to today's gating; never lower a threshold silently; no workflow change that skips a gate without an explicit input; no secrets; explicit-path commits; no push. Completion = commits, diff stat, the memo's recommendation table, and the exact inputs the overseer would pass to try the smoke path on the next promotion. Auditor: no git fetch; verify defaults unchanged, the smoke path requires a green full dev eval for the same sha, and the memo's numbers cite the workflow/log sources.

EVIDENCE (added 2026-09-08 00:05 PT): promotion 34170161587 (sha 845bc89) failed its UAT primary eval gate TWICE on multistep/multi-table-and-data-import while dev passed 16/16 both times. First: NO_RESPONSE 1200 s on all 4 attempts -> infra-error -> exit 1. Re-run: attempt 1 NO_RESPONSE 1200 s, then a QA_FAIL_MAX exec (153833, 1035 s) logged as outcome=success immediately followed by a second QA_FAIL_MAX line for a different exec id (153859) with 0 s that was logged as outcome=fail -> SUITE 15/16, 1 fail -> gate red. Investigate whether the 0-second duplicate exec read is a classification bug (stale artifact / double read after a retry) and whether the UAT app (single instance, /opt/powerplatform-uat on CT100) starves the multi-table build. Cite eval_runs rows 6691af82, 72a6ec5b, ee694728, 17d2c2cb, 140d676c, 8e235694, 85f12324.

PHASE 2 EVIDENCE (2026-09-08 03:05 PT, third occurrence): promotion 34191344068 UAT gate, case multi-webhook-crud: QA_FAIL_MAX after 752 s (exec 154030) then a 0-second QA_FAIL_MAX read of a DIFFERENT exec id (154048) -> "dead-build signature" -> fail, while the webhook->CRUD step succeeded. Dev 16/16. The gate must dedupe by exec id and never treat a 0-second read as a new attempt; classify such cases as infra/unknown, not fail.
