BUILD + TEST (code; the overseer merges and deploys through the billing lanes dev -> uat -> prod env): make the "seven successful test-mode closes before production" release gate a CONFIGURABLE SETTING in the billing admin web UX instead of a procedural wait. Paxton (2026-09-08 00:20 PT): "for guest access we need to skip the 7-day wait for this pre-production deadline; temp fix this or make it changeable somewhere in a settings in a web ux admin". Today the rule exists only in plans (cognizioware-powerplatform design/plans/cognizioware-guest-launch-plan.md, -handoff.md, guest-launch/E2E-GAP-SLICES: "seven consecutive test-mode day closes with zero unexplained differences" gates the first external PAID release); the only code guard is Configuration/ProdStartupGuard.cs (refuses Stripe live mode unless Stripe__AllowLiveMode=true). Work ONLY in /home/harness/work/mcp-cognizioware on branch feat/release-readiness-setting (run `git fetch origin && git checkout -B feat/release-readiness-setting origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live Stripe calls (test-mode client mocked); env NAMES only.
READ FIRST: src/McpCognizioware.BillingService/Configuration/BillingSettings.cs and ProdStartupGuard.cs (+ tests), Services/NightlyCloseHostedService.cs (how a close is recorded and where its result lives), the admin SPA under src/ (the /admin/ pages served at billing.easybutt0n.ai/admin, Entra SPA 5651b092), the existing admin settings/health endpoints, docs/deploy/billing-lanes.md, and the ensure-customer work on branch feat/billing-ensure-customer if present (avoid conflicts: touch different files).
SLICES (one per round; code + tests + explicit-path commit each):
 (1) settings + model: BillingSettings gains Release { RequiredTestModeCloses (int, default 7), Enforced (bool, default true), Note (string) } bound from Billing__Release__* env / appsettings; a ReleaseReadiness service computes {closesSucceeded, closesRequired, unexplainedDifferences, ready} from the recorded nightly closes (test-mode only, consecutive, zero unexplained differences per the plan's definition); unit tests for the counting rule and the defaults. Commit: feat(billing): release readiness settings + service.
 (2) enforcement point: ProdStartupGuard (or a new ReleaseGuard invoked at the same place) refuses Stripe LIVE mode unless readiness.ready OR Release.Enforced=false, logging which; test-mode operation is NEVER blocked by this gate (guest pre-production runs on Stripe test mode). GET /api/admin/release-readiness returns the computed state + the settings; PUT /api/admin/release-readiness/settings (admin SPA auth) updates RequiredTestModeCloses / Enforced / Note, persisted in the billing DB (additive migration, no data loss) so it survives deploys and overrides the env default; every change writes an audit row (who, when, old -> new). Tests: ready/not-ready/enforcement-off, test mode unaffected, audit row. Commit: feat(billing): release gate enforcement + admin API.
 (3) admin UX: a "Release readiness" card on the admin settings page: closes succeeded / required, last close date + result, unexplained differences, Enforced toggle, required-closes number input, note field, Save; plain-language copy ("Production with live Stripe is allowed when N consecutive test-mode closes succeeded; switch enforcement off only for a pre-production test-mode launch"); component test. Commit: feat(admin): release readiness card.
 (4) docs: docs/deploy/billing-lanes.md section "Release readiness gate" (default 7, how to lower/disable for a test-mode pre-production launch, that live mode still needs Stripe__AllowLiveMode, rollback = Enforced=true), CHANGELOG, and a patch under docs/patches/ for the three powerplatform plan docs replacing the procedural sentence with a pointer to the setting. Commit: docs(billing): release readiness gate.
HARD RULES: test mode never blocked; live mode still double-gated (readiness or Enforced=false, AND Stripe__AllowLiveMode); no secrets; additive migration only; explicit-path commits; no push. Completion = commits, diff stat, test summary, the env NAMES (Billing__Release__RequiredTestModeCloses, Billing__Release__Enforced) and the exact operator steps to set Enforced=false on the dev/uat/prod lanes for the pre-production window. Auditor: no git fetch; verify test mode is never blocked, live mode is still guarded, settings persist in DB with audit rows, and the migration is additive.
