BUILD + TEST (code; hotfix; the overseer deploys via promote-billing dev -> uat -> prod): the prod billing rollout of sha d712263 (mcp-cognizioware develop, PR #85/#87) on CT100 2026-09-08 11:10 UTC recreated mcp-billing-service and the service answered /api/v1/admin/auth-config in 7 ms, but EVERY health route hung: GET /api/health/live, /api/health/ready and /api/health all exceeded 15 s with zero bytes (curl from the host and from inside the container). The container healthcheck (curl /api/health/live, 8 s) therefore failed and the autoheal container restarted the service every ~2.5 min until the overseer rolled back to the legacy image. Logs at the same time: "Secondary database connection test failed (CanConnect returned false). Falling back to primary-only mode." followed by "System.InvalidOperationException: The secondary PostgreSQL database is required but unavailable. Refusing primary-only fallback." (DualDatabase__SecondaryRequired=true in prod; the secondary host is external, port 15526, SslMode=Require). On the dev lane the same image answers /live and /ready in milliseconds. Work ONLY in /home/harness/work/mcp-cognizioware on branch fix/health-live-independent (run `git fetch origin && git checkout -B fix/health-live-independent origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to CT100 or the secondary DB (hermetic tests only); env NAMES only.
READ FIRST: src/McpCognizioware.BillingService/Services/HealthCheckService.cs and the health controller(s) added in PR #85 (git log -p origin/develop -- '*Health*'), Program.cs middleware order (HostRedirectMiddleware from PR #82, request/response logging middleware that INSERTs into http_request_response_logs, any DualDatabase/secondary startup service), the DualDatabase options and where SecondaryRequired is enforced, docs/ops/health-endpoint-hang-2026-09-07.md, deployments/tier/docker-compose.prod.yml (healthcheck), and the legacy live compose semantics described in docs/deploy/billing-lanes.md.
FIND THE ROOT CAUSE FIRST (round 1, no code): explain precisely why /api/health/live blocks when the secondary DB is unreachable (route ordering / a catch-all /api/health controller shadowing /live, a middleware that awaits the dual-write DbContext or the secondary CanConnect on every request, a startup hosted service holding a lock, the request-logging middleware writing to a DbContext that is wired to the secondary, or the HostRedirect path). Reproduce hermetically: an integration test with WebApplicationFactory where the secondary connection string points at a black-hole address (non-routable 10.255.255.1:15526) and SecondaryRequired=true must return /api/health/live 200 within 2 s and /api/health/ready 503 within the configured bound.
SLICES: (1) make /api/health/live truly dependency-free: its own minimal endpoint mapped before any health controller, no DbContext resolution, no middleware that touches the database on that path (short-circuit the request-logging middleware and the host-redirect for /api/health/*); (2) ready: every dependency probe bounded (WaitAsync with a hard cap, default 5 s, configurable Health:ReadyTimeoutSeconds), and when SecondaryRequired=true and the secondary is unreachable return 503 with a JSON body naming the failing dependency instead of hanging or throwing InvalidOperationException; (3) startup: SecondaryRequired must not turn every request into an exception path (the log shows the exception on ordinary requests); decide and document the semantics: refuse to START (fail fast, exit non-zero so the rollout fails cleanly) OR run degraded with ready=503 - implement fail-fast at startup with a clear log line and keep serving nothing (prefer fail-fast; the lane rollout then reports the failure and the previous container is not replaced until the operator fixes connectivity), and add the secondary reachability check to the provision-prod-lane dry-run output (advisory line, no live calls in tests); (4) hermetic tests for all of the above + CHANGELOG + docs/ops/health-endpoint-hang-2026-09-07.md addendum with this incident timeline.
HARD RULES: no schema change; no secrets; explicit-path commits; no push; `dotnet test` green except the known offline DataIntegrity tests (report counts). Completion = root-cause statement, commits, diff stat, test summary. Auditor: no git fetch; verify the black-hole integration test exists and passes, /live has no DbContext dependency, and SecondaryRequired semantics are documented.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
