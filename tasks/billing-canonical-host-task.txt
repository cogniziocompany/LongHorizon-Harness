FIX (repo only; lanes deploy): make billing.easybutt0n.ai the CANONICAL public host of the billing service and deprecate mcp-cognizioware.easybutt0n.ai (kept as a redirecting alias for one release, then removed). Paxton 2026-09-07: the ship plan names billing.easybutt0n.ai; the mcp-cognizioware name was an internal working name that leaked into callers. Work ONLY in /home/harness/work/mcp-cognizioware on branch feat/billing-canonical-host (run `git fetch origin && git checkout -B feat/billing-canonical-host origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live calls. DELIVER: (1) inventory (docs/deploy/host-rename-2026-09.md): every place the old host appears in this repo and in the sibling checkouts you may READ (cognizioware-powerplatform-evalfix: BILLING_API_URL defaults/docs; cognizioware-qa: prod.env docs, admin-config manifest; cognizioware-mcp-tools: any gateway alias/docs) with file:line, plus the out-of-repo items the overseer must change (Entra redirect URIs on app 5651b092 - both /admin/ URIs already present; Cloudflare published routes - both exist; lane env files BILLING_API_URL on the pp dev/uat/prod tiers and the QA runner env). (2) code/config: prod compose + instances.json healthUrl -> https://billing.easybutt0n.ai; CORS/allowed origins and any absolute self-URLs in the SPA/backend include the new host first; the old host answers 308 redirects for browser paths (/admin/*) and keeps serving API paths unchanged until callers are moved (never break the API alias). (3) callers: patch files for the pp and qa repos (docs/patches/*.patch) rather than editing siblings. (4) tests: config/redirect unit tests. Completion = commits, diff stat, the inventory, and the ordered cutover list (change callers first, then flip healthUrl, then redirect, then remove the alias in a later task). Auditor: no git fetch; verify API paths on the old host are NOT redirected in this change.
