AUDIT + TRIAGE (repo only; the overseer merges/deploys and cleans the dev PC): every cognizioware-powerplatform branch that is NOT in production (origin/main) must end up either promoted through the lane, salvaged into a PR against develop, or archived and deleted. Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch docs/branch-register-2026-09-07 (run `git fetch --prune origin && git checkout -B docs/branch-register-2026-09-07 origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no branch deletions from this run (the overseer deletes); never touch /home/harness/venv.

STATE (verified by the overseer 2026-09-07 03:20 PT, 51 remote branches): origin/develop is ahead of origin/main — everything "in-develop" (guest slice 1, nav-archive, the promote/eval-gate fixes, the v2 design docs, the multistep eval-prompt fix pp #77) reaches prod through the pinned promotion the overseer dispatches; the last proof promotion (sha d2e3784) failed the DEV primary eval gate on three multistep cases (see design/evals/multi-webhook-crud-diagnosis-2026-09-06.md and tasks/webhook-crud-fix in the overseer records), so the promotion may need the eval-flakiness fix first. Five remote branches are NOT in develop either:
1. origin/fix/dataverse-mcp-bootstrap-auth (2026-06-10, docs(graphify-skill)…)
2. origin/codex/prod-cicd-handoff (2026-08-09, docs(handoff): RSI cases API verified live; prod-cicd-cutover)
3. origin/evals/tool-level-suite (2026-08-11, docs(deployments): CT100 static-IP DNS gotcha…)
4. origin/feat/code-first-orchestrator (2026-09-05, design(orchestrator): verification fixes — validate_con…) — its content was reconciled as docs/orchestrator-reconciled-v2 and the Phase A work (feat/orchestrator-phase-a-verify-pin, both in develop): confirm what, if anything, is unique.
5. origin/fix/eval-runner-webhook-timeout (2026-09-05, the original #43; superseded by #62 main / #63 develop): confirm nothing unique remains.
Also on the dev PC (not reachable from here; the overseer handles them): local-only branch design/n8n-code-orchestration-20260905 (worktree …-orchestration-design, 2026-09-05 "docs: require BMAD sprint baselines and TRMS conformance") and local fix/dataverse-mcp-bootstrap-auth; WSL worktrees for docs/guest-launch-decisions, feat/login-saved-orgs-mru, feat/web-ux-nav-archive, docs/orchestrator-reconciled-v2, feat/orchestrator-phase-a-verify-pin (all in develop).

DELIVER, one commit each:
(1) design/plans/BRANCH-REGISTER-2026-09-07.md: a table of ALL 51 remote branches: name, last commit date/author, merged into main / develop / neither, unique commits vs develop (`git log --oneline origin/develop..origin/<b>` count and one-line summaries), unique FILES touched, and a verdict per branch: PROMOTE (already in develop → prod via the lane), SALVAGE (has unique valuable content → PR to develop; say what), ARCHIVE (tag `archive/<branch>-2026-09-07` then delete), or DELETE (fully merged, no tag needed). For the five "neither" branches read their unique diffs and decide with reasons; for docs-only branches prefer SALVAGE into the matching docs folder if the docs are still true, else ARCHIVE.
(2) For each SALVAGE verdict: cherry-pick or re-apply the unique content onto a fresh branch `salvage/<name>` from origin/develop in this workspace (small, docs/config only; if code, stop and describe instead), commit by explicit path, and list the branch names in the register so the overseer can push + PR them. Do not push.
(3) scripts/git/branch-cleanup-2026-09-07.sh (idempotent, dry-run by default, `--apply` to execute): for every DELETE/ARCHIVE verdict emits the exact `git tag archive/… <sha>` + `git push origin --delete <branch>` commands; for the dev-PC-only items emits the local `git branch -D` / `git worktree remove` commands with the rule "never `git worktree prune/remove` a WSL-created worktree from Windows — do those from WSL". Commit: chore(git): branch register + cleanup script.
HARD RULES: no pushes, no deletions, no tags created here (script only), no secrets; commit by explicit path. Completion = the register with verdict counts, the salvage branch list, and the dry-run output of the cleanup script. Auditor: no git fetch (the overseer fetched); verify every branch in the register has a verdict and every SALVAGE has a corresponding local branch.
