# TASKS — one harness task per open repo problem on the Ship Plan (2026-09-07), with the solution each run implements

**Asked by Paxton 2026-09-07 00:25 PT:** "create an lh-harness task for each of the repo problems [from the ship plan artifact] and come up with
solutions to resolve." Source: ship-plan artifact https://claude.ai/code/artifact/66bed19d-14a9-4bbf-8c9a-15f77a7fc290 (updated 23:05 PT) plus the
problems found tonight while running its lanes. Follows `TEMPLATE-overseer-hierarchy.md`; overseer manages all four; launches go through the
capacity-aware queue `C:\tmp\launch_queue.py` (queue dir `C:\tmp\queue\`, cap 2 kimi runs on ≥2 healthy keys, one qwen run; busy workspaces skipped).

| # | Repo · problem (from the plan) | Root cause | Solution the run delivers | Run |
|---|---|---|---|---|
| 1 | **cognizioware-powerplatform** — multistep evals fail intermittently (`multi-webhook-crud@prod` 15/16 on run 16; d2e3784 proof promotion red on 3 multistep cases at dev while the same code was green at 476811f) | Provider quota/timeouts inside a multistep case are reported as product "REGRESSION"; the n8n QA agent runs out of iterations on the long verification path | `infra-error` outcome class with retry/backoff and a distinct gate exit code 3 ("blocked by infrastructure"), QA-agent iteration budget + schema-tool checklists for the other two multistep prompts, gate summary that never lies about the product | `fix/multistep-eval-flakiness` in the evalfix workspace, task `C:\tmp\pp-eval-flakiness-task.txt`, kimi trio, 8 rounds |
| 2 | **mcp-cognizioware** — prod was built on the box from a drifted source tree (six CS0246 errors, the #66 "reconciliation trap"); prod has no lane | No prod instance in `deployments/instances.json`, no promotion workflow; hand builds on CT100 | `prod` instance + `ROLLOUT_APPROVED` guard, `promote-billing.yml` dev → uat → prod (pinned sha, QA `billing-*` statuses, `production` environment approval), prod startup guards (live-mode only in prod, APP_VERSION required), lanes runbook | `feat/billing-prod-lane`, task `C:\tmp\billing-prod-lane-task.txt`, kimi, 8 rounds |
| 3 | **cognizioware-mcp-tools** — (a) `:pool` groups 500 on one exhausted account while others answer; (b) doctor missed the OOM-killed runner unit and the hung Ollama span (container "healthy", inference dead 2 h); (c) ops-oauth2-proxy healthcheck uses wget on a distroless image (cosmetic red); (d) RSI adapter tool names long / suite 35B queries a shortcut endpoint that cannot answer | Router retries the same deployment (no per-group fallback, long cooldown); doctor probes container state not inference; wrong healthcheck binary; missing dash-free aliases + wrong query shape | Pool fallbacks + short cooldown (Ollama-family only), three doctor rows (runner units via GitHub API, span inference call, cloud-key quota), healthcheck fix, `opsstatus`/`qarun` aliases + JSON-RPC query in suite 35 | `fix/ops-resilience-2026-09-07`, task `C:\tmp\mcp-tools-resilience-task.txt`, kimi, 8 rounds (waits for the audio QA run to free the workspace) |
| 4 | **cognizioware-qa** — the `litellm-uat` gate went red three times on provider quota errors and blocked the mcp-tools lane ~3 h; every target has the same exposure | Suites treat router 500/429 as product failures; status posted as `failure` | Shared infra-error classifier, retry/backoff, `skipped:infra` subtests, commit status `error` ("blocked by infrastructure") when failed == 0, exit-code 3 contract + `scripts/gate-status.mjs` for callers, env-driven litellm model matrix | `fix/gate-infra-aware`, task `C:\tmp\qa-gate-quota-task.txt`, kimi, 6 rounds |

Already resolved, not re-tasked: "#41 blocked" (merged; #43 → #62/#63), main CI deploy step (pp #79 auto-merge armed), deploy-lane `while read` + ssh
defects (#66–#68), GITHUB_TOKEN cross-repo build (#67), oauth2-proxy identity header (#73), runner OOM (CT203 drop-in), adapters build context (#82).

## Overseer follow-through (each)
Review → PR to the repo's integration branch → the repo's lane (pp: dev lane + CE gate → pinned promotion; billing: develop → dev lane → billing gate;
mcp-tools: main lane E2E → CT204 → QA gate → CT202; qa: master + the callers' promote/deploy workflows updated for the `error` state).
Caller updates after #4 lands: pp promote.yml and mcp-tools deploy-mcp-tools.yml poll steps treat `error` as "retry later".

## Progress
- 00:30 PT: four task texts written; queue entries created; launch queue started (workspaces: qa + mcp-cognizioware + evalfix free; mcp-tools busy until the audio QA run ends).
- 2026-09-07 15:25 PT: **billing prod lane** run 93fdd703 (kimi) complete: feat/billing-prod-lane @ a87d90b - prod instance in deployments/instances.json (CT100 /opt/mcp-cognizioware-prod :20251, requiresApproval), rollout.sh approval guard (ROLLOUT_APPROVED=1), promote-billing.yml (dev status -> release ff -> uat status -> environment-protected production job -> prod branch ff -> rollout -> billing-prod status), startup guards (Stripe__AllowLiveMode only when Billing__Environment=prod; APP_VERSION required), runbook docs/deploy/billing-lanes.md. 300 tests pass. Overseer: PR to develop next; the GitHub `production` environment with required reviewer = Paxton must exist before the first promotion (prod billing still waits for seven test-mode closes).
- 15:30 PT: billing prod lane PR #74 merged to develop (dev lane runs). GitHub `production` environment created on mcp-cognizioware with prax211 as required reviewer and deployment branch policy release/prod, so the first `promote-billing.yml -f is_production=true` waits for Paxton's approval in GitHub. Prod billing rollout itself remains gated on the seven test-mode nightly closes.
- 16:20 PT: pp develop deploy (CI 34165084379, guest slices 1+4) failed "no space left on device" on pp-dev-uat (CT100 on ptait07, 192.168.21.163, 40 GB): 27 sha images, 35 GB unused. One-time relief: `docker image prune -a --filter until=48h` (containers/volumes untouched), then re-run the deploy job. Recurrence fix queued as 06c-pp-rollout-disk-hygiene (rollout.sh pre-pull disk check + keep current+rollback only). Note for the template: deploy targets with 40 GB disks (CT202, pp-dev-uat) need lane-side hygiene - see also 09b for mcp-tools.
- 16:50 PT: **qa gate infra-aware** run f28bec57 complete on fix/gate-infra-aware (e3ca60b, d6a455c, 3702b0c, 66a0575, 14d8143): shared infra/product classifier, retry (QA_INFRA_RETRIES=3, QA_INFRA_BACKOFF_MS=60000), `skipped:infra` + `infra=N` summary, commit status `error` = "blocked by infrastructure" (re-run later) vs `failure` = product regression, gate-status.mjs exit 0/1/3, QA_LITELLM_MODELS matrix + names-only x-litellm logging. PR to main next. Follow-up task: callers (powerplatform promote.yml, mcp-tools deploy-mcp-tools.yml) treat `error` as re-run, not regression.
- 16:55 PT: qa gate PR #6 merged to master (cognizioware-qa). Caller follow-ups queued: 10b (mcp-tools deploy lane) and 10c (powerplatform promote.yml) - bounded retry on `error`, distinct "blocked by infrastructure" outcome; the primary eval gate stays with 06b.
- 17:00 PT: PROD billing /api/health hangs (dev/uat fine) - public https://mcp-cognizioware.easybutt0n.ai/api/health times out. Queued 07c-billing-prod-health-hang (bounded dependency timeouts, live/ready split, probe updates). Not hand-touched.
- 18:45 PT: 06c rollout disk hygiene delivered (23188b2 rollout.sh pre-pull df/system df + thresholds 6 GB / 85%, 48h prune then oldest-first per-instance images keeping current + rollback, KEEP_IMAGES=2 after rollout, --dry-run; f229b5c test harness 9/9; 0f79ba8 README). PR to develop next.
- 19:10 PT: Paxton hit ERR_NAME_NOT_RESOLVED on billing.easybutt0n.ai/admin from the ship plan - that name has no DNS record anywhere (pihole, pihole2, public). The billing admin is https://mcp-cognizioware.easybutt0n.ai/admin/ (200). Ship plan corrected. Option for Paxton: add billing.easybutt0n.ai as a Cloudflare CNAME to the same tunnel if he wants the short name (outward DNS change, his call).
- 20:10 PT: billing.easybutt0n.ai - Paxton made the CNAME proxied. The prod billing tunnel is config-file mode (tunnel cognizioware02-prod, /opt/mcp-cognizioware/cloudflare/prod.config.yaml; the tunnel he named, litellmv3-mcp-cognizioware_uat..., is the UAT one). Hostnames are routed by that file, not by the dashboard, so queued 07d-billing-hostname-ingress (add the ingress entry + docs). DNS: the CNAME must target the tunnel (mcp-cognizioware.easybutt0n.ai or <tunnel-id>.cfargotunnel.com), not the bare apex.
- 20:50 PT: fleet.easybutt0n.ai -> Caddy on CT202 proxies to `fleet-admin:8787`, a live-only 2026-08-09 deploy of the device registry (runner repo fleet-admin/) that was never in the mcp-tools compose and no longer exists on CT202 (likely stopped on the disk-full event and removed by the container prune). Public DNS is grey-cloud 76.51.14.4, LAN split-horizon to .161; result 502. Hydra is the fleet control plane; queued 05e to point fleet.easybutt0n.ai at the Hydra orchestrator and retire fleet-admin in the handoff doc.
- 21:00 PT (Paxton): billing.easybutt0n.ai is the CANONICAL billing host (the ship plan always said so); mcp-cognizioware.easybutt0n.ai is deprecated as an internal working name. Queued 30-billing-canonical-host (low priority, after the prod pushes): inventory of callers, 308 redirects for browser paths, API alias kept until callers move, patch files for pp/qa, ordered cutover. The QA admin gate maps billing as the surface with the old name as a temporary alias.
- 21:20 PT: 05c QA backend resilience delivered (qa PR #7 merged: unless-stopped compose, /api/health wait, infra-blocked reporting, runner start helper, ops doc). 06d prod branch guard delivered (pp PR #88 open; merge after the restore promotion; then seed PROD_PROMOTED_SHA). 05a response cache: slices 1-3 committed, +3 rounds granted for the e2e suite + runbook.

## Eval-gate latency (queued 2026-09-07 23:40 PT as 05h4)
Paxton asked for a hybrid approach to relieve the eval-gate delay (restore promotion >6 h wall clock, NO_RESPONSE infra errors, single runner). Task text `C:	mpval-gate-latency-task.txt`: decision memo with options A-G (tiered smoke gate + async full suite, parallel cases, promote-once verify-per-stage, result reuse, early NO_RESPONSE timeout + key failover, model routing, runner scheduling + nightly pre-validation) and a first safe slice (smoke suite input gated on a green full dev eval for the same sha; early no-response knob; defaults unchanged). Second LAN runner ct210-pp2 already added.

- 2026-09-08 03:05 PT: promotion 34191344068 (cbd0e88) failed its UAT primary eval gate on multi-webhook-crud with the same signature as the two 845bc89 failures: a real attempt (752 s, exec 154030) followed by a 0-second duplicate exec (154048) classified as a dead build -> fail, although the webhook fired and created the Dataverse record (eval_runs 709d3b0a). Dev passed 16/16. Treated as the classification defect, not a regression. Dispatched the sanctioned smoke path from pp #90 (suite=smoke, allow_smoke=true: full on dev, smoke on uat/prod) with the prod approver. Follow-up for the eval gate: the 0-second duplicate exec after a QA_FAIL_MAX / NO_RESPONSE attempt must not be read as a new result (dedupe by exec id, ignore 0 s reads) - add to 05h4 phase 2.

- 2026-09-08 03:25 PT: smoke promotion 34198528281 of cbd0e88 failed its DEV gate: 4 cases returned QA_FAIL_MAX in 0 s ("dead-build signature") without building, 40 min after the same sha passed dev 16/16. Not a regression; app/eval state defect (per-case QA attempt counter or stale exec reads). Stopped retrying promotions. Queued 05h0a-eval-gate-dead-build at the front (pp-evalfix workspace) with all three occurrences as evidence. Prod for restore + Env->Session v2 + guest slices waits on it; dev is unaffected for the click-through.

- 2026-09-08 06:05 PT: 05h0a done (run 57039535): dead-build QA_FAIL_MAX at 0 s -> infra/unknown + fresh-session retry, exec-id dedupe, fresh session per case, gate exit 3 for app-state dead builds, runbook section; 33+6 tests. PR pp #91; chain merges it, waits for the dev deploy, then dispatches the pinned promotion of develop head (full suite) with the prod approver. Hypothesis 3 (unhealthy n8n build worker after back-to-back redeploys) stays open if the signature recurs.

- 2026-09-08 07:05 PT: Ollama key 7 + direct :cloud failover across keys 4-7 LIVE on prod (mcp-tools #101/#103 lane green through prod QA verify); fleet-admin and audioqa containers healthy on CT202. 05h2c delivered (run c56e87ff): shared ce-auth helper clicks the app sign-in button, handles redirect/popup, passkey bypass, stay-signed-in No, timing + diagnostics, qa_runs.signin_timing persisted -> cognizioware-qa PR #12 (chain merges and re-runs the web-ux gate on dev). 09b delivered -> mcp-tools #105 (chain after #104).
