BUILD + TEST (code; QA repo; the overseer dispatches the gate after merge): add end-to-end UX steps for the Fleet Window, because nothing today would notice that it is empty. Work ONLY in /home/harness/work/cognizioware-qa on branch feat/fleet-window-ux-gate (run `git fetch origin && git checkout -B feat/fleet-window-ux-gate origin/master` first); plain git; commit by EXPLICIT PATH; no push; NO live writes anywhere - the flow is READ-ONLY against the fleet window and the harness API; env NAMES only.
WHAT WAS MISSED, and must be caught in future (overseer evidence, 2026-09-08): https://fleet.easybutt0n.ai/ rendered correctly, said "connected", and showed Nodes 0 / Active 0 / Gates 0 / 24h Events 0 with the body text "No fleet nodes registered", while the harness node had five runs in flight and Hydra's own sidebar listed them. `GET https://fleet.easybutt0n.ai/api/fleet/overview` returned {"nodes":[],"runs":[],"gates":[],"counts":{...all 0},"issues":[]}. Root cause: the window is a PUSH plane - it never queries the harness - and the deployed harness build had no reporter module, so there was no producer at all even though the node was enrolled (LH_HARNESS_FLEET_URL/NODE/LABELS/KEY were all present). Every existing check passed because health was 200 and the page rendered; nothing asserted that the page had CONTENT that agrees with the harness.
SLICES (one commit each, explicit paths, following this repo's existing runner-flow and gate-report conventions - copy the web-ux target's structure rather than inventing a new one):
 (1) new gate target `fleet-window` with a runner flow (packages/runner/src/flows/fleet-window.ts or the repo's equivalent path): steps FW-HEALTH (GET /health is 200), FW-OVERVIEW (GET /api/fleet/overview returns the documented shape: nodes, runs, gates, counts{nodes,active_runs,open_gates,events_24h}, issues), FW-RENDER (browser: the page loads, the header chip reads connected, and the four counters are present), FW-CONTENT (browser: when the overview reports nodes>0 the table lists a row per node with its status, and when it reports 0 the page shows the explicit empty state rather than a blank body). Every selector must cite the fleet-admin source file:line it came from, the same rule the web-ux flow now follows. Commit: feat(fleet-window): read-only UX gate flow.
 (2) THE CHECK THAT WOULD HAVE CAUGHT THIS - cross-plane agreement, FW-AGREE: read the harness API directly (base URL and bearer from env names LH_HARNESS_WEB_URL / LH_HARNESS_WEB_TOKEN, no values in the repo) and compare with the window's overview. If the harness reports one or more runs in a non-terminal state and the window reports zero nodes AND zero active runs, the step FAILS with a message naming both numbers and the likely cause ("the report plane has no producer: check that the deployed harness build contains the fleet reporter and that the node is enrolled"). When the harness itself is idle, the step passes and says so. Tag it `drift` so the gate report classifies it. Commit: feat(fleet-window): cross-plane agreement check.
 (3) producer presence, FW-PRODUCER: assert the window has seen a heartbeat or an event inside a configurable window (default 30 minutes) whenever the harness is non-idle, using only what the read API exposes (counts.events_24h and, if present, a node last_seen). This is the check that distinguishes "enrolled but silent" from "reporting". Commit: feat(fleet-window): producer freshness check.
 (4) wire `fleet-window` into qa-gate.yml exactly like the other targets - a job with the same report/tee/artifact conventions and the [tag:] handling - and add it to gate-report.mjs's target list. IMPORTANT: the workflow's workflow_dispatch already carries 9 inputs and GitHub allows at most 10, so do NOT add a new input; derive the base URL from the existing base_url input and the harness URL from a repo variable or an env name. Say in the commit body which you used. Commit: ci(qa-gate): fleet-window target.
 (5) docs: a short section in the QA README and docs/ describing the two planes in one paragraph - Hydra pulls from the harness, the fleet window is pushed to by the harness reporter and the device agents - so the next person reads the empty window correctly; plus how to run the flow locally with env names. CHANGELOG. Commit: docs(fleet-window): two planes and how to run the gate.
HARD RULES: read-only against every live surface; no secrets, env names only; do not add a workflow_dispatch input (10-input cap); do not touch other gate targets; explicit-path commits; no push. Completion = commits, diff stat, unit-test counts, the step -> selector -> fleet-admin source file:line table, and the exact gh workflow run dispatch line for target=fleet-window. Auditor: read-only - verify FW-AGREE genuinely fails when the harness is busy and the window is empty (drive it with fixtures both ways), and that no new workflow input was added.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

EXPERT REVIEW APPLIED (Sonar Pro via the gateway, 2026-09-08; full text in LongHorizon-Harness docs/handoffs/fleet-surfaces-expert-review-2026-09-08.md). Assert to these decisions:
 - The falsifiable parity assertion to implement is exactly this: for every run id the harness reports at time T, BOTH surfaces must show that same run id with the same status within 5 seconds, harness as source of truth; a missing id or a mismatched status on either surface fails with "Consistency check failed: Hydra and Fleet Window UIs do not match harness fleet state within 5s. At least one surface is showing missing or incorrect run statuses." The 5s tolerance covers a ~10s Hydra pull and a ~20s window poll while still catching staleness.
 - Freshness must be visible on the surface itself: a top-right status line carrying both age and cadence, e.g. "Fleet data refreshed 12s ago (push + poll every ~20s)". Assert the element exists and that its age is plausible.
 - Disagreement is a first-class UI state, not only a test result: when the surfaces differ, each shows a non-modal top banner naming the situation, which source is displayed, and pointing at reporter/harness sync. Assert the banner appears when fixtures disagree and is absent when they agree.
 - An unreachable node is an explicit GAP ("Node unreachable - fleet data unavailable since <age>"), visually distinct from degraded; assert both states render differently.
 - Session ids may be truncated in a row for display only, with a copy affordance and the full string in a tooltip/detail; assert the underlying value is the untruncated string.
 - Origin (push vs poll) belongs in the API and diagnostics, not the visible row; assert it is present in the API payload.

ADDITIONAL ASSERTIONS (Paxton, 2026-09-08): the window now also surfaces the harness's three-level memory read-only - per node an Environment (L3) disclosure, a Policies (L2) list, and per run a Traces (L1) view - fed by GET /api/experience/environment, /api/experience/policies and /api/experience/runs/{run_id}/traces, plus the task queue from GET /api/queue. Add steps that assert: each section renders when the fixture API returns data; each shows an HONEST empty state when the API returns an empty collection ("No policies learned yet" rather than a hidden section); and each shows the single quiet unavailable line - never an error banner, never an empty list implying empty memory - when the API returns 404, which is what the deployed node does today. Also assert that every id shown (policy id, trace id, queue id, run id, device id) is the untruncated value from the API, and that an item's origin (seeded vs learned) is visible on L3 items.
