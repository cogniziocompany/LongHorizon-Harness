DOCS + SCRIPTS (no deploy): produce a thorough, least-privilege ACCESS REQUEST PACKAGE that Paxton can execute click-by-click to give the overseer (GitHub prax211 / Entra principal ai-dev03 / the LiteLLM gateway MCP servers) the visibility and control it currently lacks, so routine changes stop waiting on him. Work ONLY in /home/harness/work/LongHorizon-Harness on branch docs/visibility-grants (run `git fetch origin && git checkout -B docs/visibility-grants origin/main` first); plain git; commit by EXPLICIT PATH; no push; no live calls; no secrets (names only).
CONTEXT (verified 2026-09-07 by the overseer): (a) Cloudflare: a cloudflare-mcp server already runs on CT202 (mcp-tools infrastructure/docker/cloudflare-mcp, tools dns_list/upsert/delete_record, tunnel_get/list/list_routes, turnstile_*, access_list_apps; zone aliases via CLOUDFLARE_ZONE_ALIASES; env CLOUDFLARE_API_TOKEN, CLOUDFLARE_DNS_API_TOKEN, CLOUDFLARE_MCP_TOKEN set, CLOUDFLARE_ACCOUNT_ID UNSET). Gaps: no tool writes tunnel published routes (dashboard-managed tunnel c5fdb916 "litellmv3-mcp-cognizioware_uat..." routes mcp-cognizioware.easybutt0n.ai and billing.easybutt0n.ai), account id missing, token scope unknown. (b) GitHub org cogniziocompany: auto-merge disabled on mcp-cognizioware ("Auto merge is not allowed for this repository"); production environments on cognizioware-powerplatform and mcp-cognizioware need a reviewer that the overseer's account can satisfy (prax211 currently approves via `gh api pending_deployments`); branch protection unknown on cognizioware-qa, cognizioware-hydra-fleet, ptait09-easybutt0n-ai, LongHorizon-Harness. (c) Entra: app registrations (SPA 5651b092-e9ac-4595-b112-083df3648633 needed a redirect URI added by hand), group Ai Agents-Dev, ops enterprise app roles; ai-dev01/02/03 principals; no admin role on ai-dev03. (d) Power Platform: environment powerplatform-dev (id 00a55060-2baa-e95b-bcb7-29d717d7c104, org9130dfc5.crm.dynamics.com) is the guest test environment; need environment admin / System Customizer for the test principal and the ability to create sandbox environments. (e) Ollama Cloud: four accounts, keys OLLAMA_CLOUD_KEY_1..4 in mcp-tools.env; quota visible only in each dashboard; no API. (f) Stripe test mode: STRIPE_SECRET_KEY (sk_test) present in the billing lane env; a restricted read-only key would be safer for verification. (g) Proxmox hosts ptait01 (192.168.21.110), ptait07 (.138), corsairai300 (.151): root SSH exists; a PVE API token with least privilege would let Hydra do status/migrate without root.
DELIVERABLE docs/handoffs/visibility-grants-request-2026-09-07.md, one section per platform, each with: WHY (the concrete waits from the last 48 h, cite the task files in tasks/), WHAT exactly (role/scope names as the platform spells them, least privilege, what NOT to grant), CLICK PATH (dashboard breadcrumbs, 1-2-3 steps), WHERE THE SECRET GOES (GitHub secret name / box env file + mode 600 / gateway MCP env name - never chat), VERIFY (a command the overseer runs, e.g. `curl https://api.cloudflare.com/client/v4/user/tokens/verify`, `gh api repos/.../environments`, `az ad app show`, `pac env list`, Stripe `GET /v1/balance`), and ROLLBACK (how to revoke). Include a summary table (platform | grant | blocks today | risk | 5-minute?) and a "do these three first" ordering. Second deliverable: scripts/access/verify-grants.sh that runs every VERIFY step it can with env NAMES only and prints a status table (skip when a name is unset), plus a unit/shell test with fake commands. Third: a ready-to-apply patch under docs/patches/ for cognizioware-mcp-tools adding a `tunnel_upsert_route` tool to cloudflare-mcp (guarded by the existing hostname allow-list and approval gating in lib/guard.mjs; describe the Cloudflare API call for tunnel configurations `PUT /accounts/{account}/cfd_tunnel/{tunnel}/configurations`), with tests, so the route wait disappears once Paxton adds the Tunnel:Edit scope. Also add the request doc's summary to tasks/TEMPLATE-overseer-hierarchy.md under "Standing access" (names only).
SLICES: (1) research + request doc; (2) verify-grants.sh + tests; (3) cloudflare-mcp tunnel route patch + tests; (4) template section + README pointer. Explicit-path commits each. Completion = commits, diff stat, the "do these three first" list. Auditor: no git fetch; no secret values anywhere; every grant names least privilege and a rollback.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
