BUILD + TEST (code; the overseer deploys the cron on corsairai300): make the hivemind ingester carry the valued MSCE experience traces (Phase 1, task 2 of tasks/msce-experience-layer-2026-09-07.md in LongHorizon-Harness). Work ONLY in /home/harness/work/cognizioware-hydra-rsi on branch feat/hivemind-experience-ingest (run `git fetch origin && git checkout -B feat/hivemind-experience-ingest origin/main` first); plain git; commit by EXPLICIT PATH; no push; no live calls to CT103/CT110/the gateway (fixtures only); env NAMES only (MEMORY_MCP_TOKEN, LITELLM_API_KEY, HARNESS_RUNS_ROOT).
READ FIRST: scripts/hivemind-ingest.mjs (what it walks today: run snapshots -> remember_session; content_hash dedupe; cron every 5 min), its tests, docs on hivemind_sessions.session_memories (columns source, source_ref, session_id, repo, branch, tier, occurred_at, text, content_hash, metadata JSONB), and the LongHorizon-Harness spec of role_orchestration/experience.jsonl (one JSON object per round: run_id, task_context_id, round_index, alpha, value, reward_terms {goal,process,satisfaction}, domain_tags, tool_names, error_signature, repo, branch, models, state_summary, action, observation, occurred_at) - you may READ /home/harness/work/LongHorizon-Harness/docs/experience-layer.md and src/lh_harness/experience/trace.py if present; never write there.
SLICES (one per round; code + tests + explicit-path commit each):
 (1) reader: for every run dir the ingester already visits, also read role_orchestration/experience.jsonl when present (tolerate a missing file, a truncated last line, unknown fields); map each trace to a remember_session call with source "lh-harness", tier "run", session_id run_id, repo/branch from the trace, occurred_at, text = a compact rendering (state_summary / action / observation clipped) and metadata {R, V, alpha, reward_terms, domain_tags, tool_names, error_signature, task_context_id, round_index, models}; dedupe by the existing content_hash (stable across re-runs); NO schema change on CT103 - metadata JSONB only. Unit tests with fixture run dirs (valued run, failed run with negative values, missing file, truncated tail, re-ingest = no duplicates). Commit: feat(ingest): experience.jsonl -> session_memories.metadata.
 (2) optional cost enrichment behind a flag (HIVEMIND_LANGFUSE_COST=1): fetch per-run cost from Langfuse by tag lh-run/<id> and add metadata.cost_usd; off by default; network failure is non-fatal. Tests with a fake fetch. Commit: feat(ingest): optional Langfuse cost term.
 (3) a read-side helper scripts/hivemind-recall-valued.mjs: recall(repo, min_value, limit) printing valued traces first (metadata.V desc) with their evidence anchors - the overseer's first retrieval experiment; docs section in the repo README describing the two tasks and how to run the experiment by hand; cron line unchanged (names only). Commit: docs+tool: valued recall experiment.
HARD RULES: no secrets; no schema change; ingest stays idempotent; explicit-path commits; no push. Completion = commits, diff stat, test summary, the exact deploy steps for corsairai300 (git pull on the cron checkout, one manual ingest run, a recall query) and what is UNVERIFIED. Auditor: no git fetch; verify dedupe on re-ingest, tolerance of a missing/truncated file, and that metadata is the only place the new fields land.
