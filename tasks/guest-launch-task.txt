DOCS-ONLY, READ-ONLY ON SOURCE. Cognizioware Guest Launch — Step 1 of the expert handoff: land the planning package, verify the baselines read-only, and produce the decision register + bounded first-slice proposal for Paxton. Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform-guest (branch docs/guest-launch-baseline from origin/develop @ edd45a1); plain git commands only; commit by EXPLICIT PATH (never git add -A); no push. Other checkouts may be READ (git log/status/show, file reads) but never modified, checked out, reset, cleaned or pulled — the handoff forbids it: "Do not reset, clean, force-checkout, pull over or discard local changes".

Read FIRST, in this order, all three already placed (untracked) in design/plans/ of this worktree: cognizioware-guest-launch-handoff.md, cognizioware-guest-launch-plan.md, cognizioware-guest-launch-backlog.md. They are the authoritative planning baseline; the handoff explicitly says implementation is NOT authorized by it — this run does F01 and the P01 decision spike as documents plus the read-only defect enumeration that the handoff's "recommended first implementation sequence" step 1 asks for. No application code, config, workflow, migration, Stripe object or scheduled task is changed by this run.

Facts (verified by the overseer 2026-09-05 22:20 PT):
- The three plan files are untracked in the user's checkout C:\Users\PaxtonTait\source\cognizioware-powerplatform (branch ci/env-promotion) and exist nowhere in git; committing them is part of this task.
- Inspected baselines in the handoff: app working tree C:\Users\PaxtonTait\source\cognizioware-powerplatform\cognizioware-powerplatform (WSL /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform/cognizioware-powerplatform) branch v2-n8n-orchestrator HEAD 2e8558a with 387 dirty paths; clean main d0325562; MCP billing C:\Users\PaxtonTait\source\mcp-cognizioware (WSL /mnt/c/Users/PaxtonTait/source/mcp-cognizioware) branch docs/youtrack-gap-analysis HEAD 4a9824f0 with 137 dirty paths — mcp-cognizioware's integration branch is `develop` (origin/develop is what runs in dev/uat/prod today; prod was rebuilt from develop 2026-09-06). Current pp integration branch is `develop` (this worktree's base); `main` carries the promotion workflow.
- Claims365 reference: /mnt/c/Users/PaxtonTait/source/claims365-lab/post-seminar-zip/Production-Ready-AI-Agents-08-Control-Center/ControlCenter (read-only).
- Live facts the plan did not have: LiteLLM router is on CT202 (litellm.easybutt0n.ai), model groups include the `:pool` Ollama Cloud groups; billing service prod healthcheck is /api/v1/admin/auth-config; the Ops Control Center (ops.easybutt0n.ai) is a separate Blazor infra console — not the guest admin center.

Slices, one per round:
(1) Land the package: `git add design/plans/cognizioware-guest-launch-handoff.md design/plans/cognizioware-guest-launch-plan.md design/plans/cognizioware-guest-launch-backlog.md` and commit `docs(guest-launch): land the expert handoff package (handoff, plan, backlog)`; record sha256 of each file in the commit body (use sha256sum) so Paxton's "hashes match" claim stays verifiable.
(2) F01 baseline record → design/plans/guest-launch/F01-baseline-2026-09-05.md: for each of (a) this worktree base, (b) the nested v2-n8n-orchestrator tree, (c) the user's ci/env-promotion checkout, (d) mcp-cognizioware docs/youtrack-gap-analysis and origin/develop: branch, HEAD, dirty-path count by top-level directory (git status --short | cut -d/ -f1 | sort | uniq -c), commits ahead/behind origin/develop and origin/main, and the package versions that matter (root package.json name/version, packages/backend and packages/frontend versions, litellm pin if any, mcp-cognizioware .NET target + Stripe.net version from the csproj). Then a "material differences from the inspected baselines" section: diff the handoff's file findings (AdminPage.tsx sections, reportUsage call sites, DualDatabaseService secondary-failure handling, StripeService.RecordBillingMeterEventAsync, secret masking) against CURRENT origin/develop of both repos with exact file:line citations — state what still holds, what changed, what could not be found. Never claim a runtime test; this is static inspection.
(3) F03/F04 defect enumeration → design/plans/guest-launch/F03-F04-authorization-defects-2026-09-05.md: enumerate, with file:line on current develop, every authorization/ownership/redaction defect the plan names (billing-check token validation, session ownership, target-environment ownership, step/tool reads, internal-test payment approval, overage confirmation, system eval/harvest privilege, name-only secret masking / DATABASE_URL exposure) plus any you find on the same paths; for each: current behaviour, exploit sketch (one line), proposed fail-closed fix, test that would prove it, backlog id. This is the input to the first authorized code slice.
(4) P01 decision spike → design/plans/guest-launch/P01-portal-and-licensing-2026-09-05.md: compare Power Pages + Stripe hosted Checkout vs the existing SPA as guest front door on: identity path (Entra External ID / B2B guest vs app-local), per-user Microsoft licensing needed for a guest to use Dataverse through the product (cite Microsoft docs URLs; mark anything not verifiable as UNVERIFIED), incremental monthly cost at 1/10/100 guests, implementation effort, rollback, and the $39/user/month economics under both; end with a recommendation and the exact decisions Paxton must make (owner, default, why). Also write design/plans/guest-launch/DECISION-REGISTER.md: every open decision from the plan's "Decisions and release sequence" table and the handoff's "unresolved decisions" list, one row each: id, decision, proposed default, options, what blocks on it, owner, status=OPEN. Finally design/plans/guest-launch/FIRST-SLICE-PROPOSAL.md: the bounded first code slice (authorization/redaction fixes from slice 3 + Claims365-pattern shell/navigation in the existing React admin with "unavailable" panels, no mock data), as a numbered list of concrete changes with files, tests, and acceptance evidence, sized in days, explicitly requiring Paxton's go.
Commits per slice by explicit path: docs(guest-launch): F01 baseline record · docs(guest-launch): F03/F04 authorization defect enumeration · docs(guest-launch): P01 portal/licensing spike, decision register, first-slice proposal. Verify at the end: `git diff --stat origin/develop..HEAD` touches only design/plans/**; `node tests/ci/workflow-guards.mjs` passes if present.

HARD RULES: docs only — no changes outside design/plans/**; no edits to any other checkout; no network calls except reading public Microsoft/Stripe documentation for citations; no secrets, keys, connection strings or customer data in any document (redact anything found in source as [redacted] and cite the file:line instead); do not report static inspection as a runtime test; no push. Completion = commit hashes, `git diff --stat`, and the list of decisions Paxton must make (verbatim from DECISION-REGISTER.md). Auditor: no git fetch; verify docs-only diff, presence of file:line citations in F01 and F03/F04, UNVERIFIED markers where licensing claims lack a citation, and that no document claims implementation happened.
