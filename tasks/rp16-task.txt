BUILD-ONLY. Read the authoritative plan FIRST and follow it exactly: /home/harness/work/cognizioware-hydra-rsi/design/rsi-org-loop/00-rsi-org-loop-architecture.md §3 and /home/harness/work/cognizioware-hydra-rsi/design/rsi-org-loop/10-openwebui-wiring.md §4.2. Work ONLY in this worktree /home/harness/work/cognizioware-mcp-tools on branch feat/rsi-surfacing-adapters from origin/main @ HEAD; never cd into or run git against any other checkout; plain git commands only. This run delivers two thin read-only surfacing adapters for the RSI org loop: ops-status and qa-run. Slices, one per round:
(1) ops-status adapter: add a small MCP or OpenAPI server under packages/ or services/ that calls Ops Control Center GET /api/status sections (doctor, catalog, e2e, ci, cloud, langfuse, actions) with the X-Ops-Ingest-Key header. Return redacted summaries only; do not expose customer payloads, raw cost tokens, or provider secrets. Map each section to a tool name ops_status_<section>.
(2) qa-run adapter: add a small MCP or OpenAPI server that reads cognizioware-qa checkpoint/run results (qa_runs, checkpoints) and returns pass/fail/regression summaries. No test execution from the tool; no mutation of QA runner state.
(3) LiteLLM registration: update infrastructure/litellm-config.yaml to register both adapters under a new access_group rsi-loop (do not reuse fleet-runners, billing, or repo-tools). Ensure the gateway only exposes these tools to callers whose virtual key carries x-mcp-servers: ops,qa or the rsi-loop access group. Cite existing registration patterns at cognizioware-mcp-tools/infrastructure/litellm-config.yaml:214-235.
(4) Verification + docs: run each adapter locally with a throwaway X-Ops-Ingest-Key / QA read token and confirm the responses contain only redacted summaries. Run npm/pip tests if available, otherwise a self-check script. Update design/rsi-org-loop/10-openwebui-wiring.md §4.2 with final tool names and deviations. Verify git diff --stat origin/main..HEAD stays within the adapter source directories and infrastructure/litellm-config.yaml.
HARD RULES: no mutation of Ops Control Center or QA runner state; no customer payloads in responses; no secrets in code or docs; commit by EXPLICIT PATH; no push. Completion = commit hash, git diff --stat origin/main..HEAD, the self-check output showing redacted summaries, and the LiteLLM config snippet. Auditor: no git fetch; confirm the adapters are read-only, the rsi-loop access group is separate from billing/fleet-runners, and no secrets appear.
