BUILD + TEST (repo only): make the cognizioware-qa gates distinguish infrastructure outages from product failures, so a deploy lane is never marked red (or green) because the LLM provider was rate-limited. Work ONLY in /home/harness/work/cognizioware-qa on branch fix/gate-infra-aware (run `git fetch origin && git checkout -B fix/gate-infra-aware origin/master` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls.

EVIDENCE: the `litellm` target (qa-gate.yml job `litellm`, suite in the litellm-acceptance workspace) failed 3 of 6 subtests at 2026-09-06 21:08Z, 23:52Z and 02:11Z with HTTP 500 bodies containing "you (...) have reached your session usage limit" from the Ollama relay - a provider quota wall, not a router regression - and posted `cognizioware-qa/litellm-uat = failure`, which blocked the mcp-tools deploy lane for ~3 hours. The gate passed at 16:42Z before the quota wall and again at 02:35Z after it. The `ce`, `billing` and `web-ux` targets have the same exposure wherever they call a model through the router.

DELIVER, one slice per round:
(1) Shared classifier `packages/runner/src/infra-errors.mjs` (or the equivalent location in this monorepo - find where the suites share helpers): given an error/response, returns `infra` for provider quota / rate limit / router 5xx / connection reset / timeout patterns (session usage limit, 429, "No fallback model group", litellm.APIConnectionError, ECONNRESET, ETIMEDOUT, 502/503/504) and `product` otherwise; unit tests with fixture strings from the evidence above.
(2) Gate semantics: each target's suite wraps model calls with retry (env QA_INFRA_RETRIES default 3, QA_INFRA_BACKOFF_MS default 60000) and, if still infra-failing, reports the subtest as `skipped:infra` with the reason; the summary line (`score=.. passed=.. failed=.. suites=..`) gains `infra=N`; the commit status posted to the caller becomes state `error` (not `failure`) with description "blocked by infrastructure: <reason>" when infra > 0 and failed == 0, and stays `failure` when any product subtest failed. Never count an infra skip as a pass toward the score threshold. Update qa-gate.yml `report-status` accordingly and the README status table.
(3) Caller contract: document for callers (cognizioware-powerplatform promote.yml, mcp-tools deploy-mcp-tools.yml) that `error` means "re-run later, not a regression"; add a small `scripts/gate-status.mjs` that callers can poll which returns exit 0 success / 1 failure / 3 error with the description, matching the eval runner's exit-code convention (3 = infrastructure).
(4) Suite 35-style hygiene for the litellm target: the acceptance suite's model matrix reads from env (QA_LITELLM_MODELS, default the current three) so a limited model can be swapped without a code change; log which deployment/key family answered when the router exposes it (x-litellm headers), names only.
Verification: `npm test` in the workspaces you touch; workflow YAML parses. HARD RULES: no secrets; no live calls; explicit-path commits; no push. Completion = commit hashes, diff stat, the new status-state table (success / failure / error), and a note on which callers must be updated (with file paths in the other repos). Auditor: no git fetch; confirm infra skips never raise the score and that `error` is used only when failed == 0.
