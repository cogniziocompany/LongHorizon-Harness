BUILD + TEST (code; the run pushes and opens its own PR, the overseer merges, deploys fleet-admin through 05h7/19b, and repoints devices): "Fleet emission" - every remote-PC runner function on every fleet device pushes start/finish events to fleet.easybutt0n.ai over socket.io, into the SAME fleet-admin ingest and /fleet namespace the Harness Fleet Window (05h5 = task 19a) builds. Planning session 54360c27 (PTAIT09, 2026-09-07); the full plan is in /home/harness/work/LongHorizon-Harness/tasks/fleet-device-emission-2026-09-07.md - READ IT FIRST (never write there). Also READ the 19a work if present on branch feat/harness-fleet-report in this same repo (socket.io server, requireDevice() middleware, /fleet namespace, read key): REUSE it, do not build a second socket.io server or auth scheme; if 19a is not merged yet, base your branch on it (`git fetch origin && git checkout -B feat/fleet-device-events origin/feat/harness-fleet-report || git checkout -B feat/fleet-device-events origin/easybutt0n-runner-v2`). Work ONLY in /home/harness/work/ptait09-easybutt0n-ai; plain git; commit by EXPLICIT PATH; no push; no live calls to devices, agent-db, or CT202; secrets are env NAMES only (FLEET_ADMIN_KEY, FLEET_READ_KEY, device_key via the enroll flow, FLEET_BRIDGE_URL, RUNNER_EVENT_URL).
CONTEXT (verified 2026-09-07): all four "device repos" (ptait09, ptait-desk03, htpc01, ptait10am5) are checkouts of this ONE repo on diverged branches; easybutt0n-runner-v2 is the superset (49 ahead / 3 behind main). Devices dial OUT only (NAT/tunnel-safe); no inbound path. The C# supervisor svc/FleetAgent.cs owns enrollment, the device key and the HMAC contract (POST /enroll, POST /checkin: HMAC-SHA256 over raw body, X-Fleet-Host + X-Fleet-Signature) but fleet.url is "" everywhere so checkin is off. src/runner.js:190 emitCompletionEvent() is an HMAC-signed fire-and-forget emitter behind RUNNER_EVENTS_ENABLED=1, wired only to agent helpers, NOT to /exec (runner.js:1052). Decisions confirmed by Paxton: (1) promote easybutt0n-runner-v2 -> main, main becomes the single pipeline branch, devices track main; (2) the emitter lives in the C# supervisor (socket.io client, device key, HMAC in one place), fed by the runner over a loopback-only bridge; (3) events = invocation start + finish (tool/shell, host, task id, cwd, status, exit code, duration) and command + truncated output (last ~4 KB, secret-masked); NOT live stdout streaming.
SLICES (one per round; code + tests + explicit-path commit each):
 (1) Phase 0 prep - branch consolidation PLAN + mechanical part only: docs/fleet/main-consolidation.md listing the 3 main-only commits to reconcile into v2, and a per-device-branch triage table (keep / drop / fold) using the plan's representative items (ptait09 graphify scripts keep, patch-cursor*.mjs / qa_ptait09_local_pipeline.js / pm2-resurrect.bat drop; ptait-desk03 restart-runner.ps1 + dedupe graphify; ptait10am5 e2e/harness-e2e.mjs keep, ecosystem.config.cjs drop (PM2 superseded, wrong cwd); htpc01 deploy/install-linux.sh + fix-htpc01-service.sh fold into scripts/onboard-device.sh). Cherry-pick the KEEP items onto your branch where they apply cleanly; list the rest for the overseer. Per-device config stays data: svc/runner.<host>.svc.json + .env; connectors/fleet.json remains the host registry (names only). Commit: docs+chore(fleet): main consolidation plan + kept device scripts.
 (2) fleet-admin (Phase 1): schema fleet.device_events(id, host, task_id, tool, shell, phase, cwd, status, exit_code, duration_ms, command text, output text, truncated bool, ts) with indexes (host, ts desc) and (host, task_id), daily prune > 14 d; device-side socket.io auth on the shared /fleet namespace: handshake {host, nonce, signature} where signature = HMAC-SHA256(host+nonce, device_key) validated by the shared requireDevice() logic (nonce replay-protected); devices emit device.event -> persisted -> fanned out to browser subscribers as device.event; room device:<host>; REST fallback POST /device/events (HMAC path) for hosts without websockets; read API GET /api/fleet/devices (hosts, last event age, live status) and GET /api/fleet/devices/:host/events?limit=&since= behind the optional FLEET_READ_KEY; /health gains last device-event age. Tests (node --test, socket.io-client): handshake accept/reject/replay, persistence, fan-out, prune. Commit: feat(fleet-admin): device event ingest over socket.io + REST fallback.
 (3) runner bridge (Phase 2a): generalize emitCompletionEvent() into emitFleetEvent(task, phase 'start'|'finish'); call it at BOTH ends of every function incl. /exec, startShell, execShell and every entry in AGENT_PROVIDERS + SHELL_NAMES (run_command, claude_prompt/claude_session, cursor_prompt/cursor_session, aider_prompt, opencode_prompt, gemini/codex/copilot/goose/amp/qwen/crush, write_file/read_file/stat_file, get_task/list_tasks/cancel_task); target FLEET_BRIDGE_URL default http://127.0.0.1:7335/events, bridge binds 127.0.0.1 ONLY; shared secret-masking helper (sk-..., ghp_..., Bearer ..., PRIVATE KEY, password=, hex >= 32) applied to command and output before send; output truncated to the last 4 KB with truncated=true; fail-open; keep the existing RUNNER_EVENT_URL webhook working. Unit tests with a fake bridge: every function emits start+finish, masking, truncation, fail-open. Commit: feat(runner): fleet event bridge on every function.
 (4) supervisor client (Phase 2b): svc/FleetAgent.cs gains a loopback HTTP listener on the bridge port (127.0.0.1 only) and a socket.io v4 client (SocketIOClient NuGet in svc/EasySvc.csproj, .NET 10) that connects to fleet.url + /fleet with the device-key handshake, relays device.event with reconnect/backoff and a bounded in-memory queue (drop-oldest, log once), falls back to the REST path when websockets fail, and keeps the existing checkin. svc/*.svc.json gain fleet.url + bridge settings (names/defaults only; fleet.url stays "" until the overseer sets it per device). Tests: xunit or the repo's existing test style with a fake socket.io server (connect, relay, backoff, fallback). Commit: feat(easysvc): socket.io fleet relay.
 (5) docs + rollout: docs/fleet/device-emission.md (architecture diagram runner -> bridge -> easysvc -> fleet.easybutt0n.ai, env names, per-device enable steps via scripts/onboard-device.{ps1,sh}: set fleet.url, enroll, verify with one run_command and watch it appear on the /fleet namespace), README, CHANGELOG. Commit: docs(fleet): device emission rollout.
HARD RULES: one socket.io server and one device auth scheme shared with 19a; bridge and listener bind loopback only; secrets masked before any send; no key values in repos; explicit-path commits; no push; never touch device hosts or PTAIT09. Completion = commits, diff stat, tests, the exact per-device rollout order (fleet-admin deployed via 05h7 FIRST, then devices repointed to main one at a time, PTAIT09 last) and the Phase 0 items left for the overseer. Auditor: no git fetch; verify every runner function emits both phases (grep AGENT_PROVIDERS/SHELL_NAMES coverage), loopback-only binds, masking tests, and that no second socket.io server exists.
