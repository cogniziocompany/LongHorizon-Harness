BUILD + TEST (code; cognizioware-qa): build the end-to-end test for the GUEST PURCHASE JOURNEY. There is currently no automated coverage of it at all - the only written matrix (Easy Button A1-A8, branch codex/easybutton-a1-runner, 293 commits behind) tests a DEPRECATED flow against a deprecated price and a deprecated portal, so it must not be revived or used as a template for what to assert. Work ONLY in /home/harness/work/cognizioware-qa on branch feat/guest-journey-e2e (run `git fetch origin && git checkout -B feat/guest-journey-e2e origin/master` first - this repo's default branch is `master`, NOT main); plain git; commit by EXPLICIT PATH; no push; env NAMES only.

THE JOURNEY UNDER TEST (canonical as of 2026-09-08):
 1. An anonymous visitor with no account and no tenant relationship opens the PUBLIC purchase page in the powerplatform React app - a route outside the AuthGuard, being built in parallel as harness task 53 (branch feat/public-purchase-route, routes /pricing plus the two Stripe return pages moved out of the guard). Write the test against that contract; if the route is not deployed yet, the suite must SKIP with a clear reason, never fail blind and never silently pass.
 2. They pay $39/month through Stripe's embedded snippet using the test card 4242 4242 4242 4242, any future expiry, any CVC, any postcode.
 3. THE EMAIL THEY PAY WITH IS THE ACCOUNT. Generate a unique address per run.
 4. Stripe redirects to the success page, which must render for a SIGNED-OUT visitor and tell them their email is now the account.
 5. Usage metering records against the guest meter, isolated from the task meter.

CANONICAL OBJECTS - assert against these and no others: base price `price_1UCpYaQVmqxwMkjoBX4ATB3L`, metered price `price_1UCpYbQVmqxwMkjoqmvb8wYT`, meter `cognizioware_guest_tokens`, served by GuestSubscriptionController and GuestWebhookController. DEPRECATED, must never appear in an assertion or a fixture: price `price_1TsBP9CXyLTnTERyMFHhpOn7`, SignupController, PartnerBillingController, and the portal easybutt0n.powerappsportals.com. Read ids from env NAMES the suite already uses for lane config; the ids above are stated so you can assert the CONFIGURED value is the guest one and fail loudly with a clear message if a lane is still pointed at the deprecated price.

WHAT TO ASSERT, in order, with dual evidence (UI and system-of-record) at each step - a screenshot alone proves nothing:
 (a) The purchase page renders with NO auth token, NO MSAL account and NO cookies, in a clean browser context. It must not redirect to /login. This is the single most important assertion in the suite, because it is the only state a real buyer is ever in.
 (b) Stripe Checkout completes with the test card and returns to the success route.
 (c) The success page renders for a signed-out visitor and names the email as the account. It must NOT claim the account is ready to sign in if provisioning is still asynchronous - assert the honest wording, not an optimistic one.
 (d) Billing system of record: a subscription exists for that email against the CANONICAL base price, in test mode. Query the billing lane, not Stripe alone.
 (e) Stripe: the subscription and its invoice are in the expected state for a $39 recurring test-mode purchase.
 (f) Metering isolation: a guest usage event increments the guest meter while the task meter does NOT move. Capture both before and after; assert the delta on each. This isolation has regressed before and is the assertion most worth having.
 (g) The cancel path: abandoning Checkout charges nothing, and the cancel page renders signed-out and says so.

PROVISIONING IS OUT OF SCOPE BUT MUST NOT BE FAKED: turning the paid email into an Entra guest, a licensed user and a new Power Apps environment is being designed by others and does not exist yet. Do NOT assert it works and do NOT stub it into a pass. Instead leave ONE clearly-named pending/skipped test per provisioning step with the reason recorded, so the suite documents the gap and turns green honestly the moment those steps land. Say in the completion report exactly which steps are pending.

TEST DATA AND TEARDOWN - non-negotiable, this is money-adjacent:
 - Every run uses a unique marker address, pattern `+gtest<timestamp>` on the existing test mailbox convention, so rows are always attributable and never collide with the older `+ebtest` data.
 - Teardown runs after EVERY run including failures: remove the created billing rows for that marker and delete the matching Stripe TEST customer. Model the SQL on the teardown already documented for the Easy Button runner (onboarding_requests, billing_records, subscriptions, user_organizations by email pattern) but scope it to YOUR marker only - never a broad ILIKE that could catch another run's data.
 - TEST MODE ONLY. The suite must refuse to run and exit non-zero with a clear message if it is ever pointed at a live Stripe key or a production lane. Assert the key prefix and the lane hostname before doing anything else.

WHERE IT RUNS: dev lane first (billing dev on :20252, powerplatform dev app), then UAT. Both had guest execution and charging switched on for the first time on 2026-09-08, so this suite is also the check on whether those switches actually work. Follow the repo's existing suite structure, numbering and reporting conventions exactly - read a neighbouring suite before writing this one and match it rather than inventing a new shape. Use the repo's existing shared sign-in helper where a signed-in step is needed; do NOT write a new login routine.

COMMITS (explicit paths): the suite itself, then the teardown helper, then any lane/config wiring, each separately.

HARD RULES: no Stripe secret key, no live keys, no real card numbers other than Stripe's documented test card; no assertions against the deprecated price, controllers or portal; never run against production; teardown always; explicit-path commits; no push. Completion = commits, diff stat, which assertions pass against the dev lane today, which are pending because provisioning does not exist, and the exact skip reason emitted when the public purchase route is not yet deployed. Auditor: read-only - verify no live key or deprecated id appears anywhere in the diff, that teardown is scoped to the run's own marker and cannot delete another run's rows, that the production guard exists and fails closed, and that no provisioning step is stubbed into a false pass.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
