BUILD + TEST (repo only; the overseer deploys through the main lane): close the four operational gaps found on 2026-09-06/07 in cognizioware-mcp-tools. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch fix/ops-resilience-2026-09-07 (run `git fetch origin && git checkout -B fix/ops-resilience-2026-09-07 origin/main` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no docker here; never call CT202/CT204 live. infrastructure/docker-compose.yml is CRLF - add/replace lines only; verify with `git diff --stat` that only the intended lines changed. Do not touch the LiteLLM image digest.

GAPS (all verified live):
G1 Pool routing: the `:pool` model groups (kimi-k2.7-code:pool, kimi-k3:pool, glm-5.3:pool; 4 deployments each over OLLAMA_CLOUD_KEY_1..4) return HTTP 500 "session usage limit" with "No fallback model group found ... LiteLLM Retried: 2 times" when ONE account is exhausted while others still answer - the router retried the same exhausted deployment instead of another key. Fix in infrastructure/litellm-config.yaml router_settings: for the pool groups set retries across deployments (num_retries >= 3, retry_after / allowed_fails 1, cooldown_time short e.g. 60 s), and add explicit fallbacks `kimi-k2.7-code:pool -> [kimi-k2.7-code:cloud, glm-5.3-flash:cloud]`, `kimi-k3:pool -> [kimi-k3:cloud, kimi-k2.7-code:cloud]`, `glm-5.3:pool -> [glm-5.3:cloud, glm-5.3-flash:cloud]` (Ollama-family only - no paid providers); document why in the config comments. Add unit-ish coverage in e2e suite 34/35 style: a test that parses the config and asserts every `:pool` group has a fallback entry and per-deployment rpm.
G2 Ops doctor blind spots (infrastructure/docker/ops-control-center, the doctor rows): add rows (a) "GitHub self-hosted runner unit" - for each runner in a config list (gh-runner-lan on CT203, ct210-pp/qa/billing on CT210) query the GitHub API runners endpoint (token env name GH_RUNNER_STATUS_TOKEN) and fail when status is offline; (b) "Ollama span inference" - a real generate call to http://192.168.21.110:11442/v1/chat/completions with model qwen3.8:27b, max_tokens 1, timeout 120 s; warn > 30 s, fail on timeout/error (the span hung for 2 h on 2026-09-06 while its container stayed "healthy"); (c) "Ollama Cloud key quota" - one 1-token call per OLLAMA_CLOUD_KEY_n, report OK/LIMIT per key (names only), warn when < 2 OK. Probe code follows the existing doctor row pattern; secrets by env name.
G3 ops-oauth2-proxy healthcheck uses wget which the distroless image lacks -> Docker shows unhealthy while it serves the 302. Replace with a healthcheck the image can run (the proxy's own /ping via its binary if supported, else drop the healthcheck and rely on Caddy's upstream health) - explain the choice in a comment.
G4 Adapter aliases: add dash-free `mcp_aliases` for the RSI adapters (`opsstatus` -> ops_status_mcp, `qarun` -> qa_run_mcp) so their tool names shorten for the Open WebUI reporting agent; update e2e suite 35 to accept both prefixed forms; and fix suite 35B to query the gateway the way it actually works (POST /mcp/ JSON-RPC tools/list with Accept: application/json, text/event-stream and x-mcp-servers) instead of the /mcp/tools/list shortcut that answers "Client must accept text/event-stream" (soft-skip today masks this).
Deliver one gap per round, each with its commit: fix(litellm): pool fallbacks + cooldown; feat(ops): doctor rows for runner units, span inference and cloud-key quota; fix(compose): oauth2-proxy healthcheck; feat(mcp): rsi adapter aliases + suite 35 gateway query. Verification: config YAML parses; e2e suites 34/35 soft-skip off-LAN; ops control center unit tests pass. HARD RULES: no secrets; no live calls; explicit-path commits; no push. Completion = commit hashes, diff stat, the fallback table, and the operator env names (GH_RUNNER_STATUS_TOKEN). Auditor: no git fetch; confirm no paid-provider fallbacks were added, CRLF preserved in compose, and the doctor rows never log key values.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
