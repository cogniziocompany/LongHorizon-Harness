BUILD + TEST (git integration + tests; HIGHEST PRIORITY - this is the change that makes the harness emit its own events to the fleet plane): make LongHorizon-Harness PR #3 (branch feat/fleet-reporter, 17 files, +1792, from run 878831fc) mergeable into origin/main and green. Work ONLY in /home/harness/work/LongHorizon-Harness-b on branch feat/fleet-reporter (run `git fetch origin && git checkout feat/fleet-reporter && git merge origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to the fleet plane or CT110 (hermetic tests only); env NAMES only.
WHY IT CONFLICTS: main moved a long way since the branch was cut - it now carries the queue API and the service-side launcher, the fleet queue MCP tools, the snapshot cache with ETag/304 and ?fields=summary, the e2e happy path, the MCP profiles per role, the auditor lock and the session-id headers. The overseer confirmed the exact conflicts: src/lh_harness/supervisor/service.py and src/lh_harness/webapi/server.py (content), while cli.py, dashboard/state.py and webapi/snapshot.py auto-merge.
RESOLUTION RULES: keep BOTH sides. From main keep the queue endpoints and the background launcher task, the snapshot caching and summary fields, and the role/profile plumbing. From the branch keep the reporter: run and round events, the heartbeat, and the round content push to the fleet plane. Where both sides register startup/shutdown work on the same service object, compose them rather than choosing one - the launcher task and the reporter must both start. Where both add routes to the web API, keep every route from both sides. After resolving, run the FULL test suite with the workspace .venv-dev (never pip into /home/harness/venv) and report the count; main was at 516 passing before this merge, so a lower number means something was dropped.
ALSO IN SCOPE: reconcile the reporter with the session id that main already emits - the reporter's events should carry the same `<run_id>.<round_tag>.<role>` session id used on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so a fleet event can be joined to a Langfuse trace and to the run directory without a lookup. If the branch invented its own id, switch it to the shared helper and say so.
HARD RULES: no behaviour change beyond conflict resolution and the session-id reconciliation; do not touch the deploy scripts; explicit-path commits; single merge commit plus at most one follow-up commit for the session-id reconciliation; no push. Completion = the merge commit, diff stat against origin/main, the full test count, and a list of every route and startup hook that exists on the merged branch, so the overseer can confirm nothing was lost. Auditor: read-only - compare the route list and the startup hooks between origin/main, the branch tip before the merge, and HEAD, and confirm none disappeared; do not run the test suite yourself.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
