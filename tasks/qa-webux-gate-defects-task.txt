BUILD + TEST (code; the overseer merges and re-dispatches the gate): fix the two job-level defects that make the cognizioware-qa `web-ux` qa-gate target fail before any scenario runs (seen in run 34174385728): (1) scripts/gate-report.mjs rejects `--source junit` ("Error: unknown --source junit") - add the junit source (parse the Playwright JUnit XML into the same gate-report rows the other sources produce, with infra-aware statuses) or change the workflow to the source the reporter supports, whichever keeps the report shape; (2) the Playwright spec step fails with `Project(s) "chromium" not found. Available projects: ""` - add a QA-owned playwright config (WEBUX_SPEC_DIR or the runner package) that defines the chromium project the workflow references, with headless defaults and artifact paths under the gate artifact dir. Work ONLY in /home/harness/work/cognizioware-qa on branch fix/web-ux-gate-job (run `git fetch origin && git checkout -B fix/web-ux-gate-job origin/master` first; if PR #8 feat/web-ux-nav-archive-flow is merged, base on it); plain git; commit by EXPLICIT PATH; no push; no live calls; env NAMES only (CE_USERNAME, CE_PASSWORD, QA_ENVIRONMENT_ID). READ FIRST: .github/workflows/qa-gate.yml web-ux job, scripts/gate-report.mjs, packages/runner, docs/web-ux-gate.md, the failed job log excerpt above. Unit tests for the junit parser with a fixture XML; a workflow-lint step (actionlint or the repo's equivalent) if present. Completion = commits, diff stat, tests, and the exact dispatch command (`gh workflow run qa-gate.yml -f target=web-ux -f env=dev -f base_url=https://powerplatform-dev.easybutt0n.ai -f env_id=<QA_ENV_ID_DEV variable>`). Auditor: no git fetch; the workflow references only a project the config defines; reporter handles the junit source with a fixture; no secrets.
