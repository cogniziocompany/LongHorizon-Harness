RESEARCH + AUDIT + PLAN (docs only in this run; no application code): "How close are we to a FULL Cognizioware guest-launch end-to-end test with a real Stripe TEST card, and what is the shortest path to it?" Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch docs/guest-e2e-readiness-audit (run `git fetch origin && git checkout -B docs/guest-e2e-readiness-audit origin/develop` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls to Stripe, Dataverse, n8n or the lanes. You MAY READ (never write, never run git in) the sibling checkouts on this node: /home/harness/work/mcp-cognizioware (billing service: guest billing slice 1 = ledger, tariff, single Stripe writer, hosted Checkout, webhook inbox, nightly close; RP-14 receiver), /home/harness/work/cognizioware-hydra-rsi/design (RSI loop, revenue pipeline specs), /home/harness/work/LongHorizon-Harness/tasks (overseer task records: guest-launch-baseline-2026-09-05.md, guest-launch-slices-2026-09-06.md, env-to-session-flow-2026-09-07.md, ship-plan-integration-spec-2026-09-05.md, rsi-org-loop-and-rp02-2026-09-06.md).

THE TARGET FLOW (Paxton, 2026-09-07 02:55 PT): as a guest customer, using the EXISTING Stripe test customer id we already have, enter a Stripe TEST card in hosted Checkout → a customer account exists in Cognizioware → Power Platform environment(s) provisioned/granted for that customer → open the Env → Session wizard → upload spec files → design phase runs → approval gate (sign-off + internal-test ladder) → build → extract. Usage is metered per token (Stripe billing meter `cognizioware_guest_tokens`, observation-only $0 metered price today) and closed NIGHTLY (settlement at 03:15 LA). Paxton wants to know WHEN this can be exercised end to end and what blocks it.

TWO DESIGN DECISIONS TO FOLD IN (Paxton):
(D-A) E-sign: if the real e-sign integration is not done, use the lightweight path instead — render the exported spec to a PDF (or PDF.js-rendered thumbnail) and record a signature stamp (name, time, hash of the exported spec) on it; that stamped artifact IS the e-sign rung for now. Specify it precisely (library choice already in the repo if any; otherwise pdf.js/pdf-lib; where the artifact is stored; how the ladder verifies it).
(D-B) The "Guide-Rail Ledger" (Paxton 2026-09-07 03:10 PT): not just approvals - a per-solution AUDIT LOG of everything the Cognizioware guide-rail solution deployment process does, stored in the solution's own Cognizio environment variables so it travels with the solution: publisher ensured/reused, solution created or adopted (Greenfield/Brownfield), environment variables written, docs location created/reused, specs stored (names + hashes), each design agent's run (Analyst, PM, Architect, Scrum Master, Stories) with artifact versions, every gate decision (sign-off, ladder rungs incl. the lightweight e-sign stamp, build approval, auto-approvals with the policy that fired), build dispatch/outcome, export/extract (version, sha256), and every CGZ-coded failure with its incident number. Each entry carries a plain-language summary that the orchestrator chat can read back to the user ("here is what happened to Toll Road Fees so far") and that an auto-approve policy can evaluate. Name the pieces with a little creativity but keep them consistent across docs: the log itself (e.g. "Guide-Rail Ledger" / "Solution Passport"), the chat read-back ("Ledger recap"), the auto-approve policy ("Green-light rules"), the e-sign stamp ("Wax seal") - propose the final names in the audit doc and use them everywhere after. Storage: variable name pattern cgz_<Name>_GuideRailLedger (JSON array, newest last), a size cap with rollover into cgz_<Name>_GuideRailLedger_<n> archives, and a pointer variable cgz_<Name>_GuideRailIndex; every solution Cognizioware creates gets it; a second solution or session in the same environment REUSES the existing publisher, docs location and variable structure via get-or-create with deterministic schema names - never a duplicate. Specify the variable set, the entry schema, the get-or-create order, the idempotency checks, who writes (the backend session-setup service and the artifact/gate routes; agents never write it directly), and the read API the chat uses.

DELIVER (docs under design/plans/guest-launch/, one commit per doc):
(1) design/plans/guest-launch/E2E-READINESS-AUDIT-2026-09-07.md — for EACH step of the target flow: what exists on develop today (file:line), what is on a branch/in a running harness task (name it), what is missing, and the operator prerequisites (env names only, never values: Stripe__Guest*, Billing__*, YouTrack__*, BMAD_DESIGN_BROWNFIELD_WORKFLOW_ID, ATTACHMENT_STORAGE_ROOT, the existing test customer id as a NAME reference). Include a "first runnable test" definition: the minimal sequence Paxton can click through on the dev lanes with the test card 4242 4242 4242 4242, and the exact assertions (ledger row, meter event in Stripe test mode, nightly close job outcome, environment grant, session, artifacts, approval-log entries).
(2) design/plans/guest-launch/E2E-GAP-SLICES-2026-09-07.md — the ordered build slices to close the gaps, each with repo, files, size (S/M/L), dependencies, and which existing harness task already covers it (do not duplicate the Env → Session build or RP-14); include the D-A lightweight e-sign slice and the D-B approval-log + reuse slice as concrete specs (schema, API, UI touchpoints, tests).
(3) design/plans/guest-launch/E2E-TIMELINE-2026-09-07.md — an honest "when": critical path in harness-run-days assuming two concurrent runs, what Paxton must do himself (Stripe dashboard items, environment admin, the sign-in proofs), and the first date the full flow can be exercised on dev, then uat, then prod (prod only after the seven successful test-mode nightly closes the plan requires).
(4) e2e/guest-e2e/README.md + e2e/guest-e2e/checklist.json — a machine-readable checklist of the flow's assertions (ids, lane, endpoint, expected) that a later Playwright/QA-gate `guest` target can execute; no secrets.
HARD RULES: docs only; no secrets or ids that are secrets (the Stripe customer id is referenced by env name STRIPE_TEST_CUSTOMER_ID, not by value); cite file:line for every "exists" claim; label anything you could not verify as UNVERIFIED; commit by explicit path; no push. Completion = the four commits, the timeline's headline answer ("first full dev run possible on <date> after slices X, Y, Z"), and the operator list. Auditor: no git fetch; check every "exists" claim has a file:line, the two decisions are specified (not deferred), and the reuse/no-duplicate rule is concrete.
