INVESTIGATE + FIX (repo only; n8n and Dataverse are read-only from here): the multistep eval cases in cognizioware-powerplatform fail intermittently and block promotions. Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch fix/multistep-eval-flakiness (run `git fetch origin && git checkout -B fix/multistep-eval-flakiness origin/develop` first); plain git; commit by EXPLICIT PATH; no push; never commit dist/ artifacts; never touch /home/harness/venv; do not run the live eval suite (no credentials here) - the overseer runs it on the dev lane.

EVIDENCE (verified by the overseer): promotion run 34067791009 (sha 476811f, 2026-09-06 ~17:00-21:00 PT) passed the dev, uat AND prod primary eval gates at 100. Promotion run 34082568730 (sha d2e3784 = the same code plus pp #77, which only changed the multi-webhook-crud qaPrompt in e2e/evals/catalog.mjs and docs) failed the DEV primary eval gate at 05:21Z with three multistep cases marked "fail REGRESSION": multi-mini-app@dev, multi-table-and-data-import@dev, multi-webhook-crud@dev; all micro cases passed. Between the two runs the Ollama Cloud accounts behind the LiteLLM router hit their 5-hour session limits (HTTP 500 "you (...) have reached your session usage limit" from `kimi-k2.7-code:pool` and relays; 3 of 4 keys limited at 20:05-22:12 PT). Earlier in the day multi-webhook-crud@prod failed with "Agent stopped due to max iterations" in the n8n QA agent (executions 150730, 150736, 150809, 152080, 151997, 151193 fail; 150744, 150883, 151106 pass) - see design/evals/multi-webhook-crud-diagnosis-2026-09-06.md. #62/#63 already fixed the eval runner waiting past undici's 300 s cap and sweeping orphaned eval_runs.

DELIVER, one slice per round:
(1) Diagnosis doc design/evals/multistep-eval-flakiness-2026-09-07.md: read e2e/evals/run.mjs, e2e/evals/catalog.mjs, the eval-gate workflow (.github/workflows/eval-gate.yml) and the n8n workflow JSON that runs the QA agent (n8n-workflows/live/*.json) and answer: (a) how an LLM/provider error (HTTP 429/500 quota, timeouts) inside a multistep case is classified today - is it reported as "fail REGRESSION" (a product verdict) or as an infrastructure error? cite file:line; (b) why micro cases survive the same conditions (fewer LLM calls?); (c) what "REGRESSION" means in the gate (compare against baseline?) and where the baseline comes from.
(2) Fix A - classify infrastructure failures honestly: in e2e/evals/run.mjs (and the gate summary), an attempt whose failure reason matches provider quota/rate-limit/timeout patterns (session usage limit, 429, litellm.APIConnectionError, ECONNRESET, 5xx from the router, n8n execution timed out before the QA verdict) must produce outcome `infra-error` (not `fail`), be retried up to N times with exponential backoff (N and delay from env EVAL_INFRA_RETRIES / EVAL_INFRA_BACKOFF_MS, defaults 3 / 60000), and if still failing, mark the case `infra-error` so the gate reports "GATE BLOCKED (infrastructure)" with exit code 3, distinct from a product regression (exit 1). Never convert an infra-error into a pass. Unit tests with fixture error strings.
(3) Fix B - the QA agent's iteration budget: in the n8n workflow JSON, the QA agent node's max-iterations and the prompt are what produce "Agent stopped due to max iterations"; raise the budget where the JSON defines it (document the value) AND tighten the three multistep qaPrompts in catalog.mjs the way #77 did for multi-webhook-crud (schema-tool-only verification checklist, explicit tool names, no form-XML/app-tool paths) for multi-mini-app and multi-table-and-data-import. Do NOT push to n8n - the overseer imports the JSON; document the import step.
(4) Fix C - gate resilience: eval-gate.yml gets `EVAL_INFRA_RETRIES` / `EVAL_INFRA_BACKOFF_MS` inputs, and a step that, when the run ends with exit code 3, posts a "blocked by infrastructure" summary (not a red regression) and fails the job with a clear message so the promotion stops without lying about the product. README (e2e/evals/README.md) outcome table gains `infra-error`.
Verification: unit tests for the classifier and retry loop (node --test), `npm run build` in packages/backend passes, workflow YAML parses. HARD RULES: no secrets; no live calls; do not change .github/workflows/promote.yml semantics beyond the gate summary; commit by explicit path; no push. Completion = commit hashes, the diagnosis answers (a)-(c) with file:line, the new outcome table, and the exact operator steps (n8n import, env inputs). Auditor: no git fetch; confirm infra errors can never become passes and that exit codes 1 vs 3 are distinct.
