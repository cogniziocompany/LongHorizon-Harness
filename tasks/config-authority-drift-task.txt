BUILD + TEST (code + docs; cognizioware-mcp-tools): make it impossible to edit a configuration file that does nothing. Several config surfaces in this fleet are DATABASE-BACKED at runtime, so the committed YAML is a SEED, not the authority - and nothing in the file says so. Annotate every such surface with where the live value actually lives and how to change it correctly, and add a drift check that fails when the file and the database disagree. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/config-authority-and-drift (run `git fetch origin && git checkout -B feat/config-authority-and-drift origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO writes to any live database or router; env NAMES only.

WHY, WITH THE INCIDENT THAT PROVES IT (2026-09-08): the LiteLLM router was benching a model for 300 seconds after 2 failures, so a momentary rate limit became a five-minute outage and every harness run that drew that model died at round zero. TEN RUNS were lost. The overseer edited `infrastructure/litellm-config.yaml`, set `cooldown_time: 20` and an `allowed_fails_policy`, restarted the router, verified the file was correct INSIDE the container, probed three models and got 200s - and the next run failed with the identical error. The edit was INERT. `general_settings.store_model_in_db: true` means LiteLLM loads its config from Postgres, and the `LiteLLM_Config` table's `router_settings` row still held `allowed_fails: 2, cooldown_time: 300`. The fix only worked once it was applied through `POST /config/update`, which writes the database. An entire afternoon was lost to a file that reads correctly and does nothing.

THE TRAP IS WORSE THAN ONE ROW - facts observed live, use them as your starting inventory and VERIFY each:
 - The `LiteLLM_Config` table holds at least: `router_settings`, `general_settings`, `litellm_settings`, `mcp_servers`, `guardrails`. Each shadows the same-named block in the YAML.
 - **The same setting exists in two rows with different values.** `general_settings` held `allowed_fails: 5, cooldown_time: 60` while `router_settings` held `allowed_fails: 2, cooldown_time: 300`. Which one the router honours is NOT obvious from either file or table, and the YAML at line ~22 carries a comment asserting they "live in router_settings (2 / 300s) - not duplicated here", which the database contradicts. Determine and document which actually wins, with evidence.
 - **THE WRITE API SILENTLY DROPS KEYS IT DOES NOT KNOW.** On 2026-09-08, `POST /config/update` was called twice with `router_settings.allowed_fails_policy` (the per-exception thresholds that would stop a rate limit benching a deployment). BOTH returned **HTTP 200**, and BOTH times the key was absent from the `LiteLLM_Config` row afterwards - no error, no warning, no partial write. `cooldown_time` and `allowed_fails` in the same payloads DID persist, so the request shape was right; the key is simply not in this version's accepted schema and is discarded. A 200 from this API is NOT evidence the write took effect. The drift check and the e2e suite must therefore VERIFY BY READING BACK, never by trusting the response code, and the reconcile procedure must read back and diff after every write. Determine whether `allowed_fails_policy` is supported at all on the pinned version and say so plainly.
 - `routing_strategy` was `least-busy` in the YAML and `simple-shuffle` in the database. Silent divergence in the thing that decides which deployment serves every request.
 - `model_list` behaviour differs from settings blocks: hand-added models DID take effect from the file. Establish and document exactly which keys are file-authoritative, which are database-authoritative, and which merge.

SLICES (one commit each, explicit paths):
 (1) INVENTORY, evidence-based. For every top-level key in `infrastructure/litellm-config.yaml`, determine its authority: FILE (the file wins), DB (the database wins and the file is a seed consumed only on first load), or MERGE (state the merge rule). Do NOT guess from documentation - derive it from the LiteLLM version actually pinned in this repo's compose file, citing the code path or the released docs for that exact version, and say plainly where you could not establish it rather than asserting. Write the result as a table in `docs/config-authority.md`. Commit: docs(config): authority inventory for the LiteLLM config surfaces.
 (2) ANNOTATE THE FILE. A prominent header block at the top of `infrastructure/litellm-config.yaml` stating that `store_model_in_db: true` makes the database authoritative for the settings blocks, that editing them here and restarting DOES NOTHING, and naming the correct change path. Then a short comment ABOVE each DB-backed block naming the table and row that shadows it and the one-line command that actually changes it. Keep it terse - a wall of text gets skipped. Correct or delete the existing line ~22 comment, which is now known to be wrong. Commit: docs(litellm): mark the database-backed blocks in the config.
 (3) THE DRIFT CHECK - this is the part that survives people not reading comments. A script (follow the repo's existing tooling conventions and language) that reads the committed YAML and the live `LiteLLM_Config` rows and reports, per DB-backed key, whether they agree. READ-ONLY: it must never write to the database or the router. It needs the master key by env NAME and a database URL by env NAME; it must refuse to run and exit non-zero if either is missing rather than silently comparing nothing. Output a diff a human can act on - key, file value, database value - and exit non-zero on divergence. Commit: feat(ops): LiteLLM config drift check.
 (4) E2E SUITE - this is a required deliverable, not an afterthought. Add a suite that VALIDATES CONFIG AUTHORITY end to end, following the numbering and structure of a neighbouring suite in this repo's e2e directory (READ ONE FIRST and match its shape, naming and reporting exactly - do not invent a new pattern). It must assert, against the live gateway using read-only calls: that every key the inventory in slice 1 marks DB-backed actually agrees between the committed YAML and the live `LiteLLM_Config` rows; that the drift check exits non-zero when they diverge and zero when they agree; and - the assertion that would have caught 2026-09-08 - that `router_settings.cooldown_time` and `allowed_fails` in the DATABASE match the committed intent, so a future inert file edit is caught by a failing suite rather than by ten dead runs. Include the two-rows-one-setting hazard: assert that `general_settings` and `router_settings` do not disagree with each other on any key they share, because today they do. NO WRITES of any kind. Commit: test(e2e): config authority and drift suite.
 (4a) LANE WIRING. Run the drift check in the deploy lane so divergence is reported at deploy time. It must NOT hard-fail the lane on day one - divergence exists right now and blocking every deploy on it would be its own outage. Emit a WARNING with the diff in the step summary and leave a single clearly-marked switch to promote it to blocking once the known divergences are reconciled. State in the report exactly which divergences exist today. Commit: ci(lane): report LiteLLM config drift.
 (4b) SURFACE IT WHERE A PERSON ACTUALLY LOOKS. The Ops Control Center in THIS repo is the operator surface: `infrastructure/docker/ops-control-center/`. Verified 2026-09-08, it exposes exactly SEVEN routes - `/`, `overview`, `catalog`, `e2e`, `certs`, `admin-centres`, `audit`. THERE IS NO `/doctor` ROUTE: the doctor is a python script (`doctor/ops_doctor.py`) reading `doctor/targets.json`, and its results are RENDERED ON `/overview` (`Components/Pages/Overview.razor`, which carries the 'Run doctor' button), with slices of it on `certs` and `admin-centres`. So adding a row to targets.json is necessary but NOT sufficient - confirm the new row actually appears on /overview and say how you confirmed it. Add a config-drift row: green when the file and database agree on every DB-backed key; on divergence, name the specific keys and the file-versus-database values, not a bare red light. Read an existing target and match its schema, cadence and failure-text style exactly. READ-ONLY; never print a key, token or database URL.

 (4c) DISK, THE HOLE THAT CAUSED THE OUTAGE. `infrastructure/docker/ops-control-center/doctor/targets.json` has 45 entries across gateway, compose, off_box, fleet, dns, certs and admin_centres, and **NOT ONE of them checks disk** - `ops_doctor.py` never calls df or statvfs. On 2026-09-08 CT202's root filled to 100% with zero bytes free: it killed containers mid-write, left stale docker network endpoints that survived forced disconnects, took the PROD LiteLLM router down for roughly an hour, and killed every harness run with 'connection refused'. None of it was visible on any operator surface until a deploy failed. The only figure anywhere was a hand-typed '78%' in a status page, already stale by the time it mattered.
   Add disk capacity rows to the doctor (and confirm they render on /overview) for every box that carries a service in this registry - at minimum CT202 (the gateway), CT204 (uat) and CT210 (the runners) - with a WARN threshold well before the cliff (80%) and a CRITICAL one that still leaves room to recover (90%), and show the actual free figure rather than a bare colour so a person can see it trending. Read an existing row and match its schema, cadence and failure-text style exactly. READ-ONLY, and never print a key, token or connection string. If the doctor's check contract cannot express a threshold, say so and surface the raw figure with a pointer.
   State in the completion report what each of those disks reads TODAY, so the first run of this check is itself the baseline.

 (4d) THE HARNESS IS INVISIBLE IN OPS - add a HEALTH row, not a second wallboard. On 2026-09-08 Paxton looked for the lh-harness in the Ops Control Center and found nothing: harness state exists only in Hydra and fleet.easybutt0n.ai. Hydra's view is a flat list of run ids with a status, no time grouping and no failure reason, so a single one-hour outage that killed 23 runs reads as a wall of unrelated red.
   DO NOT build a run wallboard here - fleet.easybutt0n.ai owns that by explicit design and duplicating it is the mistake this estate keeps making. Add ONE doctor row answering the INFRASTRUCTURE question ops is for: is the harness healthy? Sensible signals, all read-only against the harness API on CT110 (`http://192.168.21.168:8799`, bearer by env NAME): the service answers, the node is registered and online, how many runs are active, how deep the queue is, and how many runs failed in the last hour. Green when it answers and recent failures are not spiking; on trouble, name the numbers rather than showing a bare red light. Confirm it renders on /overview.
   In the completion report, state where an operator should look for WHICH question - infrastructure health in ops, run detail in the fleet window, device and control in Hydra - so the three surfaces stay distinct instead of converging.

 (5) THE RECONCILE PATH. A documented, dry-run-by-default procedure for pushing the committed YAML values into the database through the supported API when a human decides the file should win - `POST /config/update` merges rather than replaces (verified 2026-09-08: updating two keys left all 21 fallback chains and 5 context-window fallbacks intact), so document that it merges and warn that a naive full-object PUT would destroy the fallback chains. Include taking a backup of the affected row FIRST and verifying the backup is non-empty before changing anything - an attempt on 2026-09-08 produced a zero-byte backup through a quoting error and nobody noticed until afterwards. Commit: docs(ops): reconcile config drift safely.
 (6) LOOK FOR THE SAME CLASS ELSEWHERE. Search this repo for other committed configuration that is overridden at runtime by a database, an admin UI, or an environment file on a box - the same "edit does nothing" shape. Candidates worth checking rather than assuming: anything with a `store_*_in_db` flag, anything an admin UI can edit, and any compose `env_file` whose values are also committed in an example file. Record what you find in `docs/config-authority.md` under a clearly separate heading, and if you find nothing say so explicitly - a short honest list beats a padded one. Do NOT fix them in this task; name them. Commit: docs(config): other runtime-overridden configuration.
 (7) TESTS: the drift check reports agreement when values match, divergence when they do not, exits non-zero on divergence, refuses to run with a missing env name, and never issues a write. Use fixtures or a fake database - NO live calls. Report the count and state the baseline first. Commit: test(ops): config drift check.

HARD RULES: read-only against every live system - this task must not write to the LiteLLM database, the router, or any box; no credentials, master keys, database URLs or tokens in the repo or in any output - env NAMES only, and redact anything key-shaped in diff output; do not "reconcile" the live divergences as part of this work, only report them; do not restart any service; explicit-path commits; no push. Completion = commits, diff stat, test counts, the authority table, the list of divergences that exist today, the current disk figure for every box you added a row for, what the new doctor row shows RIGHT NOW against the live gateway, and a one-paragraph answer to: if someone edits this YAML next month and restarts the router, what now tells them whether it will take effect? Auditor: read-only - verify the drift check, the e2e suite and the doctor row all contain no write path and no key literal, that the e2e suite asserts the database values rather than the file's, that the lane wiring warns rather than blocks, that the annotations name a specific table and command rather than a vague warning, and that the wrong line ~22 comment was corrected or removed.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
