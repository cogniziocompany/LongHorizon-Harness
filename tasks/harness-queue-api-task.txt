BUILD + TEST (code in the lh-harness repo; the overseer deploys to CT110 at an idle window and runs the happy path live): move task queuing INTO the harness service and make it reachable from the fleet, add a standard e2e happy path, and fix the slow web UX when switching runs. Work ONLY in /home/harness/work/LongHorizon-Harness on branch feat/queue-api-and-ux-speed (run `git fetch origin && git checkout -B feat/queue-api-and-ux-speed origin/main` first); plain git; commit by EXPLICIT PATH; no push. HARD: never pip-install into /home/harness/venv (the SERVICE venv, root-owned); create `python -m venv .venv-dev && .venv-dev/bin/pip install -e .[dev]` inside the workspace and run tests with it; never restart the lh-harness service (the overseer deploys); tests must not need the live service.
CONTEXT: today the overseer keeps a queue of task JSONs on a Windows PC (C:\tmp\queue: {name, task_file, workspace, max_rounds, trio, base_check}) and a launcher script polls capacity (kimi trio needs >=2 healthy Ollama Cloud keys and <3 kimi runs; qwen trio = QA only, one at a time; busy workspaces skipped) and POSTs /api/runs. That is the anti-pattern: the queue must live in the service so any client (Open WebUI fleet chat via the LiteLLM MCP gateway, Hydra fleet view, curl) can enqueue and watch. The web UI (webapi/server.py, dashboard/) is slow when switching runs: the snapshot endpoint (_snapshot_for ~line 533; snapshot ~850) rebuilds from events.jsonl + round artifacts on every call and the UI refetches everything on each switch.
SLICES (one per round; each = code + tests + explicit-path commit):
 (1) Queue model + API: POST /api/queue {name, task (text) | task_file (server path), workspace, max_rounds, roles|trio, priority, base_check?, requested_by} -> {queue_id}; GET /api/queue (pending/launched/done/failed with reasons); DELETE /api/queue/{id}; POST /api/queue/{id}/priority. Persist under the runs root (.lh-harness/queue/*.json, atomic writes). Trios are named server-side in config.toml ([queue.trios] kimi=..., qwen=...) with the capacity rules above expressed as config ([queue.capacity] kimi_max=3, qwen_max=1, min_healthy_keys=2, key_health_url=...). Auth = the existing bearer token.
 (2) Launcher inside the service: a background asyncio task that every N seconds evaluates capacity (key health via the configured probe, active runs per trio, workspace busy) and launches the highest-priority eligible entry through the same code path as POST /api/runs; records launch/skip reasons on the entry; never launches into a workspace with an active run; idempotent across restarts. Emit queue.launched / queue.skipped events for the UI.
 (3) MCP exposure: extend the existing fleet MCP surface (see mcp_profiles.py / webapi) with tools harness_enqueue_task, harness_list_queue, harness_run_status, harness_resolve_gate (ASCII user_input only) so the LiteLLM MCP gateway can alias it as lhharness; tool descriptions carry the operating rules (scoped task text: repo, branch, deliverables, hard rules; kimi for dev, qwen for QA only; prod deploys through lanes). Document the gateway alias + access group in docs/ (names only).
 (4) Web UX speed: (a) cache the snapshot per run keyed by (events.jsonl mtime+size, rounds dir mtime) and serve ETag/304; (b) split the snapshot: GET /api/runs/{id}/snapshot?fields=summary for the run list/switch, full transcripts only on demand (/rounds/{n}), gzip responses; (c) the dashboard fetches the summary first and streams the rest; (d) a benchmark script scripts/bench_snapshot.py printing p50/p95 for a run with 16 rounds before/after (target: switch < 300 ms server-side on the cached path). Keep the existing API shape backward compatible.
 (5) e2e happy path e2e/happy_path.sh + e2e/happy_path.py (repo-only, throwaway git workspace + the echo/stub test agent used in tests): start the service on a free port -> POST /api/queue -> launcher starts the run -> run reaches the completion gate -> resolve stop -> assert report + queue entry done + events include queue.launched. Runs in CI (make e2e-happy / pytest marker) in < 2 min. This is the reusable "path of one change" check.
 (6) Docs: docs/queue.md (API, config, capacity rules, how Open WebUI / Hydra / curl enqueue), docs/ux-performance.md (what was slow, what changed, bench numbers), CHANGELOG entry; the release checklist for CT110 (idle window; config.toml additions; verify /api/queue).
Completion = commit hashes, git diff --stat origin/main..HEAD, test + bench summary, and the exact config.toml block the overseer adds on CT110. Auditor: no git fetch; verify no code path installs into /home/harness/venv, the launcher never double-launches into a busy workspace, the API stays backward compatible, ASCII enforcement on resolve.
