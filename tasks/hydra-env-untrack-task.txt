FIX (cognizioware-hydra): stop tracking `.env` in git and move secret delivery to the host. Work ONLY in /home/harness/work/cognizioware-hydra on branch fix/untrack-env-secrets (run `git fetch origin && git checkout -B fix/untrack-env-secrets origin/main` first); plain git; commit by EXPLICIT PATH; push and open the PR against main, then STOP - never merge.

THE DEFECT, read from GitHub 2026-09-21 17:10 PT: commit 13356dd "fix(deploy): track .env so production gets DEVICE_TOKENS and HYDRA_API_KEY" force-added `.env` (995 bytes, blob 05084aff) over the repo's own `.gitignore` (line 4 is `.env`). Live HYDRA_API_KEY, DEVICE_TOKENS and CLOUDFLARE_TUNNEL_TOKEN are therefore in git history on `main`. On 2026-09-21 the interactive session delivered four NEW GitHub-App secrets (APP id 5026860) to corsairai300 `/opt/cognizioware-hydra/.env` and `secrets/lh-reviewer.pem` BY HAND ON THE HOST, precisely so they would not be committed - that is the pattern to keep.

SLICES:
 (1) `git rm --cached .env` (file stays on disk); confirm `.gitignore` already excludes it; add `.env.example` carrying every variable NAME the compose/deploy path reads (HYDRA_API_KEY, DEVICE_TOKENS, CLOUDFLARE_TUNNEL_TOKEN, and the four lh-reviewer App names present in the host .env) with EMPTY values and a one-line comment each. NO real values anywhere in the diff. Commit: fix(secrets): untrack .env; ship .env.example instead.
 (2) Find every place the deploy path assumes `.env` arrives via git checkout (the Hydra deploy lane, compose files, docs, scripts) and change it to assume the host-managed file at /opt/cognizioware-hydra/.env - a pre-deploy check that fails LOUDLY, naming the missing variable, when a required name is absent. Include a unit test for the check. Commit: fix(deploy): require host-managed .env; fail loudly on missing names.
 (3) docs: a short section in the deploy README saying secrets live on the host, how to add one, and that `.env` must never be re-added (`git add -f` is the trap). Commit: docs: host-managed secrets.

HARD RULES: do NOT rewrite history, do NOT force-push, do NOT rotate or print any secret value; do NOT touch /opt/cognizioware-hydra/.env on the host; the running Hydra stack must not be restarted by this task. Rotation of the exposed values is Paxton's separate call and is NOT a blocker here. Completion = the PR URL, diff stat, test count, and the exact grep proving no secret value is in the diff. Auditor: read-only - verify `.env` is no longer tracked, `.env.example` holds names only, and no secret string from the old tracked file appears anywhere in the branch diff.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace. MANAGER and EXECUTOR run on the `default` MCP profile. Quote the run id in the completion report. Before declaring completion, `git fetch origin && git merge origin/main` so the branch is not stale, and report the merge result.
