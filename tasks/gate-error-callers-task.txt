FIX (repo only; lanes deploy): the cognizioware-qa gate now posts commit status `error` with description "blocked by infrastructure: ..." when the only failures were provider/router infra (quota walls, 429, 5xx, connection errors), `failure` only for product regressions, and `scripts/gate-status.mjs` exits 0/1/3. Callers still treat anything not `success` as a regression. Update the mcp-tools caller: work ONLY in /home/harness/work/cognizioware-mcp-tools on branch fix/qa-gate-error-status (run `git fetch origin && git checkout -B fix/qa-gate-error-status origin/main` first); plain git; commit by EXPLICIT PATH; no push; no live calls. In .github/workflows/deploy-mcp-tools.yml every place that polls a cognizioware-qa status (qa-gate-uat, qa-verify-prod, smoke): on `error` with that description, retry the dispatch up to 2 times after 10 minutes (sleep + re-dispatch with the same inputs), and if still `error` fail the job with `::error::QA gate blocked by infrastructure (not a product regression) - re-run later` and a distinct step outcome so the overseer can tell it apart; on `failure` keep failing as today. Add the same handling as a reusable bash function in a script under infrastructure/scripts/ used by all poll steps. Unit-test the function with a stubbed `gh api` (bats or plain bash test). Commit: ci(qa-gate): treat status error as infra-blocked with bounded retry. Also write docs/ci/qa-gate-statuses.md (success/failure/error, what each means for the lane). Completion = commit, diff stat, test output. Auditor: no git fetch; the retry is bounded; `failure` semantics unchanged. NOTE: the powerplatform caller (promote.yml) gets the same change in its own task.

ADDED (overseer 2026-09-08 04:05 PT): the mcp-tools "QA Verify (prod)" job treats an `error` commit status ("blocked by infrastructure: 429 RateLimitError" from Ollama Cloud while harness runs saturate the keys) as a verification failure and then fails again on `could not add label: 'qa-gate' not found` (label now created). Make the job distinguish infra-blocked (status state=error) from a real failure: re-poll up to N times with backoff, then exit 3/neutral with a clear annotation instead of opening an issue; only state=failure opens the issue. Evidence: lane runs 34197087520 (keys 6) and the key-7 lane, sha 767bd0f: litellm-uat success 6/6, litellm-prod error 429.
