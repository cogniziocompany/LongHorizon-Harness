BUILD-ONLY (Phase 1 of "Open-Source LLM Audio QA / E2E Voice Testing on the Cognizioware LiteLLM Gateway"): a VERSA-based audio-metrics MCP server as a new build context in cognizioware-mcp-tools, ready for the deploy lane to build and register. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/audioqa-mcp (run `git fetch origin && git checkout -B feat/audioqa-mcp origin/main` first); plain git; commit by EXPLICIT PATH after each slice; no push; never call the live routers (CT202/CT204) or Docker (there is no docker on this host — the pipeline builds the image on CT202); never touch /home/harness/venv; use python3 -m venv .venv-audioqa for any local unit tests.

GATEWAY CONSTRAINTS (verified by the plan author; follow exactly):
- `mcp_servers:` in infrastructure/litellm-config.yaml (block starts ~line 245) has NO stdio entries — every server is HTTP (Streamable HTTP). Mirror the `cognizioware_xrm_mcp` entry pattern (~line 539): url, transport "http", auth_type "bearer_token", auth_value os.environ/<TOKEN>, description, access_groups.
- Every new server needs an `access_groups` list (else silently filtered for group-scoped keys) and a DASH-FREE alias in `general_settings.mcp_aliases` (LiteLLM splits tool names on the first dash): alias "audioqa" -> "audioqa_mcp".
- The LiteLLM image is digest-pinned in infrastructure/docker-compose.yml (~line 32) — do NOT touch it.
- docker-compose.yml is CRLF in the repo: add lines only, never rewrite the file (verify with `git diff --stat` that only your added lines changed). Build contexts MUST live under infrastructure/docker/<name> (the lane syncs only infrastructure/<ctx>; a repo-root path is never shipped — this bit us on 2026-09-07).
- Compose service pattern: copy the shape of an existing Python/Node MCP service (e.g. gateway-skills-mcp or puppeteer-mcp): `build: {context: ./docker/audioqa-mcp, dockerfile: Dockerfile}`, `image: cognizioware-audioqa-mcp:latest`, `container_name: cognizioware-mcp-tools-audioqa-mcp`, `expose: ["3110"]`, same network as litellm-router, `restart: unless-stopped`, `init: true`, healthcheck on GET /health, explicit `environment:` with `AUDIOQA_MCP_TOKEN: "${AUDIOQA_MCP_TOKEN}"` (NO env_file — the adapters must not inherit provider keys), a CPU/memory limit (2 CPUs, 3 GB) because VERSA models load into RAM.

SLICES (one per round):
(1) infrastructure/docker/audioqa-mcp/Dockerfile — python:3.12-slim; install ffmpeg + libsndfile; `torch==2.9.1+cpu` from https://download.pytorch.org/whl/cpu; onnxruntime; speechmos; VERSA via `pip install "git+https://github.com/wavlab-speech/versa.git#egg=versa-speech-audio-toolkit[audio,ml]" --no-build-isolation`; pre-download DNSMOS ONNX (P.835 + P.808) and UTMOS weights at BUILD time (a `prefetch.py` step) so first requests never fetch; non-root user; HEALTHCHECK curl /health. Keep the image lean (target <= 1.5 GB): document the size estimate. Also `requirements.txt` with pins. Commit: build(audioqa): VERSA audio-metrics MCP image.
(2) infrastructure/docker/audioqa-mcp/server.py — Python MCP SDK (`mcp` package, Streamable HTTP transport) on port 3110 with bearer auth from AUDIOQA_MCP_TOKEN (401 otherwise), GET /health -> {"ok":true,"metrics":[...]}, tools: `score_audio(path_or_base64, metrics=["dnsmos_p835","dnsmos_p808","utmos","squim_objective"], fs=16000)` -> per-metric scores + wall-clock ms per metric; `compare_audio(candidate, baseline, threshold_delta=0.3)` -> per-metric deltas + verdict "pass"/"regressed" (RELATIVE gates only — never absolute MOS thresholds); `transcribe_wer(audio, reference_text)` -> WER/CER via VERSA's whisper metric (local model, no API); `list_metrics()`. Inputs: accept a base64 WAV or a path under /data (read-only mount); resample to 16 kHz mono with soundfile/librosa; reject files > 60 s with a clear error. Never log audio contents or the token. Plus `bench.py`: scores N synthetic 10-second clips (generated in-process: tone + noise, no downloads) and prints ms/clip per metric as a markdown table — this is the latency measurement the plan requires; it RUNS ON CT202 later (overseer), you only write it. Unit tests (pytest, in `tests/`) for auth, input validation, the relative-gate logic and the markdown formatter, with the metric engines mocked (do not download models in tests). Commit: feat(audioqa): MCP server with score/compare/transcribe_wer tools + bench script.
(3) Registration: compose service (rules above); `mcp_servers.audioqa_mcp` entry in litellm-config.yaml with `access_groups: ["qa-tools","audio"]` and alias `"audioqa": "audioqa_mcp"` in general_settings.mcp_aliases; AUDIOQA_MCP_TOKEN added to infrastructure/.env.example (name only) and to the deploy workflow's env handling only if other tokens are listed there (check .github/workflows/deploy-mcp-tools.yml and deploy-mcp-tools.ps1 steps 2/3/health-tail — add audioqa-mcp wherever sibling services are enumerated). e2e suite `e2e/suites/36-audioqa-mcp.test.js` in the style of 32–35 (soft-skip off-LAN; checks /health and that `GET /mcp/tools` with `x-mcp-servers: audioqa` lists exactly the 4 tools). Commit: feat(audioqa): compose service, gateway registration, e2e suite 36.
(4) Skill + docs: infrastructure/docker/gateway-skills-mcp/skills/audio-qa/SKILL.md (follow the existing skill format in that directory) explaining `audioqa-score_audio` / `audioqa-compare_audio` / `audioqa-transcribe_wer`, how to read relative deltas, the "never absolute thresholds" rule and the judge-ensemble rule for Phase 2; `infrastructure/docker/audioqa-mcp/README.md` with the operator steps: set AUDIOQA_MCP_TOKEN on CT202 (name only), merge -> lane deploys, run `docker exec cognizioware-mcp-tools-audioqa-mcp python bench.py` and paste the table into docs/audioqa-bench.md (create the doc with a placeholder table). Commit: docs(audioqa): skill + operator runbook + bench doc placeholder.
HARD RULES: no secrets; no live calls; no docker; no changes outside infrastructure/docker/audioqa-mcp/**, infrastructure/docker-compose.yml (added lines only), infrastructure/litellm-config.yaml (added lines only), infrastructure/.env.example, the deploy workflow/ps1 enumeration lines, e2e/suites/36-*, infrastructure/docker/gateway-skills-mcp/skills/audio-qa/**, docs/audioqa-bench.md; commit by explicit path; no push. Completion = commit hashes, `git diff --stat origin/main..HEAD`, pytest summary, the exact env names, and the verification commands the overseer runs after deploy. Auditor: no git fetch; verify docker-compose.yml/litellm-config.yaml diffs are pure additions, no env_file on the new service, alias is dash-free, access_groups present, tests mock the model engines.
