BUILD + TEST (code; cognizioware-powerplatform frontend + a thin backend read): add the PUBLIC, UNAUTHENTICATED purchase surface to the existing React app, so a stranger can buy at $39/month with nothing but an email and land somewhere that tells them what happens next. Paxton's direction, 2026-09-08. Work ONLY in /home/harness/work/cognizioware-powerplatform on branch feat/public-purchase-route (run `git fetch origin && git checkout -B feat/public-purchase-route origin/develop` first); plain git; commit by EXPLICIT PATH; no push; env NAMES only; NO live Stripe calls in tests (fake HTTP / stubbed Stripe only).

SETTLED 2026-09-08 - WHICH BILLING TRACK IS REAL. Two parallel $39 tracks existed. The SEPTEMBER GUEST LAUNCH track is canonical: base price `price_1UCpYaQVmqxwMkjoBX4ATB3L`, metered price `price_1UCpYbQVmqxwMkjoqmvb8wYT`, meter `cognizioware_guest_tokens`, served by GuestSubscriptionController and GuestWebhookController. The older "Easy Button" design is DEPRECATED: price `price_1TsBP9CXyLTnTERyMFHhpOn7`, SignupController/PartnerBillingController, and the Power Pages portal at easybutt0n.powerappsportals.com. Do NOT wire anything to the deprecated price, controllers or portal, and do NOT delete them in this task either - retiring them is separate work. The price id still comes from runtime config by env NAME; the ids above are stated so you can VERIFY the configured value is the guest one and fail loudly if it is the Easy Button price.

THE FLOW THIS SERVES (do not redesign it): a prospect with no account and no tenant relationship lands on a public page, pays $39/month through STRIPE'S OWN EMBEDDED SNIPPET, and the EMAIL THEY PAY WITH BECOMES THEIR ACCOUNT. After payment they are told plainly that this email is now the account they use to sign in. Provisioning that email into its own Power Apps environment happens server-side afterwards and is NOT part of this task - other people are planning it. Your job is the public storefront and the post-payment landing, nothing more.

WHY IN THIS APP AND NOT A NEW SITE: packages/frontend/src/App.tsx already has a public area - /login, /signup and /auth are declared OUTSIDE the <Route element={<AuthGuard />}> wrapper, and everything else is inside it. A public purchase route belongs beside them. Do NOT create a new app, a new repo or a Power Pages site.

SLICE 1 - THE DEFECT THAT MUST BE FIXED FIRST (packages/frontend/src/App.tsx). `/checkout/success` and `/checkout/cancel` are currently declared INSIDE the AuthGuard. A buyer who has just paid is redirected back by Stripe, hits the guard, and is bounced to a login page for an account they do not have yet - so a successful purchase looks like a failure at the exact moment trust matters most. MOVE BOTH ROUTES OUTSIDE THE AUTHGUARD, beside /login. Then make packages/frontend/src/checkout/CheckoutStatusPage.tsx work for a signed-OUT visitor: it currently polls /api/checkout/subscription with a bearer token, which an anonymous buyer does not have. Drive it instead from the Stripe session id that Stripe puts on the return URL, resolved through a backend endpoint that takes the session id and returns a SAFE, MINIMAL public status - paid or not, and the email that was used, nothing more. Never return subscription internals, customer ids, price ids or anything else to an unauthenticated caller. Keep the existing authenticated behaviour working when a token IS present. Commit: fix(checkout): Stripe return pages must work for a signed-out buyer.

SLICE 2 - the public purchase route. Add a public /pricing route (and, if it costs nothing, alias /buy to it) OUTSIDE the AuthGuard. It renders Stripe's embedded snippet - use the Stripe pricing table or embedded Checkout, NOT a hand-rolled payment form and NOT a redirect we construct ourselves. The Stripe PUBLISHABLE key and the price/table id come from the runtime configuration the app already loads, by env NAME; if either is missing the page must say so visibly in plain language rather than rendering a broken or empty frame. THE SECRET KEY MUST NEVER APPEAR IN FRONTEND CODE OR CONFIG - the billing service remains the only holder of server-side Stripe credentials and the only server-side Stripe writer. Set the Stripe return URLs to the two routes from slice 1. The page must render correctly with no session, no MSAL account and no backend token: test it signed out, in a private window, because that is the only state a real buyer will ever be in. Commit: feat(pricing): public $39 purchase route using the Stripe embedded snippet.

SLICE 3 - the post-payment message. On the success return, state plainly: the payment succeeded, the email they paid with IS their account, roughly how long provisioning takes, and what to do if nothing arrives. Do NOT claim the account is ready if provisioning is asynchronous and unconfirmed - say it is being set up. On the cancel return, say nothing was charged and offer the way back. Neither page may imply the user can sign in before provisioning has actually completed. If the backend cannot yet report provisioning state, say so honestly in the copy and leave a clearly-marked seam for it rather than inventing a fake progress indicator. Commit: feat(checkout): post-payment guidance naming the email as the account.

SLICE 4 - tests (whatever the repo already uses for frontend tests; report counts). Cover: /pricing, /checkout/success and /checkout/cancel all render with NO auth token and NO MSAL account and never redirect to /login; the pricing page shows a clear message when Stripe configuration is absent; the success page resolves status from the session id rather than a bearer token; the public status endpoint refuses to return anything beyond the safe minimal shape; the authenticated path still works when a token is present. Commit: test(checkout): public purchase and return pages work signed out.

HARD RULES: no Stripe secret key anywhere in the frontend or in committed config - publishable key and price/table id by env NAME only; no price literals in source; do not create a new app or repo; do not touch Power Pages; do not modify the billing service's Stripe writer or any metering code; do not add a purchase control inside the authenticated app - a buyer has no account, so a control behind the login is visible only to people who have already bought; explicit-path commits; no push. Completion = commits, diff stat, test counts, and a walk-through of exactly what an anonymous visitor sees from landing on /pricing to reading the success page. Auditor: read-only - verify no secret key or price literal appears in the diff, that all three public routes sit OUTSIDE the AuthGuard, that the public status endpoint cannot leak subscription internals to an unauthenticated caller, and that the success copy does not promise a working sign-in before provisioning is confirmed.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

## RE-SCOPED 2026-09-10 04:57 - SLICE 1 IS DONE AND MERGED-PENDING. START FROM WHAT EXISTS.
Run `20260909T084904Z_caad6e1a` reached its round budget and reported its own state honestly. Read that first
rather than rediscovering it. **DO NOT REBUILD SLICE 1.**
**SLICE 1 - COMPLETE, audited, and already in a PR.** Commit `8734749`, pushed as branch
`fix/checkout-signed-out-return`, **PR #101** against `develop`. 4 files +76/-13. `/checkout/success` and
`/checkout/cancel` sit outside the auth guard; the status page shows a neutral "Checking subscription status..."
state instead of fabricating paid/unpaid. It deliberately does NOT resolve `{paid, email}` - that needs the
billing read endpoint queued as **task 73** - and leaves a marked seam. **Leave slice 1 alone.**
**SLICE 2 - COMMITTED BUT BROKEN. This is your first job.** Commit `95341ef` ("public $39 purchase route using the
Stripe embedded snippet") is on `feat/public-purchase-route` and **does not compile**. Evidence on disk:
 - an untracked **`temp_vite_env.ts`** declaring `/// <reference types="vite/client" />` and an `ImportMetaEnv`
   interface - a half-finished workaround for an `import.meta.env` typing error;
 - an uncommitted refinement to `PricingPage.tsx` setting `pricing-table-id` / `publishable-key` on the custom
   element via a ref.
**Fix the typing properly.** `vite/client` types belong in the frontend package's real type configuration
(`vite-env.d.ts` / `tsconfig`), NOT in a file named `temp_`. **Delete `temp_vite_env.ts`.** Decide the uncommitted
`PricingPage.tsx` change on its merits, commit it by explicit path if it is correct, discard it if it is not, and
say which. **`packages/frontend/dist/` and `packages/frontend/node_modules/` are untracked build output and must
NEVER be committed.**
**Prove it compiles before claiming the slice** - run the frontend typecheck/build and report the command and its
output. "Won't compile" is exactly what the previous run reported; do not repeat it.
**SLICES 3 and 4 - never started.** Do them after slice 2 compiles, one commit per slice.
**Branch:** continue on `feat/public-purchase-route` (it already carries `8734749` + `95341ef`). Rebase or merge
`origin/develop` before completion and report the result - develop has moved and now carries the nav flyout
(`ui/navRoutes.ts`, `ui/index.ts`, `ui/tokens.ts`) plus `App.tsx`/`Shell.tsx` edits, merged and verified live at
03:24. Do NOT revert those. Note `App.tsx` is touched by BOTH slice 1 and the flyout, so expect to resolve it
deliberately rather than accepting either side blindly.
**Do NOT add a Stripe SDK call or any server-side Stripe credential to powerplatform** - that app holds none and
must not start.
