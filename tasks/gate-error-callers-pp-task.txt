FIX (repo only; lanes deploy): the cognizioware-qa gate now posts commit status `error` ("blocked by infrastructure: ...") for provider/router infra failures and `failure` only for product regressions; `scripts/gate-status.mjs` exits 0/1/3. Update the powerplatform caller. Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch fix/qa-gate-error-status (run `git fetch origin && git checkout -B fix/qa-gate-error-status origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live calls. In .github/workflows/promote.yml (and any other workflow polling cognizioware-qa statuses - the secondary CE QA gates), on `error` with that description retry the dispatch up to 2 times after 10 minutes with the same inputs; if still `error`, fail with `::error::QA gate blocked by infrastructure (not a product regression) - re-run later` and a distinct step outcome; `failure` unchanged. Factor the polling into one reusable bash script under scripts/ci/ with a stubbed-`gh` test. Do NOT change the primary eval gate logic (task 06b owns it). Commit: ci(promote): treat qa-gate status error as infra-blocked with bounded retry; docs/ci/qa-gate-statuses.md. Completion = commit, diff stat, test output. Auditor: no git fetch; retry bounded; eval gate untouched.
