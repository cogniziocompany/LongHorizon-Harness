BUILD + TEST in the LongHorizon-Harness repo (Python package `lh_harness`, src layout; tests under tests/, run them with `.venv/bin/python -m pytest -q` — the venv is pre-made in this workspace; the baseline must stay green). Work ONLY in this workspace /home/harness/work/LongHorizon-Harness on branch feat/mcp-profiles-auditor-lock (already checked out from origin/main; run `git status` first). Plain git only, commit by EXPLICIT PATH after every slice, no push, never run git fetch/pull/checkout of other branches. Part D also touches the SEPARATE workspace /home/harness/work/cognizioware-hydra-fleet (branch feat/harness-fleet, TypeScript Hydra fleet plane) — same rules there. The overseer deploys (CT110 systemd `lh-harness`, non-editable pip install) and reviews; you never restart services or call live endpoints except the read-only GET noted below.

Three problems to fix in one change, in this ORDER: A (auditor git lock) -> C (session id) -> B (MCP profiles) -> D (Hydra fleet + ops) -> docs.

CONTEXT (verified by the overseer 2026-09-06): the Claude Code adapter (src/lh_harness/adapters/claude_code.py) always passes --strict-mcp-config and loads MCP config only from the never-set env LH_HARNESS_CLAUDECODE_MCP_CONFIG (line ~69), so harness runs on CT110/WSL load no MCP servers. episode_env(label) (lines 139-149) already stamps ANTHROPIC_CUSTOM_HEADERS "x-litellm-tags: lh-run/<run_id>,<round_tag>,<role>". Role deny-lists live in policy_for_role() in src/lh_harness/adapters/claude_permissions.py (auditors keep Bash; roles: manager/final_response, gui_executor/cli_executor, _AUDITOR_ROLES). Auditor prompt text is CLI_AUDITOR_INSTRUCTIONS and the GUI auditor text in src/lh_harness/prompt_texts.py (~189-215); integrity findings come from infer_integrity_findings() in src/lh_harness/auditor_agent.py (~540). Web API: src/lh_harness/webapi/server.py (GET /api/meta defaults at ~754, POST /api/runs roles parsing at ~795); the React frontend is prebuilt into src/lh_harness/_frontend (do NOT rebuild the frontend in this run — if UI changes are needed, change the TSX source under the frontend source dir if present and record "frontend rebuild required: yes" in the handback; otherwise expose the fields via API + CLI + config only). The MCP gateway is the cognizioware LiteLLM MCP endpoint: prod https://litellm-gateway-api.cognizioware.com/mcp/ , LAN http://192.168.21.161:4000/mcp/ (TRAILING SLASH MANDATORY), Streamable-HTTP, auth header "Authorization: Bearer <key>", server allow-list via header "x-mcp-servers: <comma list>". Server alias names are the gateway's own; the built-in profiles below use these aliases (verify the exact spelling with ONE read-only tools/list call against the LAN endpoint only if LH_HARNESS_MCP_GATEWAY_KEY is present in your environment; otherwise keep them as given and mark them UNVERIFIED in the handback): none; audit (read-only: ssh list/read, kb/docs, langfuse read); default (audit + github read, youtrack read, ssh exec); ops (default + docker/pct control); full (no allow-list header).

A) AUDITOR GIT LOCK (why: an auditor running git fetch/pull rewrites .git/objects, the workspace guard flags integrity: violation and the round is lost; today enforcement is prompt text only). In claude_permissions.py add AUDITOR_NETWORK_GIT_DENY (Claude permission deny rules, e.g. "Bash(git fetch*)", "Bash(git pull*)", "Bash(git push*)", "Bash(git remote*)", "Bash(git clone*)", "Bash(git submodule*)", "Bash(git lfs*)", "Bash(gh *)" and the "git -C" / "git --git-dir" variants) and AUDITOR_NETWORK_GIT_ENV (env for the auditor episode: GIT_TERMINAL_PROMPT=0, GIT_ASKPASS=/bin/false, SSH_AUTH_SOCK removed, plus a GIT_CONFIG_COUNT-based override that makes network git fail fast, or an equivalent you can test). Wire both into the auditor role policy/episode; add a finding hint "network_git_op" in auditor_agent.py so a guard violation whose diff is confined to .git/** carries that hint (plus the human-readable reason) instead of a bare violation. Tighten the auditor prompt text (both languages) with one line naming the ban. Tests: policy contains the rules; env applied only to auditor roles; hint inference on a synthetic .git-only diff. Commit: feat(auditor): deny network git ops and tag .git-only guard violations.

C) SESSION ID (derived, never random; no lookup needed to join run dirs to Langfuse). Add episode_session_id(run_id, label, role) -> "<run_id>.<round_tag>.<role>" next to episode_env in claude_code.py (round_tag exactly as episode_env computes it; "round_unknown" fallback). Emit it in three places: (1) append ",lh-session/<session>" to the existing x-litellm-tags header value; (2) header "X-LH-Session: <session>" inside the generated MCP config (part B) under mcpServers.cognizioware.headers, rewritten per episode; (3) result.metadata["lh_session_id"] on every Claude episode result so it appears in claude_stream.jsonl / run events. Do NOT parse Claude Code's own stream-json session_id anywhere. Tests: determinism, tag format, metadata presence for all three roles. Commit: feat(adapter): derived lh session id in LiteLLM tags, MCP headers and episode metadata.

B) MCP PROFILES PER RUN AND PER ROLE. New module src/lh_harness/mcp_profiles.py: built-in profiles {none, audit, default, ops, full} (name, description, servers allow-list or None for full, read_only flag); user profiles from ~/.lh-harness/mcp_profiles.json (or $LH_HARNESS_STATE_ROOT/mcp_profiles.json); project profiles from .lh-harness/config.toml ([run.mcp_profiles.<name>] servers = "a,b", description = "..."); selection precedence: API/CLI role override > API/CLI run-level > config [run.roles.<role>] mcp_profile > config run.mcp_profile > web-defaults env > built-in default (manager/executor "default", auditors "audit"). Gateway settings come from node env ONLY: LH_HARNESS_MCP_GATEWAY_KEY (required for any non-none profile; if missing, resolve every profile to "none" and record provenance reason "gateway key not configured" — never fall back to the LLM token / ANTHROPIC_AUTH_TOKEN), LH_HARNESS_MCP_GATEWAY_URL (default = prod URL; the literal alias "lan" = the LAN URL; or a full URL; normalise to a trailing slash), LH_HARNESS_MCP_GATEWAY_HEADERS_JSON (optional extra headers). Per episode, render <run_dir>/harness/mcp/<role>.mcp.json (harness-owned, mode 0600, outside the agent-visible workspace) with {"mcpServers":{"cognizioware":{"type":"http","url":...,"headers":{"Authorization":"Bearer ...","x-mcp-servers":"...","X-LH-Session":"..."}}}} and pass it via the adapter's existing mcp_config path (keep --strict-mcp-config); profile "none" renders nothing and keeps today's behaviour. Auditor profiles must be read_only=true; refuse (ValueError with a clear message) a non-read-only profile on an auditor role unless config sets allow_auditor_write_mcp = true. Surfaces: CLI flags --mcp-profile and --manager-mcp-profile / --executor-mcp-profile / --auditor-mcp-profile; POST /api/runs body mcp_profile and roles.<role>.mcp_profile; GET /api/meta -> mcp_profiles[] (name, description, read_only, source), defaults.roles.<role>.mcp_profile, mcp_gateway_configured (bool, never the key); snapshot/run provenance records the resolved profile per role and the reason. Web-defaults env: LH_HARNESS_WEB_DEFAULT_MCP_PROFILE and LH_HARNESS_WEB_DEFAULT_{MANAGER,EXECUTOR,AUDITOR}_MCP_PROFILE. Never log the key; redact Authorization in any debug output; unit-test the redaction. Tests: precedence table, rendering (0600, headers, session id), none/no-key path, auditor read-only refusal, API round-trip via the existing webapi test-client pattern in tests/webapi/. Commits: feat(mcp): profile registry + resolver; feat(mcp): per-role generated MCP config wired into the Claude adapter; feat(api,cli): mcp_profile selection surfaces.

D) HYDRA FLEET PLANE + OPS (workspace /home/harness/work/cognizioware-hydra-fleet, branch feat/harness-fleet): pass mcp_profile and roles.<role>.mcp_profile through the fleet run-create/migrate payloads and the hydrafleet MCP tool schema to the node's POST /api/runs; surface node meta mcp_profiles / mcp_gateway_configured in the fleet node listing; node compose/env docs gain LH_NODE_MCP_PROFILE and LH_NODE_AUDITOR_MCP_PROFILE (mapped onto the LH_HARNESS_WEB_DEFAULT_* envs). Keep the change minimal and typed; run that repo's unit tests / tsc if present. Commit there: feat(fleet): pass-through of MCP profile selection to harness nodes.

DOCS: README / how-to section "MCP profiles and session ids" (env NAMES only, never values), a table of the built-in profiles, the precedence rules, the auditor lock, and the deploy note for node operators (set LH_HARNESS_MCP_GATEWAY_KEY in /home/harness/.lh-harness-secrets.env; optional LiteLLM follow-up: add X-LH-Session to a server's extra_headers so the gateway forwards it). Commit: docs: MCP profiles, session ids, auditor git lock.

HARD RULES: no secrets in files; never print env values; no network calls other than the optional read-only tools/list GET; no frontend rebuild; commit by explicit path; no push; do not touch .lh-harness/, tasks/, docs/handoffs/. Completion = commit list per repo, pytest summary (before/after counts), the precedence table as implemented, the exact env/CLI/API names, and "frontend rebuild required: yes/no". Auditor: no git fetch (you are the first subject of part A); verify no secret strings in the diff, generated files live under <run_dir>/harness/mcp, auditor profiles are read-only, tests pass.

OVERSEER NOTE (2026-09-06 18:20 PT, after three attempts): slice A is already committed on this branch twice (bc8a1d8, 7021e3a — same message; keep the later, drop or squash the duplicate by amending history ONLY if no other commits exist yet, otherwise leave both and note it). It has a live bug that you must fix first: claude_code.py:97 turns an env value of None into a shell word "--unsetenvvar=SSH_AUTH_SOCK" passed to /bin/sh, which fails with "not found" and kills every auditor episode. Remove variables by deleting the key from the subprocess env dict (Popen env=...), never via shell arguments; add a test that the rendered command contains no "--unsetenvvar" token and that SSH_AUTH_SOCK is absent from the auditor env while present for the executor. The pre-made venv is .venv (`.venv/bin/python -m pytest -q`; baseline was 404 passed / 2 skipped before slice A). Continue with C, B, D, docs as specified above.

OVERSEER NOTE 2 (2026-09-06 20:05 PT, after the fourth attempt died on a provider quota): NEVER touch /home/harness/venv (the node's service venv; it is root-owned now and any pip into it is forbidden) — the ONLY interpreter you use is this workspace's `.venv/bin/python` (`.venv/bin/pip install -e ".[test]"` is already done). Branch state you inherit: commits for slice A (bc8a1d8, 7021e3a duplicate, ac7ddfd fix, 9d51331) and slice C (24f95a4) are on the branch; the worktree ALSO holds UNCOMMITTED, half-finished slice-B edits in claude_code.py, cli.py, config.py, supervisor/service.py, webapi/protocol.py from the previous executor (pytest currently reports collection errors). First step: `git status` + `git diff --stat`, read the diff, and either finish it or `git checkout -- <files>` the parts you cannot vouch for; the suite must be green (404 passed / 2 skipped baseline plus the new tests) before the next commit. Then continue B, D, docs.
