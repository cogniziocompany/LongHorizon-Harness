BUILD + TEST (repo only; the overseer deploys): give mcp-cognizioware a real PROD lane so prod is never again built on the box from a source checkout. Work ONLY in /home/harness/work/mcp-cognizioware on branch feat/billing-prod-lane (run `git fetch origin && git checkout -B feat/billing-prod-lane origin/develop` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls (no CT100, no Stripe, no YouTrack).

CONTEXT (from the ship plan): dev lane :20252 rolls on push to develop; uat lane :20253 rolls on fast-forward of release; prod :20251 was BUILT ON CT100 from a source checkout whose lineage had drifted (six CS0246 errors, a reconciliation PR that would have resurrected 1.1M lines) and was later rebuilt from develop by hand. The CI (.github/workflows/ci.yml) already builds and pushes ghcr.io/cogniziocompany/mcp-cognizioware/billing-service:sha-<7> for every push and the deploy job rolls the lanes listed in deployments/instances.json (name, sshHost, dir, healthUrl, branch, compose) via scripts/deploy/rollout.sh from the self-hosted runner ct210-billing (Linux, label lan-deploy). The cognizioware-qa gate `billing` target runs per lane with a 40-hex caller_sha commit status. Prod env on CT100 has extra keys (ADMIN_SPA_CLIENT_ID, Stripe live-mode guards) that must NOT be copied from the lanes.

DELIVER, one slice per round:
(1) Instances + compose: add a `prod` entry to deployments/instances.json (sshHost the existing CT100 alias, dir /opt/mcp-cognizioware-prod or the directory the running prod compose uses - read deployments/ and docs to find it and say so, healthUrl http://192.168.21.153:20251/api/health, branch `prod`, compose file deployments/tier/docker-compose.prod.yml if a prod compose exists in the repo, else create one from the uat lane compose with the image tag variable and NO env values) and make rollout.sh honour a per-instance `requiresApproval: true` flag by refusing to roll unless env ROLLOUT_APPROVED=1. Commit: feat(deploy): prod lane instance + approval guard.
(2) Promotion workflow .github/workflows/promote-billing.yml (workflow_dispatch: ref (sha), is_production bool): job dev-check (require the caller_sha's `cognizioware-qa/billing-dev` status success), fast-forward `release` to the sha (uat rollout happens via the existing ci.yml on release), poll `cognizioware-qa/billing-uat` on the same sha (reuse the poll/status pattern from cognizioware-powerplatform promote.yml: bearer auth, freshness window, 40-hex sha), then a `production` environment-protected job that fast-forwards the `prod` branch to the sha and runs the rollout for the prod instance with ROLLOUT_APPROVED=1, followed by the billing-prod QA gate dispatch + poll. Every job `permissions: contents: write (for the ff), statuses: read, packages: read`. No secrets in the file; env names only. Commit: feat(ci): billing promotion workflow dev -> uat -> prod (pinned sha).
(3) Prod safety: a startup check in the service (Program.cs) that refuses to start when `Stripe__AllowLiveMode=true` unless `Billing__Environment=prod`, and refuses when the image tag/version is unknown (APP_VERSION missing) in prod; document the prod-only env keys (names) in deployments/tier/README or docs. Tests for the check (hermetic). Commit: feat(billing): prod startup guards.
(4) Docs: docs/deploy/billing-lanes.md - the three lanes, what rolls on what, the promotion command (`gh workflow run promote-billing.yml -f ref=<sha> -f is_production=true`), the approval gate, rollback (retag previous sha, compose up --no-deps), and the rule "prod is never built on the box". Commit: docs(deploy): billing lanes + promotion runbook.
Verification: `dotnet test` hermetic suite (filter FullyQualifiedName!~DataIntegrity) stays green; workflow YAML parses; instances.json parses. HARD RULES: no secrets; no live calls; commit by explicit path; no push. Completion = commit hashes, diff stat, the instances entry, and the operator checklist (GitHub environment `production` with required reviewer, `prod` branch creation, prod env keys by name). Auditor: no git fetch; confirm the prod rollout cannot run without ROLLOUT_APPROVED=1 and the production environment job.
