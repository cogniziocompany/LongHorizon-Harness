BUILD-ONLY. Hivemind memory today captures lh-harness runs and Hydra fleet activity (scripts/hivemind-ingest.mjs -> memory-mcp) but NOT the Claude Code overseer sessions that manage them. Build `scripts/overseer-session-ingest.mjs`: a standalone Node 22 ESM script (no npm dependencies; node built-ins only) that walks Claude Code session transcripts and posts turn summaries into memory-mcp with source "overseer". Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-hydra-overseer (branch feat/overseer-session-ingest from origin/main @ c7651d1); never cd into or run git against any other checkout; plain git commands only; commit by EXPLICIT PATH (never git add -A); no push. Read scripts/hivemind-ingest.mjs FIRST and mirror its style, env handling and memory payload contract exactly.

Facts (verified live, do not re-derive):
- Transcripts: `<home>/.claude/projects/<project-slug>/<sessionId>.jsonl` (Windows example C:\Users\PaxtonTait\.claude\projects\c--Users-PaxtonTait-source-LongHorizon-Harness\1ffe9406-....jsonl; from this WSL worktree the same files are under /mnt/c/Users/PaxtonTait/.claude/projects). Each line is JSON with a `type`. Relevant types: `user` and `assistant` (fields: sessionId, uuid, parentUuid, timestamp, cwd, gitBranch, version, isSidechain, promptSource, message:{role, content}), `ai-title` (aiTitle), `pr-link` (prNumber, prRepository, prUrl, timestamp), `last-prompt` (lastPrompt). Ignore bridge-session, queue-operation, attachment, file-history-*, atis-latch, mode, and anything else unknown (skip silently, never throw on an unknown or malformed line).
- `message.content` is EITHER a string OR an array of blocks: {type:"text",text}, {type:"tool_result",...}, {type:"tool_use",...}, {type:"image",...}, {type:"thinking",...}. A "turn" = a `user` record whose content has at least one text block (or a string) of >= 12 chars after stripping `<system-reminder>...</system-reminder>` and other `<...>...</...>` wrapper blocks; user records that are only tool_result blocks are NOT turns. Skip isSidechain=true records. The assistant answer for a turn = concatenated text blocks of all subsequent `assistant` records until the next turn (cap 1500 chars; never include thinking or tool_use blocks).
- Memory contract (same as hivemind-ingest.mjs `remember()`): POST `${MEMORY_URL}/api/memories` with `authorization: Bearer ${MEMORY_TOKEN}`; body {source, session_id, text, source_ref, device, repo, tier, metadata}. Required: source, session_id, text. Dedupe is content-hash based server-side, so re-posting an identical text is harmless; still keep a local state file to avoid re-reading. Second transport (MUST also be implemented, selected when MEMORY_URL is unset and MEMORY_MCP_URL is set): the LiteLLM MCP gateway - JSON-RPC 2.0 over HTTP POST to `${MEMORY_MCP_URL}` (e.g. https://litellm.easybutt0n.ai/mcp) with headers `authorization: Bearer ${MEMORY_MCP_KEY}`, `x-mcp-servers: memory`, `content-type: application/json`, `accept: application/json, text/event-stream`; method `tools/call`, params {name:"remember_session", arguments:{source, session_id, text, tier, device, repo, source_ref, metadata}} (tool required fields: source, session_id, text). Parse either a JSON body or an SSE `data:` line. Treat HTTP 4xx as a hard error for that record (log, continue), 5xx/timeout as retry-once.
- Payload for this ingester: source "overseer"; tier "overseer"; session_id `${device}/${sessionId}` where device = os.hostname().toLowerCase(); repo = basename of cwd (or the `origin` remote name if `<cwd>/.git/config` is readable - do this cheaply, never spawn git); source_ref = absolute transcript path; text = one paragraph: `Overseer session <sessionId> on <device> in <cwd> (<gitBranch>)[, titled "<aiTitle>"]: user asked: <prompt, cap 600 chars> - assistant: <answer, cap 1500 chars>[. PRs: <owner/repo#N ...>]`; metadata {cwd, git_branch, claude_version, title, turn_index, ts, prompt_source, prs:[...], transcript_offset}.
- State: `--state <path>` (default `<home>/.claude/hivemind-overseer-ingest-state.json`) maps transcript path -> {offset (bytes consumed), turns, lastUuid}. Read incrementally from the stored byte offset; only advance the offset past complete lines. `--once` (default) processes and exits; `--watch <seconds>` loops. `--dry-run` prints the payloads as JSON lines and posts nothing. `--root <dir>` overrides the projects dir. `--max <n>` caps posts per invocation (default 200) so the first catch-up run is bounded. `--since <ISO>` skips turns older than the timestamp. `--env-file <path>` loads KEY=VALUE lines into process.env before anything else. Exit 0 always unless arguments are invalid.
- Privacy: never post tool_result bodies, file contents, or anything matching /(sk-[A-Za-z0-9]{8,}|Bearer [A-Za-z0-9._-]{12,}|ghp_[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{12,}|-----BEGIN [A-Z ]*PRIVATE KEY)/ - redact such spans to "[redacted]" before posting.

Slices, one per round:
(1) `scripts/overseer-session-ingest.mjs` complete per above (walker, parser, redaction, two transports, state, flags), plus `scripts/overseer-session-ingest.README.md` (env vars MEMORY_URL/MEMORY_TOKEN or MEMORY_MCP_URL/MEMORY_MCP_KEY, flags, payload example, how it differs from hivemind-ingest.mjs).
(2) Tests with node:test (`scripts/__tests__/overseer-session-ingest.test.mjs`, run via `node --test scripts/__tests__/`): a synthesized fixture transcript under `scripts/__tests__/fixtures/` (INVENTED content only - never copy a real transcript) covering: string content, text+tool_result mix, sidechain skip, system-reminder stripping, malformed line, ai-title + pr-link enrichment, redaction, incremental offset (append lines, re-run, only the new turn is emitted), and both transports against a local http.createServer stub (assert headers x-mcp-servers and the JSON-RPC shape). All tests must pass; quote the output.
(3) Windows scheduling: `scripts/install-overseer-ingest.ps1` that registers a Scheduled Task "Cognizioware Hivemind Overseer Ingest" running `node <repo>\scripts\overseer-session-ingest.mjs --once --env-file %LOCALAPPDATA%\cognizioware\overseer-ingest.env` every 5 minutes as the current user, plus `-Uninstall`. Also a `scripts/overseer-ingest.cron.example` line for Linux hosts. Verify the .ps1 parses with pwsh if pwsh exists in this environment; otherwise state it was not run.
(4) Docs + verification: add a section to the harness-fleet handoff doc under design/ (find it with `ls design`) describing the overseer tier, the payload, the transports and the install; README row under the hivemind section. Run the script `--dry-run --max 3 --root /mnt/c/Users/PaxtonTait/.claude/projects` from this worktree and quote 1 payload with its text truncated to 200 chars (reading real transcripts for the dry-run is fine; do not commit that output). Commits: feat(hivemind): overseer session ingester / test(hivemind): overseer ingester tests / feat(hivemind): overseer ingest scheduler install / docs(hivemind): overseer tier.

HARD RULES: no npm dependencies, no package.json changes; no changes outside scripts/**, design/** and README.md; never touch hivemind-ingest.mjs; no secrets or key values anywhere (env only); no real transcript content committed; no push. Completion = commit hashes, `node --test` output, dry-run sample, and a checklist of the four slices done/not-done. Auditor: no git fetch; verify the redaction regex is applied to both prompt and answer, that tool_result bodies never reach a payload, and that the state offset only advances past complete lines.
