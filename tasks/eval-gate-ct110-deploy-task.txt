BUILD + TEST (code; LongHorizon-Harness): give the three eval suites a home in CI as a POST-DEPLOY REGRESSION CHECK, and add a versioned dev->prod CT110 deploy. Implement PHASE 0 ONLY - everything buildable with NO fleet dependency. Planning session bd834eeb-05db-401f-af55-140aa6d90cf4, plan authored 2026-09-08 against main @ 1fe7b3ce5876e058cdf3e8f1a565191009e5b20d; the plan file lives on the overseer PC and you cannot read it, so everything you need is here. Work ONLY in /home/harness/work/LongHorizon-Harness-b on branch feat/eval-gate-and-ct110-deploy (run `git fetch origin && git checkout -B feat/eval-gate-and-ct110-deploy origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to PyPI, GitHub, the fleet or any model provider; env NAMES only.

PAXTON'S DECISIONS, 2026-09-09 - THESE OVERRIDE THE ORIGINAL PLAN WHEREVER THEY DISAGREE:
 (a) THE EVALS DO NOT BLOCK A RELEASE. They are a RETROACTIVE test that runs AFTER a live deployment, not a gate in front of publishing. Nothing in this task may prevent `pypi` from running or a release from being cut. Build the plumbing so the suites run on demand and record their result against the deployed commit; a failure raises a signal, it does not hold the release.
 (b) THEY RUN ON OUR DEFAULT LOCAL MODELS, not a paid cloud model and not a Claude judge. That removes the cost argument entirely and is why (a) is affordable to run at all. Pin the eval route to the DEFAULT LOCAL MODEL served by the router - the same model the harness executor uses on the local span - and record that identity in every manifest.
 (c) THEY RUN ONLY on the first deployment and whenever the DEFAULT LOCAL MODEL CHANGES. Not per release, not on a schedule. The trigger is a model change, which means the whole question of "how long is an attestation still valid" answers itself - see below.
 (d) STALENESS IS NOT A CLOCK. Do NOT implement a `--max-age-days` window. A time window was only ever a proxy for the real question, which is "has the thing under test changed since this was measured?" Bind validity to IDENTITY instead: a manifest records the commit sha, the exact model identity behind the route (model name plus whatever version, digest or build string the router exposes) and the sha256 of the thresholds file, and it stays valid until one of those changes. `verify` compares the recorded model identity against what the router reports TODAY and reports STALE only when they differ. This is strictly better than a window: it never expires a result that is still true, and it never trusts one that has silently stopped being true. If the router cannot expose a stable model identity, say so plainly and propose the closest thing it can give - do not fall back to a date.
 (e) PRIORITY: this task and the eval work are the LOWEST priority in the queue. Everything else is higher. Do not let it consume rounds that other work needs.

THIS REPO IS A FORK of AMAP-ML/LongHorizon-Harness and `.github/` currently has ZERO divergence from upstream. Every line you add there is future merge-conflict surface, so keep those diffs surgical and put first-party logic in `scripts/`, never inside `eval/` (which is vendored and will be re-synced).

FOUR BLOCKERS ALREADY FOUND IN THE CODE - do not rediscover them, design around them:
 - `.gitignore` ignores `results/`, so any manifest written under `eval/results/` is silently dropped.
 - `/api/meta` does NOT expose the package version (`build_meta()` in src/lh_harness/webapi/protocol.py returns service/api_version/protocol_version/capabilities). A post-deploy "did the new code load?" check must import `lh_harness.__version__`, NOT read /api/meta.
 - There is NO launcher pause/drain endpoint in src/lh_harness/webapi/server.py, so an automated idle-window wait cannot stop new launches and a drain poll may never converge under load.
 - There is NO `pull_request` CI at all, only the tag-triggered release. Anything you add ships untested unless CI is added first - which is why that is item 1.

THE CIRCULARITY, ALREADY SOLVED - do not re-litigate: a manifest attesting "commit S passed" cannot live IN S, because writing it produces S', which no eval ran on. Committing to `main` is rejected (arbitrary code could ride along in S'), as are release assets (the release is what the gate is deciding to create) and artifacts-as-primary (90-day expiry). Use an ORPHAN `eval-attestations` branch, content-addressed by sha, outside main's history:
  manifests/<40-hex sha>/{weavebench,osworld_v2,terminal_bench_21}.json
  manifests/<40-hex sha>/index.json   # roll-up: per-suite status + overall_pass
Per-suite files, not one blob, so partial failure is cheap. Every manifest records github_run_id / run_attempt / workflow_ref / runner_name, and `verify` cross-checks via the GitHub API that the workflow was eval.yml, head_sha matches and conclusion == success - a hand-forged manifest fails there.

PHASE 0 SLICES (one commit each, explicit paths):
 (1) .github/workflows/ci.yml on `pull_request` + `push: main`: `make test`, the frontend `npm test`, and `shellcheck scripts/deploy/*.sh`. This is a real standing gap and everything below ships untested without it. Commit: ci: add pull-request CI.
 (2) eval/thresholds.toml, committed and reviewable, with `policy = "informational"` - and it STAYS informational, because per decision (a) these never block a release. Note in the file that the published baselines below were measured on the upstream authors' models, NOT on our default local model, so our first local run establishes OUR baseline and these numbers are a starting reference only. Absolute floors approx 1.5 sigma below the published baselines: WeaveBench pass_rate 75.0 / overall 0.780; OSWorld binary 4.5 / partial 28.0; TB success 71.0. Record in a comment the two honest caveats: OSWorld *binary* at an 8.3 baseline is too weak to be a real regression detector (partial is the meaningful signal), and these are one-sample estimates to be recalibrated from observed spread after 2-3 runs. Commit: feat(eval): committed thresholds.
 (3) scripts/eval_manifest.py - schema, `build`, `verify`, threshold/waiver/coverage logic. Schema (schema_version 1, one file per suite): commit_sha, suite, status, metrics[] (name/value/baseline/floor/policy/pass), coverage (tasks_expected/tasks_scored/tasks_missing/tasks_errored), thresholds_source (path + sha256), waiver, run (timings + github_run_id/run_attempt/workflow_ref/runner_name), model (model/judge/endpoint/api_key_fingerprint/max_rounds/num_envs), raw_results (result dir, primary file, SHA256 OF THE RESULT BYTES, artifact ref, log path). NEVER log a raw key - WeaveBench's runner already has a `redact_key()` helper; mirror it. Add a hard COVERAGE FLOOR (tasks_scored >= 0.95 * tasks_expected) so a run that crashed after 20 tasks cannot post a great mean and pass. A waiver is a reviewed edit to thresholds.toml carrying reason/approved_by/expires, and the gate FAILS on an expired waiver. An infra abort writes status "error", distinct from a score "fail"; both block. Commit: feat(eval): manifest build and verify.
 (4) parsers, formats already verified in the suites' code:
   - WeaveBench: `<result_dir>/summary_<ts>.json`, a list of records with task_id/mode/score (written by eval/WeaveBench-harness/WeaveBench/weavebench/eval/orchestrator.py around L1717). overall = mean(score); pass_rate = 100 * fraction(score >= 1.0). The runner also writes run_provenance.json (model, harness, domains, limit, num_envs, redacted key, host, exact command) - REUSE IT VERBATIM as the manifest's provenance block.
   - OSWorld-V2: per-task score.json ({"score","error"}) or result.txt (bare float). REPLICATE the walk in eval/OSWorldv2-harness/OSWorld-V2/experiments/osworld_v2_hybrid/scripts/summarize_results.py rather than importing it (it is a path-brittle __main__), and add a PARITY TEST that shells out to the frozen summarizer on a fixture dir to catch drift.
   - Terminal-Bench: Harbor's jobs/**/result.json. ITS SCHEMA IS NOT IN THE REPO (it comes from the conda env), so this parser CANNOT be finalized offline. Build it schema-tolerant with a STRICT contract: if expected keys are absent, RAISE and fail the job - never fall back to a default that scores 0 or silently drops tasks. Leave an explicit NotImplementedError path and say so in the report; it is finalized against the first real run.
   Commit: feat(eval): suite parsers.
 (5) tests/fixtures/eval/** synthetic result trees + tests/test_eval_manifest.py, UNMARKED so `make test` runs them: golden manifests, floor pass/fail/waiver/expired, the coverage floor, the OSWorld parity test, and `verify` negatives (wrong sha, missing suite, stale because the recorded model identity no longer matches the router, forged run id). Report the count; state the baseline first. Commit: test(eval): manifest, thresholds and verify negatives.
 (6) Makefile targets `eval-manifest` and `eval-gate` in the existing style, alongside `test` / `e2e-happy`. Commit: build: eval make targets.
 (7) scripts/deploy/ct110_deploy.sh + ct110_verify.sh, POSIX bash with `set -euo pipefail` (the scratch script uses a bare `set -e`). Interface: --version X.Y.Z --sha <40hex> --wheel-sha256 <hex> [--force] [--drain-timeout] [--rollback] [--dry-run]. Hardening over the scratch version: NO `git clone` - the workflow scp's the wheel that release.yml already builds to /home/harness/releases/<version>/ and the script verifies sha256sum before installing (pip install lh-harness==X.Y.Z from PyPI is the documented MANUAL fallback); versioned release dirs with `current`/`previous` symlinks and --rollback; because the install is --no-deps, a guard that diffs the wheel's Requires-Dist against pip freeze and ABORTS if dependencies changed; idempotent (if installed lh_harness.__version__ already matches and the service is active, exit 0 without restarting unless --force); keep and extend the existing guards - active-run check via /api/runs, the LH_HARNESS_MCP_GATEWAY_KEY assertion, plus free disk, service-active-before, and secrets file not world-readable. Deploy AS ROOT (the CT110 venv is root-owned). --dry-run must print every command it would run and touch nothing. THE DRAIN GUARD, tier 1 (no code change): poll /api/runs every 60s up to --drain-timeout, require TWO consecutive zero readings 30s apart, and on timeout exit NON-ZERO listing the blocking run ids. NEVER auto-abort a run. State its honest weakness in the script's header comment: the launcher can start a new run mid-drain, so a human still holds queue traffic. Commit: feat(deploy): versioned CT110 deploy and verify scripts.
 (8) .github/workflows/eval.yml - `workflow_dispatch` ONLY (never pull_request), inputs sha / suites (default all) / dry_run. THREE INDEPENDENT JOBS, NOT A MATRIX: different runner labels, secrets and setup, 6-30h runtimes, and matrix fail-fast cancelling siblings is the opposite of what you want when one suite's KVM dies ten hours in. Each job: checkout at the pinned sha -> the suite's own check_env.sh as a fail-fast preflight, TREATING ITS /dev/kvm WARNING AS A HARD FAILURE (without KVM these are TCG-emulated and never finish) -> the full runner under a generous timeout-minutes -> write ONLY its own manifest and push with retry-on-race. An `attest` job (if: always()) regenerates index.json. Partial failure is handled by re-dispatching just that suite against the same sha. Commit: ci(eval): dispatch-only eval workflow.
 (9) .github/workflows/deploy-ct110.yml - workflow_dispatch with version / sha / force / drain_timeout_minutes / dry_run. A `preflight` job on ubuntu-latest asserts tag v<version> points at exactly `sha`, that manifests/<sha>/index.json is green (so prod cannot receive an unevaluated build via an independent dispatch), and that the version exists on PyPI; it emits the wheel sha256 as an OUTPUT so what the reviewer approves is exactly what installs and nothing re-resolves after approval. The `deploy` job runs on [self-hosted, lan-deploy] with environment: {name: ct110-prod}, and carries verification as POST-STEPS so a failure can roll back within the same job. Verification order: systemctl is-active after settle -> lh_harness.__version__ equals --version (the load-bearing check, and why it cannot come from /api/meta) -> /api/meta asserts mcp_gateway_configured and the expected profiles -> /api/queue/config asserts the kimi/qwen trios and capacity. Any failure triggers --rollback and re-verification; if the ROLLBACK also fails, fail loudly with "manual intervention required" - NEVER leave the job green. The smoke queue entry is deliberately NOT automated (it spends real model budget and needs a human at an approval gate): print the exact curl into $GITHUB_STEP_SUMMARY as the operator's next step. Commit: ci(deploy): CT110 deploy workflow.
 (10) POST-DEPLOY EVAL SIGNAL, NOT A RELEASE GATE (per decision (a)). Do NOT add `pypi: needs: [build, eval-gate]` and do NOT put anything in front of publishing. Instead: a job that runs AFTER a deployment has happened, verifies the manifest for the DEPLOYED sha, and writes a clear summary saying whether the deployed build has a passing eval record for the current default local model. It must be incapable of blocking a release - if it fails, it reports. Keep the diff surgical; `.github/` has zero divergence from upstream and every line is future merge-conflict surface. Commit: ci(release): post-deploy eval signal.
 (11) docs/release-pipeline.md documenting the resulting procedure (bump version -> merge -> commit S -> dispatch eval.yml with sha=S -> confirm index.json green -> tag vX.Y.Z AT S -> release publishes -> dispatch deploy-ct110.yml and approve), and update docs/release-checklist-ct110.md section 2 to point at the real script instead of describing one that does not exist. Commit: docs: release pipeline and corrected checklist.
 (12) APPROVED BY PAXTON 2026-09-08 - BUILD IT, it is no longer optional: `POST /api/queue/pause` and `POST /api/queue/resume` so a deploy can close the intake valve and drain deterministically, plus `package_version` in build_meta(). Both with tests. Pause must be idempotent, must NOT abort or affect any in-flight run, must survive a service restart in a defined way (state which you chose and why), and must be visible in /api/queue/config so an operator can see the valve is shut. Wire the deploy script's drain guard to use it: close the valve, drain, deploy, reopen - and reopen in a trap so a failed deploy cannot leave the queue permanently shut, which is the obvious way this feature turns into an outage. Commit: feat(api): queue pause/resume and package version.

EXPLICITLY OUT OF SCOPE - do not attempt, do not stub as done: provisioning the KVM eval VM on ptait07; registering any GitHub runner; creating the eval / ct110-prod environments, their secrets or the branch ruleset; creating the orphan eval-attestations branch on the remote; running any suite for real; finalizing the Terminal-Bench parser against real output; flipping thresholds from informational to floor; removing continue-on-error. Those are Phase 1 and 2 and need operator access you do not have.

DECISIONS SETTLED BY PAXTON 2026-09-08 - implement these, do not re-open:
 - EVAL PARALLELISM: run the full task count at REDUCED parallelism (NUM_ENVS 1-2). Full coverage is preserved and only wall-clock suffers (days, not hours). Do NOT shrink or relocate CT210 to free RAM - it hosts every CI runner and both billing lanes.
 - OSWORLD: enforce the PARTIAL metric only. Binary is REPORTED but never gates - at an 8.3 baseline it cannot detect a regression honestly. Encode that in thresholds.toml as policy per metric, not by omitting the metric: binary must still appear in the manifest with its value and an explicit non-gating policy, so the number stays visible.
 - CT110 DEPLOY APPROVAL: the standing rule extends to CT110 - prod deploys through the lanes need no per-deploy go, gates decide. So create the `ct110-prod` environment for SECRET SCOPING (so CT110_SSH_KEY and CT110_SSH_KNOWN_HOSTS are unreadable until the job runs) but do NOT configure a required-reviewer block on it. There is no `eval-bypass` environment and no bypass input: with the evals no longer gating a release, there is nothing to bypass. Say that plainly in docs/release-pipeline.md so nobody reintroduces one.
 - The API additions in slice 12 are approved (see above).

ALL PREVIOUSLY OPEN QUESTIONS ARE NOW SETTLED by decisions (a)-(e) above: evals do not gate releases, they run post-deploy on the default local model, only on first deployment and on a model change, and staleness is identity-based rather than time-based. Nothing here needs Paxton.

COLLISIONS: task 49 (dynamic model routing) and task 55 (orchestrator migration) are also in this repo. Keep your diffs to scripts/, eval/, .github/ and docs/ and you will not overlap them; if you must touch src/lh_harness/webapi/, say exactly what you changed.

HARD RULES: no credentials, tokens, provider URLs or key literals anywhere - env NAMES only, and never log a raw key; never put first-party logic inside eval/; never `StrictHostKeyChecking=no` in any script; the drain guard must never auto-abort a run; explicit-path commits; no push. Completion = commits, diff stat, test counts (state the baseline first), which slices landed and which were skipped, the answers to the report-do-not-decide list, and a plain statement of what a release now does differently. Auditor: read-only - verify no first-party code was added under eval/, that no secret or key literal appears in the diff, that the eval gate is genuinely in audit mode (continue-on-error) and cannot block a release yet, that the deploy script's --dry-run touches nothing, that the drain guard cannot abort a run, and that the Terminal-Bench parser raises rather than silently defaulting when keys are missing.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
