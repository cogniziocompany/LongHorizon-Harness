BUILD + TEST (config + docs; the overseer deploys through the lane, UAT CT204 first then prod CT202): add the new Synthetic LLM provider to the LiteLLM router and document it where an operator will find it. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/synthetic-provider (run `git fetch origin && git checkout -B feat/synthetic-provider origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to the provider or the routers (the overseer already probed it); env NAMES only - the key must NEVER appear in the repo, in a comment, in a test fixture or in a doc.
VERIFIED BY THE OVERSEER (2026-09-08, live against the provider from CT202, so treat as fact):
 - OpenAI-compatible base URL `https://api.synthetic.new/openai/v1` (there is also an Anthropic-compatible `https://api.synthetic.new/anthropic/v1`; use the OpenAI one).
 - Auth is `Authorization: Bearer ${SYNTHETIC_API_KEY}`. The key is ALREADY PLACED on CT202 and CT204 in `/opt/cognizioware-mcp-tools/mcp-tools.env` as `SYNTHETIC_API_KEY`, mode 600. Reference it by that env name only.
 - `GET /models` returned 11 ids: aliases `syn:large:text`, `syn:small:text`, `syn:large:vision`, `syn:small:vision`; and `hf:zai-org/GLM-5.3-Flash` (512k, beta), `hf:zai-org/GLM-5.2` (512k), `hf:zai-org/GLM-4.7-Flash` (192k), `hf:moonshotai/Kimi-K3` (512k), `hf:Qwen/Qwen3.8-27B` (256k), `hf:openai/gpt-oss-120b` (128k), `hf:nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4` (256k). A chat completion through `syn:small:text` succeeded and returned usage.
 - Subscription: Paxton bought TWO packs. Each pack is 1000 requests per 5 hours and 2 concurrent requests per model. Do NOT assume the two packs simply add up - configure to the documented single-pack limits and leave a clearly named knob to raise it once observed.
SLICES (one commit each, explicit paths):
 (1) `infrastructure/litellm-config.yaml`: register the provider in the existing style used for the other hosted providers - one `model_list` entry per model, `litellm_params` with `api_base: https://api.synthetic.new/openai/v1`, `api_key: os.environ/SYNTHETIC_API_KEY`, and the OpenAI-compatible provider prefix this config already uses for such backends. Name the router-facing models with an explicit `:synthetic` suffix (for example `kimi-k3:synthetic`, `glm-5.3-flash:synthetic`, `qwen3.8-27b:synthetic`, `gpt-oss-120b:synthetic`, `nemotron-3-super:synthetic`, plus the four `syn:*` aliases passed through) so nothing existing changes meaning. Set per-model concurrency and rate limiting from the documented pack limits, and put the numbers in ONE place with a comment naming the subscription so a future change is a single edit. Commit: feat(litellm): register the Synthetic provider.
 (2) capacity relief, ADDITIVE ONLY: append the Synthetic entries to the END of the existing fallback chains for the model families they can serve (Kimi K3 and the GLM family especially), so an exhausted Ollama key falls through to Synthetic instead of failing the round. Do not reorder or remove any existing member, do not change any pinned model a lane depends on, and do not touch the `qwen3.8` local entries. Add a short comment on each chain saying why Synthetic is last. Commit: feat(litellm): Synthetic as a tail fallback.
 (3) the operator-facing instructions Paxton asked for - put them where the router's own guidance lives: extend the gateway guides MCP catalog (`infrastructure/docker/gateway-guides-mcp/src/index.ts`, the preset vocabulary block) with a Synthetic entry describing what it is, which model ids exist and their context sizes, the alias-to-model mapping, the rate and concurrency limits, that the key is an env name on the routers, and when to prefer it (long context up to 512k, or when the Ollama pool is rate limited). Follow the existing entry shape exactly; one how-to, not prose. Commit: docs(gateway-guides): Synthetic provider entry.
 (4) e2e: extend the existing model/registry suite so the provider is covered the way the others are - assert the Synthetic model ids are present in the router catalogue and that each declares a non-empty api_base and an env-referenced key (never a literal). Do NOT add a live inference test for it to the default run; if the suite has an opt-in live tier, add it there behind an env flag. Commit: test(e2e): Synthetic registered in the catalogue.
 (5) `infrastructure/README.md` (or the existing provider table) gains a row with the base URL, the env name, the models and the limits; CHANGELOG. Commit: docs(litellm): Synthetic in the provider table.
HARD RULES: the API key never enters the repo - env name only, and no test fixture may contain a token-shaped string; do not change the pinned LiteLLM image digest; do not alter existing model names, pools or their order; do not touch `.github/workflows/`; explicit-path commits; no push. Completion = commits, diff stat, the YAML validation output, the test summary, and the exact operator sequence for the overseer (apply to UAT CT204, verify `/v1/models` lists the new ids and one completion succeeds, then prod CT202). Auditor: read-only - verify no literal key anywhere in the diff, that every existing chain member kept its position, and that the new entries reference `os.environ/SYNTHETIC_API_KEY`.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
