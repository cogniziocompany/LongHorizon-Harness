BUILD + TEST: implement the "Environment → AI Session" flow redesign in cognizioware-powerplatform (Modernist design), Greenfield end-to-end first, then the BROWNFIELD n8n workflow. Work ONLY in /home/harness/work/cognizioware-powerplatform on branch feat/env-to-session-flow (already checked out from origin/develop); plain git; commit by EXPLICIT PATH after every slice; no push; never touch /home/harness/venv; do not run docker; do not call n8n, Dataverse, SharePoint or LiteLLM live APIs (no credentials here — the operator deploys and tests live). Never commit build artifacts (packages/backend/dist, frontend dist) — a previous run did and it had to be reset.

READ FIRST (authoritative, in this order): docs/handoff/Env-to-Session-Spec.md — this is the designer's **v2 spec** (screens 2a–2i are current; 1c/1h are superseded by 2a/2e; every review decision is folded in — where v2 and the older decisions block below disagree, v2 wins); design/ux/modernist-v2/ (canvas "Env to Session Flow.dc.html"); docs/handoff/Env-to-Session-Spec-REVIEW.md (gaps, conflicts, the ORDERED SLICE LIST in §3 and the answers in §4 — follow §3's order and file paths, adapted to v2); docs/handoff/Env-to-Session-DESIGNER-NOTES.md; design/ux/modernist/_ds/ (Modernist tokens). RESUME RULE: this branch already carries committed slices from the previous run — run `git log --oneline origin/develop..HEAD` and `git status` first, keep what is committed, and adapt (do not redo) it to v2 where the v2 deltas below touch it. Existing UI conventions: packages/frontend/src/ui/* (Shell, Breadcrumb, Tabs, StatusChip, tokens.ts) and design/handoffs/phase1-golive-ux-handoff.md (the 4-step internal-test approval ladder MUST stay).

V2 DELTAS (from the v2 spec; apply on top of the decisions below): wizard step 1 has NO radio — one "Solution name" field: typing a new name shows the badge New · Greenfield and derives cgz_<Name>; picking from the list of custom UNMANAGED solutions shows Existing · Brownfield (managed solutions hidden; solutions owned by another publisher listed muted and blocked with CGZ-E202). Step 2 = "Requirements & specs": requirements textarea REQUIRED (8,000 chars), specs optional. Step 3 = Review & start with Sprint length REQUIRED (7 / 14 / 21 days), Session title OPTIONAL (blank → the Analyst names the session; typed → user title stays and the Analyst's name becomes the subtitle; the sessions table shows the subtitle under the title), and a collapsed "Technical details" row (schema name, prefix, docs path). E401 now means "docs folder / spec store failed". Approval = 2e sign-off (all five artifacts with version + status, Approve → OPEN the ladder, never starts Build) + 2f ladder (Export spec snapshot version+hash+download → Form checklist (8 items assumed) → E-sign against the exported version → Build approval dispatches Build and locks docs; docs stay editable while on the ladder) + 2g reset (any doc edited after export: Modified, version bump, rungs cleared, banner "Spec changed — export again", previous e-sign shown as no longer applying) + 2h Build started (docs re-lock, versions shown, progress + log) + 2i Extract/handoff (display name, publisher, version; Export pane with schema-name hint, Managed/Unmanaged export .zip, last export + sha256; Solution checker summary; Components created incl. Cognizio env variables and locked docs; Open app, Archive session; note that the next session in this solution starts Brownfield). Add slice S13 for 2h + 2i after S10.

DECISIONS ALREADY TAKEN (product owner Paxton + reviewer; do not re-open):
- Brownfield = an existing solution; it is a NEW n8n workflow, not a flag on bmad-design. Greenfield = new solution.
- Publisher is always Cognizio, prefix `cgz`, domain cognizio.company, unique name `cgzpublisher`, friendly name "Cognizio"; displayed, never editable; reused if present. Solutions are per session: `cgz_<PascalCaseName>`.
- The identity that creates publisher/solution/env variables is the app's service principal (application user, System Administrator) — CGZ-E102 copy: "Cognizioware's application user is missing in this environment" with the admin onboarding fix, not "you lack System Customizer".
- "pac solution create-sync" does NOT exist: implement step 3 as Web API get-or-create (extend services/provision.service.ts get-or-create pattern) + `pac solution clone` for source (add `solutionClone` to tools/pac-cli.ts using execFile array form; never shell strings with user input). All user-visible step text must say what actually runs.
- Docs location FOR NOW = the app's own attachment store on disk (ATTACHMENT_STORAGE_ROOT), reorganised per SOLUTION: `<root>/solutions/<cgz_UniqueName>/specs/` (Paxton 2026-09-07: the SharePoint/J-Space integration takes longer — do NOT build it now; leave a clearly marked adapter seam `DocsLocationStore` with the disk implementation so a SharePoint implementation can be added later). Record `cgz_<Name>_SolutionId` and `cgz_<Name>_DocsLocation` (the disk-store reference) as Dataverse environment variables (get-or-create definition + current value; check the variable BEFORE creating anything; never recreate). Upload policy: 32 MB per file, 200 MB per solution, types .md .txt .pdf .docx .csv .json (+ .xlsx via SheetJS→CSV if cheap; reject .png); over-limit = CGZ-E301, checked client-side before sending and server-side.
- Free-text requirements and sprint length STAY, unchanged in meaning (Paxton 2026-09-07: the design must not change the flow without a reason): the requirements textarea remains a REQUIRED input in wizard step 2 (Specs) exactly as the current form requires it — the design workflow synthesises from userRequirements and uploaded specs supplement it, never replace it; sprint length remains a required row in step 3 (Review & start) with today's default and validation. Do not make userRequirements optional.
- Approval gate (1h) KEEPS BOTH SURFACES (Paxton 2026-09-07): the sign-off gate (approve / ask orchestrator to revise) AND the existing 4-step internal-test ladder (export spec → form → e-sign → build approval). "Approve → start Build" opens the ladder; Build dispatches only on build_approved_internal; editing a generated doc at the gate sets review_status=modified, bumps the artifact version and invalidates spec_export_hash so the ladder visibly resets; uploaded specs are Locked; list all five artifact kinds (brief, prd, architecture, stories, sprint_plan). Docs lock again in build.
- Title: set from the brief's first H1 in the artifact callback (no n8n change), title_source='analyst'; users may still rename (title_source='user').
- Replace the legacy "Step n/6" with the Progress column; the process strip's six steps are Environment → Refresh → Setup → Design → Approval → Build.
- Environments discovered before this change (audit_level L1 only) count as "Never refreshed".
- Cancel of a setup session = archive (never hard-delete).
- Theme (Paxton 2026-09-07): ADOPT the Modernist LIGHT palette from design/ux/modernist/_ds/ (styles.css tokens) as the app theme — replace the slate tokens in packages/frontend/src/ui/tokens.ts with Modernist values (surface, ink, accent, semantic states), keep the token NAMES so components do not change, and re-check contrast on StatusChip/ProcessStrip states. This is slice S3a, done right after S3, before the wizard.
- NAMING IN THE UI (Paxton 2026-09-07): users see the solution DISPLAY NAME everywhere a normal user looks (sessions table Workspace column, workspace header, process strip value, wizard summary, breadcrumbs) — never the schema/unique name `cgz_<Name>`. The unique name appears only in technical detail surfaces (a tooltip or the Review step's collapsible "Technical details", the failure card's Copy details payload, and the extract/handoff screen). The environment-variable schema names (`cgz_<Name>_SolutionId` etc.) are never shown in the UI.
- Error envelope everywhere: {code, message, cgz, incident}; incident numbers from a BIGSERIAL.

SLICES (one per round, in this order, each = code + tests + explicit-path commit; migrations start at 031_):
S0 Error envelope + CGZ registry (src/errors/cgz-codes.ts, AppError, middleware; convert environment/gates/workspace routes; migration 031_session_incidents; logIncident).
S1 Session setup model (migration 032: solution_mode/solution_unique_name/solution_display_name/publisher_prefix/setup_step/setup_state/title_source; status 'setup'; session_v2.phase 'setup'|'failed'; session_setup_steps table; POST /api/sessions accepts the new fields (userRequirements stays required); PATCH …/setup).
S2 Environment refresh + reads (fix discoverEnvironments ON CONFLICT key; stop last_audited_at bump on status toggle; remove the PATCH-probe write in EnvironmentList.tsx; migration 033 refresh columns; POST /environments/:id/refresh (async, progress) + GET refresh-status; live GET /environments/:id/solutions (custom, unmanaged, publisher, component count) + /solutions/check → CGZ-E201).
S3 ProcessStrip + 1a/1b (then S3a Modernist light palette in tokens.ts, same round if small; ui/ProcessStrip.tsx under the Shell bar; environments refresh column, Open sessions disabled unless refresh ok, publisher line, counts; sessions TABLE with Workspace (Solution) / Path / Progress / Status, Setup n/3 + Resume rows).
S4 Wizard 1c–1e with resume (/environments/:id/sessions/new, /sessions/:id/setup; SolutionStep, SpecsStep, ReviewStep; step 1 creates the session in status 'setup'; unique-name derivation + live check; read-only publisher block; Cancel = archive).
S5 Specs upload with limits + progress (SpecUploader: per-file + total checks, XMLHttpRequest progress, Uploaded/Uploading/Failed; server caps + CGZ-E301 through the envelope; multer errors mapped).
S6 Start orchestration service (services/session-setup.service.ts: ensure publisher → get-or-create solution guarded by the SolutionId env var + write both env variables → create the per-solution docs folder in the disk store and move staged specs into it (+ optional pac solution clone) → POST /workspace/sessions/:id/start with sessionMode + composed requirements → phase 'design'; every step recorded in session_setup_steps; failure → phase 'failed' + incident + CGZ code; POST /sessions/:id/setup/start and /setup/retry, idempotent steps).
S7 Processing lockout (1f) + failure screen (1g): ProcessingCard/FailureCard from GET /sessions/:id/setup/status + WS event setup:step; useSessionLock hook applied to rename/archive/menu/chat/artifact tabs/ladder; FailureCard actions Copy details / View audit (GET /sessions/:id/audit) / Save & exit / Retry step.
S8 Auto title from the brief H1 in the artifact callback; "Title set by Analyst"; setup rows show "Setup abandoned at step n".
S9 Approval-gate edit window (migration 034 artifact_reviews; PUT artifacts gated to phase 'gate'; GateDocList + markdown editor; hash invalidation + ladder reset; specs Locked; Approve opens the ladder).
S10 Workspace header + Docs pane (1i).
S11 BROWNFIELD WORKFLOW: author n8n-workflows/live/bmad-design-brownfield.live.json (copy the structure of bmad-design.live.json; the Analyst node receives a solution inventory — tables, forms, flows from solutioncomponents + EntityDefinitions via the per-env MCP proxy the Sprint Build tool nodes already use — plus the specs, and writes a "current state + delta" brief; PM/Architect/Scrum Master unchanged); backend: sessionMode 'brownfield' selects this workflow id via config (env var name BMAD_DESIGN_BROWNFIELD_WORKFLOW_ID; name only); wizard "existing solution" path sends solutionMode='brownfield'; Brownfield on a solution whose publisher is not cgz is blocked with CGZ-E202; add a workflow-graph test for the new JSON (no $json-from-wrong-branch defects, all node references resolvable). Do NOT push to n8n — the operator imports it; document the import + workflow-id env in docs/handoff/env-to-session-OPERATOR.md.
S12 Tests: backend idempotency (run start twice → no duplicate publisher/solution/env-var), CGZ mapping, E301 limits, gate PUT phase guard; Playwright e2e/playwright/env-to-session.spec.js (refresh → open sessions → wizard → processing → gate edit → approve) marked to run only with the dev lane env.

Verification per slice: `npm run build` in packages/backend and packages/frontend must pass; existing backend tests stay green; new migrations are additive. Completion = commit list per slice, `git diff --stat origin/develop..HEAD`, test summaries, the list of env NAMES the operator must set (BMAD_DESIGN_BROWNFIELD_WORKFLOW_ID, ATTACHMENT_STORAGE_ROOT, pac), the n8n import steps, and "frontend rebuild required: yes". HARD RULES: no secrets; no live calls; no docker; no pushes; no build artifacts committed; do not touch .github/**, deployments/**, n8n-workflows/live/bmad-design.live.json (the Greenfield workflow stays as is), design/plans/**. Auditor: no git fetch; verify migrations are additive, the publisher constants are cgz/Cognizio/cognizio.company and never editable in the UI, every failure path yields a CGZ code + incident, the Brownfield workflow JSON parses and its node references resolve, no dist/ files in the diff.
