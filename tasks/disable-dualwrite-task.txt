BUILD + TEST (code; HIGHEST PRIORITY - production is currently one setting away from refusing to boot): turn OFF the dual-write to the deprecated secondary database, leaving all of the dual-write code in place, and remove the startup blocker. Paxton's decision, 2026-09-08: "turn off the dual write from our code for now, leave it all in place, just comment out the blocker as it's a deprecated Linode/Akamai db." Work ONLY in /home/harness/work/mcp-cognizioware on branch fix/disable-dual-write (run `git fetch origin && git checkout -B fix/disable-dual-write origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NO live calls; env NAMES only.
EVIDENCE (overseer, 2026-09-08, so the comments you write can state facts): the secondary is the Akamai/Linode managed Postgres `a495163-akamai-prod-3818951-default.g2a.akamaidb.net:15526`. Its DNS resolves ONLY to an IPv6 address (2a01:7e03::2000:1aff:fe8c:76e6). TCP to :15526 fails from CT100, from both Proxmox hosts, AND from a purpose-built Akamai relay VM in the same provider that has verified working IPv6 egress to other hosts - and :5432, :25060 and :11226 are closed there too. So the endpoint is deprecated/unreachable, not a routing problem on our side. Consequences seen today: (a) every health route hung this morning because the request-logging middleware resolved a DbContext that probed the secondary unbounded, which produced an autoheal restart loop and public 502s; (b) after the health hotfix shipped, prod REFUSED TO START 13 times with "FATAL: DualDatabase:SecondaryRequired=true but the secondary PostgreSQL database ... is unreachable. Refusing startup." (Program.cs ~line 608, probe at ~line 976). Prod is running only because the overseer hand-edited the lane compose to `DualDatabase__SecondaryRequired=false`; that edit is lost on the next rollout, which is why this task exists.
SLICES (one commit each, explicit paths):
 (1) remove the startup blocker: in `src/McpCognizioware.BillingService/Program.cs`, the fail-fast that throws when SecondaryRequired is true and the probe fails must no longer stop startup. COMMENT IT OUT rather than deleting it, with a dated block comment saying exactly why (deprecated Akamai/Linode endpoint, unreachable from every host and from a same-provider relay, decision 2026-09-08 by Paxton) and what must happen to restore it (a reachable secondary endpoint, then uncomment). Downgrade the FATAL to a single WARN naming the host and stating that dual-write is disabled - one line, not per request. Keep the reachability probe itself, bounded, so the log still tells the truth. Commit: fix(billing): stop refusing startup on the deprecated secondary.
 (2) default it off, everywhere it is configured: `DualDatabase:SecondaryRequired` defaults to FALSE in the settings class, and `deployments/tier/docker-compose.prod.yml` sets `DualDatabase__SecondaryRequired=false` with a comment pointing at this decision (the overseer's hand edit on the box becomes structural). Do NOT delete the setting or the env plumbing - a future reachable secondary just flips it back. Commit: fix(deploy): dual-write off by default while the secondary is deprecated.
 (3) keep the request path clean: assert by test that when dual-write is disabled nothing on a normal request path (including the request/response logging middleware and the health routes) resolves, probes or waits on the secondary. This is the actual cause of this morning's hang, so it must be locked down by a test, not by inspection. Commit: test(billing): disabled dual-write never touches the secondary.
 (4) tests + docs: a startup test proving the service starts when the secondary is unreachable and not required, and that with SecondaryRequired=true (future) the old refusal still applies once uncommented, or if that is impossible while commented out, state clearly in the report what would need to change. Update `docs/ops/health-endpoint-hang-2026-09-07.md` with this decision and the evidence above, mark the secondary DEPRECATED in the deploy docs, and add a CHANGELOG entry. Commit: docs(ops): secondary database deprecated, dual-write disabled.
HARD RULES: leave every dual-write code path in place - this is a switch-off, not a removal; no schema change; no secrets; explicit-path commits; no push; `dotnet test` green except the known offline DataIntegrity tests (report counts). Completion = commits, diff stat, test summary, and confirmation that a fresh start with the secondary unreachable now succeeds. Auditor: read-only - verify the blocker is commented (not deleted) with the dated rationale, that the default is false, that no request path touches the secondary when disabled, and that no dual-write implementation code was removed.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
