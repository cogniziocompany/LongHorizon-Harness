BUILD + TEST (code; cognizioware-powerplatform): make "Discover Environments" work the way XrmToolBox does - the signed-in user authenticates, and the app lists exactly the Dataverse environments Microsoft says that user can access. No allow-list, no seeding, no per-user grant needed for the environment LIST. Paxton's direction, 2026-09-08, verbatim: "this should work like how the xrmtoolkit works. when a user authenticates, the app can call dataverse envs the user has access to" and "we should simply allow the MS infra to determine what envs users can access. we dont need to apply extra." Work ONLY in /home/harness/work/cognizioware-powerplatform on branch fix/env-discovery-globaldisco-scope (run `git fetch origin && git checkout -B fix/env-discovery-globaldisco-scope origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to Microsoft in tests (fake HTTP only); env NAMES only.

THE DEFECT, diagnosed from a live 403 in the browser on 2026-09-08 with account ai-dev01@cognizio.company against powerplatform-dev:
 - packages/frontend/src/auth/MsalProvider.tsx line ~165 sets `tokenScopes = runtimeConfig.msal.dataverseScope ? [runtimeConfig.msal.dataverseScope] : []`. That is ONE scope for ONE Dataverse environment, e.g. https://org9130dfc5.crm.dynamics.com/.default.
 - packages/frontend/src/environments/EnvironmentList.tsx `discover()` POSTs /api/environments/discover with that same bearer.
 - packages/backend/src/routes/environment.routes.ts line ~159 passes `req.accessToken` straight into `discoverEnvironments`.
 - packages/backend/src/services/environment.service.ts line ~102 calls https://globaldisco.crm.dynamics.com/api/discovery/v2.0/Instances with it. Global Discovery requires a token whose AUDIENCE IS GLOBAL DISCOVERY ITSELF, not a single org, so Microsoft rejects it.
 - On rejection the service throws AppError('E101'), and E101 in packages/backend/src/errors/cgz-codes.ts is httpStatus 403 with machineName `stale_environment`. So a token-audience failure reaches the browser as a 403 about a stale environment, which is why nobody could read it. The route file's own comment at line ~97 already half-documents this ("globaldisco needs a different audience") and the /environments/seed endpoint exists ONLY as the workaround for it.
 - EnvironmentList.tsx `discover()` never checks res.ok - it does `setEnvironments(data.environments ?? [])` - so the 403 renders as the empty state "No active environments" with no error at all. The user sees a button that does nothing.

SLICES (one commit each, explicit paths):
 (1) frontend token: acquire a SECOND token for discovery with the Global Discovery scope (the documented resource form is `https://globaldisco.crm.dynamics.com//.default` - note the double slash before .default; verify against the current Microsoft docs and say what you used and why). Add a function beside the existing per-environment token helper in MsalProvider.tsx that does acquireTokenSilent for that scope and falls back to acquireTokenPopup on InteractionRequiredAuthError, mirroring the existing pattern exactly. Do NOT change loginScopes and do NOT remove the existing per-environment token path - refreshing and reading a single environment still needs it. Commit: fix(auth): acquire a Global Discovery scoped token.
 (2) wire discovery to it: EnvironmentList.tsx passes the discovery token on the discover call (a dedicated header is cleaner than swapping Authorization, since the route still needs the caller identity for the user record - choose one, state which, and make the backend read exactly that). environment.routes.ts passes it to discoverEnvironments instead of req.accessToken. Keep the per-environment token for the Level 1 audit calls inside discoverEnvironments, because those DO need the per-org audience - if the caller has no token for a discovered org, record the audit as unavailable rather than failing the whole discovery. THIS IS THE SUBTLE PART: one call needs the globaldisco audience, the per-org scans need per-org audiences, and today one token is used for both. Commit: fix(environments): use the discovery-scoped token for Global Discovery.
 (3) honest errors: add a distinct error code for a discovery AUTHENTICATION/AUTHORIZATION failure (401/403 from Microsoft) separate from E101 stale_environment, with a fixText naming admin consent for the Dynamics CRM user_impersonation permission as the likely cause when it persists for every user. Map a Microsoft 401 to it. E101 must keep meaning "this environment has not been refreshed" and nothing else. Commit: fix(errors): distinguish discovery auth failure from a stale environment.
 (4) surface it: EnvironmentList.tsx must check res.ok and render the server's message and fixText in the existing error style; never silently substitute an empty list. The empty state must only appear on a genuine empty success. Add the same guard to any other fetch in that file that assumes success. Commit: fix(environments): surface discovery failures instead of an empty list.
 (5) tests: fake HTTP only. Cover - discovery uses the globaldisco-scoped token and NOT the per-environment one; a Microsoft 401 produces the new code and not E101; the frontend renders an error rather than the empty state on a non-ok response; the empty state still renders on an empty 200; per-org audit failure degrades that one environment's audit without failing discovery. Commit: test(environments): discovery scope, error mapping and surfacing.
 (6) docs: a short section on how environment visibility works - Microsoft decides, we do not maintain an allow-list - and a note that /api/environments/seed is a provisioning-time workaround that is no longer required for the environment list. CHANGELOG. Commit: docs(environments): Microsoft decides environment visibility.

DO NOT: add, extend or "improve" any per-user environment allow-list; make the environment list depend on a seeded grant; call Global Discovery from the backend with an application (service principal) identity, which would show every tenant environment to every user and is the opposite of the requirement.

OPEN RISK to report rather than silently work around: if Global Discovery still rejects the new token for every user, the cause is the Entra app registration lacking a delegated Dynamics CRM (user_impersonation) permission or admin consent. That is a tenant admin action, not a code fix. Detect it, report it through the new error's fixText, and say so in the completion report.

HARD RULES: no secrets or tenant ids in the repo - env NAMES only; explicit-path commits; no push; no live Microsoft calls in tests. Completion = commits, diff stat, test counts, the exact scope string used, and a short description of what the user now sees in each of the three cases (success with environments, success with none, Microsoft refusing the token). Auditor: read-only - verify the per-environment token path still exists and is still used for per-org calls, that no allow-list was introduced, that E101 no longer absorbs auth failures, and that the frontend cannot render the empty state on a failed response.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
