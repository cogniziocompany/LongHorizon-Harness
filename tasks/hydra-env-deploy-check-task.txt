FIX (cognizioware-hydra): loud pre-deploy check for the host-managed .env. Work ONLY in /home/harness/work/cognizioware-hydra on branch fix/env-deploy-check (run `git fetch origin && git checkout -B fix/env-deploy-check origin/main` first); plain git; commit by EXPLICIT PATH; push and open the PR against main, then STOP - never merge.

CONTEXT: task 216 (branch fix/untrack-env-secrets, its PR may or may not be merged when you start) untracked `.env` and shipped `.env.example` with names only. Secrets are delivered BY HAND on the host to /opt/cognizioware-hydra/.env - never via git. This task is slice 2 of that work, split out because 216 spent three rounds re-asking one question.

REQUIRED VARIABLE NAMES (an audited fact - do NOT ask for them, do NOT read the host file): HYDRA_API_KEY, DEVICE_TOKENS, CLOUDFLARE_TUNNEL_TOKEN, GITHUB_APP_ID, GITHUB_APP_INSTALLATION_ID, GITHUB_APP_PRIVATE_KEY_PATH, GITHUB_WEBHOOK_SECRET. Grep the compose files and deploy scripts for any OTHER name they read and include those too.

SLICE: find every place the deploy path assumes `.env` arrives via git checkout (the Hydra deploy lane workflow, compose files, deploy scripts, docs) and change it to assume the host-managed file at /opt/cognizioware-hydra/.env - add a pre-deploy check script that fails LOUDLY, naming each missing variable, when a required name is absent or empty, wired into the deploy lane before `docker compose up`. Include a unit test for the check (test with a temp file, never the real one). Commit: fix(deploy): require host-managed .env; fail loudly on missing names.

HARD RULES: do NOT rewrite history, do NOT force-push, do NOT print or commit any secret value; do NOT touch /opt/cognizioware-hydra/.env on the host; the running Hydra stack must not be restarted. If `.env` is still tracked on origin/main when you start, do NOT untrack it here (216 owns that) - just add the check. Completion = the PR URL, diff stat, test count, and the exact grep proving no secret value is in the diff. Auditor: read-only - verify the check names every required variable, the test passes, and no secret string appears in the branch diff.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR role is read-only; the EXECUTOR commits by explicit path only; NEVER merge; never ask the overseer to push - you hold GH_TOKEN, push and open the PR yourself.
