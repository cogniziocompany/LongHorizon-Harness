BUILD + TEST (web UI; Hydra orchestrator): give the harness fleet the whole right-hand side of the Hydra window, rebuild the per-run popup into something usable, and link every row out to the real lh-harness web UI. Work ONLY in /home/harness/work/cognizioware-hydra-rsi on branch feat/harness-panel-right (run `git fetch origin && git checkout -B feat/harness-panel-right origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to the orchestrator, the harness or any host (fixtures + a fake API only); env NAMES only.
GROUND TRUTH verified by the overseer on origin/main (re-check and report anything that differs): the whole UI is one page, `orchestrator/public/index.html` (~41 KB, styles and script inline). Today the harness fleet is a THIRD list crammed into a 280px left sidebar - `.sidebar` (line ~16) holds `#fleetList` (devices, ~line 117), `#instanceList` (~line 119) and then a "Harness Fleet" section with `#harnessList` (~lines 120-123) plus a `fetchHarness()` refresh button. There is already a per-run drawer below (~line 168, comment "Fleet run drawer: snapshot + gates + intervention feed for one harness run"). Server side, the data comes from `orchestrator/src/fleet-routes.js`, `harness-client.js`, `harness-registry.js` and `harness-manager.js`; MCP surface in `mcp.js`. Do not change the server contracts in this task unless a slice below says so.
WHAT PAXTON ASKED FOR, in his words: enlarge the harness fleet to take the ENTIRE right-hand side of the window, revise the popup UX, and "we need the link to the actual harness UX".
SLICES (one commit each, explicit paths):
 (1) layout: move the harness fleet out of the left sidebar into a full-height RIGHT-hand panel that owns that side of the window (the left sidebar keeps devices and instances). The panel gets its own header with the node summary (node id, online/degraded, run counts) and a refresh control, scrolls independently, and must not overlap or clip the terminal area at common widths. Below roughly 1100px it collapses to a toggle that slides over, so nothing becomes unreachable on a laptop. Keep the existing dark theme variables; no new CSS framework, no new build step - this page has no bundler. Commit: feat(ui): harness fleet owns the right-hand panel.
 (2) the list itself: group by node, then by run, each run row showing run id (monospace, full string, selectable), status, current round and active role, workspace, and elapsed time; a gate-waiting run must be unmistakable at a glance (colour plus an explicit "gate" label, not colour alone). Rows are keyed by the harness run id exactly as the API returns it - no truncation in the DOM value, no per-surface renaming - because the same ids must match the fleet window. Commit: feat(ui): harness run rows with node grouping and gate emphasis.
 (3) THE LINK Paxton asked for: every run row and every gate carries a visible external link to the running lh-harness web UI for that run, built from the node's `uiBaseUrl` (for example `<uiBaseUrl>/runs/<run_id>`), opening in a new tab with rel="noopener", labelled so it is obvious it leaves Hydra. If a node has no uiBaseUrl configured, show the row without a dead link and say why on hover. Put the same link at the top of the drawer. Commit: feat(ui): deep links to the harness web UI.
 (4) revise the popup/drawer UX (the existing fleet run drawer ~line 168): make it a working cockpit rather than a dump - a header with run id, node, status, elapsed and the harness-UI link; a body with the current round and role, the pending gate and its question, the recent activity/intervention feed with actor and rationale; and the control actions the server already exposes (stop, resume, add instructions), each requiring a rationale before it will submit, since the server enforces that invariant. Show errors inline instead of silently failing, keep focus trapped while open, close on Escape, and never block the page behind a spinner - render what is known and fill in the rest. Commit: feat(ui): usable run drawer with gated actions.
 (5) tests + docs: whatever test harness this repo already uses for the orchestrator (node --test) gets coverage for the row-rendering and link-building helpers - extract them into small pure functions in a module the page imports rather than testing the DOM: link building with and without uiBaseUrl, gate-state classification, and id passthrough (an id in must be the identical string out). A short README/docs note describing the panel and the parity rule that run ids must match the fleet window exactly. CHANGELOG. Commit: test+docs(ui): harness panel helpers and parity note.
HARD RULES: no new frontend framework, bundler or dependency - this page is plain HTML/CSS/JS on purpose; do not change the orchestrator's REST or MCP contracts; do not touch auth; keep the existing authenticated-polling model (no unauthenticated live feed); explicit-path commits; no push; orchestrator tests green (report counts). Completion = commits, diff stat, test counts, and a plain description of the new layout at a wide and a narrow width so the overseer can check it after deploy. Auditor: read-only - verify the ids are passed through untouched, that every external link is built from uiBaseUrl and opens safely, that a rationale is required before any control action, and that no server contract changed.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

EXPERT REVIEW APPLIED (Sonar Pro through the gateway, 2026-09-08; full text in LongHorizon-Harness docs/handoffs/fleet-surfaces-expert-review-2026-09-08.md, brief in fleet-surfaces-design-handoff-2026-09-08.md). Build to these decisions, and where one contradicts a slice above, the decision here wins:
 - FRAMING: Hydra is the control cockpit; the Fleet Window is the monitoring wallboard. Duplicate only what people and agents must quote to each other - node identity and status, run id, non-terminal status, current round tag and role, gate id/state/question, the full per-round session id, the harness-UI link, and started_at/last_update. Controls (stop, resume, add instructions, anything needing a rationale) stay Hydra-only. Aggregate counts and the event/alert timeline stay window-only.
 - LAYOUT: at >1200px the left 280px sidebar keeps devices and instances; the right pane is split vertically - harness fleet on top, the terminal area as a band below (~30-40% height) with Pin and Collapse in its toolbar; collapsed shows a 32px bar with the last log line. At ~1024px the right pane stacks with the terminal COLLAPSED BY DEFAULT so the fleet wins the vertical space. The run cockpit docks to the right edge of the pane at 35-40% width on desktop and becomes a full-width overlay at laptop width.
 - RUN TABLE COLUMNS: run id, session id (compact), status, round, role, gate summary, last update, and an explicit "Open cockpit" affordance; row click opens the cockpit.
 - COCKPIT: header carries the full run id, the session id, the node id, a status pill and the "Open in Harness UI" link. Body: identity/state block (round, role, started_at, last_update, a one-line origin note), a gate block (id, question truncated to 1-2 lines with More, status, who it awaits), an activity feed with actor + action + timestamp + rationale, then the controls. Open OPTIMISTICALLY with the identity already known from the row and skeletons for the rest, so a human can quote ids immediately; render each field the moment it arrives; never block the whole panel behind a spinner. On load failure show a banner naming the reason and the last successful update, keep last-known fields badged "May be stale", and disable controls with a tooltip. On action failure show the error inline under that control and do NOT add an activity event unless the server confirms. When a gate resolves while the cockpit is open, flip the pill inline, append "Resolved at <ts> by <actor>", remove the gate-specific affordances and drop a brief "Updated" badge - no modal.
 - FRESHNESS: a status line top-right on each surface, worded with both age and cadence, e.g. Hydra "Fleet data refreshed 8s ago (auto every ~10s)".
 - DISAGREEMENT (the expert took a position, adopt it): surfaces must DETECT and SHOW disagreement, not leave it to tests. When Hydra's run set/statuses differ from the window's, show a non-modal top banner naming the situation, stating which source is being displayed, and pointing at reporter/harness sync. Mirror the wording on both surfaces so nobody has to guess which one is wrong.
 - UNREACHABLE NODE (this OVERRIDES the earlier slice-1 wording): an unreachable node is an explicit GAP, not degraded-with-last-known-rows - "Node unreachable - fleet data unavailable since <age>" at node level. Degraded means known-bad; unreachable means unknown, and the two must look different.
 - SESSION IDS: truncate in the row with an ellipsis and a copy affordance, full string in a tooltip and in the cockpit; the underlying value is never truncated.
 - ORIGIN (push vs poll): keep it out of the visible row; expose it in the API and diagnostics only.
 - PARITY ASSERTION to implement in the helper tests and hand to the queued UX gate: for every run id the harness reports at time T, both surfaces must show that same run id with the same status within 5 seconds, harness as source of truth; a missing id or a mismatched status on either surface fails with "Consistency check failed: Hydra and Fleet Window UIs do not match harness fleet state within 5s. At least one surface is showing missing or incorrect run statuses."
 - NOTED, NOT ACTED ON: the expert judged Hydra the more redundant surface against the harness's own web UI, since the window is uniquely shaped for reporter-driven monitoring. We are not acting on that; it is recorded for Paxton.

ADDITIONAL SLICE (7) - THE HARNESS TASK QUEUE MUST BE VISIBLE IN THE WEB UX (Paxton, explicit). The harness owns a durable, priority-ordered task queue and its own docs (LongHorizon-Harness docs/queue.md) already name Hydra as the queue client: "Hydra | Deep links to /api/queue + optional queue panel that POSTs enqueue". Build that panel here, in the same right-hand pane, as a section under the fleet list (collapsed by default when empty).
 - READ: GET <node baseUrl>/api/queue returns {entries, groups, counts} and accepts ?status=pending|launched|done|failed; GET /api/queue/config returns the capacity rules. Show, per entry: name, workspace, trio, priority, requested_by, status, and the queue id (q-<16hex>) in full and selectable, with created/launched timestamps. Group exactly as the API groups them; do not invent a grouping. Counts go in the section header next to the fleet counts.
 - WHY IT MATTERS ON THIS SURFACE: a person watching the fleet needs to see not only what is running but what is waiting and why it has not started - a queue entry whose workspace is busy is the single most common reason a "queued" item appears stuck, so the row must make that visible (for example a "waiting: workspace busy" hint derived from the entry's workspace matching a non-terminal run).
 - WRITE (Hydra is the control plane, so it may write, but every write is explicit): enqueue via POST /api/queue, re-prioritise via POST /api/queue/{queue_id}/priority, remove via DELETE /api/queue/{queue_id}. Enqueue is a small form - name, workspace, trio, max_rounds, priority, and either task text or task_file (exactly one, the API rejects both) - with requested_by set automatically to identify Hydra. Every write requires a rationale, the same rule the run controls follow, and a failed write shows its error inline and does not optimistically mutate the list.
 - NOT YET AVAILABLE ON THE LIVE NODE, and the UI must handle it honestly: the deployed harness build predates the queue API - GET /api/queue currently returns 404 and the node serves no dashboard at its web root. When the queue endpoints answer 404, show a single quiet line, "Queue API not available on this node (needs the harness release)", rather than an error banner or an empty table that implies an empty queue. Feature-detect per node; do not disable the whole panel.
 - The queue id and the run id it becomes must be quotable and identical to what the API returns, the same parity rule as every other id here.

CORRECTION AND THE ACTUAL USE CASE (Paxton, 2026-09-08). Ignore the expert's aside that Hydra is redundant against the harness's own web UI - it is not, and neither surface is dropped. The planes are:
 - fleet.easybutt0n.ai = every lh-harness run, everywhere: the wallboard.
 - Hydra = the DEVICE fleet and the control plane: terminals, shells, heads on real machines, in real time.
 - An lh-harness run is an AGENT that may reach into Hydra's device fleet to execute (bash, a terminal head, a device). A run is therefore not only a row; it is a consumer of devices.
THE USE CASE THIS PANEL MUST DELIVER, in his words: "I access hydra, can see all the harness in the right side panel, then if any are using the hydra tools, I should see it in the terminal and the heads on the left hand side of the screen."
SLICE (8) - CORRELATE RUNS (right) WITH HEADS AND TERMINALS (left):
 - When a harness run is driving a Hydra device, terminal or head, that pairing must be visible on BOTH sides at once: the run row on the right shows which head/terminal it is using, and the corresponding device/instance entry in the left sidebar shows which run is driving it. Use the identifiers the server already returns - device id, instance/terminal id, run id - unchanged and identical on both sides, so a person can follow one to the other by eye.
 - Selecting a run on the right highlights (does not steal focus from, does not auto-open) the head(s) it is using on the left, and selecting a head on the left highlights the run driving it. A run using no device shows nothing extra; an idle head shows nothing extra.
 - Opening the terminal for a head that a run is driving is the existing terminal flow - do not build a second terminal. The panel's job is to make the connection obvious and one click away.
 - Derive the pairing ONLY from data the orchestrator already exposes (device manager, instance list, harness registry/manager, activity log). Do NOT change server contracts to obtain it. If the current data genuinely cannot tell which run is driving which head, do not fake it: implement everything else, and state precisely in the completion report which field the orchestrator would need to expose for the correlation to be real, so the overseer can queue that separately.
 - Where the correlation exists, the run cockpit gains a line naming the device(s) and a link into the terminal for that head.

SLICE (9) - MAKE THE THREE MSCE MEMORY LEVELS VIEWABLE (Paxton, 2026-09-08). Each harness instance carries a three-level memory: L1 traces (per run), L2 policies (generalised routines), L3 environmental cognition (stable fleet knowledge - hosts, what each can run, standing constraints). They are seeded (hardcoded) for now and learned later, and each item carries an origin of `seeded` or `learned` with a timestamp. The harness exposes them read-only at `GET /api/experience/environment`, `GET /api/experience/policies` and `GET /api/experience/runs/{run_id}/traces` (same bearer boundary as the rest of /api/*).
 - PER NODE, in the fleet panel's node header area: an "Environment (L3)" disclosure listing the seeded knowledge for that instance, each item showing its origin badge; collapsed by default, because it is reference material rather than live state.
 - PER INSTANCE: a "Policies (L2)" section, paginated, empty-stated honestly as "No policies learned yet" rather than hidden, so a person can see the layer exists and is simply unlearned.
 - PER RUN, in the cockpit: a "Traces (L1)" tab or section showing that run's trace units in round order - state summary, action, observation, reflection, the tools used, and the device/head/terminal and Hydra node where an execution actually happened. This is what makes a run's history explain which devices it drove.
 - FEATURE-DETECT exactly as with the queue: the deployed harness build predates these endpoints, so a 404 shows one quiet line ("Experience API not available on this node (needs the harness release)"), never an error banner and never an empty list that implies the memory is empty.
 - Ids (policy id, trace id, run id, device id) pass through untouched, same parity rule as everywhere else. This view is READ-ONLY; editing memory is not in this task.
