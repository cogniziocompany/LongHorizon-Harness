BUILD-ONLY. Implement the Claude Design handoff "Web UX: Navigation + Session Archive" in the cognizioware-powerplatform web app. Read the authoritative spec FIRST and follow it exactly: design/handoffs/web-ux-nav-archive-handoff.md in this worktree (sections 1-8; sibling context design/handoffs/phase1-golive-ux-handoff.md and design/ux/HANDBACK.md for invariants and Playwright gotchas). No design canvas package exists yet, so build directly from the handoff's W1-W6 specifications and the existing screenshots' look (design/ux/screenshots/*.png, same slate palette). Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform-navarchive (branch feat/web-ux-nav-archive from origin/develop @ edd45a1 — develop is the integration branch that the dev lane deploys; the handoff's "cut from main" is superseded by the promotion pipeline: develop -> dev lane -> promote.yml dev/uat/prod); never cd into or run git against any other checkout; ignore the stale untracked ./cognizioware-powerplatform duplicate tree if present; plain git commands only; commit by EXPLICIT PATH (never git add -A; the repo has CRLF phantoms); no push.

Environment facts: node/npm in this WSL shell may need `source ~/.nvm/nvm.sh` (or use `/usr/bin/node` if present) — check `node --version` first and state what you used; `npm ci` in packages/frontend and packages/backend if node_modules are missing. Backend contract is live and documented in handoff section 3 with line numbers (verify them on this tree before relying on them). Custom Dataverse only in sample copy; no "Delete" wording anywhere; no pricing copy; no "BMAD" in UI copy; INTERNAL TEST framing stays on approval/terms UI; no secrets.

Slices, one per round (each ends with `tsc --noEmit` and `vite build` clean in packages/frontend and a commit by explicit path):
(1) W6 first, minimal: packages/frontend/src/ui/tokens.ts (the palette in handoff section 2 + spacing/radius) and components Shell, Breadcrumb, SubHeader, StatusChip (absorb AdminPage's local Chip without changing AdminPage behaviour), Tabs, OverflowMenu, ConfirmDialog, EmptyState — inline styles / CSS variables only, no CSS framework, no new dependencies. Commit: feat(ui): tokens + shared shell components.
(2) W1 app shell + breadcrumb on every guarded route (/sessions*, /environments*, /admin): 48px bar, every crumb a link, Admin/Logout move into the bar, ad-hoc headers in SessionList/EnvironmentList and the "Back to sessions" links removed; Workspace and Flow bodies become height: calc(100vh - 48px) with the three-pane layout, draggable dividers and useWorkspaceSocket untouched; deep link to /sessions/:id/workspace renders a skeleton crumb then the full crumb after GET /sessions/:id; env crumb "Unassigned" when target_environment_id is null. Commit: feat(web): app shell + breadcrumb.
(3) W2 + W5: new route /environments/:id/sessions (SessionList reused with env filter, title "Sessions in {display_name}"); EnvironmentList cards as entry points with "N active · M archived" counts (client-side from GET /sessions?status=all filtered by target_environment_id — G2 server filter optional), "Open sessions" primary and "Audit" secondary actions; global /sessions = "All environments" with environment filter chips and an "Unassigned" group; "+ New Session" inside an environment pre-selects targetEnvironmentId. Session SubHeader on /sessions/:id*: inline-editable title (PATCH title), status chip, environment name + production badge, the relocated Session Env strip items (install context · privacy mode · gate state · spec-hash) and an explicit "← Sessions in {env}" button. Commit: feat(web): environment-scoped sessions + session subheader.
(4) W3 archive-not-delete: overflow menu on session cards and detail -> Rename (PATCH title) and Archive (ConfirmDialog -> DELETE /sessions/:id, which is the backend's soft archive; warning copy, not a block, when the session is live); lists default to GET /sessions?status=active with paused shown inside Active; Active | Archived tab pair with count badge; archived rows muted with Restore (PATCH { status: 'active' }); empty states for both tabs; archived sessions still open read-only with an "Archived" chip + Restore in the subheader. Network must show only DELETE then PATCH for archive/restore. Commit: feat(web): archive and restore sessions.
(5) W4 + G1: backend migration packages/backend/src/db/migrations/028_environment_status.sql (ALTER TABLE environments ADD COLUMN status VARCHAR(20) NOT NULL DEFAULT 'active' + index; follow the 014/015 migration pattern), PATCH /environments/:id { status: 'active' | 'inactive' } and GET /environments?status= (default active) in environment.routes.ts (never a hard delete); UI: Mark inactive / Reactivate in the environment overflow menu, Active | Inactive tabs on /environments, warning listing the active-session count when marking inactive (sessions not auto-archived), and the affordance hidden when PATCH returns 404. packages/backend `tsc` clean; run the existing backend e2e suites 11 and 12 if they can run in this environment (state clearly if they cannot). Commit: feat(environments): inactive status (migration 028 + PATCH) and W4 UI.
(6) Verification + handback: Playwright spec e2e/playwright/nav-archive.spec.js per handoff section 6 (deep-link crumb -> env crumb -> filtered list; archive -> Archived tab with badge -> Restore; workspace body calc(100vh - 48px) with no page scrollbar and working dividers; never waitForLoadState('networkidle'); SSO button getByRole('link', { name: /Continue with Microsoft/i }); --host-resolver-rules for new hostnames). Run it against a local `vite preview` + local backend if runnable here, otherwise against the dev lane http://192.168.21.163:3000 read-only, and quote the pass count honestly (a spec that could not run is reported as NOT RUN, never as passing). Write design/ux/nav-archive/HANDBACK.md in the existing HANDBACK format: screens table (list what was built, mark canvas/screenshots as "not produced by this run"), deliberate divergences, kept invariants, notes for implementers, open items, the list of new src/ui components + routes added/changed, the Playwright spec name + pass count, suites 11/12 status, and G1/G2 status. Commit: docs(ux): nav-archive handback.

HARD RULES: no changes to AuthGuard/MSAL flow, useWorkspaceSocket, the three-pane workspace body, approval-gate cards, or any .github/** file; only G1/G2 backend additions; never hard-delete; the word "Delete" never appears in UI copy; no new npm dependencies; no secrets; commit by explicit path; no push. Completion = commit hashes, tsc/vite build output tails for frontend and backend, the Playwright result (or NOT RUN with reason), and a W1-W6/G1/G2 checklist done/not-done. Auditor: no git fetch; grep the diff for "Delete" in UI copy (must be absent), confirm WorkspaceLayout still imports and uses useWorkspaceSocket unchanged, confirm no .github changes and no package.json dependency additions.
