# TASK — Cognizioware Ops Control Center (claims365-lab pattern, Blazor Server .NET 10 on CT202, one Entra boundary)

**Owner session:** Claude Code `6803faad-e27d-43c0-8150-328e549e1fb1` (cwd `c:\Users\PaxtonTait\source\cognizioware-mcp-tools`).
The overseer (LongHorizon-Harness session) is the MANAGER for this task: it created the worktree, launched and monitors the harness run,
and schedules the owner's post-processes. The owner session applies its plan's post-run steps (PRs, cross-link PR) when the overseer says go.
**Harness run:** `20260905T101715Z_c80b1224` (attempt 3, 2026-09-05 10:17 PT, manager/executor `kimi-k2.7-code:pool`, auditor `kimi-k3:pool`). Earlier: 2ff4e39d (wrong payload keys), 4a316bf3 (died on Ollama quota: prax211 session limit; PR-A committed as 375326a).
**Authoritative spec (read FIRST, follow exactly):** `C:\Users\PaxtonTait\.claude\plans\c-users-paxtontait-source-claims365-lab-snoopy-rossum.md`
(WSL `/mnt/c/Users/PaxtonTait/.claude/plans/c-users-paxtontait-source-claims365-lab-snoopy-rossum.md`).
**Harness workspace (dedicated worktree, WSL-created and LOCKED):** `/mnt/c/Users/PaxtonTait/source/cognizioware-mcp-tools-ops`
— branch `feat/ops-control-center` from `origin/main` @ a708f23. Windows git shows "not a git repository" there; use WSL git.
Never `git worktree prune`/`remove` from Windows git in this repo while WSL worktrees exist (see TEMPLATE-overseer-hierarchy, WSL worktrees).
**Pattern source (read-only):** `C:\Users\PaxtonTait\source\claims365-lab\post-seminar-zip\Production-Ready-AI-Agents-08-Control-Center\ControlCenter\`.
**Live Caddyfile snapshot (for the reconcile step):** `C:\Users\PaxtonTait\source\.lh-harness\live-snapshots\ct202-Caddyfile-2026-09-05.txt`
(two live-only hunks vs repo: terranas01 alias on @tos; harness.lan.easybutt0n.ai route). Refresh it before any later re-run.
**Toolchain note:** WSL has docker 29.7 and python3, NO dotnet SDK — the run verifies via `docker build`/`docker run` only.

## Model trio (cloud-ollama standard)
manager `glm-5.3:cloud` · executor `kimi-k2.7-code:cloud` · auditor `kimi-k3:cloud`

## Launch payload
```json
{"model":"kimi-k2.7-code:cloud","agent":"claude_code",
 "workspace":"/mnt/c/Users/PaxtonTait/source/cognizioware-mcp-tools-ops",
 "roles":{"manager":{"agent":"claude_code","model":"glm-5.3:cloud"},
                 "executor":{"agent":"claude_code","model":"kimi-k2.7-code:cloud"},
                 "auditor":{"agent":"claude_code","model":"kimi-k3:cloud"}},
 "max_rounds":22,"task":"<task text below>"}
```
POST to `http://127.0.0.1:8799/api/runs` (WSL workbench), JSON via stdin `--data-binary @-`.

## Task text (paste verbatim, one slice per round)
BUILD-ONLY, UAT-FIRST. Read the authoritative spec FIRST and follow it exactly: /mnt/c/Users/PaxtonTait/.claude/plans/c-users-paxtontait-source-claims365-lab-snoopy-rossum.md (Windows C:\Users\PaxtonTait\.claude\plans\c-users-paxtontait-source-claims365-lab-snoopy-rossum.md). Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-mcp-tools-ops (branch feat/ops-control-center from origin/main @ a708f23); never cd into or run git against /mnt/c/Users/PaxtonTait/source/cognizioware-mcp-tools or any other checkout; plain git commands only (never --git-dir/--work-tree). Pattern source to COPY from (read-only, NO edits there): /mnt/c/Users/PaxtonTait/source/claims365-lab/post-seminar-zip/Production-Ready-AI-Agents-08-Control-Center/ControlCenter/. Deliver spec sections 1-9 as four commit groups, one slice per round, in this order:
(A) PR-A app: infrastructure/docker/ops-control-center/** per spec section 1 — Dockerfile (sdk:10.0 -> aspnet:10.0 + python3, dotnet publish, MapStaticAssets, Production), .dockerignore, README.md (claims README structure), doctor/ops_doctor.py (stdlib-only Python 3, exact m3_doctor.py contract {ok, checks:[{name, ok, detail, fix}]} + optional severity "warn", --json | human, parallel probes 8 s each < 20 s total, NEVER prints a secret — set/not-set only) and doctor/targets.json (NON-SECRET registry built from CLAUDE.md admin table, e2e suite 26 CADDY_ROUTES, e2e/fixtures/fleet.json lan), src/OpsControlCenter/** (csproj net10.0 zero PackageReference; Program.cs; verbatim lifts: App/Routes/_Imports, MainLayout(+css), ReconnectModal, Error, NotFound, wwwroot/app.css; pages ReadMeFirst "/", Overview "/overview" (Setup.razor.css doctor list + column-reverse terminal panel + Run doctor), Catalog, E2e, CertsDns, AdminCentres, Audit; Models/OpsModels.cs; Services OperatorIdentity, DoctorRunner (RunAsync timeout+Kill(entireProcessTree), DoctorAsync parse last "{" line, non-zero exit is a result), DoctorStore+DoctorPoller (BackgroundService+PeriodicTimer), CatalogStore (+SSE-frame parser ported from e2e/suites/22-aggregate-baseline.test.js), E2eStore (tmp+rename, keep 50), CiStatusStore (optional, OPS_GITHUB_TOKEN), AuditLog (/data/audit.jsonl), IngestEndpoints (MapGroup /api: GET /healthz no key; GET /api/doctor (?run=1 -> 202), POST /api/ingest/e2e 2 MB cap, dryRun -> 202; bearer OPS_INGEST_KEY constant-time compare, 401 otherwise, 503 if unset; never returns identity or secrets)). Delete FraudDesk/Frontline/Triage, DemoStores, FraudDeskStore, Data/, RangePicker, /subimg. Purpose comment at the top of every file; every failed check carries its fix. Doctor checks per spec section 7 (gateway, compose stack via ops-docker-socket-proxy GET /containers/json only, off-box backends warn, DNS via raw UDP query pinned to Pi-hole 192.168.21.3 + DoH 1.1.1.1, certs >= OPS_CERT_MIN_DAYS with tailnet certs warn-only unless OPS_TS_ENFORCE=1, admin centres warn, self). There is NO dotnet SDK on this box: verify with docker only — docker build the image, run it under ASPNETCORE_ENVIRONMENT=Production with a throwaway env, then quote: curl -I http://localhost:8089/_framework/blazor.web.js -> 200, GET /healthz -> ok, GET /api/doctor without key -> 401 and with key -> 200 JSON. Also run python3 doctor/ops_doctor.py --json from this WSL box (it is on the LAN) and quote the check names + ok flags (never secret values).
(B) PR-B wiring: FIRST reconcile live -> repo for infrastructure/docker/caddy/Caddyfile: the live CT202 file is snapshotted at /mnt/c/Users/PaxtonTait/source/.lh-harness/live-snapshots/ct202-Caddyfile-2026-09-05.txt; it differs from the repo in exactly two hunks (the @tos matcher also lists terranas01.lan.easybutt0n.ai with its 4-line comment, and a trailing @harness host harness.lan.easybutt0n.ai -> reverse_proxy 192.168.21.168:8799 handle block added 2026-08-29) — port both into the repo file verbatim so a synced file never drops live routes, THEN add the ops.easybutt0n.ai block after the fleet.easybutt0n.ai block (tls dns cloudflare {env.CLOUDFLARE_DNS_API_TOKEN} resolvers 1.1.1.1 8.8.8.8; encode gzip; reverse_proxy ops-oauth2-proxy:4180). Then infrastructure/docker-compose.yml: append after aidev02-oauth2-proxy the three services ops-control-center (build ./docker/ops-control-center, expose 8080, env_file /opt/cognizioware-mcp-tools/mcp-tools.env, volume ops-data:/data, networks cognizioware-mcp-tools + ops-internal, all env per spec section 2 as ${VAR:-default}, healthcheck wget /healthz), ops-oauth2-proxy (quay.io/oauth2-proxy/oauth2-proxy pinned >= v7.7, provider entra-id, CLIENT_ID=${OPS_AZURE_CLIENT_ID:-${AZURE_CLIENT_ID}}, CLIENT_SECRET=${OPS_AZURE_CLIENT_SECRET:-${AZURE_CLIENT_SECRET}}, COOKIE_SECRET=${OPS_COOKIE_SECRET:-${OAUTH2_COOKIE_SECRET}}, COOKIE_NAME=_ops_oauth2, OIDC_GROUPS_CLAIM=roles, ALLOWED_GROUPS=${OPS_ALLOWED_ROLES:-Global.Admin,Billing.Admin}, SKIP_AUTH_ROUTES=^/healthz$,^/api/doctor$,^/api/ingest/.*, REDIRECT_URL=https://ops.easybutt0n.ai/oauth2/callback, upstream http://ops-control-center:8080), ops-docker-socket-proxy (tecnativa/docker-socket-proxy pinned, CONTAINERS=1 PING=1 VERSION=1 POST=0, socket :ro, ONLY on networks.ops-internal internal: true, no ports) plus networks.ops-internal and volumes.ops-data. infrastructure/.env.example: "# --- ops control center ---" block with OPS_COOKIE_SECRET=generate-with-openssl-rand-hex-16, OPS_INGEST_KEY, OPS_LITELLM_KEY, OPS_AZURE_CLIENT_ID, OPS_AZURE_CLIENT_SECRET, OPS_GITHUB_TOKEN= (optional), OPS_ALLOWED_ROLES=Global.Admin,Billing.Admin (placeholders only). design docs/mcp-placeholder-services-audit-and-plan.md: new "## 10. Ops control center — secrets runbook" per spec section 9. .github/workflows/deploy-mcp-tools.yml — workflow edits ARE explicitly allowed for this task but ONLY these three additions from spec section 8: (1) e2e-tests: after "Upload test results", if: always(), non-blocking node -e payload from e2e/results.json + .skips.json + baseline.json, curl -sf -X POST https://ops.easybutt0n.ai/api/ingest/e2e with Bearer ${{ secrets.OPS_INGEST_KEY }}, ::warning:: on failure; (2) deploy: extend the sync file list with the Caddyfile, push to Caddyfile.new, caddy validate, mv into place, caddy reload (graceful; never restart the container); (3) post-deploy public health block: add https://ops.easybutt0n.ai/ping warning-only. Do not touch job names, concurrency groups, or any other step. CLAUDE.md admin-centres table row "| Infra control center | https://ops.easybutt0n.ai/ | CT202 ops-oauth2-proxy -> ops-control-center:8080 (Entra App Roles) |" plus one bullet naming the three services. UAT substitute (there is no UAT lane for CT202 compose services — say so in the handoff): docker compose config must validate, and run a local docker compose smoke of the three new services with a throwaway gitignored env file (dummy values), quoting docker compose ps and, from inside the app container, curl -X POST http://ops-docker-socket-proxy:2375/containers/x/restart -> 403.
(C) PR-C e2e: e2e/suites/29-ops-control-center.test.js (soft-fail via e2e/lib/skip-registry.js with RUN_LIVE_REGRESSION escalation; copy getPeerCert/piholeResolver/isSkippable from suite 26; cases A Pi-hole ops. -> .161 and public DoH A == litellm. A; B cert on .161 SNI ops. LE >= 21 d; C /ping 200, unauth / -> 302/403, /healthz 200; D /api/doctor schema with key, 401 without; E POST /api/ingest/e2e dryRun -> 202, no key -> 401), e2e/package.json "test:ops", e2e/.env.example OPS_INGEST_KEY=, workflow .env block adds it (this is the only other workflow line allowed), suite 26 additions (ops.easybutt0n.ai in GATEWAY_RECORDS for 26A and an own-cert case in 26B2, NOT 26E — note why in the suite header), e2e/README.md row 29. Run npm run test:ops and npm run test:fleet-dns-tls from e2e/ and quote the output (expected to soft-skip until deployed).
(D) PR-D handoff: design docs/handoff-docs/ops-control-center-handoff.md (spec identity header, machines touched, what shipped with commit hashes, verification transcript, open items: UAT-lane gap, v2 actions design, the mcp-cognizioware cross-link PR) and the operator-steps checklist.
HARD RULES: no secrets or real key values anywhere (placeholders only); no host/LXC/CT202/DNS/Cloudflare/Entra/GitHub-secret changes (all operator steps, listed in the handoff); no edits to claims365-lab; no edits outside the allowed workflow lines; the mcp-cognizioware toolbar cross-link PR is OUT OF SCOPE (owner session does it); commit by EXPLICIT PATH per commit group (CRLF phantoms — never git add -A); no push. Completion = commit hashes for A/B/C/D, the docker build/run + compose smoke + doctor + e2e outputs quoted, and the operator checklist restated (Entra Web redirect URI https://ops.easybutt0n.ai/oauth2/callback + client secret on admin app 240a59df with expiry noted; Cloudflare grey-cloud CNAME ops -> litellm.easybutt0n.ai; Pi-hole dns.hosts 192.168.21.161 ops.easybutt0n.ai via pct snapshot 105 + pihole-FTL --config; five OPS_* values in CT202 mcp-tools.env; GitHub secret OPS_INGEST_KEY; LiteLLM virtual key with all MCP access groups, no model budget). Auditor: no git fetch.

## After completion (post-processes)
1. **Overseer:** review commits A-D on `feat/ops-control-center`; confirm the Caddyfile reconcile kept both live hunks; confirm the workflow diff is
   limited to the three allowed additions + the `.env` line (run the strict duplicate-key YAML loader before push).
2. **Owner session 6803faad (on overseer go):** push the branch; open PR(s) to `main` (A app, B wiring, C e2e, D handoff — one PR or stacked, its call);
   the separate one-line toolbar cross-link PR in `mcp-cognizioware` (`wwwroot/admin/index.html` toolbar, target the `develop`/44c4ccc2 lineage).
3. **Manual one-time steps (Paxton):** Entra portal — Web redirect URI `https://ops.easybutt0n.ai/oauth2/callback` + client secret on admin app
   240a59df (record expiry); Cloudflare grey-cloud CNAME `ops` → `litellm.easybutt0n.ai`.
   **Operator (overseer, with Paxton's go):** Pi-hole dns.hosts record (snapshot 105 first); five `OPS_*` values in CT202 `mcp-tools.env`
   (durability rule: env file, `--env-file` reload only); GitHub secret `OPS_INGEST_KEY`; LiteLLM virtual key with all MCP access groups.
4. **cognizioware-qa gate — compatibility notice:** QA has no suite for the ops control center. The in-repo suite 29 + the local compose smoke are the
   UAT substitute (no UAT lane exists for CT202 compose services — first deploy is prod after the smoke). Before merge, run the standard gate
   (target=litellm on UAT) to prove the LiteLLM config is untouched; if QA cannot cover it, notify Paxton (noted here).
5. Merge → normal lane (E2E gate → UAT litellm → QA gate → CT202 deploy → QA verify → smoke). Watch the first run: the .NET SDK image pull is ~800 MB
   against the 30-min job timeout. Then verification per spec: `docker compose ps` healthy ×3, `/ping` 200, unauth `/` → 302 to login.microsoftonline.com,
   Global.Admin sign-in shows the footer email (Paxton), Caddy log shows the `ops.` cert, next `main` push ingests e2e (202) and the E2E page shows it.

## Operator log (2026-09-05)
- Entra: Paxton created a DEDICATED app `ops.easybutt0n.ai`, client `2562701c-211c-44cf-bb2c-25c605ad875e`, tenant `d647920c-…`; secret `control-panel-ops`
  expires **2028-09-04** (spec §3 fallback path — App Roles `Global.Admin`/`Billing.Admin` must be defined on THIS app and assigned; Web redirect
  URI `https://ops.easybutt0n.ai/oauth2/callback`; optional `email` claim). Not the 240a59df admin app.
- CT202 `mcp-tools.env` (backup `mcp-tools.env.bak-<ts>` beside it): OPS_PUBLIC_URL, OPS_AZURE_CLIENT_ID, OPS_AZURE_CLIENT_SECRET, OPS_COOKIE_SECRET,
  OPS_INGEST_KEY, OPS_ALLOWED_ROLES, OPS_GITHUB_REPO, OPS_LITELLM_KEY (virtual key alias `ops-control-center`, all 22 MCP access groups, no budget). Done.
- GitHub secret `OPS_INGEST_KEY` on cogniziocompany/cognizioware-mcp-tools. Done.
- Pi-hole (LXC 105, snapshot `pre-ops-record-20260905`): `192.168.21.161 ops.easybutt0n.ai` added; resolves. Done.
- Cloudflare: CNAME `ops` → `litellm.easybutt0n.ai`, grey cloud (Paxton, via dashboard).
- Side fix while here: the CT202 LiteLLM DB `litellm_settings` had `mcp_semantic_tool_filter` re-enabled with `text-embedding-3-small` (no deployment →
  ~8k "no healthy deployments" errors/24h since 06:39). Removed the key from `LiteLLM_Config` (same surgical recipe as 2026-04-05). Container restart
  pending a quiet window if the errors persist.

## Completion (2026-09-05 ~11:15 PT)
Run `20260905T101715Z_c80b1224` completed (8 rounds, final audit complete/clean/aligned) and was ended at its completion gate.
Branch `feat/ops-control-center`: 375326a PR-A · de005b5 PR-B · 236e653 e2e suite **32** (29-31 taken) + suite 26 · 1945dad/f740635 handoff ·
51c6b36 kb-hook fix (overseer) · **ea19a1f (overseer)**: the run's Caddyfile sync step would have failed on deploy (copied PVE /tmp inside pct exec,
validated a host path inside the caddy container, `mv` on a single-file bind mount → old inode); replaced with pct push → docker cp + validate
in-container → overwrite in place with `cat >` → reload. Overseer review: workflow diff limited to the allowed additions, strict YAML clean,
both live Caddyfile hunks retained, no secret values. Owner 6803faad given GO to push + PR (no merge: merge = prod deploy, needs Paxton).

PRs: mcp-tools **#64** `feat/ops-control-center` → main (do-not-merge note); mcp-cognizioware **#65** `chore/admin-infra-link` → develop (toolbar Infra link).

## Deployed (2026-09-05 ~20:55 PT)
mcp-tools #64 merged (98ba424). The first lane was "green" but deployed nothing (deploy job bug: ssh inside `while read` synced only the
first build context; `| tail -25` masked the failed compose up) — fixed in #66; #67 (xrm build needs a cross-repo token: secret
XRM_BUILD_GH_TOKEN) and #68 (dataverse-data-mcp has no lockfile → npm install) were the next latent failures. Lane bded802 (run
33987710207) is the first honest full deploy: ops-control-center, ops-oauth2-proxy, ops-docker-socket-proxy up; `/ping` 200; `/` → 302
to login.microsoftonline.com with client 2562701c; `/healthz` 200; `/api/doctor` 401 without key; socket-proxy POST → 403; LE cert valid
to 2026-12-04. Leftover: ops-oauth2-proxy healthcheck uses wget (distroless image has none) → shows "unhealthy" while serving correctly.
Waiting on Paxton's sign-in proof (footer email, seven pages, Run doctor).
