BUILD + TEST (code; QA repo; the overseer dispatches the web-ux gate after merge): third pass on the web-ux runner flow for the Env->Session v2 app. Gate run 34228185809 (dev, after qa PR #19) now creates sessions fine (4/7 pass) but three browser steps fail: WU-SHELL "App shell + breadcrumb render on Environments page" -> actual "shell=false breadcrumb=false processStrip=false title=Cognizioware - Power Platform Builder crumbs="; WU-ARCHIVE "row/overflow menu not found"; WU-RESTORE "archived row not found". Ground truth the overseer verified against cognizioware-powerplatform origin/develop (deployed on https://powerplatform-dev.easybutt0n.ai, bundle contains process-step- and aria-label Breadcrumb): routes in packages/frontend/src/App.tsx are `/environments` (EnvironmentList), `/environments/:id/sessions` (SessionList), `/environments/:envId/sessions/new` (SessionWizard) - there is NO `/sessions` route, and the strings `tab=archived` / `status=archived` do not exist anywhere in the frontend (the Archived view is component state, not a URL). The Breadcrumb component is packages/frontend/src/ui/Breadcrumb.tsx; crumbs are defined per page (e.g. SessionDetail.tsx and flow/FlowVisualizer.tsx pass [{label:'Environments', to:'/environments'}, ...]); check whether EnvironmentList renders the Breadcrumb/shell at all (if not, WU-SHELL must assert on the sessions page instead and say so). Work ONLY in /home/harness/work/cognizioware-qa on branch fix/web-ux-v2-routes (run `git fetch origin && git checkout -B fix/web-ux-v2-routes origin/master` first); READ the pp frontend from /home/harness/work/cognizioware-powerplatform-evalfix (READ-ONLY: `git -C /home/harness/work/cognizioware-powerplatform-evalfix show origin/develop:packages/frontend/src/...` after a fetch there; never checkout/commit in that repo): App.tsx, ui/Breadcrumb.tsx, ui/Shell* (or whatever renders the header), environments/EnvironmentList.tsx, sessions/SessionList.tsx (row markup, overflow menu button label/aria, Active/Archived toggle, archive/restore ConfirmDialog titles), sessions/SessionDetail.tsx. Then fix packages/runner/src/flows/web-ux-nav-archive.ts: navigate to `/environments/<WEB_UX_ENV_ID>/sessions` (never /sessions), find the throwaway session row by title in the v2 SessionList markup, open its overflow menu using the real accessible name, archive via the real dialog, switch to the Archived view via the real control (button/tab), restore, and make WU-SHELL assert the real shell/breadcrumb markup on the page that actually renders it. Every selector you use must be quoted from the pp source in a comment above it (file:line). Unit tests with fixtures updated (no live calls); docs/web-ux-gate.md v2 contract corrected (remove the ?tab=archived claim); CHANGELOG. HARD RULES: explicit-path commits, no push, no live calls, no screenshots written during unit tests, env names only. Completion = commit list, diff stat, test count, and a table step -> selector -> pp source file:line. Auditor: verify each selector cites a real line in the pp source and that /sessions is not used anywhere.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
