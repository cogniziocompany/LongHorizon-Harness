BUILD-ONLY. Read the authoritative plan FIRST and follow it exactly: /home/harness/work/cognizioware-hydra/design/ship-plan-integration/10-ship-plan-reporting-hydra.md and /home/harness/work/cognizioware-hydra/design/inputs/ship-plan.html. Work ONLY in this worktree /home/harness/work/cognizioware-hydra on branch feat/ship-plan-reporting from origin/main (current tip; includes PRs 8 and 9 with the ship-plan design); never cd into or run git against any other checkout; plain git commands only; commit by EXPLICIT PATH; no push. Node is /usr/bin/node (v22). This run delivers the Hydra Ship Plan data model, REST API, and generated web UI page. Slices, one per round:
(1) Database / model: add orchestrator/src/ship-plan-store.js with Plan/Lane/Step entities (id, plan_id, repo, lane_name, base_ref, target_env, sort_order, seq, pr_ref, action, gate_before_next, rollback, state, owner, youtrack_issue_id, needs_paxton, notes, rules_version). Use the existing SQLite or persistent store pattern if present; otherwise a JSON-backed store under /data with atomic writes. Keep existing fleet/terminal routes untouched.
(2) REST API: add routes under /api/ship-plan mirroring 10-ship-plan-reporting-hydra.md §3.3 — GET /api/ship-plan (current plan JSON), GET /api/ship-plan/steps/:stepId, POST /api/ship-plan/steps, POST /api/ship-plan/steps/:stepId/comment, POST /api/ship-plan/steps/:stepId/time. Reuse the existing requireApiKey / actorFrom middleware (orchestrator/src/index.js:24-29, orchestrator/src/activity-log.js:48-54). Log every mutation to ActivityLog with target shipplan:<planId|stepId>.
(3) Web UI: add a new /ship-plan route in orchestrator/public/index.html (or a standalone ship-plan.html served by the orchestrator) that renders the status strip, per-repo lanes table (#, PR, Action, Gate before next, Rollback, State, Owner), Needs Paxton list, and rules footer. Reuse the artifact's CSS variables and chip colors (ship-plan.html:4-16, :46). Fetch /api/ship-plan live; states update without a page reload.
(4) Verification + docs: add self-check tests or run the existing npm test suite if present; open the generated page in a browser and compare the static artifact structure. Verify git diff --stat origin/main..HEAD touches only orchestrator/src, orchestrator/public, and design/ship-plan-integration/ (docs updates only). Quote the rendered page structure match.
HARD RULES: no direct YouTrack calls from Hydra (comment/time endpoints call mcp-cognizioware through the event bridge or Phase-1 MCP path only); no secrets in UI or API responses; no changes outside orchestrator/src, orchestrator/public, and design docs; commit by EXPLICIT PATH; no push. Completion = commit hashes, git diff --stat origin/main..HEAD, the test/self-check output, and a description of the rendered page vs. ship-plan.html. Auditor: no git fetch; confirm the diff contains no YouTrack token and no changes to existing /fleet/*, /instances/*, /devices/*, /mcp, or /fleet-mcp behavior.
