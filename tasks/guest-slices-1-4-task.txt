BUILD + TEST (code; the overseer merges, deploys through the dev lane and runs the click-through): implement guest-launch gap SLICES 1 and 4 from design/plans/guest-launch/E2E-GAP-SLICES-2026-09-07.md (READ IT FIRST, plus E2E-READINESS-AUDIT-2026-09-07.md and e2e/guest-e2e/checklist.json - they cite file:line for everything that exists). Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch feat/guest-slices-1-4 (run `git fetch origin && git checkout -B feat/guest-slices-1-4 origin/develop` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls to Stripe, Dataverse, n8n or the lanes. You MAY READ (never write) /home/harness/work/mcp-cognizioware (billing service: signup, guest checkout, user_organizations) to match its contracts exactly; if the billing service needs a change, STOP and describe it precisely instead of editing it.

GOAL (Paxton): first full dev click-through with the Stripe TEST card on 2026-09-11. Slices 1 and 4 are the dependency root.

SLICE 1 - Guest onboarding + customer binding (M): `POST /api/auth/guest-onboard` {email, tenantId} -> ensures a billing row keyed by the env-named test customer (env NAME `STRIPE_TEST_CUSTOMER_ID`; the mapping of the test principal email to that customer is allowed ONLY when NODE_ENV != production and a `GUEST_TEST_PRINCIPAL_EMAIL` env matches - production must refuse). verifyAuth for the test principal returns a UserContext whose stripeCustomerId equals that env value; unknown customer -> redirect to guest signup. Tests: guest-onboard.service.test.ts (row created; production refuses the mapping), auth.service.test.ts extension.
SLICE 4 - Environment grant API + UI (M): `POST /api/environments/:id/grants` {principalEmail, expiresAt?} (admin or service principal only), `GET /api/environments/:id/grants`, `DELETE /api/environments/:id/grants/:grantId` (soft revoke), migration 031_environment_grant_admin.sql (029_environment_grants.sql is already applied - extend, never rewrite), "Add guest grant" button in the admin Environments panel, and the grant check at `POST /api/sessions` with targetEnvironmentId -> 201 with a live grant, 403 when revoked/expired. Tests: environment-grants.test.ts (CRUD + revoke), extend guest-slice-2.auth.test.ts.

RULES: display names never schema names where users look; no secrets (env names only; the test customer id is referenced by NAME); every new route has an integration test; keep existing tests green (`npm test` in packages/backend and packages/frontend); one commit per slice plus one for the migration; update e2e/guest-e2e/checklist.json entries that these slices satisfy (mark `implementedBy`). Completion = commit hashes, `git diff --stat origin/develop..HEAD`, test summary, and the exact dev-lane env NAMES the operator must set (STRIPE_TEST_CUSTOMER_ID, GUEST_TEST_PRINCIPAL_EMAIL, Stripe__Guest*, BILLING_API_URL, BILLING_API_KEY). Auditor: no git fetch; verify production refuses the test mapping, the 403 path is tested, and no secret values appear.
