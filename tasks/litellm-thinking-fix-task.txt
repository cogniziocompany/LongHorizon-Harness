BUILD + TEST (LiteLLM gateway code/config in cognizioware-mcp-tools; deploy is the overseer's, uat router first). Goal: make thinking/reasoning models usable behind the LiteLLM Anthropic-messages adapter for Claude Code clients, so `glm-5.3:cloud` can return as the harness MANAGER and `qwen3.8` (the ONLY approved local model family per Paxton: qwen3.8 and newer; anything older is an explicit exception) can serve every role including the executor. Work ONLY in this workspace /home/harness/work/cognizioware-mcp-tools (run `git fetch origin && git checkout -B fix/litellm-thinking-blocks origin/main` first); plain git only; commit by EXPLICIT PATH; no push; never call the live routers (CT202 prod, CT204 uat) from this run — the overseer applies and tests live. Node is /usr/bin/node; python3 is available; the LiteLLM image is `ghcr.io/berriai/litellm` v1.93.0 (pinned by digest in infrastructure/docker-compose.yml — do not change the pin without saying so).

Verified failures (overseer, 2026-09-06 — cite them in the doc):
F1. `Content block is not a thinking block` — raised by LiteLLM's Anthropic adapter (`/v1/messages`) when a Claude Code client echoes a previous assistant turn that contains a `{"type":"thinking","thinking":"…","signature":null}` block back to a non-Anthropic deployment. Seen with `ollama_chat/glm-5.3:cloud` (manager, 2026-09-05) and `openai/qwen3.8:27b` @ Ollama `/v1` (executor, 2026-09-06). `merge_reasoning_content_in_choices: true` did not prevent the adapter from emitting the thinking block.
F2. `500 {"error":"no user query found in messages"}` from Ollama `/api/chat` when LiteLLM's `ollama_chat` provider translates a Claude Code `user` turn whose content is only `tool_result` blocks (plus an optional `<system-reminder>` text block). Direct Ollama calls with a trailing role=tool message work.
F3. `400 OpenAIException - "qwen3-coder:30b" does not support thinking` from Ollama `/v1` when Claude Code sends `thinking: {type: enabled, budget_tokens: …}` and LiteLLM forwards it as a reasoning parameter to a non-thinking model. Workaround in use: `MAX_THINKING_TOKENS=0` in the workspace's `.claude/settings.local.json`.
Working today: `kimi-k2.7-code:pool` / `kimi-k3:pool` (ollama_chat, cloud) for all roles; `openai/qwen3-coder:30b` with F3 worked around.

Deliverables, one slice per round:
(1) Reproduction harness: `infrastructure/litellm-tests/thinking-roundtrip.mjs` (node, no deps) that, given LITELLM_BASE_URL/LITELLM_KEY/MODEL env, runs the Anthropic-format sequence Claude Code produces — (a) tools + user ask → expect tool_use; (b) echo the FULL assistant content back (including any thinking block) + tool_result user turn → expect end_turn; (c) same with `thinking:{type:"enabled",budget_tokens:1024}` in the request; (d) a user turn with tool_result blocks only. It prints PASS/FAIL per step with the raw error, and a summary matrix per model. Include fixture payloads under `infrastructure/litellm-tests/fixtures/` (invented content). Commit: test(litellm): thinking/tool round-trip reproduction harness.
(2) Root-cause the three failures in LiteLLM 1.93.0 source (read the installed package: `pip download litellm==1.93.0` is NOT available offline — instead read the vendored code path names from the error strings and LiteLLM's public GitHub at the matching tag via `git clone --depth 1 --branch v1.93.0 https://github.com/BerriAI/litellm` into /tmp if network allows; otherwise document what you could not read as UNVERIFIED). Write `design/litellm/thinking-blocks-rca.md`: for each of F1–F3, the code path (file:line at v1.93.0), why it fails, and whether a newer LiteLLM release fixes it (cite the changelog/PR if found; else UNVERIFIED). Commit: docs(litellm): RCA for thinking-block and tool-turn translation failures.
(3) The fix, in this order of preference: (a) a LiteLLM `CustomLogger`/pre-call hook (`infrastructure/docker/litellm/hooks/thinking_normalizer.py`, wired via `litellm_settings.callbacks` in litellm-config.yaml) that, for deployments whose provider is not `anthropic`, (i) strips `thinking`/`redacted_thinking` blocks from incoming assistant messages, (ii) drops the `thinking` request parameter (or maps it to nothing) for models flagged `supports_reasoning: false` in model_info, (iii) rewrites a `user` turn that is tool_result-only for `ollama_chat` deployments into the shape Ollama accepts (tool messages followed by a minimal user text) — each rule behind a config flag with defaults on; (b) if a hook cannot intercept the Anthropic-messages route in 1.93.0, propose the minimal LiteLLM version bump with evidence and the exact config changes, and still ship the hook for the parts it can reach. Unit tests for the hook with the fixtures (pytest if available, else a plain python test runner). Commit: feat(litellm): thinking-block/tool-turn normalizer hook for non-Anthropic deployments.
(4) Config + rollout: litellm-config.yaml changes (hook registration; `supports_reasoning: true` on qwen3.8 and glm-5.3 deployments; keep `qwen3.8` on `openai/…/v1`); `infrastructure/litellm-tests/README.md` with the exact rollout recipe for the overseer: apply to CT204 (uat) with `--env-file mcp-tools.env`, run thinking-roundtrip.mjs against uat for models `qwen3.8` (primary target), `glm-5.3:cloud`, `kimi-k2.7-code:pool` (`qwen3-coder` was a one-off diagnostic and is NOT a target), then CT202. Add an e2e suite `e2e/suites/34-litellm-thinking-roundtrip.test.js` that runs the matrix against the router under test (soft-skip off-LAN, like suite 32/33). Commit: feat(litellm): config, rollout recipe and e2e suite 34.
HARD RULES: no live router calls or restarts from this run; no secrets in files (env names only); no changes outside infrastructure/**, e2e/suites/34-*, design/litellm/**; commit by explicit path; no push. Completion = commit hashes, the RCA file:line table, unit-test output, and the rollout recipe. Auditor: no git fetch; verify the hook only touches non-Anthropic deployments, defaults are conservative (never alters Anthropic-provider traffic), fixtures are invented, and no secrets in the diff.
