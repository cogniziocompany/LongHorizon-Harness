BUILD + TEST (code; cognizioware-powerplatform frontend): add a left flyout command bar listing every page of the signed-in app. Paxton's direction, 2026-09-08. Work ONLY in /home/harness/work/cognizioware-powerplatform on branch feat/spa-nav-and-checkout-entry (run `git fetch origin && git checkout -B feat/spa-nav-and-checkout-entry origin/develop` first); plain git; commit by EXPLICIT PATH; no push; env NAMES only; NO live Stripe or Microsoft calls in tests.

CONTEXT: this task is NAVIGATION ONLY. Do NOT add, mount or design any purchase, checkout, pricing or subscribe control. Purchasing does not happen in this app at all - a prospect buys at $39/month on a separate standalone marketing site using Stripe's own embedded snippets, and the email they pay with becomes their account, after which they are provisioned into their own Power Apps environment. This SPA is what they use AFTER that. If you find yourself touching anything under packages/frontend/src/checkout/, stop - it is out of scope.

PART B - the left flyout command bar.
 PRIMARY SURFACE, named by Paxton: https://powerplatform-dev.easybutt0n.ai/sessions* - the sessions list and every session sub-page. It must be present and correct there first. Put it in the shared authenticated app shell so it is also available on /environments and /admin rather than duplicating it per page, but /sessions* is what gets checked.
 Add a left-hand navigation flyout listing every page in the app. Derive the list from the routes already declared in packages/frontend/src/App.tsx rather than hand-maintaining a parallel list that will drift: /sessions, /environments, /admin, and the session sub-surfaces (setup, flow, workspace) shown in context when a session is open. Do not list the two Stripe return routes or the auth routes (/login, /signup, /auth) - they are destinations for redirects, not places a person navigates to.
 Behaviour: collapsed by default so the existing layout is unchanged at rest; opens on click of a control in the top bar; closes on Escape, on clicking outside, and on navigating; marks the current page; and traps focus while open. It must be keyboard reachable and screen-reader sane - a labelled nav landmark, aria-expanded on the toggle, and visible focus. Respect prefers-reduced-motion for the fly-out animation. It must not overlap or hide the existing six-step wizard strip (Pick environment, Refresh, Session setup, Design, Approval, Build) that runs across the top of these pages.
 Match the existing visual language exactly - the app is white surfaces, a strong red accent for primary actions, and a dark step strip. Do NOT introduce a component library, a CSS framework or a new icon set; use what the repo already uses.

TESTS (whatever the repo already uses for frontend tests - report the counts): the flyout opens and closes on click, Escape and outside-click; the current route is marked; the route list excludes the auth and Stripe-return routes; focus returns to the toggle on close. Commit tests in their own commit.

COMMITS (explicit paths): `feat(nav): left flyout command bar listing every page`, then `test(nav): flyout behaviour`.

HARD RULES: do not touch checkout, billing or any backend route; do not change the existing wizard strip; do not add a second source of truth for subscription state; explicit-path commits; no push. Completion = commits, diff stat, test counts, and a plain description of what a signed-in user sees. Auditor: read-only - verify nothing under packages/frontend/src/checkout/ was modified, that the flyout list is derived from the declared routes, and that the wizard strip is untouched.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
