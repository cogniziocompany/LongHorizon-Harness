BUILD-ONLY follow-up (RP-16b) in /home/harness/work/cognizioware-mcp-tools on the EXISTING branch feat/rsi-surfacing-adapters (already checked out; commit f51a8d8 added services/ops-status-mcp and services/qa-run-mcp plus their litellm-config.yaml registration under access group rsi-loop). Never cd into or run git against any other checkout; plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv. Goal: make the two adapters deployable through the repo's normal stack so the config no longer references unresolvable hosts. Slices, one per round:
(1) Dockerfiles: services/ops-status-mcp/Dockerfile and services/qa-run-mcp/Dockerfile (node:22-alpine, npm ci --omit=dev, non-root user, HEALTHCHECK on the server's health route — add a GET /health returning {"ok":true} to each server.js if missing). Commit: build(rsi-loop): Dockerfiles for the surfacing adapters.
(2) Compose: add services `ops-status-mcp` (port 3130) and `qa-run-mcp` (port 3131) to infrastructure/docker-compose.yml following the pattern of an existing MCP service in that file (same network as litellm-router, restart policy, `init: true`, env from mcp-tools.env by NAME only: MCP_API_KEY for the bearer the router presents; OPS_STATUS_BASE_URL + OPS_INGEST_KEY for the ops adapter; QA_API_URL + QA_API_KEY for the qa adapter — reuse existing variable names if the file already defines equivalents, otherwise add them to the env EXAMPLE/README, never with values). Keep the file's existing line endings (it is CRLF — do not rewrite unrelated lines; verify with `git diff --stat` that only your added lines changed). Commit: feat(rsi-loop): compose services for ops-status-mcp and qa-run-mcp.
(3) e2e: add e2e/suites/35-rsi-surfacing-adapters.test.js in the style of suites 32–34 (soft-skip off-LAN; checks each adapter's /health through the compose network hostnames and that the router's /mcp tools/list with access group rsi-loop exposes exactly the nine tools). Update infrastructure/litellm-tests/README.md or the ops docs with a short "RSI surfacing adapters" section (env names only). Commit: test(rsi-loop): e2e suite 35 + docs.
Verification: `docker compose -f infrastructure/docker-compose.yml config -q` must pass (use the repo's example env if compose needs one; do NOT start containers); node syntax check of both servers; the two self-check scripts still run. HARD RULES: no secrets, no changes outside services/ops-status-mcp, services/qa-run-mcp, infrastructure/docker-compose.yml, infrastructure/litellm-config.yaml (only if a hostname/port must be aligned), e2e/suites/35-*, docs; no push; no container starts. Completion = commit hashes, `git diff --stat origin/main..HEAD`, compose config output, and the env variable names the operator must set. Auditor: no git fetch; confirm CRLF preserved in docker-compose.yml (diff touches only added lines), no secrets, Dockerfiles non-root.
