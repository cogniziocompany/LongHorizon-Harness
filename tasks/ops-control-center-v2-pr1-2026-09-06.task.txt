BUILD-ONLY, PR 1 of 5 ("v1.1 read-only revisions"). Read the authoritative plan FIRST and follow it exactly: /mnt/c/Users/PaxtonTait/.claude/plans/c-users-paxtontait-source-claims365-lab-snoopy-rossum.md (Windows C:\Users\PaxtonTait\.claude\plans\c-users-paxtontait-source-claims365-lab-snoopy-rossum.md) — "Ops Control Center v1.1 -> v2". This run delivers ONLY the "PR 1 — v1.1 read-only revisions" row of section 8, i.e. sections 1 (identity/role tiering + ActionGuard/ConfirmActionModal/audit record scaffolding, NO actions wired), 2 (discovery-driven targets) and the GET /api/status half of section 3 (NOT ops-mcp, which is PR 4), plus the handoff addendum. Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-mcp-tools-opsv2 (branch feat/ops-control-center-v2 from origin/main @ d55d6df, which already contains v1 + fixes #69 #71 #72 #73); never cd into or run git against any other checkout; plain git commands only. Live facts: oauth2-proxy v7.8.0 forwards the email in X-Forwarded-User (PREFER_EMAIL_TO_USER) and roles in X-Forwarded-Groups; identity enforcement is ON again in the live env; the key-gated API paths are routed by Caddy straight to the app (@keyed at Caddyfile:99) and the ingest key travels in X-Ops-Ingest-Key. Slices, one per round:
(1) Section 1: OperatorIdentity gains Roles parsed from X-Forwarded-Groups (comma-joined, trim, case-insensitive), IsGlobalAdmin, CanRead; thread OperatorRoles through App.razor -> Routes.razor exactly like the existing three strings; footer role chip. New Services/ActionGuard.cs Authorize(identity, action, typedName, reason) checking in order OPS_ACTIONS_ENABLED == "true" (read in code), authenticated, IsGlobalAdmin, typedName == identity.Display (case-insensitive), reason.Trim().Length >= 10, returning a refusal string. Components/Shared/ConfirmActionModal.razor (typed name + reason + impact text). AuditLog: keep WriteAsync(action, actor, detail) and add the record overload {ts, actor, roles, action, target, reason, result, durationMs}. Do NOT wire any real action (no restarts, no CI dispatch, no DDNS) in this PR; ActionGuard must refuse when OPS_ACTIONS_ENABLED is unset. Note in ReadMeFirst that a role removed in Entra applies at next sign-in. Unit-style test for ActionGuard refusal paths if a test project pattern exists; otherwise a self-check script.
(2) Section 2: Services/TargetsComposer.cs writes /data/targets.generated.json before each doctor run; ops_doctor.py gains --targets <path> (default unchanged); DoctorRunner passes it. Merge rule over static doctor/targets.json: REPLACE compose.service_checks with discovered rows (read-only socket proxy GET /containers/json?all=1, filter label com.docker.compose.project=cognizioware-mcp-tools, service -> http://{service}:{firstPrivatePort}/mcp, default accepted statuses [200,400,401,404,405,406]); UNION dns.gateway_records and certs.own_cert_hostnames with hostnames parsed from the Caddyfile bind-mounted read-only at /etc/ops/Caddyfile (regex on site-block headers for *.easybutt0n.ai); keep gateway, off_box, fleet, certs.pve_nodes/tailnet_nodes, admin_centres static. New compose.overrides map in targets.json for path/statuses/why (azure-inference-adapter, ops-control-center, monday-mcp REST :3113) and skip: true for non-HTTP services (postgres, proxies). Add gateway.expected_servers from /data/e2e/baseline.json keys so a server missing from the aggregate tools/list becomes its own doctor row. Compose: bind-mount /opt/cognizioware-mcp-tools/Caddyfile:/etc/ops/Caddyfile:ro into ops-control-center.
(3) Section 3 (status only): in IngestEndpoints.cs add GET /api/status and /api/status/{doctor|catalog|e2e|ci|cloud|langfuse|actions}, key-gated with the existing CheckKey (X-Ops-Ingest-Key), shape per the plan ({generatedAt, ok, sections:{doctor:{ok,generatedAt,counts:{ok,warn,fail},failing:[...]}, catalog:{...}, e2e:{...}, ci:{...}, cloud:[], langfuse:{}, actions:{enabled:false}}) — cloud/langfuse/actions return empty/placeholder shapes now (PRs 2-3 fill them); NEVER include secrets, keys, tokens or ciphertext in any response. Caddy: extend @keyed at infrastructure/docker/caddy/Caddyfile:99 with /api/status*; compose: OAUTH2_PROXY_SKIP_AUTH_ROUTES adds ^/api/status.*; middleware exemption for /api/status. Add e2e suite 33 cases to e2e/suites/32-ops-control-center.test.js or a new 33 file per the plan: /api/status 200 with key, 401 without, no secret-looking strings in the body (regex sk-|Bearer |ciphertext).
(4) Verification + docs: dotnet is NOT on this box — verify with docker build of infrastructure/docker/ops-control-center (Release, 0 errors) and a local docker compose smoke of ops-control-center + ops-oauth2-proxy + ops-docker-socket-proxy with a throwaway env: curl -H "X-Ops-Ingest-Key: $K" http://localhost:8080/api/status | jq '.. | strings | select(test("sk-|Bearer |ciphertext"))' returns nothing; python3 doctor/ops_doctor.py --targets <generated> runs from this LAN box and quote the compose.service_checks rows it discovered. Handoff addendum in design docs/handoff-docs/ops-control-center-handoff.md ("v1.1 read-only revisions": what changed, how to verify, what PRs 2-5 will add) and the README page/lessons rows. Commit by explicit path per slice: feat(ops): role tiering + ActionGuard scaffolding · feat(ops): discovery-driven targets · feat(ops): GET /api/status · docs(ops): v1.1 handoff addendum.
HARD RULES: no write actions reachable anywhere; no /mcp endpoint yet (PR 4); no litellm-config.yaml changes; no .github/** changes except none; no secrets or key values anywhere; commit by EXPLICIT PATH (CRLF phantoms — never git add -A); no push. Completion = commit hashes, docker build output tail, compose smoke output, doctor discovery rows, and the exact list of section-8 PR-1 items with done/not-done. Auditor: no git fetch; confirm no action endpoint exists and /api/status bodies contain no secret-looking strings.
