BUILD-ONLY. mcp-cognizioware BillingService — YouTrack integration Phase 1 ("fast-option closure"). Authoritative spec, read FIRST: `design docs/youtrack-integration-gap-analysis.md` in this worktree (untracked; the overseer copied it from the docs/youtrack-gap-analysis branch — commit it by explicit path in slice 1), sections 1.2, 2, 3, 6 (Phase 1 row), 7 and 8.2 "Phase 1". Also read `design docs/Youtrack Time log service completion.md`. Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/mcp-cognizioware-youtrack (branch feat/youtrack-phase1 from origin/develop @ 880f3d9f — develop is what dev/uat/prod run); never cd into or run git against any other checkout; plain git commands only; commit by EXPLICIT PATH (never git add -A; CRLF phantoms); no push. dotnet SDK: check `dotnet --version`; if it is not installed here, build with `docker build` of the billing service image (infrastructure/docker/... or the Dockerfile the repo uses for mcp-billing-service) and state exactly what you used. Never run docker compose against any live stack.

Scope decisions already made (do not re-open): YouTrack (https://youtrack.cognizio.company) is the canonical hour and comment record; CE plugin code is out of scope; D365CeService is NOT part of this flow; Phase 0 credential rotation is DROPPED by Paxton (dev lab) — but you must not add, move or log any secret, and anything hard-coded that you touch becomes configuration (env/appsettings) with a redacted placeholder in docs. Business context: this is the backbone for the company revenue pipeline — AI agents and humans both log time (work items), comment and manage tickets through this service; downstream, Hydra's ship-plan reporting and the CRM sync (separate runs) call these endpoints and MCP tools.

Slices, one per round (each ends with a clean build and a commit by explicit path):
(1) Author/config binding (gap a): YouTrackSettings.DefaultUserEmail/DefaultProjectIssueId/ProjectId come only from configuration (YouTrack__* keys, legacy YOUTRACK_DEFAULT_USER_EMAIL fallback), no hard-coded email in code; the time-entry author is the authenticated user's email claim, falling back to the configured default; add a startup readiness log line that prints the bound DefaultUserEmail/DefaultProjectIssueId/BaseUrl (never the token) and a `GET /api/billing/youtrack/readiness` that reports config-bound + reachability (ValidateIssueAsync on the default issue) without secrets. Commit the gap-analysis doc in this slice too. Commit: fix(youtrack): bind author/config from configuration + readiness probe.
(2) Comments + issue CRUD in YouTrackService (gap b): GetCommentsAsync(issueId), AddCommentAsync(issueId, text, authorEmail), GetIssueAsync(issueId), QueryIssuesAsync(query, top), CreateIssueAsync(projectId, summary, description, fields), UpdateIssueAsync(issueId, summary?, description?, fields?), plus GetWorkItemsAsync(issueId, from, to) for time sheets; typed models in Models/YouTrackModels.cs; Polly retry/circuit policies reused from Program.cs; unit tests with a fake HttpMessageHandler covering each method's request shape and error mapping (404 → NotFound result, 401 → configuration error, no exceptions leaking). Commit: feat(youtrack): comments, issue CRUD and work-item queries.
(3) AI-vs-human hours convention (gap c): replace the `[COGNIZIO_METADATA:{json}]` text-blob with a real convention — work-item type name from configuration (`YouTrack:AiWorkItemType`, default "AI Work") plus an optional custom field (`YouTrack:AiHoursField`, e.g. cognizioAIHours) written when configured; keep the text-blob path behind a feature flag (`YouTrack:LegacyMetadataBlob`, default false) so existing projects can migrate; document the YouTrack admin steps (create the work-item type and custom field per project) in `design docs/youtrack-phase1-handback.md`. Commit: feat(youtrack): AI-hours work-item type + custom field convention (flagged).
(4) Real MCP handlers (gap d): ToolExecutionService `youtrack:*` handlers become real — create_issue, get_issue, query_issues, update_issue, add_comment, get_comments, log_time (work item with the AI convention), get_work_items (time sheet for an issue/date range) — with agent attribution (AgentProfile identity recorded as the YouTrack author when the agent has a mapped YouTrack user via FindUserByEmailAsync, else the configured service account with the agent name in the comment/work-item text prefix `[agent:<name>]`); register any new tool names where the MCP tool list is declared; unit tests for parameter validation and the attribution rule. Commit: feat(mcp): real youtrack tools (issues, comments, time).
(5) Inbound events (gap e): `POST /api/youtrack/events` receiver protected by a shared-secret Bearer header following the LindyWebhookAuthAttribute pattern (new `YouTrackWebhookAuthAttribute`, secret from `YouTrack:WebhookSecret`), accepting YouTrack workflow/webhook payloads for issue-updated / comment-added / work-item-added, validating shape, persisting to a new `youtrack_events` table through the existing dual-write repository pattern (migration via the repo's migration mechanism — EF migration if that is what the service uses), returning 202; kill switch `YOUTRACK_EVENTS_ENABLED` (default false) makes the endpoint return 503 and MCP write tools return a clear "disabled" result. Commit: feat(youtrack): inbound events receiver + kill switch.
(6) Verification + handback: full build clean; all unit tests green (quote counts); an integration test project or script that, when `YOUTRACK_SANDBOX_ISSUE` + a token are provided via env, creates a comment and a work item on the sandbox issue and reads them back — run it ONLY if those env vars are present in this environment, otherwise report NOT RUN; update Swagger annotations; write `design docs/youtrack-phase1-handback.md` (what changed per gap a–e, config keys with placeholders, YouTrack admin steps, endpoints and MCP tools table, tests + results, what Phase 2/Hydra will call). Commit: docs(youtrack): phase 1 handback.

HARD RULES: no changes to Stripe, billing meters, subscription/partner endpoints, StripeWebhookController, Program.cs beyond DI registrations/route additions for this feature, .github/**, docker-compose files or .env files; no secrets or tokens anywhere (config keys only); no CE plugin changes; no D365CeService changes; commit by explicit path; no push. Completion = commit hashes, build + test output tails, the endpoints/tools table, and a gap (a)–(e) checklist done/not-done. Auditor: no git fetch; grep the diff for `perm:` tokens, client secrets and emails hard-coded in code (must be none — configuration only); confirm the legacy metadata blob is flag-off by default and the events endpoint is 503 when the kill switch is off.
