BUILD + TEST (code; the overseer deploys via the Hydra lane on corsairai300): move Claude Remote Control off PTAIT09 and into Hydra, the control plane, as a skill/tool usable from chat.easybutt0n.ai and Hydra's terminals. Today (task 04, ptait09-easybutt0n-ai PRs #36-#42) the Remote Control host runs as a Windows Scheduled Task on PTAIT09 (svc/claude-rc.session.json, trust file, easybutt0n-runner-v2), which ties the fallback control path to the developer PC. Paxton's decision (2026-09-07 22:30 PT): "this should not be hosted on ptait09; this should run through hydra, if it is the actual controlling plane, as a skill or tool for chat.easy and its terminals". Work ONLY in /home/harness/work/cognizioware-hydra-rsi on branch feat/hydra-remote-control (run `git fetch origin && git checkout -B feat/hydra-remote-control origin/main` first); plain git; commit by EXPLICIT PATH; no push; no live calls; secrets are env NAMES only (CLAUDE_RC_TOKEN or the existing session-file convention, HYDRA_* , DEVICE_TOKEN names).
READ FIRST: Hydra's fleet MCP + orchestrator (apps/*, packages/*, /fleet/* routes, the terminal/tunnel containers hydra-terminal-*, hydra-tunnel-1, hydra-orchestrator-1 on corsairai300), the control-plane doctrine in LongHorizon-Harness tasks/expert-handoff-program-2026-09-07.md and tasks/fleet-entry-point-and-harness-ux-2026-09-07.md (13a fleet-chat lhharness tool, 14c control plane apply) which you may READ under /home/harness/work/LongHorizon-Harness (never write there), and the current RC host implementation you may READ at /home/harness/work/ptait09-easybutt0n-ai (svc/, scripts/claude-rc*, docs/HANDOFF-*; PRs #36-#42 fixed trust-file parsing, here-strings, SYSTEM-profile probe, cmd.exe launch, session-id underscores, file:// import) - never write there. Devices enrolled today: desk03, htpc01, ptait10am5, the CT compose profile, and the qa check.
SLICES (one per round; code + tests + explicit-path commit each):
 (1) design note docs/remote-control-on-hydra.md: where the RC session host runs (a Hydra-managed container on corsairai300 or an LXC, NOT a Windows PC), how a session is started/stopped/rotated by the orchestrator, how the session file/trust material is stored (Hydra secret store / mode-600 volume, names only), what PTAIT09 keeps (nothing but an enrolled device), the enrolment flow for desk03/htpc01/ptait10am5 against the Hydra-hosted RC, and the rollback (re-enable the PTAIT09 Scheduled Task). Commit: docs(rc): remote control on hydra design.
 (2) rc host service: packages/rc-host (or the repo's service layout): a Linux container image running the Claude Remote Control host (the same claude CLI remote-control entry the PTAIT09 task runs), supervised by Hydra's orchestrator (start, health, restart, session rotation), session file on a mounted secret volume, health endpoint, logs to the fleet plane store; compose entry for corsairai300 with restart unless-stopped. Tests with a fake claude binary (start/stop/health/rotation). Commit: feat(rc-host): hydra-managed remote control host.
 (3) tool + skill: expose it in Hydra's fleet MCP as tools rc.status / rc.sessions / rc.exec (target device, command, timeout; approval-gated for destructive commands per the doctrine invariants) and register a `remote-control` skill/prompt for chat.easybutt0n.ai and Hydra terminals that routes to those tools (never to the PTAIT09 proxy). Reuse the device allow-list and the audit-row pattern; every exec writes an audit row (who, device, command, exit code). Tests: tool schema, approval gating, audit row, device allow-list refusal. Commit: feat(fleet-mcp): remote-control tools + chat skill.
 (4) migration + retirement: scripts/rc-migrate.sh (enrol the three devices against the Hydra host, verify each with a no-op command, then disable the PTAIT09 Scheduled Task PTAIT09-claude-rc - the overseer runs it), the qa check in cognizioware-qa points at the Hydra RC health (patch file under docs/patches/, never write to that repo), CHANGELOG, README. Commit: feat(rc): migration + ptait09 retirement.
HARD RULES: PTAIT09 is never rebooted or reconfigured by the run (operator step, documented); no secrets; approval-gated exec; explicit-path commits; no push. Completion = commits, diff stat, tests, the exact deploy steps for corsairai300 and the migration order (Hydra RC up + verified BEFORE the PTAIT09 task is disabled). Auditor: no git fetch; verify exec is approval-gated and audited, device allow-list enforced, and no path still points at the PTAIT09 proxy.
