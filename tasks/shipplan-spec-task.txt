DOCS-ONLY (spec + backlog + harness task files), READ-ONLY on every source tree. Cognizioware revenue pipeline: integrate (1) the overseer's Ship Plan reporting into Hydra, backed by YouTrack through mcp-cognizioware, so both AI agents and users can comment, create/update tickets and record time sheets in the right YouTrack places; (2) customer administration for Power Platform customers (accounts, contacts, and their admin operations) going through the mcp-cognizioware API, kept in sync with the company's own Dynamics 365 CRM used by the sales department (accounts, contacts, opportunities, leads); (3) the YouTrack time-sheet integration tied to the other end of the company business flow (delivery hours → CRM/billing); (4) the whole thing framed as the company revenue pipeline that all repos serve — the MAIN BUSINESS OUTLAY: the company's cost structure, pricing, capacity and revenue targets from the commercialization model, with the planned future RSI loop pushing and scaling toward the variable revenue metrics. Paxton's words (2026-09-05): "this all should support all the docs in here as our revenue pipeline; all repos are in support of this mission-critical company task"; "our RSI loop planned for a future phase is to push and scale to achieve those variable revenue metrics". Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-hydra-shipplan (branch docs/ship-plan-integration-spec from origin/main @ 1d83c00); plain git only; commit by EXPLICIT PATH; no push. Other checkouts may be READ, never modified.

Inputs — read in this order (all read-only):
A. design/inputs/youtrack-integration-gap-analysis.md in this worktree (mcp-cognizioware's verified current-state map: BillingService is the YouTrack backbone; sections 1–3, 5, 6, 8). Phase 1 of it is being BUILT right now by harness run on branch feat/youtrack-phase1 in /mnt/c/Users/PaxtonTait/source/mcp-cognizioware-youtrack (read-only; its endpoints/MCP tools are: comments read/post, issue CRUD/query, work items/time sheets, `youtrack:*` MCP tools with agent attribution, `POST /api/youtrack/events` receiver, kill switch YOUTRACK_EVENTS_ENABLED). Design against that surface; do not redesign it.
B. The revenue-pipeline document corpus, /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform/design/plans/ (read-only): cognizioware-guest-launch-handoff.md (start here), cognizioware-guest-launch-plan.md, cognizioware-guest-launch-backlog.md (34 items; $39/user/month guest model; LiteLLM usage/cost backbone; one Stripe writer in mcp-cognizioware; Claims365-pattern admin), powerplatform-greenfield-factory-system-design.md, file-to-eval-requirements-plan.md, cognizioware-2027-commercialization-and-market-model.xlsx (the variable revenue metrics — extract sheet names, the metric names and their target values with python3 openpyxl if available, else by unzipping the xlsx and reading the sheet XML; quote them with sheet/cell references; mark anything you cannot read as UNREAD), Cognizioware-Orchestration-Comparison-Handoff.docx (extract text via unzip word/document.xml if needed). A parallel harness run (branch docs/guest-launch-baseline, worktree /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform-guest, read-only) is producing F01/F03/F04/P01 baseline docs under design/plans/guest-launch/ — read them if present.
C. The overseer's Ship Plan page (the reporting to reproduce): design/inputs/ship-plan.html in this worktree (untracked copy of the live artifact; do not commit it) — a per-repo table of PRs/actions with gate-before-next, rollback, state chip, owner; a status strip; a "Needs Paxton" list; rules footer. Also the LongHorizon-Harness task records at /mnt/c/Users/PaxtonTait/source/LongHorizon-Harness/tasks/*.md (what an overseer-managed task looks like).
D. Hydra: this repo's orchestrator/src (fleet-routes.js, activity-log.js, mcp.js, harness-*.js), orchestrator/public/index.html (the web UI), design/HANDOFF.md and design/ARCHITECTURE.md; scripts/hivemind-ingest.mjs and scripts/overseer-session-ingest.mjs (memory pipeline).
E. mcp-cognizioware current develop (read-only at /mnt/c/Users/PaxtonTait/source/mcp-cognizioware-youtrack): Controllers/LeadContactsController.cs + ContactEventsController.cs (existing `api/lead/contacts`, `api/lead/contactevents` — the Lindy-fed lead intake), Services/D365CeService.cs (unregistered, msdyn_timeentries, hard-coded secret — to be retired/replaced, never reused as-is), UserOrganizationService.cs, StripeService.cs, the admin SPA (src/.../wwwroot/admin or the admin index.html), Program.cs route map.

Deliverables (design/ship-plan-integration/ in this worktree), one slice per round:
(1) `00-revenue-pipeline-overview.md`: the main business outlay (cost structure, pricing tiers, capacity assumptions and revenue targets from the xlsx, cited by sheet/cell) and the end-to-end business flow as the company's revenue pipeline — lead → opportunity → account/contact (company CRM) → guest/customer provisioning (Power Platform product, mcp-cognizioware billing) → delivery work (Hydra/harness runs, human work) → YouTrack tickets/comments/time sheets → invoicing/collection (Stripe via mcp-cognizioware, nightly token billing per the guest plan) → variable revenue metrics (from the xlsx, cited) → RSI loop (future phase: automated push/scale toward those metrics; define its inputs, controls and the kill switches, explicitly NOT built now). A repo-responsibility matrix: which repo owns which step (cognizioware-powerplatform, mcp-cognizioware, cognizioware-hydra, cognizioware-mcp-tools, cognizioware-qa, LongHorizon-Harness, the CE plugin repo). Invariants carried from the guest plan (one Stripe writer, LiteLLM cost authority, no hard deletes, fail-closed authz, YouTrack canonical for hours).
(2) `10-ship-plan-reporting-hydra.md`: Hydra "Ship Plan" module design — data model (plan → repo lanes → steps/PRs with gate/rollback/state/owner; each step bound to a YouTrack issue id; comments and time entries live in YouTrack and are mirrored read-through), REST + MCP surface on Hydra (`/fleet-mcp` tools for agents: shipplan_list/get/upsert_step/comment/log_time — thin wrappers that call mcp-cognizioware's youtrack endpoints/MCP tools with agent attribution; no direct YouTrack calls from Hydra), the web UI page (mirror the overseer's Ship Plan page structure: status strip, per-repo tables, Needs-Paxton list, rules footer; live from the API), YouTrack project/issue conventions (which project(s), issue types, tags for repo/lane/environment, who may create vs comment), permissions (users via Hydra auth; agents via gateway keys with attribution), event flow (YouTrack events → mcp-cognizioware receiver → Hydra outbox/consumer per gap-analysis Phase 2), failure modes (YouTrack nightly auto-off → pending rows), and how the overseer's current artifact page becomes a generated view of this data.
(3) `20-customer-admin-and-crm-sync.md`: Power Platform customer administration through mcp-cognizioware — the customer/account/contact/environment/subscription admin operations (enumerate from the guest plan's admin-center scope and mcp-cognizioware's existing endpoints), API surface (`/api/v1/admin/customers|accounts|contacts` with tenant scoping and roles per the guest plan's fail-closed rules), and the bidirectional sync with the company CRM (Dynamics 365 Sales: accounts, contacts, opportunities, leads) — identity mapping (Entra subject ↔ CE contact ↔ Stripe customer ↔ YouTrack user, per guest plan F03), sync direction rules per entity, conflict policy, idempotency keys, the connector (a properly registered `ID365SalesService` replacing the unregistered D365CeService, credentials from configuration/broker only — never hard-coded), what the Lindy lead intake (`api/lead/*`) becomes in this model, and the timesheet tie-in: YouTrack work items → CRM (project/account) hours summary → billing evidence, with the CE plugin's existing pull-sync left untouched.
(4) `30-backlog.md`: prioritized backlog (P0/P1/P2) across repos with ids, dependencies (explicitly depending on gap-analysis Phase 1/2 and guest-launch F01–F04/P01), acceptance criteria, and the decision register (every choice Paxton must make: YouTrack project(s), CRM org/entities, sync direction per entity, revenue metric owners, RSI loop guardrails) — one row each, status OPEN. `40-harness-tasks/`: one LongHorizon-Harness task text per P0 build item in the exact style of /mnt/c/Users/PaxtonTait/source/LongHorizon-Harness/tasks/*.task.txt or the "Task text" sections there (BUILD-ONLY, slices per round, hard rules, completion evidence, auditor checks), ready for the overseer to launch. Verify `git diff --stat origin/main..HEAD` touches only design/**.
Commits: docs(ship-plan): revenue pipeline overview · docs(ship-plan): Hydra ship-plan reporting design · docs(ship-plan): customer admin + CRM sync design · docs(ship-plan): backlog, decision register, harness task texts.

HARD RULES: docs only; no code; no changes outside design/**; never modify another checkout; no secrets, tokens, connection strings or customer data in any document (cite file:line, redact values); every claim about existing code carries a file:line citation on the tree you read; mark unverifiable licensing/vendor claims UNVERIFIED; do not present this as implemented. Completion = commit hashes, `git diff --stat`, the decision register rows verbatim, and the list of harness task texts produced. Auditor: no git fetch; check docs-only diff, citations present, no secrets, xlsx metrics quoted with sheet/cell references or marked UNREAD.
