DOCS-ONLY, Step 0 of the approved plan. Read the authoritative plan FIRST and follow it exactly: /mnt/c/Users/PaxtonTait/.claude/plans/we-want-to-swithc-wild-crab.md (Windows C:\Users\PaxtonTait\.claude\plans\we-want-to-swithc-wild-crab.md) — "n8n -> code (LangGraph JS) orchestration: reconciled design and delivery plan". Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform-orch (branch docs/orchestrator-reconciled-v2 from origin/develop @ e119e98, which already contains ci/env-promotion); never cd into or run git against /mnt/c/Users/PaxtonTait/source/cognizioware-powerplatform or any other checkout or worktree; plain git commands only (never --git-dir/--work-tree). The design inputs are reachable as commits in this repo: feat/code-first-orchestrator = 50e98ff 1fa3cb4 6d2ec44; design/n8n-code-orchestration-20260905 = 42a532b 6bc6729. Deliver plan section "Step 0 — Land one canonical spec" as commits, one slice per round, in this order:
(1) git cherry-pick 50e98ff 1fa3cb4 6d2ec44 (resolve conflicts by keeping develop's version of anything outside design/**; the result must add design/orchestrator/{code-first-orchestrator-handoff.md, orchestrator-framework-comparison.md, graphs.json}). Then git checkout 6bc6729 -- design/handoffs/n8n-code-orchestration-20260905/ (README, graph-spec, agent-profiles, trms-delivery-contract) — do NOT take that branch's .gitignore or kb-hook.py changes. If design/plans/Cognizioware-Orchestration-Comparison-Handoff.docx exists in this worktree, delete it (the README supersedes it); it is untracked in the main checkout, so it may simply be absent here — say so.
(2) Write design/orchestrator/orchestration-migration-audit-2026-09-05.md from the plan's Appendix (verified claims, gaps G1-G14 with paths and fixes, Revision 2 findings R1-R8) plus the plan header table; commit as docs(orchestrator): migration audit 2026-09-05.
(3) Revise design/orchestrator/code-first-orchestrator-handoff.md to v2 exactly per plan Step 0 item 4: add the "Decision" block verbatim from the plan and a "Supersedes" block naming design/plans/powerplatform-greenfield-factory-system-design.md:62 and design/v2-n8n-orchestrator-gap-closure-plan.md:39; section 2 as-is: model baseline kimi-k2.7-code:cloud, general-chat alias policy, n8n instance scope table (CT103 shared by dev/uat/prod and ~50 other workflows; n8n cloud out of scope; Easy Button runner out of scope; support family CE-spine-only); new section 2.4 "Coupling inventory" (eval harness, RSI routes, eval-gate.yml, workflow-guards.mjs, frontend n8nRun/allowed_n8n_workflow, orchestrator-mcp, N8N_WEBHOOK_SECRET consumers, eval_runs ssh->psql writes); section 4 add G6 sprint_cycle and G7 delivery_pack from graph-spec.json 0.2 nodes baseline_validate ... final_acceptance_gate with reuse decisions R1-R5; section 6 agent-node contract: profileHash, max_tokens guidance for reasoning_content models, typed QA verdict; section 7 keys: cw-orch-chat, cw-orch-docs, residency rule = per-key fallbacks: [] verified by live.probe.test.ts, orchestrator host gets dataverse-read for whoAmI; section 9 data model: execution_engine, thread_id columns, baselines, sprints, change_requests (as decisions.kind), acceptance_policies, allowed_n8n_workflow -> allowed_workflow; section 10 entry points: requestId idempotency, ORCHESTRATOR_SECRET alias kept until cutover and rotated at cutover; section 12 migration replaced with Phases A, B, C, D, G (G1-G4), E, F copied from the plan with their exit criteria; section 13 estimate updated; Appendix A pins: @langchain/langgraph 1.4.14, langgraph-checkpoint-postgres 1.0.5, @langchain/core 1.2.9, @langchain/mcp-adapters 1.1.4, @langfuse/langchain 5.11.0, bump @langfuse/* to 5.11.x. Also copy the plan's "Separation of the code-only version from the n8n version" table into the handoff as its own section. Commit as docs(orchestrator): handoff v2 — reconcile with TRMS delivery contract.
(4) Add a pointer paragraph to design/plans/powerplatform-greenfield-factory-system-design.md section 14 and to the header of design/v2-n8n-orchestrator-gap-closure-plan.md, both pointing at design/orchestrator/code-first-orchestrator-handoff.md v2 and the audit. Commit.
(5) Verify and report: git diff --stat origin/develop..HEAD must touch only design/**; run node tests/ci/workflow-guards.mjs from the worktree root and quote its output (it must still pass; if node is missing on this box, say so and quote the exact command for the operator). Squash is NOT required; keep the cherry-picks as they are and finish with the summary commit message from the plan: docs(orchestrator): reconcile code-first handoff v2 with TRMS delivery contract and migration audit (use it for the final commit of step 3 or 4 if that is the last one).
HARD RULES: docs only — no code, infrastructure, workflow, key, or env changes; nothing under packages/**, .github/**, n8n-workflows/**, scripts/**; no secrets or key values anywhere; commit by EXPLICIT PATH (CRLF phantoms — never git add -A); no push. Completion = the list of commits with hashes, the git diff --stat, the workflow-guards output, and the exact list of plan items 1-6 with done/not-done and why. Auditor: no git fetch; verify the diff touches only design/** and that the handoff v2 contains every section named above.
