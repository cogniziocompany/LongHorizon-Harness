TASK 188 (was 181; renumbered 2026-09-16 after a collision with the overseer's 181 readback-script-hang) - Branch protection, auto-merge setting, merge doctrine, and re-scoping the reviewer tasks so
@claude/Copilot are the FALLBACK, not the gate.

*** OVERSEER-EXECUTED. IN queue/blocked/ DELIBERATELY. *** It changes repo settings on all eight repos and
LOOP-PROMPT.md; a run does neither. BLOCKED ON: task 185's reusable workflow on mcp-tools main and at least one stub
merged, and task 187 posting `harness-review`.

SOURCE: Paxton 2026-09-15/16; plan C:/tmp/PLAN-pr-review-gate-2026-09-15.md step 5; powerplatform concerns list in
C:/tmp/HANDOFF-github-app-lh-reviewer-2026-09-15.md section 4 (measured: pp main + release require only `gate`,
develop unprotected, allow_auto_merge=false).

SCOPE
 1. Branch protection (gh api, read back each): every repo's default working branch requires `pr-gate` +
    `harness-review`, strict=false, 0 reviews. powerplatform: apply to `develop` (unprotected today) and ADD to
    `release` only after verifying what promote.yml pushes there; `main` keeps `gate` only (promote.yml
    fast-forwards it; an unrun required check would block promotion). enforce_admins stays false (emergency path).
 2. Repo setting `allow_auto_merge=true` on every repo where impact:docs auto-merge applies (pp: develop only).
    `delete_branch_on_merge=true` everywhere (the queue keeps leaving merged branches around).
 3. LOOP-PROMPT STEP 3 rewritten (backup first): "MERGE = both checks green AND not needs-window. impact:docs
    auto-merges; impact:code the overseer merges on its tick; needs-window merges only in a drain window. The
    overseer NEVER posts @claude by hand; only task 187's fallback path may, after two harness failures, from the
    App identity. Copilot the same." Task 117's note updated to match (ban narrowed, not lifted).
 4. Reviewer tasks re-scoped: 130 (qa), 132 (powerplatform, still parked on Paxton's go), 149 (BMAD), 116 (hydra
    flow): they install claude-code.yml AS THE FALLBACK - add the App's author association to the workflow's gate
    (byte-for-byte copy otherwise; the collapsed always-true gate bug stays fixed), no primary-review role.
 5. Read-back for each repo: `gh api repos/.../branches/<b>/protection` shows both contexts; `gh api repos/...` shows
    allow_auto_merge; a docs-only PR auto-merges; a code PR waits; an impact:deploy PR is labelled needs-window.

HARD RULES: one repo at a time, read back before the next; powerplatform main is never given new required checks
without Paxton's explicit yes; record every setting change in the ledger with the before/after JSON.

DONE MEANS: eight repos protected as specified, auto-merge proven on one docs PR, STEP 3 rewritten, 117 narrowed,
reviewer tasks re-scoped, all read back.
