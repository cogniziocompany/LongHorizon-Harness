BUILD + TEST (code; pp repo; the overseer runs the live seeding and dispatches the lanes): make the guest-access seeding tier-agnostic so powerplatform-uat is used the same way powerplatform-dev is. Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch feat/guest-seed-any-tier (run `git fetch origin && git checkout -B feat/guest-seed-any-tier origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to Dataverse, the backend, or any lane host (fixtures + a fake HTTP server only); env NAMES only, never values.
GROUND TRUTH verified by the overseer on pp develop 969ae91 (re-check it yourself and say so if it differs): scripts/guest/seed-dev-grant.mjs requires TARGET_ENVIRONMENT_URL plus AZURE_TENANT_ID/AZURE_CLIENT_ID/AZURE_CLIENT_SECRET, posts through the backend with N8N_WEBHOOK_SECRET and GUEST_TEST_PRINCIPAL_EMAIL, and calls the auth check. deployments/instances.json carries per-tier dataverseUrl/orgId/mcpEnv: dev = https://org9130dfc5.crm.dynamics.com, uat = https://orgff9dcfec.crm.dynamics.com (orgId b100f72c-687d-f111-b27b-6045bd07ba0c, mcpEnv 2dd00c35-f536-4234-ad59-c28dd534f905), prod = https://org937bd6b5.crm.dynamics.com. .github/workflows/eval-gate.yml already maps tier -> ENV_ORG_URL the same way.
SLICES: (1) rename scripts/guest/seed-dev-grant.mjs to scripts/guest/seed-guest-grant.mjs (git mv; keep the old path as a 3-line shim that requires the new one and prints a deprecation notice) and add a `--tier dev|uat|prod` option that reads the Dataverse URL from deployments/instances.json when TARGET_ENVIRONMENT_URL is unset, refuses `--tier prod` unless GUEST_SEED_ALLOW_PROD=1, prints every Dataverse/backend error verbatim, and is idempotent (re-running must not create a second environment record or grant). Unit tests with a fake backend cover: tier resolution for all three tiers, TARGET_ENVIRONMENT_URL winning over the manifest, the prod guard, idempotence, and verbatim error passthrough. Commit: feat(guest): tier-aware guest seeding script. (2) documentation: docs (or the existing guest runbook) gains a "seed a tier" section listing the exact command per tier and the env NAMES the lane file must carry (TARGET_ENVIRONMENT_URL, AZURE_TENANT_ID, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, N8N_WEBHOOK_SECRET, GUEST_TEST_PRINCIPAL_EMAIL, STRIPE_TEST_CUSTOMER_ID, BILLING_API_URL, BILLING_API_KEY), plus a note that the UAT lane env (/opt/powerplatform-uat/.env) is missing the guest vars today and the operator adds them. Commit: docs(guest): seeding per tier. (3) a `.env.example`-style block or deployments/README addition showing the UAT row next to dev, and a CHANGELOG entry. Commit: docs(deploy): uat guest vars.
HARD RULES: no secrets or ids that are not already in the repo; do not change lane compose files; do not touch .github/workflows; explicit-path commits; no push; tests green (report counts). Completion = commits, diff stat, test summary, and the exact operator command lines for dev and uat. Auditor: no git fetch; verify the prod guard, idempotence test, and that the old script path still works via the shim.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
