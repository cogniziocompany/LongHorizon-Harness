BUILD + TEST (code; fleet-admin source repo): make the Fleet Window show EVERY harness session by adding a read-through PULL path alongside the existing push path - hybrid, additive, and it must NOT rewrite or disturb the push pipeline that the harness reporter and the device agents use. Work ONLY in /home/harness/work/ptait09-easybutt0n-ai on branch feat/fleet-window-harness-pull (run `git fetch origin && git checkout -B feat/fleet-window-harness-pull origin/easybutt0n-runner-v2` first - fleet-admin's SOURCE lives here; the copy under cognizioware-mcp-tools/infrastructure/docker/fleet-admin is vendored and the overseer re-vendors it after this merges, so do not edit that repo); plain git; commit by EXPLICIT PATH; no push; NO live calls to the harness, the fleet window or any host (fake HTTP servers and fixtures only); env NAMES only, never token values.
WHY (overseer evidence, 2026-09-08): https://fleet.easybutt0n.ai/ renders and says "connected" but shows Nodes 0 / Active 0 / Gates 0 / 24h Events 0 and "No fleet nodes registered", while the harness node has runs in flight and Hydra's sidebar lists them. The two planes are fed differently: **Hydra pulls** (the harness is registered there as an external node and its fleet routes call the harness API live), while **the fleet window is push-only** (nodes, runs, gates and events appear only when the harness reporter and device agents post them). The node IS enrolled - LH_HARNESS_FLEET_URL / _NODE / _LABELS / _KEY are all present on the harness box - but the deployed harness build has no reporter module, so there is no producer. The reporter is landing separately; this task makes the window correct with or without it, and backfills sessions the reporter never sent.
DESIGN - HYBRID, PUSH STAYS AUTHORITATIVE:
 - Add a poller that reads the harness API and upserts into the SAME tables the push path already writes, tagging each row with its origin (for example source='pull' vs source='push'). A pushed row must never be overwritten by a pulled one: on conflict, keep the pushed values and only fill fields the pull knows and the push has not set. Pulled rows are replaced freely by later pushes.
 - Nodes come from configuration, not invention: a JSON env (name it FLEET_HARNESS_NODES_JSON, same shape Hydra already uses: nodeId, baseUrl, uiBaseUrl, tokenEnv, labels) so a node can be listed without a device key. Read each node's bearer from the named env var; never log or return a token.
 - Poll interval configurable (default 20s), every request bounded by a short timeout, and the poller is READ-ONLY against the harness: GET the run list and each run's snapshot for status, current round and pending gates. It must never POST, resolve a gate, or mutate anything.
 - A node that fails to answer is marked degraded with its last error and last-seen time; it must NOT blank rows that were already known, and the window must keep rendering.
SLICES (one commit each, explicit paths):
 (1) config + client: FLEET_HARNESS_NODES_JSON parsing with validation and clear errors, plus a small harness client (list runs, get snapshot) with timeouts and no retries beyond a single bounded retry. Unit tests with a fake harness server, including the unreachable case. Commit: feat(fleet-admin): harness read-through client and node config.
 (2) poller + merge: the interval loop, the upsert with push-wins semantics, degraded-node handling, and a metric or log line per cycle (nodes polled, runs seen, rows written, failures). Tests must prove push-wins: seed a pushed row, run a poll that would change it, assert the pushed values survive. Commit: feat(fleet-admin): poll harness nodes and merge without clobbering pushes.
 (3) sessions and deep links - this is what Paxton asked for: every harness session must be visible and clickable. Each run row shows its run id, node, status, current round and role, and links out to the RUNNING lh-harness web UI for that run, built from the node's uiBaseUrl (for example <uiBaseUrl>/runs/<run_id>), opening in a new tab and marked as an external link. Where the harness exposes per-round session ids of the form <run_id>.<round_tag>.<role>, show them on the expanded row so a session can be joined to a trace. Gates get the same treatment: a gate row links to the run's gate view. Commit: feat(fleet-window): sessions, rounds and deep links to the harness UI.
 (4) overview + UI: /api/fleet/overview counts include pulled data, each row carries its origin badge (pushed / polled) and the node list shows degraded state; the empty state stays explicit but now distinguishes "no nodes configured" from "nodes configured but unreachable". Keep the existing socket.io live updates working for pushed events - do not replace them with polling in the browser. Commit: feat(fleet-window): overview and rows show origin and node health.
 (5) docs + config example: .env.example gains the new names, README explains the two planes in a short paragraph (Hydra pulls; the window is pushed to AND now also polls as a backstop), and states that the poller is read-only. CHANGELOG. Commit: docs(fleet-admin): hybrid pull backstop.
HARD RULES: additive only - do not modify the ingest/socket.io push routes, the device event path, or the auth middleware beyond what the new config needs; the poller is strictly read-only against the harness; no secrets, env names only; explicit-path commits; no push; `node --test` green (report counts). Completion = commits, diff stat, test counts, the exact env names an operator must set, and a note of what the overseer must do next (re-vendor into cognizioware-mcp-tools, deploy through that lane, set FLEET_HARNESS_NODES_JSON and the token env on CT202). Auditor: read-only - verify the poller never issues a non-GET to the harness, that a pushed row survives a conflicting poll, and that no token value can reach a log line or an API response.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

USE CASE THIS MUST SATISFY (Paxton, verbatim intent): "I should see, the human user and the AI agent, an lh-harness in both fleet and hydra. SAME FLEET." One fleet, two surfaces, two kinds of consumer. Concretely, all four of these must show the same harness node and the same sessions at the same time:
 (a) HUMAN in the browser at https://fleet.easybutt0n.ai/ - the node row is present with its status, every non-terminal harness run is listed with its status, current round and role, and each row links out to the running lh-harness web UI for that run.
 (b) HUMAN in the browser on the Hydra side - the same node and the same runs, which Hydra already gets by pulling the harness API live.
 (c) AI AGENT through the fleet window's read API (/api/fleet/overview and the per-node/per-run read routes) - same ids, same statuses, machine-readable.
 (d) AI AGENT through Hydra's fleet MCP tools - same ids, same statuses.
ACCEPTANCE, and write it as a test: with a fake harness exposing N non-terminal runs, the window's overview must report the same node id and the same N run ids with the same statuses that a direct read of that harness returns, within one poll interval; ids must be identical strings on both sides (no re-keying, no truncation, no per-surface renaming), so a human and an agent can quote the same id to each other and both find it. Where a run carries per-round session ids of the form <run_id>.<round_tag>.<role>, those strings must be identical on both surfaces too. Add a short section to the README stating this parity contract in one paragraph, so a future change that re-keys or filters rows on one surface is recognised as breaking it.
