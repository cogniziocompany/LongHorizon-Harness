BUILD + TEST (code; the overseer deploys via promote-billing dev -> uat -> prod): the admin-API half of the "E2E model registry" plan (planning session 3e71c218; full plan in tasks/e2e-model-registry-2026-09-08.md of LongHorizon-Harness - READ IT FIRST via `git show origin/main:tasks/e2e-model-registry-2026-09-08.md` in /home/harness/work/LongHorizon-Harness after `git fetch origin`; never write there). Work ONLY in /home/harness/work/mcp-cognizioware on branch feat/e2e-model-registry-api (run `git fetch origin && git checkout -B feat/e2e-model-registry-api origin/develop` first - develop is the dev lane branch; if AdminE2eController.cs is missing on develop, base on origin/main where it landed in 438890c6 / PR #60 and say so); plain git; commit by EXPLICIT PATH; no push; no live calls; env NAMES only (E2E_INGEST_KEY = admin config row E2E:IngestKey; LITELLM_MASTER_KEY never touched).
READ FIRST: src/McpCognizioware.BillingService/Controllers/AdminE2eController.cs (IsAuthorizedAsync: Entra admin OR X-E2E-Ingest-Key; IAppConfigurationService; how the aggregate baseline row is stored), the app_configuration table/model (category E2E-Monitoring, config_value JSON, is_secret), existing controller tests under McpCognizioware.BillingService.tests/Controllers/.
SLICES: (1) GET /api/v1/admin/e2e/models -> returns the JSON registry row E2E:ModelRegistry (404 when absent); PUT /api/v1/admin/e2e/models -> validates the document shape from the plan (version int, policy.critical non-empty, backends[].url http(s), no policy.retired glob matching any id in models.*), stamps updatedAt/updatedBy, UpsertAsync on key E2E:ModelRegistry (scope global, is_secret=false, category E2E-Monitoring); optional `?merge=discovered` mode that replaces ONLY the discovered block (used by test runs). No schema change. Commit: feat(admin-e2e): model registry GET/PUT. (2) xUnit tests: auth (Entra admin, ingest key, anonymous 401/403), validation errors (400 with field names), merge=discovered keeps policy/models, round-trip GET after PUT. Commit: test(admin-e2e): model registry. (3) docs: the controller's contract doc / README section listing both routes and the document schema (copy the JSON shape from the plan), CHANGELOG. Commit: docs(admin-e2e): model registry.
HARD RULES: reuse IsAuthorizedAsync and IAppConfigurationService (no second auth path, no schema change); no secrets; explicit-path commits; no push; `dotnet test` green except the known offline DataIntegrity tests (report counts). Completion = commits, diff stat, test summary, and the exact curl examples (env names) the overseer uses to seed the first registry document. Auditor: no git fetch; verify the retired-glob validation, merge=discovered isolation, and that is_secret=false.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
