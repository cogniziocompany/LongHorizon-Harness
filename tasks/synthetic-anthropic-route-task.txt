BUILD + TEST (config + docs + e2e; cognizioware-mcp-tools): the Synthetic Anthropic-route fix is ALREADY LIVE on the prod router. This task CODIFIES it in the repository and adds the guard that would have caught it. Work ONLY in /home/harness/work/cognizioware-mcp-tools-b on branch feat/synthetic-anthropic-route (run `git fetch origin && git checkout -B feat/synthetic-anthropic-route origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to Synthetic and NO writes to the prod router (the overseer has already done the live work - the evidence is below); env NAMES only, the key must NEVER appear in the repo.

READ THIS FIRST - THE DIAGNOSIS IS SETTLED, DO NOT RE-DERIVE IT. On 2026-09-09 the overseer established the mechanism by testing candidate FIXES rather than re-probing the symptom, and applied the fix live. Your job is NOT to rediscover it.

THE MECHANISM. LiteLLM decides which upstream API to call from the model's PROVIDER PREFIX, not from api_base:
  openai/<model>        -> serving /v1/messages, LiteLLM calls the provider's OpenAI RESPONSES API (/v1/responses).
                           Synthetic does not implement it. Result: 404 on /v1/messages, 200 on /v1/chat/completions.
  custom_openai/<model> -> chat-completions. VERIFIED 200 on /v1/messages.
  hosted_vllm/<model>   -> chat-completions. VERIFIED 200 on /v1/messages.
  openai_like/<model>   -> 400, unmapped in the pinned version. Do not use.
This is why "Synthetic is OpenAI compatible" and "Synthetic 404s for the harness" were BOTH TRUE: they describe different routes. A 200 on /v1/chat/completions proves nothing about the route the harness speaks.

WHAT IS ALREADY LIVE ON THE PROD ROUTER (registered through POST /model/new, so it lives in the LiteLLM_Config Postgres rows, NOT in the committed YAML - see the authority note below). Four twins, each verified on /v1/messages both streaming and non-streaming:
  qwen3.8-27b:synthetic-anthropic     custom_openai/hf:Qwen/Qwen3.8-27B                  extra_body reasoning_effort=none   784ms, no thinking block
  glm-5.3-flash:synthetic-anthropic   custom_openai/hf:zai-org/GLM-5.3-Flash                                                 stream clean
  kimi-k3:synthetic-anthropic         custom_openai/hf:moonshotai/Kimi-K3                                                    stream clean
  glm-5.2:synthetic-anthropic         custom_openai/hf:zai-org/GLM-5.2                                                       stream clean but ~12s to first token
All four use api_base https://api.synthetic.new/openai/v1, api_key os.environ/SYNTHETIC_API_KEY, rpm 15. The four have been added to the lh-harness virtual key's allowed models (33 -> 37, read back and confirmed).

THE ORIGINAL openai/-PREFIXED :synthetic ENTRIES STAY. They serve non-Anthropic callers correctly. This is deliberately TWO models per upstream, one per route, each verified on the route it serves - that is Paxton's instruction, 2026-09-09: separate models for the anthropic and the non-anthropic route. Do NOT "clean up" by deleting or repointing the openai/ entries.

SLICES (one commit each, explicit paths):

 (1) CODIFY THE TWINS IN THE COMMITTED YAML. Add the four :synthetic-anthropic entries to litellm-config.yaml next to the existing :synthetic block, reusing the anchor pattern already there where it fits. Above them, a comment that states the prefix->route mapping in the four lines given above, why both sets exist, and that a 200 on /v1/chat/completions is not evidence for /v1/messages. Match the file's existing comment voice - it already carries this kind of hard-won detail and yours should read like it belongs.

 (2) THE AUTHORITY NOTE - THIS IS THE SUBTLE PART. general_settings.store_model_in_db is true, so the DATABASE is authoritative and the YAML is a seed. The four twins are live in Postgres and are NOT in the file; adding them to the file does NOT make the file the source of truth, and does not by itself change anything on a running router. Say so explicitly in the comment so the next person does not edit the file, restart, and conclude the change worked. This overlaps task 57 (config authority inventory) - if 57's docs/config-authority.md exists on your base, reference it rather than restating it, and say in the report which you did.

 (3) E2E - THE ASSERTION WHOSE ABSENCE COST A DAY. Follow the numbering and structure of a neighbouring suite in this repo's e2e directory (READ ONE FIRST and match its shape exactly). Assert, read-only against the live gateway, that every model expected to serve a harness role answers on BOTH routes it is expected to serve - specifically that each :synthetic-anthropic twin returns 200 on /v1/messages, and that the assertion is made on /v1/messages itself rather than inferred from /v1/chat/completions. Add the streaming check too: a non-streaming call to kimi-k3 returns a thinking block with signature null, while the STREAMING path is clean - and Claude Code streams. A model whose streaming path emits a thinking delta with a null signature breaks Claude Code with "Content block is not a thinking block"; four runs died that way in September. NO WRITES.

 (4) THE ALIAS-IS-NOT-A-BACKEND ASSERTION. Verified 2026-09-09: kimi-k3:cloud is silently SERVED BY glm-5.3-flash:cloud because a fallback chain redirects it. Anything that spreads work across "different models" by name can collapse onto one backend without saying so. Add an assertion that reads the model name reported back in the message_start event and fails when a requested alias is served by a different deployment than intended. Report which aliases currently fail it - do not fix the chains here, only surface them.

 (5) DOCS. A short section in the repo's routing/handoff doc recording the prefix->route table, the two-models-two-routes rule, and the rule that a harness role model must be verified with the HARNESS's own virtual key rather than the master key - a master-key probe passes models the harness cannot reach, and on 2026-09-09 exactly that let two runs die instantly on "403 key not allowed to access model".

TESTS: report counts, baseline first. Hermetic unit tests for anything you add; the e2e suite is read-only against the live gateway in the pattern neighbouring suites already use.

HARD RULES: no key literal anywhere - env NAMES only, and redact anything key-shaped in any output; do NOT delete or repoint the existing openai/-prefixed :synthetic entries; do NOT write to the prod router or any database; do not restart any service; explicit-path commits; no push. Completion = commits, diff stat, test counts, the four twins as committed, a plain statement that the database and not the YAML is authoritative for them, and the list of aliases that fail the alias-is-not-a-backend assertion today. Auditor: read-only - verify no key literal, that both model sets are present rather than one having replaced the other, that the e2e asserts on /v1/messages directly and covers the streaming path, that nothing writes to a live system, and that the authority note is present and correct.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
