BUILD + TEST (code; the overseer merges and re-dispatches the gate): bring the cognizioware-qa web-ux runner flow up to the Env->Session v2 app that is live on dev (cognizioware-powerplatform develop cbd0e88+, Modernist v2 redesign, pp #85). After the sign-in fixes (qa #12-#17) the gate run 34212690000-ish (latest web-ux gate, 2026-09-08 09:55Z) reaches the app: Sign-in timing PASS, Login PASS, Environment-scoped sessions list PASS, but (a) "App shell + breadcrumb render on Environments page" FAILS (selectors from the pre-v2 shell), and (b) "Create throwaway session" FAILS with 500 E901 "targetEnvironmentId and userRequirements are required" - the v2 session-create API needs targetEnvironmentId (use the gate's env id, env WEB_UX_ENV_ID / the qa-gate env_id input) and a userRequirements payload; the flow still posts the v1 body. Work ONLY in /home/harness/work/cognizioware-qa on branch fix/web-ux-v2-flow (run `git fetch origin && git checkout -B fix/web-ux-v2-flow origin/master` first); plain git; commit by EXPLICIT PATH; no push; no live calls (fixtures); env NAMES only (CE_USERNAME, CE_PASSWORD, WEB_UX_ENV_ID, QA_API_KEY, QA_USER_EMAIL).
READ FIRST: packages/runner/src/flows/web-ux-nav-archive.ts (createAppApi, the create-session call, the app-shell/breadcrumb assertions, archive/restore steps, G1 environment status PATCH/GET), packages/runner/src/lib/ce-auth.ts, docs/web-ux-gate.md, and the v2 contract you may READ in /home/harness/work/cognizioware-powerplatform-evalfix (never write there): docs/handoff/Env-to-Session-Spec.md + DESIGNER-NOTES, packages/backend/src/routes/session*.ts (the POST that returns E901: required fields, validation, the minimal valid userRequirements shape), packages/frontend/src (App.tsx routes, the environments page + breadcrumb components, session list/archive controls and their data-testid / aria labels).
SLICES: (1) create-session: post the v2 body {targetEnvironmentId, userRequirements: <minimal valid shape from the backend validator>, title <= 80 chars, ...} and handle the v2 response; keep the throwaway cleanup (archive) working with the v2 archive endpoint; unit tests with a fake API for v1-vs-v2 detection if the backend exposes a version, else v2 only. (2) shell + breadcrumb + archive/restore selectors aligned to the v2 components (prefer data-testid/role selectors; add data-testid hints to a docs/patches/ file for the pp repo if the UI lacks stable hooks), each assertion tagged drift/real/test-debt per the existing convention; screenshot on failure. (3) docs/web-ux-gate.md "v2 contract" section + CHANGELOG. Explicit-path commits. Completion = commits, diff stat, tests, the dispatch command. Auditor: no git fetch; verify the create body matches the backend validator, the cleanup archives the throwaway session, and no "Delete" copy assertion regressed.
