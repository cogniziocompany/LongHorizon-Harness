BUILD + TEST (code; the overseer merges and dispatches the gate): make the cognizioware-qa `web-ux` gate target exercise the powerplatform navigation shell + session archive handoff (design/handoffs/web-ux-nav-archive-handoff.md in cognizioware-powerplatform; W1-W6 + G1; Playwright spec e2e/playwright/nav-archive.spec.js there has never run because it needs MSAL credentials - the QA repo owns those via its env files and packages/runner/src/lib/ce-auth.ts loginPassword()). Work ONLY in /home/harness/work/cognizioware-qa on branch feat/web-ux-nav-archive-flow (run `git fetch origin && git checkout -B feat/web-ux-nav-archive-flow origin/master` first); plain git; commit by EXPLICIT PATH; no push; no live calls (fixtures/fakes; the overseer dispatches the gate). You MAY READ /home/harness/work/cognizioware-powerplatform-evalfix (the handoff, e2e/playwright/nav-archive.spec.js, packages/frontend routes/components for selectors) - never write there.

FIRST SLICE (found 2026-09-07 21:40 PT, blocks the whole web-ux target): with the backend healthy, the `web-ux` job now reaches "Gate report (web-ux)" and dies with `Error: unknown --source junit` from scripts/gate-report.mjs (run 34174385728). Either the reporter must accept `--source junit` (parse the Playwright JUnit XML the job writes: suites/testcases -> checks with the [tag:...] prefix convention, failures -> failed, skipped -> skipped) or the job must emit the JSON reporter the script already understands - pick the one consistent with the other targets and keep both formats working. Unit-test it with a fixture junit.xml. Commit first, so the gate can be dispatched again before the nav-archive flow lands.

ALSO in the first slice: the "Run Power Platform web-UX Playwright spec" step fails with `Error: Project(s) "chromium" not found. Available projects: ""` - the job passes --project chromium but the Playwright config it runs against (the pp repo's e2e/playwright config, or the QA runner's) defines no projects. Fix by running with a QA-owned playwright config that defines a chromium project (and the storageState/setup project for ce-auth), never by editing the pp repo from this workspace; keep the spec path configurable (WEBUX_SPEC_DIR) so the nav-archive flow and the pp spec can both run.

DELIVER: (1) packages/runner/src/flows/web-ux-nav-archive.ts: sign in with the QA account through ce-auth, then assert: app shell + breadcrumb render on the environments page; environment-scoped sessions list + subheader; archive a throwaway session via the UI -> it disappears from active and appears under archived (status filter) with "Delete" nowhere in the copy; restore it; environment status PATCH/GET (G1) via API with the session token; screenshots per step into the gate artifacts; each check tagged with the [tag:...] convention (drift/real/test-debt) from the infra-aware gate. (2) wire it into the `web-ux` target of .github/workflows/qa-gate.yml (keep existing web-ux checks; add the flow; base_url = the tier under test) and gate-report rows. (3) unit tests with a fake page/route layer; docs/web-ux-gate.md (what it checks, selectors source, how to add scenarios). (4) a note in README's target table. HARD RULES: no secrets (env names ADMIN_CENTER_AI_USERNAME/PASSWORD or the existing ce-auth env names); no thresholds lowered; explicit-path commits. Completion = commits, diff stat, tests, the dispatch command for dev (`gh workflow run qa-gate.yml -f target=web-ux -f env=dev -f base_url=https://powerplatform-dev.easybutt0n.ai ...`). Auditor: no git fetch; verify the archive step cleans up its throwaway session and that "Delete" absence is asserted.

CREDENTIALS (Paxton 2026-09-07 18:10 PT, NAMES ONLY - values live in the cognizioware-qa env files on the qa-host runner, e.g. prod.env): the CE/web-ux flows sign in with CE_USERNAME (ai-dev01@cognizio.company) / CE_PASSWORD via ce-auth loginPassword(); admin surfaces use admin_center_ai_username (Ai-dev03@cognizio.company) / admin_center_ai_password. Read them from process.env exactly like the existing suites; never print or commit values.
