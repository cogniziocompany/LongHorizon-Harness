BUILD + TEST (config + deployment; read-only capability FIRST): wire the existing stripe-mcp and cloudflare-mcp servers into the LiteLLM gateway and into the deployment pipeline, so Stripe and Cloudflare admin work can be done through gateway tools instead of hand-rolled API calls from a lane host. Planning session e5df2fa9-3900-4134-a7ba-f10d5e941325. Work ONLY in /home/harness/work/cognizioware-mcp-tools-b on branch feat/stripe-cloudflare-mcp-gateway (run `git fetch origin && git checkout -B feat/stripe-cloudflare-mcp-gateway origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to CT202, the gateway, Stripe or Cloudflare (the overseer runs every live step); env NAMES only, never key values; do not change the pinned LiteLLM image digest.
EVIDENCE FROM THE HUMAN DIAGNOSIS (2026-09-08, read-only against CT202 = VMID 202 on ptait01, LAN 192.168.21.161; live app dir /opt/cognizioware-mcp-tools; gateway https://litellm-gateway-api.cognizioware.com/mcp/ and http://192.168.21.161:4000/mcp/, trailing slash mandatory) - treat these as facts and re-verify only in the repo:
 - The container `cognizioware-mcp-tools-stripe-mcp` IS RUNNING on CT202 (up 18h, 0 restarts, image cognizioware-stripe-mcp:latest built 2026-07-14) and answers tools/list directly on 172.18.0.36:3112/mcp with its own MCP_API_KEY: 18 tools, healthy, logging `listening :3112 tools=18 test=SET live=UNSET gateway-auth=ENABLED`.
 - But it is invisible to the pipeline and the gateway: `grep -c stripe` in the LIVE docker-compose.yml returns 0, and `grep -i stripe` in the live litellm-config.yaml returns 0 matches. The aggregate gateway tools/list with the master key returns 963 tools across 36 server prefixes with **stripe present: False**.
 - cloudflare-mcp is the opposite case and is the model to copy: present in the aggregate (13 tools), healthy directly on 172.18.0.32:3110/mcp, and its router token matches the backend key (md5 f5dbfe53060d).
 - Several registered servers currently expose zero tools through the gateway: github_mcp, azure_mcp, gateway_guides_mcp, youtrack_mcp, seq_mcp, billing_mcp, dataverse_data_mcp. Diagnose these but change nothing about them beyond what slice 4 asks.
READ FIRST: infrastructure/docker-compose.yml (how cloudflare-mcp is declared), infrastructure/litellm-config.yaml mcp_servers block (from ~line 245; how cloudflare is registered, its access groups and extra_headers), infrastructure/docker/gateway-guides-mcp/src/index.ts:37-89 for the preset vocabulary, `design docs/cloudflare-mcp-gateway-gap.md`, `design docs/mcp-placeholder-services-audit-and-plan.md`, and .claude/skills/mcp-gateway-ops/SKILL.md.
SLICES (one commit each, explicit paths):
 (1) compose: declare stripe-mcp in infrastructure/docker-compose.yml exactly as cloudflare-mcp is declared - same network, same restart policy, same healthcheck shape, port 3112, image built from its existing directory, env names only (its MCP_API_KEY and the Stripe test key name already used by that service). The container already runs on the box, so this slice makes the pipeline own it rather than creating something new; note in the commit body that the first lane deploy will recreate it. Commit: build(mcp): stripe-mcp in the compose stack.
 (2) gateway registration, READ-ONLY FIRST: add stripe-mcp to the mcp_servers block of infrastructure/litellm-config.yaml following the cloudflare entry - url, extra_headers carrying its key by env name, and an access group. Register it with a READ-ONLY allow-list of tools: everything that only reads (customers, subscriptions, prices, products, meters, meter event summaries, invoices, balance, events) and NOTHING that writes, refunds, cancels or creates. If the server cannot filter per tool, say so explicitly and register it behind an access group that only the overseer key holds, and write down exactly which of the 18 tools are writes. Commit: feat(gateway): register stripe-mcp read-only.
 (3) drift test: extend the e2e fleet/gateway suite so the aggregate tools/list must contain both a stripe_ prefix and a cloudflare_ prefix, and so a server that is running on the box but missing from the gateway config fails the suite. This is the check that would have caught the current gap. Use the existing suite conventions and the fixtures pattern; no live calls in the test itself. Commit: test(e2e): gateway must expose every running MCP server.
 (4) diagnose the zero-tool servers listed above and write findings into `design docs/mcp-placeholder-services-audit-and-plan.md` - for each, whether it is a placeholder by design, misconfigured, or dead, with the evidence line. Change no configuration for them in this task. Commit: docs(mcp): zero-tool server audit.
 (5) runbook: docs section for the operator - how to verify a server end to end (direct tools/list on its container, then aggregate through the gateway with the master key), the trailing-slash requirement, and the token-match check (router token md5 vs backend key md5). CHANGELOG. Commit: docs(mcp): verify a gateway server end to end.
WHY THIS MATTERS NOW: the overseer currently has no working gateway MCP client in-session (both gateway MCP servers failed to connect at session start), so Stripe and Cloudflare work is being done with raw API calls from a lane host. Wiring stripe-mcp read-only removes that for the guest-billing work in flight, and the drift test keeps it wired.
HARD RULES: read-only Stripe capability in this task - no write tools exposed; no secrets, env names only; do not change the LiteLLM image digest; do not touch .github/workflows; explicit-path commits; no push. Completion = commits, diff stat, the read-only tool list you exposed with the writes you excluded, the test output, and the exact operator sequence for the overseer to deploy through the lane and verify. Auditor: read-only - verify no write-capable Stripe tool is exposed, that the cloudflare pattern was copied rather than reinvented, and that the drift test fails when a server is running but unregistered.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
