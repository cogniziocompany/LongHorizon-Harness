BUILD (code + tests + docs, Stripe TEST MODE only). Cognizioware Guest Launch — BILLING SLICE 1 in mcp-cognizioware (McpCognizioware.BillingService): the retained proprietary billing service becomes the single guest-product Stripe writer with proven NON-CHARGING meter isolation, an independent (non-authoritative) comparison path, hosted Checkout for the base subscription, and the nightly close state machine — the "test-mode money path" of the guest launch handoff. Authoritative specs, read FIRST: /home/harness/work/cognizioware-powerplatform/design/plans/cognizioware-guest-launch-plan.md (§ "Proposed architecture and nonnegotiable safety invariants" 1–12, § "Event and ledger contract", § "Daily reconciliation and nightly collection" incl. the settlement state machine and failure policies, § "Commercial policy and cost model"), cognizioware-guest-launch-backlog.md rows P02, P03, U01, U03, B01–B07 (acceptance criteria are the tests you must write), design/plans/guest-launch/DECISION-REGISTER.md (Paxton 2026-09-06: defaults accepted — D08 meter mode = preserve meter code, prove non-charging product/price isolation before production = HIGH; D06 versioned provider cost basis, markup once; D07 LiteLLM usage/cost authority + one tariff + single Stripe writer + shadow comparison cannot charge; D09 daily LA window, 03:15 America/Los_Angeles close, carry-forward below threshold; D13 hosted Checkout + Stripe Billing add-on where subscription/retry features are used; D02/D03 SPA test-mode slice first), and design/plans/README-final-plan-data.md (corporate BYOK zero surcharge; managed usage cost ×1.20 default). Work ONLY in this workspace /home/harness/work/mcp-cognizioware (branch feat/guest-billing-slice-1 from origin/develop @ d26ccc5 — develop already contains YouTrack Phase 1, PR #70; do not touch YouTrack code); plain git only; commit by EXPLICIT PATH; no push. Check `dotnet --version` first; if the SDK is missing here, build with `docker build` of the billing service image and say so. No live Stripe key exists on this node: every Stripe interaction is exercised through a fake/recorded `IStripeGateway` seam in tests; the handback lists the exact Stripe TEST-mode objects the overseer must create (product, prices, meter) before the dev-lane run.

Current-code facts (from the plan's inspection, verify on this tree): StripeService.RecordBillingMeterEventAsync exists; DualDatabaseService writes primary first and treats secondary failure as non-fatal; DataIntegrityCheckService checks customer/email mismatches only; UsageTrackingService + `POST /api/v1/usage/events` (UsageEventRequest with correlation id, customer ids, channel/source/model, tokens, optional Langfuse USD cost); StripeWebhookController + user-org linkage; PartnerSettings + partner endpoints (do not change); Program.cs route map.

Slices, one per round (each ends with a clean build, tests green, and a commit by explicit path):
(1) Ledger contract (U01 + invariant 6): immutable `usage_event` (typed units, source ids, occurrence time, price version, cost state, correction lineage, tenant/payer/principal ids, `logical_request_id`/`attempt_id`), deterministic event and settlement ids persisted BEFORE any Stripe write, uniqueness constraints, reject/quarantine missing ids, NO fresh-GUID-on-retry anywhere (grep and remove the fallback). Migration via the service's existing mechanism; dual-write path reused with a repair queue table (B01: durable per-record reconciliation state, degraded status surfaced in the health endpoint). Tests: duplicate delivery → one fact; secondary failure → repair row + replay. Commit: feat(billing): immutable usage/charge ledger with deterministic ids and repair queue.
(2) Pricing + comparison (U03, B02, D06/D07): versioned tariff table (provider price basis, discount/FX policy fields, markup 1.20 applied ONCE, invoice-currency rounding once); `usage_billing_mode` byok|managed on the tenant — BYOK events rate to zero token charge and zero routing surcharge and are never invoice candidates; independent recalculation service producing NON-authoritative comparison rows (LiteLLM-reported cost vs independent receipt) that can hold a settlement but can never create, reprice or collect a liability. Tests: $10 eligible cost → $12 managed, $0 BYOK; mismatch → hold. Commit: feat(billing): versioned tariff, BYOK/managed modes, independent comparison path.
(3) Single Stripe writer + meter isolation (B03, D08, invariant 5/10): all guest-product Stripe operations go through one `GuestStripeWriter` (customer, Checkout session, customer portal, subscription, meter event, invoice, payment, credit/refund) with pre-persisted ids as idempotency keys and account/mode isolation (test mode pinned by config; refuse live-mode keys unless `Stripe:AllowLiveMode=true`); meter role = OBSERVATION ONLY: prove with tests that meter events are emitted for a non-chargeable product/price (isolation flag) and that no meter event can ever be attached to a chargeable subscription price for the guest SKU; keep existing unrelated billing behaviour unchanged. Commit: feat(billing): single guest Stripe writer with observation-only meter isolation.
(4) Base subscription via hosted Checkout (P02, D13): `POST /api/v1/guest/checkout-session` (authenticated intent from the app/portal; allowed price ids from config; saved-method agreement) → Checkout session URL; customer portal link; webhook inbox table for Stripe events with signature verification and idempotent processing (checkout.session.completed → subscription binding to the guest/payer; no second writer); `GET /api/v1/guest/subscription` status. Tests with the fake gateway. Commit: feat(billing): guest base subscription via hosted Checkout with webhook inbox.
(5) Nightly close state machine (B04, B05, D09): settlement records per customer/day (frozen), 03:15 America/Los_Angeles close with locks, durable outbox intent, explicit draft-invoice binding (never ambiguous pending items), small-balance carry-forward below `Billing:SmallBalanceThreshold`, off-session payment attempt, recovery on lost responses reusing the original invoice/intent ids, hourly reconciliation comparing LiteLLM vs ledger vs meter acceptance vs previously invoiced sets; unexplained discrepancies HOLD the customer's settlement. Kill switches `GUEST_CHARGING_ENABLED` (default false) and `GUEST_EXECUTION_ENABLED` (B07 minimal: reservation check + kill switch; full admission control later). Scheduler is a hosted service that is disabled unless `Billing:NightlyCloseEnabled=true`. Tests: duplicate job execution → one settlement; lost response → same ids reused; discrepancy → hold; kill switch → no collection. Commit: feat(billing): nightly settlement state machine with holds, outbox and kill switches.
(6) Verification + handback: full build; all tests (quote counts; the 9 DataIntegrity Postgres tests may still need TEST_DATABASE_URL — report NOT RUN if absent); Swagger for the new endpoints; `design docs/guest-billing-slice1-handback.md`: what changed per backlog row (P02, U01, U03, B01–B05, B07-min), config keys with placeholders, the Stripe TEST-mode objects to create, the acceptance-criteria checklist (✔ / partial / not in slice), invariants 1–12 evidence table, and what slice 2 adds (B06 dunning/corrections, P03 provisioning, U02 LiteLLM-first capture, C01 cloud allocation). Commit: docs(billing): guest billing slice 1 handback.

HARD RULES: Stripe test mode only, enforced in code; no live charge path reachable; no change to existing Stripe meter code paths for non-guest products beyond routing through the writer; no YouTrack changes; no secrets or key values anywhere; no .github/**, compose or .env changes; commit by explicit path; no push. Completion = commit hashes, build/test output, endpoint table, Stripe test objects list, acceptance checklist, NOT-RUN list. Auditor: no git fetch; grep for `Guid.NewGuid()` on retry paths (must be gone from settlement/Stripe writes), confirm meter isolation tests exist and pass, confirm kill switches default off, no live-mode keys accepted by default, no secrets in the diff.
