BUILD + TEST (code; billing service + admin web UX): move the guest kill switches out of the lane env files and into the admin UI, so an operator flips guest execution and guest charging per environment from the browser instead of editing a .env and restarting the app. Work ONLY in /home/harness/work/mcp-cognizioware-b on branch feat/guest-switch-admin-ux (run `git fetch origin && git checkout -B feat/guest-switch-admin-ux origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NO live Stripe or Dataverse calls; env NAMES only.
TODAY'S BEHAVIOUR (verified by the overseer on the dev lane 2026-09-08): Configuration/BillingSettings.cs exposes `GuestChargingEnabled` (master kill switch for guest charging - no invoice finalized, no payment attempted when false) and `GuestExecutionEnabled` (admission check before a close or a meter write), with env names GUEST_CHARGING_ENABLED / GUEST_EXECUTION_ENABLED, bound in the lanes as Billing__GuestChargingEnabled and Billing__GuestExecutionEnabled. Changing either needs an env edit plus a container restart, which is exactly what the overseer had to do to start testing.
FOLLOW THE PATTERN ALREADY IN THIS REPO: the release-readiness setting and its admin card store state in `app_configuration` through `IAppConfigurationService` and are read by the admin controllers; `AdminE2eController` shows the auth shape (Entra admin OR an ingest key) and `IsAuthorizedAsync`. Reuse both rather than inventing a new settings store or a second auth path.
SLICES (one commit each, explicit paths):
 (1) resolution order and service: a `IGuestSwitchService` that resolves each switch as app_configuration row FIRST, then the existing BillingSettings/env value as the fallback default, and caches with a short TTL so a flip takes effect within seconds without a restart. Every existing read of GuestChargingEnabled / GuestExecutionEnabled goes through it. Config rows: category Billing, keys `Billing:GuestExecutionEnabled` and `Billing:GuestChargingEnabled`, is_secret=false, scope global. Commit: feat(billing): guest switches resolve from admin config with env fallback.
 (2) admin API: GET returns both switches with their effective value AND where it came from (admin row or env default), plus who last changed it and when; PUT sets one switch, requires an explicit `reason` string, stamps updatedBy/updatedAt, and writes an audit row using the existing audit/activity pattern. Charging is the dangerous one: PUT of GuestChargingEnabled=true must additionally require a confirmation field (for example `confirm: "enable-guest-charging"`) and must refuse when the service is in an environment marked production unless that confirmation is present. Commit: feat(admin): guest switch endpoints.
 (3) admin UI card next to the existing release-readiness card: two labelled toggles with their current source and last-changed line, a reason box, a clear warning on charging that real money moves, and an optimistic-but-verified update that re-reads the effective value after the write. Commit: feat(admin-ui): guest switch card.
 (4) tests: unit tests for the resolution order (row wins over env, missing row falls back, TTL refresh), the confirmation requirement on charging, the audit row, and auth (Entra admin allowed, ingest key allowed, anonymous refused); a UI test if the repo has a harness for the admin page, otherwise say so. Commit: test(billing): guest switch resolution and guard.
 (5) docs: the guest runbook gains a short section - which switch does what, that the admin UI is now the source of truth, that the env value remains the boot default, and the exact rollback (clear the row, the env default applies again). CHANGELOG. Commit: docs(billing): guest switches in the admin UI.
HARD RULES: do not change what the switches DO, only where their value comes from; no schema migration beyond an app_configuration row; no secrets; explicit-path commits; no push; dotnet test green except the known offline DataIntegrity tests (report counts). Completion = commits, diff stat, test summary, and the exact curl for reading and setting each switch (env names only). Auditor: read-only - verify the env fallback still works when no row exists, that charging cannot be enabled without the confirmation field, and that every previous read of the two settings now goes through the new service.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
