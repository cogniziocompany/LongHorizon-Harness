BUILD-ONLY. Read the authoritative plan FIRST and follow it exactly: /home/harness/work/cognizioware-hydra-rsi/design/rsi-org-loop/20-youtrack-approval-trigger.md and /home/harness/work/mcp-cognizioware/design docs/youtrack-phase1-handback.md. Work ONLY in this worktree /home/harness/work/mcp-cognizioware on branch feat/rsi-approval-trigger from origin/develop @ HEAD; never cd into or run git against any other checkout; plain git commands only. This run extends the existing POST /api/youtrack/events receiver with the RSI approval-trigger contract. Slices, one per round:
(1) RSI matcher + validation: add an RsiApprovalController or equivalent handler invoked when the existing events controller sees project_id matching the configured RSI project. Validate RSI_LOOP_ENABLED=true and RSI_TRIGGER_ENABLED=true; validate Approval changed to Approved; validate Approver Identity is present and not ai-dev01@cognizio.company; validate actor.email / actor.login is not ai-dev01@cognizio.company; validate Proposed Run Type is in {build,fix,eval,docs}; validate Target Repo is in the RSI allow-list. On any validation failure, return 400 and add a rejection comment to the issue via existing youtrack:* MCP tools.
(2) harness_trigger_request builder: map Target Repo to repo, issue tags to base_ref and branch, Proposed Run Type to run_type, default node CT110 and max_rounds (build/fix 8, eval 4, docs 2), and capture approver_identity from the YouTrack field. Label model_trio as UNVERIFIED/contract-supplied (glm-5.3-flash fallback) and require the deployer to map to the nearest available LiteLLM alias.
(3) Idempotency + continuation: add a Redis hash rsi:approval:<issue_id> (or durable Postgres table) storing hydra_run_id, node_id, status, created_at with 90-day TTL. Before calling Hydra, reject with 409 if an active run exists. On re-approval after a terminal run, create a continuation run with branch feat/rsi-<run_type>-<issue_id>-continue-<n> and task_text referencing parent_run_id. Queue write-back to the pending-events table if YouTrack is down.
(4) Verification + docs: simulate YouTrack webhooks (approved, rejected due to service-account approver, duplicate active run, continuation run) using a local test harness or self-check script. Verify the receiver returns the expected status codes and comments. Update design/rsi-org-loop/20-youtrack-approval-trigger.md §4–5 with any deviations. Verify git diff --stat origin/develop..HEAD stays within src/McpCognizioware.BillingService/.
HARD RULES: the service account ai-dev01@cognizio.company must never be treated as an approver; kill switches RSI_LOOP_ENABLED and RSI_TRIGGER_ENABLED default false; no direct Hydra-to-YouTrack REST calls from this repo beyond the existing billing service; no secrets in code or docs; commit by EXPLICIT PATH; no push. Completion = commit hash, git diff --stat origin/develop..HEAD, the simulated webhook outputs (status codes + comment bodies), and the idempotency/continuation traces. Auditor: no git fetch; confirm approver validation is present, kill switches default off, idempotency prevents duplicate active runs, and the diff contains no YouTrack perm: token.
