BUILD + TEST (code; PR A of the "E2E model registry" plan; the lane runs e2e on merge): make the cognizioware-mcp-tools e2e suites build their model and Ollama-host lists at run time from a DB-backed registry instead of literals, with a committed fallback snapshot, a sync tool, and a drift test. Planning session 3e71c218; full plan in tasks/e2e-model-registry-2026-09-08.md of LongHorizon-Harness - READ IT FIRST via `git show origin/main:tasks/e2e-model-registry-2026-09-08.md` in /home/harness/work/LongHorizon-Harness after `git fetch origin` (never write there). Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/e2e-model-registry (run `git fetch origin && git checkout -B feat/e2e-model-registry origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to the gateway, admin API or Ollama hosts (fixtures + fake HTTP); do not edit .github/workflows/; do NOT touch infrastructure/litellm-config.yaml in this PR (that is PR B); env names only (LITELLM_MASTER_KEY read from e2e/.env by the operator, E2E_INGEST_KEY).
READ FIRST: e2e/lib/admin-baseline.js + e2e/baseline.json + e2e/suites/22-aggregate-baseline.test.js (the DB-backed baseline with committed fallback - copy this pattern exactly), e2e/lib/skip-registry.js, e2e/run-all.sh, e2e/package.json, e2e/README.md, e2e/setup/ (jest globalSetup if present), and the suites named below.
SLICES (one per round; code + tests + explicit-path commit each):
 (1) e2e/lib/model-registry.js mirroring admin-baseline.js: fetchRegistry() -> admin GET /api/v1/admin/e2e/models, else fallback e2e/fixtures/models.json (with a _source flag); putRegistry(doc) / putDiscovered(discovered) -> admin PUT with the ingest key (never throws); helpers isRetired(id) (glob), pulledOn(backendUrl, tag), byPhase(name); a synchronous loadRegistrySync() reading the per-run cache file e2e/.model-registry.json (gitignored) so test.each arrays can be built at module load. e2e/fixtures/models.json committed snapshot with the document shape from the plan (policy.critical ["qwen3.8"], localFloor, retired ["qwen3.5:*","qwen3.6","qwen3.6:*"], relayFloor, the five backends, models by phase, empty discovered). Unit tests with a fake admin server. Commit: feat(e2e): model registry client + fallback snapshot.
 (2) jest globalSetup (e2e/setup/jest.setup.js or the repo's equivalent): fetch the registry once, run discovery (GET /v1/models on the gateway + GET /api/tags on every backends[].url, 8 s timeouts, soft), write e2e/.model-registry.json, and when E2E_INGEST_KEY is set PUT the refreshed discovered block back (auto-fill on run). Tested with fake servers; .gitignore entry. Commit: feat(e2e): registry discovery in global setup.
 (3) e2e/scripts/sync-model-registry.js (`npm run models:sync`): --dry-run prints the diff between expected models and the gateway /model/info catalog classified by litellm_params.api_base (RFC1918 + ollama_chat|openai -> local, ollama.com -> cloud, embeddings mode -> embed, else external); --promote writes models.* from the live catalog minus policy.retired; --write PUTs to the admin API and refreshes e2e/fixtures/models.json. Reads LITELLM_MASTER_KEY + E2E_INGEST_KEY from e2e/.env (read only). Tests with fixtures. Commit: feat(e2e): sync-model-registry tool.
 (4) suite rewrites, keeping every existing hard/soft/advisory policy as documented in suite 13's header and using lib/skip-registry instead of expect(true).toBe(true): 13-litellm-model-inference (CRITICAL/LOCAL/CLOUD/EXTERNAL/EMBED models, reranker, BACKENDS, CORSAIR_SAMPLE, QWEN38_LANES, SPAN_BASE, RELAY_FLOOR from the registry; delete the two qwen3.5:4b--64k Phase 6 tests and replace with one generic test.each(localBackends) that picks a registry local model pulled on that host; CORSAIR_SAMPLE = registry local+cloud ∩ discovered.backends[.151]; add "no retired model in /v1/models" (hard fail) and "no chain targets a retired model" (extend parseChains from qwen3.6 to the retired globs)); 02-litellm-models (test.each(expectedIds) + retired-absent check; drop qwen3.5:27b); 12-external-public-endpoints:174 and 18-azure-inference-adapter:38,56,82 (qwen3.5:* -> policy.critical[0]; DIRECT_NEW_MODELS from the registry); 34-litellm-thinking-roundtrip:70 and 23-qwen38-vision-quota:26,128 (critical / relayFloor from the registry). Commit: refactor(e2e): suites read the model registry.
 (5) docs: e2e/README.md phase table + "Model registry" section next to "Aggregate baseline", .env.example (no new secrets), CHANGELOG. Commit: docs(e2e): model registry.
HARD RULES: no policy weakened; no workflow or litellm-config changes; fallback snapshot always works offline; explicit-path commits; no push. Completion = commits, diff stat, unit-test summary, the exact operator sequence (seed the registry row through the admin API once PR 20a is live; `npm run models:sync -- --dry-run` then `--write`), and what remains for PR B. Auditor: no git fetch; verify the retired-drift hard-fail test exists, suites contain no literal qwen3.5/qwen3.6 tags, and the fallback path is tested.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
