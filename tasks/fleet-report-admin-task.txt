BUILD + TEST (code; the overseer deploys through the mcp-tools lane): the fleet-admin side of the "Harness Fleet Window" plan (planning session c84a6035; full plan text is in tasks/harness-fleet-window-2026-09-07.md of LongHorizon-Harness, which you may READ at /home/harness/work/LongHorizon-Harness/tasks/ - never write there). Work ONLY in /home/harness/work/ptait09-easybutt0n-ai on branch feat/harness-fleet-report (run `git fetch origin && git checkout -B feat/harness-fleet-report origin/easybutt0n-runner-v2` first), subdir fleet-admin/; plain git; commit by EXPLICIT PATH; no push; no live calls (no agent-db, no YouTrack, no CT202); secrets are env NAMES only (FLEET_ADMIN_KEY, FLEET_READ_KEY, POSTGRES_* for agent-db, YOUTRACK_URL, YOUTRACK_TOKEN).
READ FIRST: fleet-admin/src/server.js (POST /enroll, POST /checkin HMAC over raw body with X-Fleet-Host + X-Fleet-Signature, FLEET_ADMIN_KEY reads, logEvent, GET /health), fleet-admin/schema.sql (schema fleet: hosts, enroll_tokens, heartbeats, events, view host_status), src/migrate.js (idempotent), package.json, and the lh-harness shapes you must mirror: EventEnvelope (webapi/models.py), build_run_summary (webapi/snapshot.py), the round artifact + trajectory routes (GET /api/runs/{id}/rounds/{n}/artifacts, .../artifacts/{name}, .../trajectory/{role}, .../report) in /home/harness/work/LongHorizon-Harness/src/lh_harness/webapi (read only).
SLICES (one per round; code + tests + explicit-path commit each):
 (1) schema + ingest: idempotent additions to schema.sql - fleet.harness_nodes(host FK, version, kind, labels jsonb, ui_base_url, last_heartbeat, capacity jsonb), fleet.harness_runs(host, run_id, repo, workspace, status, round, active_role, model, youtrack_issue_id, started_at, ended_at, last_event_at, summary jsonb, PK(host,run_id)), fleet.harness_events(id, host, run_id, type, ts, round, role, payload jsonb; index (host,run_id,ts)), fleet.harness_gates(host, run_id, approval_id, trigger, status, created_at, resolved_at), fleet.harness_rounds(host, run_id, round, recorded_at, summary jsonb), fleet.harness_artifacts(host, run_id, round, role NULL, name, kind artifact|trajectory|report, content_type, size_bytes, truncated bool, body bytea gz; PK(host,run_id,round,kind,role,name)). Factor the existing HMAC device auth into requireDevice() middleware (raw body kept). Routes: POST /harness/heartbeat (upsert node + runs), POST /harness/events (batch insert; upsert run status/gates from event types), POST /harness/rounds (round summary + gzip artifacts/trajectories, idempotent upsert; express.json limit 10 MB on this route only). Retention jobs: events > 14 d, artifact bodies > 30 d (summaries kept). Tests with node --test against a fake pg (or pg-mem) covering HMAC accept/reject, idempotent upserts, retention. Commit: feat(fleet-admin): harness ingest schema + routes.
 (2) read + window API: GET /api/fleet/overview (nodes + active runs + open gates + counts + issues[]), /api/fleet/nodes, /api/fleet/runs?status=&host=, /api/fleet/runs/:host/:runId (summary + last 200 events + gates), /api/fleet/runs/:host/:runId/rounds, .../rounds/:n/artifacts, .../rounds/:n/artifacts/:name (decompressed, correct content-type), .../rounds/:n/trajectory/:role (JSON, thinking preserved), .../report. Optional FLEET_READ_KEY via X-Fleet-Read-Key or ?key= (open when unset). Every non-2xx returns JSON. NO mutating routes on this surface (read-only phase). socket.io@4 on the same http server, namespace /fleet: on connect emit overview, then node.heartbeat / run.updated / event / gate.updated deltas as ingest lands; rooms run:<host>:<runId>; transports websocket+polling; handshake uses the read key. Tests: route shapes, read-key gating, socket emits on ingest (socket.io-client in tests). Commit: feat(fleet-admin): fleet read API + socket.io.
 (3) YouTrack read-through: env YOUTRACK_URL/YOUTRACK_TOKEN; server-side poll every 60 s of project MCP issues in the AI states (query per the plan; fields idReadable, summary, customFields(name,value(name)), updated); cache in memory + fleet.youtrack_issues; on error keep the last cache and set youtrack.stale=true (instance auto-offs nightly - soft-fail, never crash); join to runs on harness_runs.youtrack_issue_id; read-only. Tests with a fake fetch (ok, error->stale). Commit: feat(fleet-admin): youtrack read-through.
 (4) view app fleet-admin/web/ (React 18 + Vite + TypeScript + Tailwind + shadcn/ui components card/badge/tabs/sheet/scroll-area/tooltip, socket.io-client, @tanstack/react-virtual, react-markdown, diff, lucide-react): overview in the Ship Plane visual grammar (host bands = nodes, run cards, flow arrows, green/amber/red pills, dark/light CSS tokens, status pulses), run page with round-by-round trajectories (thinking blocks rendered, virtualized), artifacts (markdown/diff rendered), gates timeline, YouTrack issue panel; NO control actions anywhere; builds to fleet-admin/public/ served by Express. Multi-stage Dockerfile in fleet-admin/ (node:20 build web -> node:20-alpine runtime; CMD migrate.js && server.js). /health extended with db + last ingest age. Component tests (vitest) for the run page rendering a fixture trajectory with thinking; a network-log assertion test that the page issues only GET + socket traffic. Commit: feat(fleet-admin): fleet window view + Dockerfile.
 (5) docs: fleet-admin/README.md (ingest contract, read API, env names, deploy notes), .env.example names. Commit: docs(fleet-admin): harness fleet window.
HARD RULES: read-only surface (no stop/resume/instruct/resolve routes); HMAC on every ingest; secrets names only; explicit-path commits; no push; keep existing /enroll /checkin behaviour byte-compatible (tests prove it). Completion = commits, diff stat, test summary, the exact env NAMES the overseer adds to mcp-tools.env on CT202, and the enrol sequence for ct110. Auditor: no git fetch; verify no mutating fleet routes, HMAC reject test, YouTrack soft-fail, and thinking preserved in the trajectory route.
