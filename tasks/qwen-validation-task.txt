BUILD-ONLY validation task (small, three slices). Work ONLY in /home/harness/work/cognizioware-hydra-rsi on a NEW branch `chore/qwen38-validation-20260906` created from the current HEAD (`git checkout -b chore/qwen38-validation-20260906`); plain git; commit by EXPLICIT PATH after each slice; no push; never touch other checkouts or /home/harness/venv. Node is /usr/bin/node (v22).
Slice 1: add scripts/rsi/check-env-names.mjs — reads design/rsi-org-loop/20-youtrack-approval-trigger.md and 10-openwebui-wiring.md, extracts every token that looks like an environment variable name (UPPER_SNAKE_CASE, 3+ chars, containing an underscore), de-duplicates, and prints them sorted as JSON to stdout; exit code 0. Run it and paste the output in your report. Commit: chore(rsi): env-name extractor for the design docs.
Slice 2: add scripts/rsi/check-env-names.test.mjs using node:test that runs the extractor on a small inline fixture string (refactor the extractor so the core function is exported and the CLI is guarded by `import.meta.url` main check) and asserts de-duplication, sorting and the underscore rule. `node --test scripts/rsi/` must pass. Commit: test(rsi): env-name extractor tests.
Slice 3: add docs/rsi-env-names.md listing the extracted names in a table with a one-line "where used" column derived from which doc(s) mention each name; no values, names only. Commit: docs(rsi): env-name inventory.
Completion = three commit hashes, the JSON output, and the `node --test` summary. Auditor: no git fetch; verify the three commits exist, tests pass, and no secret-looking values appear.
