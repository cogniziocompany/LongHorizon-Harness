FIX (repo only; the lane deploys): production of cognizioware-powerplatform can be silently ROLLED BACK. Evidence 2026-09-07: the pinned promotion deployed develop sha 476811f (guest slice 1 + nav/archive) to prod on 2026-09-06, then PR #79 (a CI-only fix) merged to `main` and the push-to-main rollout (rollout.sh matches instance branch main) redeployed prod at sha-f59c833 = main, which is 92 commits BEHIND the promoted sha. Prod now runs old product code. Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch fix/prod-promotion-branch-guard (run `git fetch origin && git checkout -B fix/prod-promotion-branch-guard origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live calls.
DELIVER (mirror the billing repo's model: prod is only ever the promoted sha): (1) .github/workflows/promote.yml: after the prod deploy succeeds, fast-forward `main` to the promoted sha (git push origin <sha>:main, refusing non-fast-forward with a clear error and a documented recovery), so main == prod always; (2) .github/workflows/ci.yml: the push-to-main deploy job must NOT roll prod unless the pushed sha equals the last promoted sha recorded by promote.yml (record it as a repo variable PROD_PROMOTED_SHA or a `prod` tag/branch that promote.yml moves; choose the mechanism the repo can support and explain); otherwise skip with `::notice::main changed without promotion; prod stays at <promoted sha>`; (3) scripts/deploy/rollout.sh: refuse to deploy an instance whose tier is prod when the requested sha is not the promoted sha (same source), with a `--force-prod` escape that requires ROLLOUT_APPROVED=1 and prints the rollback target; (4) a tests script with a stubbed gh/git for the guard logic; (5) deployments/README.md + docs: "how prod gets code" (only via promote.yml; CI fixes go to develop first and ride the next promotion; hotfix procedure). Do not change the eval gate logic (task 06b landed) or the branch-per-env mapping for dev/uat. Completion = commits, diff stat, test output, and the exact one-time steps for the overseer to set the recorded promoted sha after the in-flight promotion of 845bc89 lands. Auditor: no git fetch; verify a plain push to main can no longer redeploy prod with an unpromoted sha.
