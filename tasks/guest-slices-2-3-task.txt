BUILD + TEST (code; the overseer merges and deploys through the dev lane): implement guest-launch gap SLICES 2 and 3 from design/plans/guest-launch/E2E-GAP-SLICES-2026-09-07.md (READ IT FIRST plus E2E-READINESS-AUDIT-2026-09-07.md). Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch feat/guest-slices-2-3 (run `git fetch origin && git checkout -B feat/guest-slices-2-3 origin/develop` first - slices 1 and 4 land on develop before you start; if `git log origin/develop --oneline -20` shows no guest-slices-1-4 merge, STOP and say so); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls to Stripe, Dataverse, n8n or the lanes. You MAY READ (never write) /home/harness/work/mcp-cognizioware to match the billing contracts (POST /api/v1/guest/checkout-session, usage endpoint).

SLICE 2 - Usage reporting call site (S): after each agent completes in packages/backend/src/orchestration/pipeline.ts (~138) call reportUsage(stripeCustomerId, agentId, tokensInput, tokensOutput, actualCostUsd) before updating agent_step_history; in billing.service.ts reportUsage add the res.ok check and retry-once with exponential backoff, and an x-cognizioware-correlation-id header. Tests: pipeline.usage-reporting.test.ts (mock billing API; body fields; second attempt on 503). Acceptance: a design-phase run produces a UsageEvent with matching customer_id, total_tokens > 0, stripe_mode = test.
SLICE 3 - SPA Checkout trigger + return (M): `POST /api/checkout/session` proxy to the billing guest checkout-session (email, tenantId, priceId, customerId; reject a wrong price id), a CheckoutButton component that redirects to checkoutUrl, `/checkout/success` and `/checkout/cancel` routes that poll the customer state and show plain-language status (display names only). Tests: Playwright guest-checkout.spec.ts (mock billing; redirect URL contains checkout.stripe.com), checkout.routes.test.ts.

RULES: no secrets (env names only); keep existing tests green; one commit per slice; update e2e/guest-e2e/checklist.json `implementedBy` for satisfied entries. Completion = commit hashes, diff stat, test summary, and the operator env NAMES (STRIPE_GUEST_PRICE_ID or the existing name in the billing service, BILLING_API_URL/KEY). Auditor: no git fetch; verify the retry path and the wrong-price rejection are tested.

OVERSEER NOTE (added 2026-09-07 21:00 PT, do this as SLICE 0 before slices 2 and 3): the non-prod Dataverse environment for the guest run is DECIDED: powerplatform-dev, URL https://org9130dfc5.crm.dynamics.com (Power Platform environment id 00a55060-2baa-e95b-bcb7-29d717d7c104), test principal = the existing dev service principal / ai-dev01 (env names GUEST_TEST_PRINCIPAL_EMAIL, MSAL_CLIENT_ID/SECRET, TEST_USER_EMAIL/PASSWORD). Add scripts/guest/seed-dev-grant.mjs: idempotent; reads TARGET_ENVIRONMENT_URL + GUEST_TEST_PRINCIPAL_EMAIL + the admin credentials from the environment (names only); (1) finds-or-creates the backend environment record for that Dataverse URL via the existing environments API, (2) POSTs the environment grant for the principal (slice 4 API, requireAdmin), (3) runs the backend verifyAuth path for the principal against that environment and prints PASS/FAIL with the Dataverse error text verbatim (Paxton: "it will surface the error"), exit non-zero on FAIL. Unit tests with a fake API. Add "guest:seed-dev-grant" to package.json and a line in e2e/guest-e2e/README.md. Commit: feat(guest): dev grant seeding + verifyAuth probe. The overseer runs it on the dev lane right after merge; do NOT call any live service yourself.
