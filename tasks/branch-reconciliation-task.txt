AUDIT + BUILD (cognizioware-powerplatform): `main` and `develop` have diverged for ten days and neither branch is a superset of the other. Reconcile them WITHOUT LOSING ANYTHING, then make this class of drift impossible to repeat silently. Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch chore/branch-reconciliation-audit (run `git fetch origin && git checkout -B chore/branch-reconciliation-audit origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NEVER force-push, NEVER rewrite either branch's history, and do not modify main or develop themselves - this task produces an audit, a reconciliation BRANCH and a policy, for a human to merge.

THE SITUATION, measured 2026-09-08:
 - merge base `ae453ef5`, 2026-08-29. `develop` is 122 ahead and 32 behind `main`.
 - PRODUCTION RUNS `e5c903b2` (develop's head), deployed and verified through twelve green gates on 2026-09-08. `main` still points at `f59c8336`. So the repo's default branch does NOT record what is in production, and anyone reading main to answer "what is live" gets the wrong answer.
 - The promotion workflow's final step tries to FAST-FORWARD main to the promoted sha. It cannot, correctly refuses, and fails the run - so every future promotion ends red on bookkeeping until this is resolved.
 - The 32 commits on main are CI/promotion work merged directly to main via PRs #41-#79 between 5 and 7 September (promotion workflow registration, QA gate dispatch on `master`, status-poll auth, `statuses: read`, eval-gate timeout, per-run concurrency, PowerShell->bash conversion for the Linux CT210 runners). Several PR titles say "cherry-picked", so some content may exist on both sides under different shas - VERIFY, do not assume.
 - `.github/workflows/promote.yml` exists on BOTH branches as INDEPENDENT files (main ~645 lines, develop ~822). Develop's version is the one that ran successfully today. They are not one lineage.
 - NON-WORKFLOW drift, and this is the part that makes a reset unsafe: `e2e/evals/run.mjs` differs by +80/-11 on main (commit 1ccb6f16, "wait past undici's 300 s cap; sweep orphaned eval_runs"), `e2e/evals/README.md` by +10, and `scripts/promotion/README.md` (220 lines) exists ONLY on main.

THE QUESTION THAT MATTERS MOST - answer it first and put the answer at the top of the audit: DOES PRODUCTION LACK A FIX THAT MAIN HAS? Specifically, does develop's `e2e/evals/run.mjs` contain the undici 300-second cap fix and the orphaned `eval_runs` sweep, in any form, under any sha? If it does not, prod is running an eval path with a known defect that was fixed on main ten days ago, and that is a live problem to be reported immediately, not filed in a document.

SLICES:
 (1) THE AUDIT - `docs/branch-reconciliation-2026-09-08.md`. Enumerate EVERY difference between main and develop across ALL paths, not just .github. For each, classify with evidence: SUPERSEDED (develop has equivalent content, cite the develop sha or the code that supersedes it), UNIQUE-TO-MAIN (exists only on main and is still wanted), or OBSOLETE (exists only on main and is genuinely dead - justify). Do the same in the other direction for anything on develop that main would need. Use `git log`, `git diff` and content comparison - a shared commit MESSAGE is not evidence of shared CONTENT when the change was cherry-picked. State plainly where you could not determine the answer. Commit: docs: main/develop divergence audit.
 (2) THE RECONCILIATION BRANCH - build a branch that carries everything both sides need, by MERGE or CHERRY-PICK FORWARD onto develop, never by resetting or force-pushing anything. Everything classified UNIQUE-TO-MAIN comes forward. Resolve each conflict deliberately and record WHY in the commit message - a conflict resolved by "take theirs" without a reason is exactly how the eval fix would get lost a second time. If two independent implementations of the same thing exist (the two promote.yml files), keep the one that DEMONSTRABLY WORKS - develop's ran twelve green gates on 2026-09-08 - and port anything main's has that develop's lacks rather than replacing wholesale. Commit each cherry-pick or resolution separately with its rationale.
 (3) VERIFY THE RECONCILIATION - run whatever the repo's own test and lint targets are, and report counts. Then prove the two specific things a reader will care about: that the reconciled branch still contains the undici/eval_runs fix, and that its promote.yml is develop's working version. Commit: test: verify the reconciliation carries both lines of work.
 (4) THE POLICY THAT STOPS IT RECURRING - this is the deliverable that matters more than the one-time fix. Document the branch model in `docs/branching.md`: which branch receives what, where CI and workflow changes belong, and how main comes to point at production. Then make it enforceable rather than aspirational: propose the specific branch-protection or ruleset settings that would have prevented 32 commits landing on main outside the promotion path, and say exactly which of them a repo admin must click, since you cannot set them yourself. Commit: docs: branch model and the protection that enforces it.
 (5) DRIFT DETECTION - a check that reports when main and develop diverge beyond an expected shape (main should normally be BEHIND develop by whole promotions and never ahead of it by unmerged work), wired into CI as a WARNING first, not a blocker, with a clearly marked switch to promote it later. It must name the specific commits and files that constitute the drift, not just a count. Include a test. Commit: ci: detect main/develop drift.

WHAT THIS TASK MUST NOT DO: force-push anything; reset main; rewrite history on either branch; merge the reconciliation branch itself (a human decides that); delete or "tidy" any commit on main; or resolve the divergence by declaring main's work obsolete without per-file evidence. If the honest conclusion for a given file IS that main's version is obsolete, say so with the evidence and let the reviewer agree.

HARD RULES: no credentials or tokens anywhere; explicit-path commits; no push; do not touch the promotion workflow's fast-forward step (a separate PR handles the bookkeeping) beyond noting how the reconciliation makes it able to succeed again. Completion = the audit document, the reconciliation branch and its commits, test counts, an explicit YES/NO on whether production lacks the undici/eval_runs fix, the list of settings a repo admin must apply, and a one-paragraph statement of what a reviewer must decide before this can be merged. Auditor: read-only - verify no history was rewritten and no force-push attempted, that every UNIQUE-TO-MAIN item is either carried forward or justified as obsolete IN WRITING, that no conflict was resolved without a recorded reason, and that the drift check warns rather than blocks.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

## THIS IS NO LONGER THEORETICAL - IT CAUSED A PRODUCTION REGRESSION ON 2026-09-09
Evidence, all read off the live system:
 - `deployments/instances.json` maps the prod instance to `branch: main` (sshHost `pve151`, dir `/opt/cognizioware-powerplatform`), and `ci.yml` declares `on: push: branches: [main, develop, release]` with `main -> prod`. **So any push to `main` deploys production.**
 - `git compare e5c903b...26d90caa` reports **diverged, ahead 33, behind 122**. `main` is ONE HUNDRED AND TWENTY-TWO commits behind the lineage that prod had been running.
 - At 02:28:23Z a PR was merged to `main` (a CI-guard-only change, pp #98). CI run `34303347915` fired on that push and deployed `sha-26d90ca` to production. Prod silently lost 122 commits of work, including the current UX. It came up **healthy on the wrong code** - nothing failed loudly, exactly as in the 2026-09-08 stale-pin incident.
 - Recovery: the prod `.env` pin already read `APP_IMAGE_TAG=sha-e5c903b` and the CI deploy had overridden it at deploy time without updating the file, so the pin and the running container disagreed AGAIN (this is task 60's defect 1, observed a second time). Pin re-applied, `docker compose up -d --no-deps app`, and read back: running image `app:sha-e5c903b`, health reports `version: sha-e5c903b`, status healthy, front page 200.
So the reconciliation this task exists to do is now a PRODUCTION SAFETY item, not housekeeping: while `main` is the prod branch AND is 122 commits behind, **every merge to main is a production rollback waiting to happen**.

REQUIRED IN THE AUDIT, before proposing any merge direction: state plainly which branch prod SHOULD deploy from, and whether the answer is to fast-forward `main` onto the current lineage or to repoint the prod instance's `branch` in `instances.json`. Whichever it is, the outcome must be that no ordinary merge can move production backwards. Also propose the guard that would have caught this: a pre-deploy assertion that the sha about to be deployed to prod is not BEHIND the sha currently running there - a deploy that reduces the commit count is a rollback and must be explicit, never incidental.
