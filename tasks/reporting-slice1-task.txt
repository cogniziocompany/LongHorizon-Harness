BUILD-ONLY. Cognizioware Reporting-First Intelligence — slice 1: the first READ-ONLY LiteLLM/harness reporting screen in Hydra (RP-00 durable read model + RP-02a evidence/reporting slice), applied to the CURRENT code base and infra. Authoritative package, read FIRST in this order, all in design/inputs/reporting-first/ of this worktree (untracked copies of cognizioware-powerplatform/design/plans/*, SHA-256-verified by their author; commit them by explicit path in slice 1): cognizioware-reporting-first-council.md (synthesis) → cognizioware-litellm-reporting-data-catalog.md (the data catalog: grains, join precedence, typed reporting contract, Hydra screen specification, aggregation/dedup rules, privacy, "First reporting slice and acceptance criteria", "Recommended implementation order") → cognizioware-reporting-first-blueprint.md (requirements P0/P1, architecture and rollout boundaries, delta to the Ship Plan design) → cognizioware-harness-current-code-review.md (verified harness contract; "Recommendation: a delta to the existing Ship-Plan work items": RP-00 durable data, RP-02a read-only slice, RP-09 identity as release gate). Also read this repo's design/ship-plan-integration/10-ship-plan-reporting-hydra.md and 30-backlog.md (RP-02/RP-11/RP-12 you are adding a slice beside, not replacing). Work ONLY in this worktree /mnt/c/Users/PaxtonTait/source/cognizioware-hydra-reporting (branch feat/reporting-first-slice-1 from origin/main @ 57da83a); plain git only; commit by EXPLICIT PATH; no push; never touch other checkouts. `node` may need `source ~/.nvm/nvm.sh`.

Current code and infra facts (verified by the overseer 2026-09-06 09:00–09:30 PT — build against THESE, and mark anything else UNVERIFIED):
- Hydra orchestrator (orchestrator/src: index.js, fleet-routes.js, activity-log.js (in-memory ring, cap 500/target), harness-client.js, harness-registry.js, auth.js, mcp.js; UI orchestrator/public/index.html) runs on corsairai300 (/opt/cognizioware-hydra, tarball deploy; compose passes only explicit environment: keys). Stack: redis:7-alpine + ioredis dependency (unused at runtime); NO Postgres in the Hydra stack. Harness nodes: CT110 external http://192.168.21.168:8799 and managed nodes; Hydra reads runs through /fleet/runs and per-run snapshot/events/activity.
- LiteLLM router on CT202 (litellm.easybutt0n.ai; container cognizioware-mcp-tools-litellm) is litellm 1.93.0 with a Postgres DB ("db": "connected"). Live surfaces checked with the admin key: `GET /spend/logs?start_date=YYYY-MM-DD&end_date=YYYY-MM-DD` returns in ~0.2 s a per-day summary keyed by API-key hash → {models, users, spend, startTime}; `...&summarize=false` returns row-level spend logs (fields per LiteLLM_SpendLogs: request_id, call_type, api_key (hash), spend, total/prompt/completion_tokens, startTime/endTime, model, model_group, api_base, user, team_id, end_user, metadata (user_api_key_alias, user_api_key_team_alias, spend_logs_metadata, requester_metadata, tags…), cache_hit, request_tags, session_id — VERIFY the exact keys at runtime; the overseer's sample capture timed out); `GET /spend/logs` WITHOUT a date window times out (>25 s) — never call it unbounded; `GET /metrics` returns 404 (Prometheus callback not enabled) → treat all Prometheus-derived fields as UNAVAILABLE with reason "metrics endpoint not enabled". Only admin keys can read spend logs; an admin key is NOT provided to this run: implement the LiteLLM adapter behind `REPORTING_LITELLM_URL` + `REPORTING_LITELLM_KEY` env and run it in tests against a recorded fixture (invent realistic rows following the field list above; mark it FIXTURE). The overseer validates live after merge.
- Langfuse: per-key logging on the lh-harness key only; global callbacks prohibited; observations reachable through the gateway `langfuse_query_observations` MCP tool — treat as UNAVAILABLE in this slice unless `REPORTING_LANGFUSE_*` env is present (it is not); show correct denominator/reason.
- Harness (company fork cogniziocompany/LongHorizon-Harness): GET /api/runs, /api/runs/{id}/snapshot (schema_version, run{status, resume_epoch, resumed_from, max_rounds…}, rounds, approvals{approval_id,status,title,context…}, events (last 200 only), controls, diagnostics); count runs by (node_id, run_id), epochs by (node_id, run_id, resume_epoch); retry mode creates a new run with resumed_from. The WSL workbench at 127.0.0.1:8799 is NOT reachable from Hydra; use fixtures built from the documented schema and Hydra's existing harness-client.js for live reads.
- Identity: Hydra auth.js (API key + email allowlist); there is no tenant/payer crosswalk yet (RP-09). This slice is INTERNAL-ONLY reporting: every reporting route requires the existing Hydra operator auth and returns no customer payloads; economics screens beyond the LiteLLM cost provenance are OUT of scope (RP-11 later).
- Durable store decision (overseer): add an optional Postgres read model. Add the `pg` dependency to orchestrator/package.json (exact version), env `REPORTING_DATABASE_URL`; when unset, the reporting API runs in "fixture/in-memory" mode and the Data Health bar shows `store: disconnected`. Migrations as plain SQL files under orchestrator/sql/reporting/ applied idempotently at boot when the URL is set. The production target for the URL is the existing CT103 agent-db Postgres (hivemind) — the overseer wires it at deploy; do not hard-code hosts.

Slices, one per round (each ends with `npm test` green in orchestrator and a commit by explicit path):
(1) Commit the four package docs (docs(reporting): land the reporting-first package). Then RP-00 data model: orchestrator/src/reporting/schema.sql + migrate.js — tables per the catalog's canonical grains: source_registry (source, last_ingest_at, watermark, status, reason), llm_attempt (attempt_id = LiteLLM request_id, logical_request_id from request_tags/metadata when present else null, key_alias, team_alias, end_user, model_group, model, api_base, provider, call_type, tokens prompt/completion/total, usage_state ok|partial|missing_usage, spend, price_mapped bool, cost_provenance provider_reported|litellm_calculated|unknown, cache_hit, started_at, ended_at, latency_ms, outcome success|error|fallback, error_class, raw_tags jsonb), harness_run_fact keyed (node_id, run_id, resume_epoch) with status, rounds, gates, audit summary, resumed_from, and source_event dedupe table (source, source_event_id unique). Idempotent upserts. Unit tests with an in-memory shim (no Postgres in tests) + a contract test that the SQL applies on a throwaway Postgres if `TEST_DATABASE_URL` is set (else NOT RUN). Commit: feat(reporting): RP-00 durable read model.
(2) Ingest adapters (read-only): reporting/litellm-adapter.js (date-windowed `spend/logs?summarize=false`, cursor by (startTime, request_id), never unbounded, maps rows → llm_attempt with usage_state/price_mapped/cost_provenance rules from the catalog; `spend == 0` with no price mapping → price_mapped=false, cost unknown, never "free"; missing usage on streams → missing_usage), reporting/harness-adapter.js (uses harness-client.js snapshots; epochs by resume_epoch; approvals summarized; last-200-events gap flagged), reporting/source-health.js (registry + watermarks + null reasons + duplicate/gap counters). A `reporting-ingest` CLI/cron entry (`node scripts/reporting-ingest.mjs --once`) mirroring scripts/hivemind-ingest.mjs style. Tests with the fixtures (invented rows: include one retry + one fallback + one interrupted stream + one unmapped price). Commit: feat(reporting): LiteLLM + harness read-only ingest with source health.
(3) Reporting API (read-only, Hydra auth): GET /reporting/health (Data Health bar: per source connected|stale|disconnected|unavailable + reason, watermarks, duplicate/gap counters, store state), GET /reporting/overview?from&to (requests vs attempts, tokens, spend with provenance split, error/fallback rates, coverage denominators), GET /reporting/models (model/route performance: attempts, latency p50/p95, error/fallback rates, cache hits, price_mapped share, per model_group/deployment), GET /reporting/runs (harness run facts with epochs, gates, audit) and /reporting/runs/:node/:run (drilldown linking to the existing fleet run snapshot route, no duplicate viewer), GET /reporting/recommendations (returns an empty list + the Finding/Recommendation shape with nullable youtrack_issue_id; NO write routes). Every route: no raw prompts/completions/keys; 4xx on unauthenticated; typed JSON per the catalog's "Proposed typed reporting contract" (status vocabulary). Tests. Commit: feat(reporting): read-only reporting API.
(4) Hydra UI screen (orchestrator/public/index.html, same conventions as the existing fleet page — vanilla, no framework): a "Reporting" tab with the Data Health bar (per-source chips, never rendering an empty state as healthy: a fetch error renders disconnected/stale), AI Operations overview cards, Model & route performance table, Harness runs table with drilldown link to the run drawer, and a read-only Recommendations card ("no recommendations yet; review workflow is a later slice"). Fixture mode banner when the store is disconnected. Commit: feat(reporting): Hydra Reporting tab.
(5) Verification + docs: `npm test` (quote counts); a fixture-driven smoke: start the orchestrator locally with REPORTING_DATABASE_URL unset and curl the four routes with the operator key (quote outputs, redact keys); update design/ship-plan-integration/30-backlog.md with RP-00 and RP-02a rows (status: built in slice 1, live validation pending) without changing other rows; write design/reporting-first/SLICE-1-HANDBACK.md: what was built, env vars, migration list, the acceptance-criteria checklist from the catalog's "First reporting slice" (each item ✔ / partial / not in slice with reason), the UNVERIFIED/NOT RUN list (live LiteLLM row schema, Langfuse, Prometheus, live harness), and what slices 2+ add (RP-09 identity gate, Langfuse evidence, RP-11 economics, review workflow, YouTrack promotion — later, separately authorized). Commit: docs(reporting): slice 1 handback.

HARD RULES: read-only — no route or job may create YouTrack issues, deploy, change prices, touch billing/Stripe, or mutate harness runs; no changes to mcp.js tool surfaces or the existing fleet routes beyond adding the reporting routes/UI tab; no secrets or key values in code/docs/fixtures (env only); fixtures are clearly labelled FIXTURE; `pg` is the only new dependency; no changes to .github/**, docker-compose.yml (document the env additions in the handback instead), or scripts/hivemind-ingest.mjs; commit by explicit path; no push. Completion = commit hashes, `npm test` output, the smoke curl outputs, the acceptance checklist, and the UNVERIFIED list. Auditor: no git fetch; confirm no write routes exist under /reporting, no unbounded spend/logs call, `price_mapped=false` never renders as zero cost, fetch errors never render as empty/healthy, and no secrets in the diff.
