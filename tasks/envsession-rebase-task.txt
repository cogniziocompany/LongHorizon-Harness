FIX (repo only; the overseer merges PR #85): rebase feat/env-to-session-flow onto origin/develop and resolve the two conflicts so the PR becomes mergeable. Work ONLY in /home/harness/work/cognizioware-powerplatform (branch feat/env-to-session-flow is checked out there; run `git fetch origin` first); plain git; commit by EXPLICIT PATH; no push (the overseer force-pushes with lease after review); never touch /home/harness/venv; no live calls. Conflicts reported by merge-tree: packages/backend/src/config/env.ts and packages/backend/src/routes/environment.routes.ts (develop gained the guest slice 4 environment-grant routes + env names STRIPE_TEST_CUSTOMER_ID / GUEST_TEST_PRINCIPAL_EMAIL via PR #84; this branch adds env-to-session refresh/solution routes + its own env keys). Do `git rebase origin/develop` and resolve keeping BOTH sides' routes and env keys (no feature dropped, no duplicate route paths, one import block), then run `npm test` in packages/backend (the env-to-session suite 19/19 and guest-slice tests 46 must both pass; the 4 pre-existing failures may remain) and `npm run build` in backend + frontend. If the rebase produces more conflicts than these two files, resolve them the same way and list every file. Migrations are already renumbered 036-040; verify no duplicate numeric prefix remains in packages/backend/src/db/migrations. Completion = the final `git log --oneline origin/develop..HEAD` (16 + 1 commits expected), the resolved-file list with a one-line description of each resolution, and the test summary. Auditor: no git fetch; verify both route sets exist and no migration prefix collides.
