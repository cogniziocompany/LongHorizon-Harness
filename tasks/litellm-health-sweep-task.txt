BUILD + TEST (code; the overseer merges and drives the lane UAT->prod): close the remaining bare LiteLLM /health sweeps in the deploy lane and widen the lint guard so they cannot come back. Planning session b9d32bfc-53e5-4dc2-b865-d27a53dc92d3; the human-written plan is C:\Users\PaxtonTait\.claude\plans\troublehsoot-known-lane-defect-cheeky-emerson.md on the overseer PC - you cannot read it, everything you need is below. Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch fix/lane-health-probe (run `git fetch origin && git checkout -B fix/lane-health-probe origin/main` first); plain git; commit by EXPLICIT PATH; no push; NO live calls to CT202/CT204/the gateway (unit-level checks and fixtures only); env NAMES only.
WHY: LiteLLM's bare `GET /health` runs a live completion against EVERY configured backend (paid Perplexity, Ollama Cloud quota, local GPUs). It is not a liveness probe: it takes minutes and burns money and GPU on every call. `/health/liveliness` and `/health/readiness` are the cheap probes; `?model=<name>` probes one model.
STATE (verified by the overseer on origin/main c323c0a; the plan was written against 9943faf, so LOCATE EVERY SITE BY CONTENT, not by the line numbers below, and report the real ones). PR #105 / 7c8125b already de-bared e2e suites 01/04/05/12/39 and added the lint guard - do not redo that. Remaining gaps:
 (1) .github/workflows/deploy-mcp-tools.yml, CT202 job "Health checks" (~line 902): still curls the PUBLIC bare https://litellm-gateway-api.cognizioware.com/health with --max-time 30. It essentially always times out, increments PUBLIC_FAIL and burns backend quota on every prod deploy; it only emits ::warning:: so it survived unnoticed.
 (2) same step's loopback loop (~line 934): `http://127.0.0.1:$ep/health` includes port 4000 (LiteLLM) alongside genuinely cheap MCP ports (4180, 3006, 3100, 3102-3104, 8020, 3111) with a 5 s budget - guaranteed 000 for 4000.
 (3) infrastructure/deploy-mcp-tools.ps1 (~line 551): manual deploy script hits :4000/health with -TimeoutSec 10.
 (4) e2e/suites/00-lint-no-bare-litellm-health.test.js: SUITES_DIR = path.resolve(__dirname) so the guard scans ONLY e2e/suites/*.test.js and can never catch (1)-(3).
 (5) FOUND BY THE OVERSEER, not in the plan: e2e/run-all.sh (~line 115) does `curl "${BACKEND_URL}/health"` with the master key as its pre-flight check, and BACKEND_URL resolves to LITELLM_BASE_URL (run-all.sh ~line 31) or http://APP_HOST:APP_PORT (~line 43) - i.e. LiteLLM. This runs at the START OF EVERY e2e run, so every lane's E2E Gate triggers a full sweep. Fix it the same way (liveliness, then readiness for the auth/db assertion) and keep the failure message pointing at the same host.
SLICES (one commit each, explicit paths):
 (1) workflow: in the CT202 "Health checks" step replace the public bare /health with the SAME two-stage probe this workflow already uses in the UAT job (the "Polling .../health/liveliness" + "/health/readiness (db + version...)" steps) and in the CT202 config-change path - copy that shape verbatim, do not invent a new one: liveliness (cheap, keep --max-time 30), then readiness asserting "db":"connected" with the auth header. When the deploy changed the model list, additionally GET /v1/models and assert the added models are listed; reuse the existing LITELLM_CONFIG_CHANGED / git-diff-on-infrastructure/litellm-config.yaml output that 7c8125b added rather than recomputing it. In the loopback loop, take port 4000 OUT of the generic /health list and probe it separately as 127.0.0.1:4000/health/liveliness; leave the other ports on plain /health. KEEP `PUBLIC_FAIL > 0` as ::warning:: in this change (single-variable edit); add a TODO comment that hard-failing is a separate decision once the probes have been green for a few deploys. Commit: ci(lane): cheap LiteLLM probes in the CT202 health checks.
 (2) infrastructure/deploy-mcp-tools.ps1: :4000/health -> :4000/health/liveliness, keep -TimeoutSec 10; leave the non-LiteLLM ports (4180/3100/3102/3103/3104) alone. Commit: chore(deploy): liveliness probe in the manual deploy script.
 (3) e2e/run-all.sh: pre-flight uses /health/liveliness (no auth) then /health/readiness (master key, assert db connected); same failure text and exit behaviour. Commit: chore(e2e): cheap pre-flight probe in run-all.
 (4) e2e/suites/00-lint-no-bare-litellm-health.test.js: walk the REPO ROOT (path.resolve(__dirname,'../..')) over .yml/.yaml/.ps1/.sh/.js, excluding node_modules, .git and graphify-out; extend BARE_LITELLM_HEALTH_RE with an unquoted `:4000/health` variant (the current regex needs a closing quote right after /health so it misses the shell form `http://127.0.0.1:$ep/health`); keep EXCLUDED_RE and add an exclusion for the guard file itself and its own doc comment. Update the header comment to say the guard now covers workflow YAML and infra scripts as well as suites - wording only, keep the existing explanation. Commit: test(e2e): lint guard covers workflows and scripts.
 (5) CHANGELOG + a short note in the e2e README table row for suite 00. Commit: docs(e2e): guard scope.
VERIFICATION (report all of it): (a) prove the widened guard catches the PRE-FIX state - stash the slice 1-3 edits, run the guard, it MUST fail naming the workflow, the ps1 and run-all.sh sites, then unstash and it MUST pass (without this the guard is unproven); (b) run the guard plus suite 01 locally green; (c) `git diff --stat origin/main..HEAD`; (d) list every remaining bare-LiteLLM-/health hit in the repo after the change, with file:line, and say why each is legitimate.
HARD RULES: touching .github/workflows/ is REQUIRED here and confined to that one step plus the loopback list - change nothing else in the workflow; do not change the pinned image digest, docker-compose.yml or litellm-config.yaml; no secrets; explicit-path commits; NO push - and this one is NOT the retired blanket rule: this task EDITS `.github/workflows/`, and
the run's scoped GH_TOKEN REFUSES workflow-file pushes by permission. So the overseer pushes, opens the PR and drives the lane: E2E Gate -> UAT CT204 -> UAT QA gate must be green before the CT202 job, no hand deploy). Out of scope: non-LiteLLM /health endpoints (oauth2-proxy, fleet-admin, ms365, kb, compose healthchecks) and suite 13's model-inference gate, which is supposed to sweep.
Auditor: no git fetch; verify the pre-fix-catch evidence exists, that port 4000 is no longer in the generic loopback list, that the UAT probe shape was copied rather than reinvented, and that no non-LiteLLM /health call was changed.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
