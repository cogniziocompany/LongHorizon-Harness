BUILD + TEST (code; the lane deploys UAT then prod): make the LiteLLM "orphan team_id" outage impossible to repeat silently. INCIDENT (2026-09-07, mitigated by the overseer live): after the 1.100.0 upgrade, virtual keys whose team_id has no LiteLLM_TeamTable row 404 on every call. On CT202 55/266 keys were dead: 51 are LiteLLM UI login sessions (team_id litellm-dashboard, one-day expiry, default_user_id - 1.100.0 now RESERVES that team_id and refuses /team/new for it; they are not tenant keys and most were already expired) and 4 were real tenant keys (teams librechat=3 incl. every LibreChat key, advanced-paste-desktop=1). Fix applied on CT202 AND CT204: POST /team/new for librechat and advanced-paste-desktop (team_id = the literal name, metadata restored_by=overseer). A probe key in team librechat answers 200 on /v1/models on both. Root cause to confirm: 1.100.0 enforces team existence at auth time (pre-1.100 tolerated a dangling team_id); the two teams were never created because those keys were minted with team_id set by hand.
Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch feat/litellm-team-integrity (run `git fetch origin && git checkout -B feat/litellm-team-integrity origin/main` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no docker here; NO live calls to CT202/CT204. Secrets are env NAMES only (LITELLM_MASTER_KEY, POSTGRES_USER). docker-compose.yml is CRLF - do not touch it. Do NOT change the pinned image digest or any .github workflow.
READ FIRST: e2e/suites/01-litellm-health.test.js (soft-fail pattern), e2e/run-all.sh, e2e/package.json, infrastructure/scripts/ (safe-recreate.sh style), design docs/mcp-placeholder-services-audit-and-plan.md (S11/S12 template), docs on the 1.100.0 upgrade if present, CLAUDE.md.
SLICES (one per round; code + tests + explicit-path commit each):
 (1) infrastructure/scripts/litellm-team-audit.sh: READ-ONLY; runs on the box (pct exec), lists keys whose team_id has no team row grouped by team_id, EXCLUDING litellm-dashboard (report it separately as "ui sessions: N, expired: M"); exit 2 when tenant orphans exist. Companion litellm-team-restore.sh: for each orphan team_id (never litellm-dashboard, never empty string - refuse and exit 1 on empty/whitespace ids) POST /team/new with team_id=team_alias=<name> via the master key from mcp-tools.env, idempotent (skip when /team/info returns 200), prints a probe result (temporary 10-minute key in the team -> GET /v1/models -> delete key). Shell-tested with a fake curl/psql under tests/. Commit: feat(scripts): litellm team integrity audit + restore.
 (2) e2e/suites/39-litellm-team-integrity.test.js: auto-discovered; soft-fail off-LAN; with the master key (env LITELLM_MASTER_KEY, soft-skip when unset) call GET /key/list?return_full_object=true (paginate) and GET /team/list; assert every non-null, non-litellm-dashboard team_id on a key exists in the team list; RUN_LIVE_REGRESSION=1 escalates to hard fail. Add "test:teams" to e2e/package.json. Commit: test(e2e): virtual-key team integrity gate.
 (3) UI session hygiene: infrastructure/scripts/litellm-ui-session-cleanup.sh deletes EXPIRED keys with team_id=litellm-dashboard via POST /key/delete (never unexpired ones, never other teams), dry-run by default, --apply to execute; document a weekly cron line (names only) for the operator. Tests with fake curl. Commit: feat(scripts): expired UI session cleanup.
 (4) docs: S13 in design docs/mcp-placeholder-services-audit-and-plan.md following the S11 template: incident summary, why 1.100.0 changed behaviour (cite the LiteLLM source/changelog line if reachable, else mark UNVERIFIED), the upgrade runbook gets a PRE-UPGRADE step "run litellm-team-audit.sh on the target box; restore before pulling the new image", the post-upgrade verify step, rollback (teams are additive; /team/delete reverses). CLAUDE.md upgrade line references the audit. Commit: docs(litellm): team integrity runbook + upgrade gate.
HARD RULES: no secrets; no thresholds lowered; scripts never write with an empty or reserved team_id; explicit-path commits; no push. Completion = commit hashes, `git diff --stat origin/main..HEAD`, test summary, the exact operator commands (pct exec 204 then 202) and what is UNVERIFIED. Auditor: no git fetch; verify the restore script refuses empty/reserved ids, the e2e gate excludes litellm-dashboard, cleanup touches only expired dashboard keys, and no workflow/compose/image changes.
