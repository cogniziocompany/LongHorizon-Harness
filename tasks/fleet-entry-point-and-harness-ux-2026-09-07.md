# Fleet entry point (chat.easybutt0n → lh-harness queue), Hydra links, harness UX speed — 2026-09-07

**Paxton (15:45 PT):** no cutover of the overseer to the Hydra fleet view has happened (correct). Wants: (a) a reusable e2e happy path for "the path of one change" starting at task queuing; (b) queuing reachable from the infra (LiteLLM gateway or API - decide); (c) chat.easybutt0n.ai configured with the correct lh-harness tool + default instructions; (d) multiple harness runs, web UX reachable any time, and the web UX is slow when switching sessions - speed it up; (e) Hydra links to the harness UI dynamically.
**Decision:** the queue moves INTO the harness service (`POST/GET /api/queue`, server-side launcher with the capacity rules) and is exposed as MCP tools through the LiteLLM gateway (alias `lhharness`), so chat, Hydra and curl all use one entry point; the PC-side C:\tmp\queue launcher is retired once deployed. Web UX: cached/ETag snapshot, summary-first fetch, gzip, bench target < 300 ms on switch. Hydra: `ui_base_url` per harness node in the fleet registry → deep links + queue panel + enqueue form.
**Harness tasks queued:** 12-harness-queue-api-ux-speed (LongHorizon-Harness, 10 rounds; HARD: workspace .venv-dev only, never the service venv), 13a-fleet-chat-lhharness-tool (ptait09-easybutt0n-ai openwebui config + default knowledge + docs + e2e), 13b-hydra-fleet-harness-links (cognizioware-hydra-rsi feat/harness-fleet). Task texts C:\tmp\harness-queue-api-task.txt, chat-lhharness-tool-task.txt, hydra-harness-link-task.txt.
**Deploy order:** harness release (c7a8ccf + task 12) → CT110 at idle window → gateway alias/key for `lhharness` (ops step, env names only) → chat redeploy via runner-v2 lane → Hydra deploy on corsairai300 → run e2e/happy_path against CT110 → cut the overseer's PC queue over.
