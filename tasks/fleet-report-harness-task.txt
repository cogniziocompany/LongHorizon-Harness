BUILD + TEST (code; the overseer releases to CT110 via the idle-window auto-deploy, never a forced restart): the harness reporter of the "Harness Fleet Window" plan (planning session c84a6035; full plan in tasks/harness-fleet-window-2026-09-07.md - READ IT FIRST). Every lh-harness node pushes status, events, and round CONTENT (artifacts + role trajectories including thinking) to fleet.easybutt0n.ai so NAT'd nodes are fully visible with no inbound path. Work ONLY in /home/harness/work/LongHorizon-Harness on branch feat/fleet-reporter (run `git fetch origin && git checkout -B feat/fleet-reporter origin/main` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live calls; no new runtime dependency (stdlib urllib.request only); config TOML is allow-listed (config.py:177) so use env vars.
READ FIRST: src/lh_harness/manager.py (_append_event ~:2400, managed_round_recorded ~:2345, _final_report), src/lh_harness/webapi/events.py (EVENT_TYPE_MAP), webapi/models.py (EventEnvelope), webapi/snapshot.py (build_run_summary), supervisor/control_bus.py (write_status ~:690), dashboard/gate.py (L148/L172), utils/run_boundary.py (safe_run_rounds/safe_run_role), webapi/server.py create_app, docker/compose.node.yml + docker/entrypoint.sh, docs/, the fleet-admin ingest contract (POST /harness/heartbeat, /harness/events, /harness/rounds; HMAC-SHA256 over the raw body with headers X-Fleet-Host + X-Fleet-Signature, same as /checkin) which you may READ at /home/harness/work/ptait09-easybutt0n-ai/fleet-admin/src/server.js - never write there.
SLICES (one per round; code + tests + explicit-path commit each):
 (1) src/lh_harness/fleet/reporter.py: enabled only when LH_HARNESS_FLEET_URL is set; identity LH_HARNESS_FLEET_NODE (default socket.gethostname()), key LH_HARNESS_FLEET_KEY (per-host device_key), optional LH_HARNESS_FLEET_LABELS (kind=ct110|cloud|local,repo=...); bounded queue + ONE daemon thread; urllib.request POST with HMAC-SHA256 over raw body; batches events every 2 s; bounded retry then drop; never blocks a run; WARN once on persistent failure; gzip bodies. Unit tests with a local HTTP stub: HMAC correctness, batching, non-blocking on failure, disabled when URL unset. Commit: feat(fleet): reporter core.
 (2) hooks: wrap _append_event to send the public-form EventEnvelope (run_id, type, ts, round, role, status, payload trimmed to summary fields, NO transcripts); ControlBus.write_status -> run.status event; gates already flow as approval_created/resolved; heartbeat every 30 s {node, version, runs: [build_run_summary(...)], capacity {active, cap}, queue_len}. Tests: event mapping fixture, heartbeat shape. Commit: feat(fleet): event + heartbeat hooks.
 (3) round content push: on managed_round_recorded read logs/role_orchestration/rounds/<n>/ via safe_run_rounds/safe_run_role and POST /harness/rounds with every artifact file and every role trajectory (the same files the /rounds/{n}/artifacts and /trajectory/{role} routes serve, thinking blocks included as stored), gzip, cap 8 MB per round; larger files truncated with {truncated: true, bytes: N} never dropped silently; logs/report.json pushed at run end. Optional additive youtrack_issue_id accepted in POST /api/runs body, stored in control/status.json meta and included in summaries. Tests: artifact walk stays inside the run dir (path traversal test), truncation marker, report push. Commit: feat(fleet): round content + report push.
 (4) wiring + docs: create_app() starts the reporter when configured; docker/compose.node.yml + docker/entrypoint.sh pass LH_HARNESS_FLEET_URL/NODE/KEY/LABELS; docs/fleet-reporting.md (env names, what is pushed, size caps, privacy note: trajectories include thinking, so the fleet read key gates them) + README env table; CHANGELOG. Full suite `pytest -q` on Linux green (report the count). Commit: docs(fleet): reporting.
HARD RULES: never block or fail a run; no new dependencies; no writes inside any workspace; secrets names only; explicit-path commits; no push. Completion = commits, diff stat, test count, the exact env lines the overseer adds to /home/harness/.lh-harness-secrets.env on CT110 (names only), and confirmation that behaviour is byte-identical when LH_HARNESS_FLEET_URL is unset. Auditor: no git fetch; verify the daemon thread cannot raise into the run, HMAC test, traversal test, and unset-URL no-op.
