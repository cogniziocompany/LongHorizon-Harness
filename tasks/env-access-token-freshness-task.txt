BUILD + TEST (code; cognizioware-powerplatform): TOP PRIORITY PRODUCTION BUG. Real users - guest or member - cannot create a session at https://powerplatform.easybutt0n.ai. Two INDEPENDENT defects, both blocking. Planning session 2fe04eea-ccf5-44ea-919a-fd4664225a8a; the plan file is on the overseer PC and you cannot read it, so everything you need is here.

BRANCH: work ONLY in /home/harness/work/cognizioware-powerplatform on `fix/env-access-and-token-refresh` cut from **origin/develop** (run `git fetch origin && git checkout -B fix/env-access-and-token-refresh origin/develop`). ⚠️ NOT `ci/env-promotion` - that branch has NONE of this code. Plain git; commit by EXPLICIT PATH; no push; NO live calls to Microsoft, Dataverse or Stripe in tests (stub `globalThis.fetch`); env NAMES only.
REPRO: `/environments/4d4ffe7f-1a69-40a6-90d6-a93069f070fb/sessions` (Cogniziodemo, demo/eval), reporter signed in with an `admin` role. Backend = packages/backend (Express + Postgres, node:test via `node --import tsx --test`). Frontend = packages/frontend (React + Vite + @azure/msal-browser). Migrations dir highest is `040_*` - **NO MIGRATION IS NEEDED** for this work; do not add one.

DO PART 1 FIRST. Nothing in Part 2 is reachable while the token is dead. If rounds run short, LANDING PART 1 ALONE IS A VALID OUTCOME - say so in the report rather than half-landing both.

## PART 1 - TOKEN FRESHNESS AND HONEST AUTH ERRORS

DEFECT 1, "Valid MSAL token required". `MsalProvider.tsx` acquires the Dataverse token ONCE at mount via `acquireTokenSilent` and parks it in React state; ~60 fetch call sites read that single `token` from context and send it raw (e.g. `sessions/SessionList.tsx:173`). Nothing re-acquires it and no call site retries on 401. A Dataverse token lives ~60-90 minutes, so a tab left open dies and the create POST 401s - page load worked because the token was still alive then.
The backend then reports it as badly as possible: `verifyAuth` NEVER checks `exp` (there is no `exp` handling anywhere in auth.service.ts; `decodeJwtPayload` validates neither signature nor expiry by design); the dead token is sent to Dataverse by `fetchDataversePermissions`, which 401s; that function swallows EVERY failure into `{ roles: [] }`; empty roles means `hasCustomizer === false` giving `reason: 'insufficient_permissions'`; and `requireAuth` (middleware.ts:13-28) collapses every `!result.verified` into one generic 401 "Valid MSAL token required". So an expired token, a Dataverse outage, a network blip and a genuinely under-privileged user are INDISTINGUISHABLE. That indistinguishability is itself a defect to fix, not just the expiry.

 (1a) FRONTEND - `packages/frontend/src/auth/MsalProvider.tsx`. Add `getAccessToken(): Promise<string>` to the auth context that calls `acquireTokenSilent({ scopes: tokenScopes, account })` on EVERY call - MSAL serves from its own cache and silently refreshes near expiry; this is the canonical MSAL SPA pattern. On `InteractionRequiredAuthError` surface the existing `verifyError` affordance, NOT a surprise popup. Add `apiFetch(input, init)` to the same context: injects a freshly-acquired Authorization header, and on a 401 re-acquires ONCE and retries EXACTLY ONCE before giving up. Keep `token` in state for render-gating (`if (!token) return;`) but it stops being the value that is sent.
 Migrate call sites to `apiFetch`, hot paths first: `sessions/SessionList.tsx` (8 sites), `sessions/wizard/SessionWizard.tsx`, `workspace/WorkspaceLayout.tsx` (10 sites), `hooks/useSession.ts`, `environments/EnvironmentList.tsx`, `sessions/SessionDetail.tsx`, then the rest. FINISH THE MIGRATION - any call site left on the stale `token` keeps 401ing after an hour. Landing `apiFetch` plus the create-session path first makes the repro pass. Commit: fix(auth): acquire a fresh token per request and retry once on 401.
 (1b) BACKEND - `packages/backend/src/services/auth.service.ts`. In `verifyAuth`, check `tokenPayload.exp` with ~60s skew BEFORE any Dataverse call and return `reason: 'token_expired'` - a Dataverse round-trip with a dead token is pure latency. Change `fetchDataversePermissions` to return a TRI-STATE `{ status: 'found' | 'not_found' | 'unavailable', roles, email }` instead of collapsing every failure to `{ roles: [] }`; map an upstream 401 to `token_rejected` and 5xx/network/abort to `unavailable`. `verifyAuth` returns distinct reasons: `token_expired`, `token_rejected`, `dataverse_unavailable`, `insufficient_permissions`, `no_billing_match`.
 `packages/backend/src/routes/middleware.ts` - `requireAuth` maps reason to code/message instead of one blanket string:
   token_expired | token_rejected -> 401 TOKEN_EXPIRED "Your sign-in has expired. Reload the page to continue."
   dataverse_unavailable          -> 503 DATAVERSE_UNAVAILABLE "Could not reach Dataverse to verify your access. Try again shortly."
   insufficient_permissions       -> 403 INSUFFICIENT_PERMISSIONS (existing message + partner referral URL)
   anything else                  -> 401 UNAUTHORIZED (existing)
 The 503 matters: today an outage reads as "you lack permissions", which sends people to the wrong fix. Commit: fix(auth): distinguish expired, rejected, unavailable and under-privileged.

## PART 2 - ENVIRONMENT ACCESS LADDER

DEFECT 2, "You do not have guest access to the selected environment". `POST /api/sessions` calls `envService.assertActiveGrant()` - a PURE POSTGRES lookup in the internal `environment_grants` table, no Dataverse, no BAP, no Graph. No admin-issued row means 403 `ENV_NOT_GRANTED`. But the dropdown comes from `listEnvironments()` filtered `WHERE user_email = $1`, and those rows were written by THE USER'S OWN delegated Global Discovery call. So the app offers an environment the user demonstrably has access to, then refuses it because a separate internal allowlist has no row. The word "guest" describes our grant table, not Entra B2B, which is why the message misleads. A third path contradicts both: `POST /api/environments/:id/sessions/new` uses the OLD `WHERE id = $1 AND user_email = $2` ownership check and runs NO grant check - same product action, two different rules.
WHAT MICROSOFT ACTUALLY SAYS: there is no separate API surface for guests. Once `restrictGuestUserAccess = false`, a B2B guest is an ordinary enabled `systemuser`, indistinguishable from a member at the API layer. Global Discovery with the user's delegated token returns only instances the caller can access, and `WhoAmI` against a specific environment with the user's delegated token is the canonical per-environment proof. STOP MODELLING "guest vs regular" AND CHECK REAL PLATFORM ACCESS. Keep admin grants as an ADDITIONAL path for eval/demo users.

 (2a) `packages/backend/src/services/environment.service.ts` - add `assertEnvironmentAccess(input)` returning `{ ok, path?: 'owner'|'grant'|'live_whoami'|'service_principal', status?, code?, message?, detail? }`. Rungs, deny only if all fail:
   0. env row exists; the `is_production` block STAYS
   1. OWNER - `environments.user_email` matches caller (that row was written by their own GDS call, so Microsoft already verified access) - same query
   2. ACTIVE GRANT - existing `hasActiveGrant`/`assertActiveGrant`
   3. LIVE DELEGATED WhoAmI, cached ~15 min - catches revoked access and admits real access with no local row
 Keep `hasActiveGrant`/`assertActiveGrant` exported and unchanged as internal rungs so existing unit tests keep passing. Add a no-network `hasEnvironmentAccessRecord(envId, email)` = owner OR active grant. Fail CLOSED on probe errors - rung 3 is only reached after 1 and 2 already failed, so this is never worse than today.
 TWO MANDATORY GUARDS, both are security holes if missed: **impersonation** - skip the probe when `principalEmail !== tokenOwnerEmail`, or `resolveScopedEmail` admin impersonation probes with the ADMIN's token and answers the wrong question; **service principal** - allow explicitly at rung 2 and audit it, but NEVER let an SP reach the probe, because an app-only token passes `WhoAmI` unconditionally and that would silently mean "holds an SP token, may target any environment". Requires adding `isServicePrincipal?: boolean` to `UserContext` (today only a local variable in `verifyAuth`; `dataverseUrl?: string` already exists at line 12).
 Log `env_access_granted` (with `path`) and `env_access_denied` through the existing `logAuditEvent`. Commit: feat(env): access ladder - owner, grant, live WhoAmI.
 (2b) `packages/backend/src/services/delegated-access.service.ts` (NEW) - the cached probe. Deliberately separate from `dataverse-token.service.ts`, which mints SERVICE PRINCIPAL tokens; put a doc comment on its `whoAmI()` warning it is never an authorization check for a user, because an SP probe always succeeds and that mistake would silently reopen the hole. `GET {envUrl}/api/data/v9.2/WhoAmI` with the USER's token and `AbortSignal.timeout(5_000)`. In-memory Map keyed `${normalizedEnvUrl}|${email.toLowerCase()}`, mirroring the existing cache in dataverse-token.service.ts:19 - no DB table, no migration. `res.ok` = allowed (TTL 15 min); 401/403 = denied (TTL 1 min so a newly assigned role recovers fast); 5xx/network/abort = indeterminate, NOT cached, treated as denied. Export `invalidateDelegatedAccess(envUrl, email?)` and call it from `DELETE /:id/grants/:grantId`. Commit: feat(env): cached delegated WhoAmI probe.
 THE KEY CONSTRAINT - the backend CANNOT do on-behalf-of: the bearer's `aud` is a Dataverse resource, not our own API, and auth.service.ts accepts it unsigned precisely because trust rests on the Dataverse round-trip. So a delegated WhoAmI only works when the presented token's audience matches the target environment. **PHASE 1, ship this: attempt rung 3 ONLY when `req.user.dataverseUrl` (already populated by `resolveTokenResource`) matches the target env URL. ZERO frontend changes.** That covers the pinned-single-env case, which is the common one. Phase 2 (do NOT build here) would send an env-scoped token as `X-Env-Token` via `getTokenForEnv()`, which already exists at MsalProvider.tsx:305 and has no callers today.
 (2c) `packages/backend/src/routes/env-access.ts` (NEW) - a thin `requireEnvAccess(req, res, envId)` helper so the two session-creation routes can never drift apart again. Then: `session.routes.ts` (~66-82) keep env-exists + `is_production`, swap the `assertActiveGrant` block for the helper. `environment.routes.ts`: `POST /:id/sessions/new` (~186) drop `AND user_email = $2`, keep the prod throw, use the helper; `GET /:id/solutions` (~212) the same, otherwise grant holders and probe-passers pass session creation then break one wizard step later. `PATCH /:id`, `POST /:id/audit`, `POST /:id/refresh` STAY owner-only - they mutate the discovered row. `env-resolution.service.ts` (~66-95): two lines - add `e.user_email AS env_user_email` to the SELECT and allow when the caller owns the env before falling back to `hasActiveGrant`; NO probe here (no user token in scope, hot path); leave the no-`principalEmail` n8n branch and its `getDefaultEnv()` fallback untouched. Commit: fix(env): one access rule for every session-creation path.
 (2d) ERROR CODES - drop the word "guest": `ENV_NOT_GRANTED` becomes `ENV_ACCESS_DENIED` (403) "We could not confirm your access to this Power Platform environment. Sign in against this environment, or ask an administrator to grant access."; `ENV_GRANT_REVOKED` becomes `ENV_ACCESS_REVOKED` (403); `ENV_GRANT_EXPIRED` becomes `ENV_ACCESS_EXPIRED` (403); add `ENV_NOT_FOUND` (404).
 (2e) FRONTEND `sessions/SessionList.tsx` - `createSession`'s catch (~178) funnels `err.message` into `createError`, rendered at ~440. Also capture `err.code`, and when it is `ENV_ACCESS_DENIED` render a remediation button under the error that calls `setUserEnvUrl(targetEnv.environment_url)` - already destructured at ~98, and it re-acquires the token with `${envUrl}/.default` so the retry's `aud` matches and rung 3 succeeds. Pure reuse, no new machinery. Commit: feat(ui): offer sign-in against the target environment on access denial.

## PART 3 - SCREEN TRANSITIONS BETWEEN STEPS

Moving between steps (Environments -> env pick -> Session setup -> Design) currently hard-jumps with no feedback, so any slow step reads as a dead click and the user re-clicks. Apply the standard treatment; nothing exotic, and REUSE `packages/frontend/src/ui/ProcessStrip.tsx` (it exists and has tests at `ui/__tests__/ProcessStrip.test.tsx`) rather than building a new stepper.
 (3a) PERSISTENT STEPPER - ProcessStrip stays mounted ACROSS the transition and never unmounts/remounts between steps; it marks done / current / upcoming. It is the continuity anchor that tells the user the jump was intentional. Routes live in `App.tsx`; keep the strip above the swapping outlet, not inside it.
 (3b) IMMEDIATE FEEDBACK ON THE TRIGGER - the control that causes the navigation (Create Session, and the env row's Open) goes disabled + busy the instant it is clicked, with `aria-busy="true"` and its label switched to a pending form ("Creating..."). A click must NEVER look ignored. This also removes the double-submit that currently creates duplicate sessions.
 (3c) DESTINATION PENDING STATE - the incoming screen renders a skeleton matching its real layout (not a centered spinner, which causes a layout jump when content lands) while its data loads. No blank white flash between screens; keep the previous screen painted until the next is ready to render.
 (3d) MOTION - a short cross-fade or slide, 150-250ms, ease-out. MUST be wrapped in `@media (prefers-reduced-motion: reduce)` to a plain instant swap. Do not animate anything longer than 300ms; this is orientation, not decoration.
 (3e) FOCUS AND SCROLL - on step change, reset scroll to top and move focus to the new step's `<h1>`/heading (give it `tabIndex={-1}`), so keyboard and screen-reader users are not stranded at the old position. WCAG 2.4.3 focus order and 3.2.3 consistent navigation.
 (3f) REAL ROUTES - every step must be a real route with working browser Back/Forward and a shareable URL; do not move between steps by hidden local state. Back out of Session setup must land on the env list, not the app root.
 Commit: feat(ui): step transitions - persistent stepper, pending states, reduced-motion safe.
 TESTS (vitest + testing-library, `packages/frontend`): clicking Create Session immediately sets the button disabled with `aria-busy="true"`; a second click while pending issues NO second POST; the destination renders its skeleton while the fetch is unresolved and never an empty container; focus lands on the destination heading after the step change; with `prefers-reduced-motion: reduce` matched, no transition class/duration is applied.

## RISKS YOU MUST HANDLE OR REPORT
1. The seed route `POST /api/environments/seed` (webhook-secret auth) BECOMES ACCESS-GRANTING once rung 1 exists, since writing an `environments` row satisfies owner. Anyone holding `N8N_WEBHOOK_SECRET` could mint env access for an arbitrary email. RECOMMENDED: make the seed route also insert an `environment_grants` row so access stays revocable through the admin API. Do it, or say clearly why not.
2. `verifyAuth` still requires System Customizer or System Administrator (auth.service.ts:165-178), so a guest with a lower-privilege role is rejected at sign-in BEFORE any of Part 2 runs. That is the next wall a real guest hits and it is OUT OF SCOPE here - note it in the report as a follow-up.
3. Update `design/plans/guest-launch/E2E-READINESS-AUDIT-2026-09-07.md:55-56`, which records "fail closed on grant" as the intended posture, or the next audit flags this as a regression.
4. `discoverEnvironments` upserts on `(user_email, environment_url)`, so two users targeting the same org get two distinct `environments.id` and a grant on one row is meaningless for the other. Unchanged, but the ownership rung makes it visible - mention it.

## TESTS
Part 1 backend - `packages/backend/tests/auth-failure-modes.test.ts` (new): expired `exp` gives `token_expired` AND NO DATAVERSE FETCH IS ISSUED; Dataverse 401 gives `token_rejected`; Dataverse 503/throw gives `dataverse_unavailable` NOT `insufficient_permissions` (that misdiagnosis is the bug being fixed); empty roles with a healthy 200 gives `insufficient_permissions`. Add `requireAuth` mapping tests for each status/code pair.
Part 1 frontend - vitest for `apiFetch`: a 401 triggers exactly one re-acquire and one retry; a second 401 propagates without looping.
Part 2 - `packages/backend/tests/environment-access.test.ts` (new). Reuse the FakePool/setDb preamble from `guest-slice-2.auth.test.ts:13-53`; stub `globalThis.fetch`. Owner match gives `path==='owner'` AND no `environment_grants` query AND fetch never called - THAT ASSERTION IS THE FIX; owner match case-insensitive; non-owner + grant gives `path==='grant'` with no fetch; missing env gives 404 `ENV_NOT_FOUND`; aud mismatch gives 403 `ENV_ACCESS_DENIED` with `detail==='probe_skipped_aud_mismatch'` and no fetch; aud match + 200 gives `path==='live_whoami'`; +403 denied; +503/throw denied AND NOT CACHED (a second call re-fetches); two identical allowed probes issue exactly ONE fetch; `principalEmail !== tokenOwnerEmail` skips the probe; `isServicePrincipal` gives `path==='service_principal'`.
Existing tests to update - `guest-slice-2.auth.test.ts` route assertions (~241/~260) to the new codes; add a `resolveSessionEnv` case where `env_user_email` matches and the grant query returns `[]` so it resolves.
Route tests - `POST /api/environments/:id/sessions/new` via the `findRouteHandlers`/`runRoute` helpers in `environment-grants.test.ts:87-105`: 201 owner, 201 grant holder (previously "Environment not found"), 403 unrelated user.
E2E - `e2e/suites/02-auth-guards.test.js`: non-owned env gives 403 `ENV_ACCESS_DENIED` and the string `ENV_NOT_GRANTED` must not appear. `e2e/suites/14-multitenant-isolation.test.js`: env owner creates a session with NO grant row present.
Report all counts, baseline first.

HARD RULES: branch from origin/develop, never ci/env-promotion; NO migration (nothing here needs one); no secrets or tenant ids in the repo - env NAMES only; do NOT weaken the `is_production` block; do NOT let a service principal reach the WhoAmI probe; do NOT build Phase 2 `X-Env-Token`; explicit-path commits; no push. Completion = commits, diff stat, all test counts, which Part landed, whether the seed route was hardened, and the exact user-facing text for every new error code. Auditor: read-only - verify the `is_production` block is intact, that the SP path can never reach the probe, that the impersonation guard exists, that indeterminate probe results are not cached, that no call site still sends the stale cached token on the create-session path, and that no migration was added.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

## RE-SCOPED 2026-09-10 05:35 - PARTS 1+2 ARE IN PR #102. START FROM THE WORKSPACE, NOT FROM SCRATCH.
**Parts 1 and 2 are DONE and pushed** as branch `fix/env-access-and-token-refresh`, **PR #102** against develop:
6 commits (`b6a264a`, `c9905ab`, `0a7e361`, `69164dd`, `6bf1b81`, `b39d1be`), 48 files +2947/-1199, merges cleanly.
**DO NOT REBUILD THEM.** Read `git log origin/develop..HEAD` first.
**PART 3 IS IMPLEMENTED BUT UNCOMMITTED - THAT IS YOUR STARTING POINT.** The previous run left **17 modified or
untracked files** in `/home/harness/work/cognizioware-powerplatform-evalfix`. It reported these as verified:
 - `aria-busy="true"` pending feedback on the Open button (prevents double-navigate) and on Create Session
   (prevents double-submit);
 - destination pending state - incoming screens render skeletons matching the real layout while data loads;
 - the stepper (`ProcessStrip`) stays mounted above the swapping outlet in `GuardedLayout`;
 - focus and scroll reset on step change, focus moving to the new step heading with `tabIndex={-1}`;
 - cross-fade animation guarded by `@media (prefers-reduced-motion: reduce)`;
 - two de-duplications in `GuardedLayout.tsx` and `GuardedLayout.test.tsx`.
**ASSESS these files, do not rewrite them and do not blindly commit them.** For each: read the diff, confirm it
does what the list above claims, then commit by EXPLICIT PATH with the reason. Anything you cannot trace to Part 3
is contamination - discard it and say so. **Never commit `packages/frontend/node_modules/` or `dist/`.**
**THE ONE THING THE PREVIOUS RUN COULD NOT DO:** durable vitest coverage for the Open/Create pending-feedback
behaviour. That is now the priority deliverable - the implementation exists, the proof does not.
**MERGE `origin/develop` BEFORE COMPLETION and report the result.** The branch is behind by 7 and develop has moved
TWICE tonight: `94df0523` (nav flyout - `ui/navRoutes.ts`, `ui/index.ts`, `ui/tokens.ts`, and a declarative
`appRoutes` table in `App.tsx`) and `d7a3b8e4` (signed-out checkout, which changed the guard filter in that same
table to `role === 'auth' || role === 'return'`). Both are LIVE and VERIFIED on pp dev. **Do not revert either.**
`App.tsx`, `Shell.tsx` and `ui/index.ts` are touched by both develop and your uncommitted work, so resolve those
deliberately and say how.
**CONTEXT ON WHY THE RUN STOPPED:** Synthetic hit its subscription rate limit at 05:29 and every model returned
429. That is a provider budget exhaustion, not a defect in the work, and it may still be in force when you start -
if your Synthetic-backed roles 429, say so plainly rather than working around it.
