BUILD + TEST (code; mcp-cognizioware): PR #91 ("feat(billing): guest kill switches move to the admin UI") maps a NEW table but ships NO EF migration, so it cannot be merged as it stands. Add the missing migration on top of that PR's branch. Work ONLY in /home/harness/work/mcp-cognizioware-b on branch feat/guest-switch-admin-ux (run `git fetch origin && git checkout -B feat/guest-switch-admin-ux origin/feat/guest-switch-admin-ux` first - this is PR #91's OWN branch, do NOT branch from develop and do NOT rewrite its existing commits); plain git; commit by EXPLICIT PATH; no push; env NAMES only.

THE DEFECT, precisely: src/McpCognizioware.BillingService/Data/ApplicationDbContext.cs now declares `DbSet<GuestSwitchAudit> GuestSwitchAudits` and configures `entity.ToTable("guest_switch_audit")` with a key on Id, indexes on ChangedAtUtc, Actor and SwitchKey, and max lengths 255/255/1000 on Actor/SwitchKey/Reason. There is NO file under src/McpCognizioware.BillingService/Migrations/ for it and ApplicationDbContextModelSnapshot.cs was NOT regenerated. src/McpCognizioware.BillingService/Program.cs runs `dbContext.Database.Migrate()` at startup (around line 642), which applies MIGRATIONS ONLY - it will not create a table that has no migration. So on the dev lane the service starts clean and then the FIRST kill-switch change returns a 500 against a missing relation. The most recent existing migration is 20260908062000_AddReleaseReadinessAudit - copy its shape, provider and conventions exactly.

WHAT TO DO:
 1. Generate the migration with the EF tooling that matches the project (the project targets net8.0; use an EF tools version matching the EF Core package version in McpCognizioware.BillingService.csproj - install the tool LOCALLY to the repo if needed, e.g. a tool manifest under .config/, and do NOT install anything globally on the host). The intended command is `dotnet ef migrations add AddGuestSwitchAudit --project src/McpCognizioware.BillingService --startup-project src/McpCognizioware.BillingService`. If the tool cannot resolve a design-time DbContext without a live database, use the existing design-time factory or a connection string from an env NAME - never a literal credential, and never point it at a real database.
 2. If and ONLY IF the tooling genuinely cannot run in this environment, hand-write the migration to match the existing ones byte-for-byte in style - the Up creating `guest_switch_audit` with the exact column names, types, nullability, key, three indexes and max lengths implied by the entity configuration and the GuestSwitchAudit model; a Down that drops the table; the .Designer.cs; and the corresponding block added to ApplicationDbContextModelSnapshot.cs. SAY CLEARLY IN THE REPORT which route you took, because a hand-written snapshot is the riskier one and the reviewer needs to know.
 3. Verify the migration is additive only: it must CREATE a new table and touch nothing else. Read the generated Up and confirm there is no DROP, no ALTER of an existing table, and no rename. If the generator emits anything beyond creating this one table, STOP and report it rather than committing - that means the snapshot was already out of sync with the model before your change, which is a separate and more serious problem.
 4. Run the hermetic test suite: `dotnet test src/McpCognizioware.BillingService.tests/McpCognizioware.BillingService.tests/McpCognizioware.BillingService.tests.csproj -c Release --filter "FullyQualifiedName!~DataIntegrity"`. The DataIntegrity tests dial a LAN database and can never pass here - that exclusion is expected and correct. Report the exact pass/fail/skip counts.
 5. Add a test asserting the model has no pending changes against the snapshot, if the project already has a pattern for that; if it does not, do NOT invent a fragile one - say so instead.

Commits (explicit paths, one per step): `feat(billing): add the guest switch audit migration` for the migration plus snapshot, and a separate `test(billing): ...` only if you added a test.

HARD RULES: do not modify the entity, the DbContext configuration, the controller, the service or the admin UI - the PR's own work is already reviewed and only the migration is missing; do not touch develop; do not run the DataIntegrity tests; no push; no live database connections; explicit-path commits. Completion = commits, diff stat, the test counts, which generation route you used, and the full text of the migration's Up method quoted in the report so a human can eyeball it without checking out the branch. Auditor: read-only - verify the migration is additive only and creates exactly one table, that the column names and lengths match the entity configuration, that ApplicationDbContextModelSnapshot.cs was updated consistently, that nothing outside Migrations/ changed, and that no credential or connection string literal appears anywhere in the diff.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
