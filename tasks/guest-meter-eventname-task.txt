BUILD + TEST (code; HIGHEST PRIORITY - guest access with the billing components is scheduled for this afternoon): the guest usage meter never receives events because the service sends Stripe a METER ID where Stripe expects an EVENT NAME. Work ONLY in /home/harness/work/mcp-cognizioware on branch fix/guest-meter-event-name (run `git fetch origin && git checkout -B fix/guest-meter-event-name origin/develop` first); plain git; commit by EXPLICIT PATH; no push; NO live Stripe calls (hermetic tests with a fake Stripe client only); env NAMES only, never key values.
PROVEN BY THE OVERSEER against the mcp-cognizioware-dev sandbox (acct_1TsqbjQVmqxwMkjo) on 2026-09-08, so treat it as fact: POST /v1/billing/meter_events with event_name=mtr_test_61VMDVxi0gJlSC4fT41QVmqxwMkjo6Rc returns invalid_request_error "No active meter found for event name ..."; the same call with event_name=cognizioware_guest_tokens and a customer that exists in that account is ACCEPTED and appears on the meter. The account's four active meters are cognizioware_guest_tokens, sms_segments, task_tokens and voice_seconds - the design is one meter per event name, and the guest one is the observation-only meter.
CODE PATH (verified on origin/develop): Controllers/UsageEventsController.cs RecordGuestMeterAsync(kind, quantity, eventId, customerId, ...) builds a GuestStripeMeterEvent with MeterId: _settings.GuestMeterId, and the `kind` argument ("task_tokens" | "voice_seconds" | "sms_segments") is then DISCARDED. Services/GuestStripeWriter.cs passes request.MeterId as the first argument of _stripeService.RecordBillingMeterEventAsync(...), and Services/StripeService.cs assigns that first argument to fields["event_name"]. So the meter id becomes the event name and Stripe rejects every guest observation.
CHANGES (one commit per slice, explicit paths):
 (1) Configuration/StripeSettings.cs: add `GuestMeterEventName` (default "cognizioware_guest_tokens"). GuestStripeWriter sends THAT as the event name, and keeps GuestMeterId for logging and validation only. Keep the existing observation-only guard exactly as it is (the price must be listed in GuestObservationOnlyPriceIds and the request must be marked observation-only). Commit: fix(billing): guest meter events use the meter event name.
 (2) carry the usage kind: RecordGuestMeterAsync's `kind` must reach Stripe as a payload key (for example payload["usage_kind"]="task_tokens") so the single observation meter can be broken down by kind; keep every existing payload key (stripe_customer_id, value, guest_event_id, guest_price_id, guest_product_id, observation_only). Commit: fix(billing): keep the usage kind on guest meter events.
 (3) make the misconfiguration impossible to repeat: at startup, if GuestMeterEventName is empty or looks like a Stripe object id (matches ^mtr_), log an error and fail the guest meter path fast with a message naming the setting; add the same check to whatever config validation already runs. Commit: fix(billing): reject a meter id used as an event name.
 (4) tests with a fake Stripe client asserting: the event name sent equals GuestMeterEventName; a meter-id-shaped value is rejected; the usage kind lands in the payload; the observation-only guard still refuses a chargeable price; and the existing non-billable-customer suppression is unchanged. Commit: test(billing): guest meter event name contract.
 (5) docs: the guest billing runbook/README section gains a short "meters" note - Stripe matches by event_name, the ids are for the dashboard only, and the four event names in use. Commit: docs(billing): meter event names.
CONTEXT THE OVERSEER ALREADY HANDLED, do not redo: the dev and uat lane env files now set Stripe__GuestMeterId=cognizioware_guest_tokens as a stopgap (backups .env.bak-guestmeter-*), so events flow before this lands; once this ships, the operator moves that value to Stripe__GuestMeterEventName and restores the real meter id in Stripe__GuestMeterId. Both guest switches (Billing__GuestChargingEnabled, Billing__GuestExecutionEnabled) are false in dev and uat and are flipped by the operator, not by this task.
FLAG IN THE COMPLETION REPORT, do not fix: the powerplatform dev lane's STRIPE_TEST_CUSTOMER_ID is cus_TPFW7YS21tEYUs, which does NOT exist in the dev billing Stripe account (that account's customers look like cus_Utwk...), so any guest usage pinned to it fails with "No such customer"; the ensure-customer endpoint should be the source of the customer id instead.
HARD RULES: no schema change; no live Stripe calls anywhere including tests; no secrets; explicit-path commits; no push; dotnet test green except the known offline DataIntegrity tests (report counts). Auditor: read-only - verify the event name sent is the configured event name and not the meter id, that the guard against a mtr_-shaped value exists, and that the observation-only refusal is untouched.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.

LIVE EVIDENCE FROM THE DEV LANE (overseer, 2026-09-08 15:35 UTC, image sha-32a365e, Billing__GuestExecutionEnabled=true, Billing__GuestChargingEnabled=false, Stripe__GuestMeterId stopgapped to cognizioware_guest_tokens): POST /api/v1/usage/events with {"customer_id":"cus_UtwkPxsTEwhMoK","channel":"guest","total_tokens":1000,...} returned success with no errors, the service POSTed to https://api.stripe.com/v1/billing/meter_events and Stripe answered 200 - but the usage landed on the TASK_TOKENS meter (aggregated 1000) and the guest observation meter did NOT move (it still shows only the 1.0 the overseer sent by hand). Critically, the log line "Recording observation-only guest meter event" from GuestStripeWriter never appeared, and neither did its "Guest meter event refused" warning, so the guest writer was never reached even though UsageEventsController calls RecordGuestMeterAsync("task_tokens", ...) unconditionally inside the taskTokens>0 branch. ADD TO THIS TASK: find out why the guest writer is bypassed on the dev image (candidates: the deployed image predates that call; the DI registration resolves a no-op IGuestStripeWriter when the guest settings are incomplete; or an exception is swallowed), fix it so a guest-channel usage event ALWAYS produces one observation event on the guest meter, and add a test that asserts the guest writer is invoked for a guest-channel event and that the event name it sends is the configured guest event name. Report which of the candidates it turned out to be.

CORRECTION AND FINAL EVIDENCE (overseer, 2026-09-08 15:45 UTC) - the "guest writer never runs" note above was MY error; ignore its three candidates. What actually happened: the dev lane had been silently downgraded to an image built from commit 44c4ccc (2026-07-14, 42 commits behind develop), which predates the guest meter code entirely, so of course nothing guest-related logged. The downgrade came from a plain `docker compose up -d --no-deps app` on the lane: deployments/tier/docker-compose.yml resolves APP_IMAGE_TAG from the lane .env, which still pinned the July tag, whereas rollout.sh passes the tag inline (APP_IMAGE_TAG=$TAG docker compose up -d) and never persists it. After pinning the current tag and recreating, a guest-channel usage event produced exactly the expected line - "Recording observation-only guest meter event: meter=cognizioware_guest_tokens price=price_1UCpYb... product=prod_VDG4R7... customer=cus_Utwk... quantity=2000" - and the guest observation meter went to 2001 while task_tokens stayed at 1000. So the guest path is CORRECT once the event name is right; the only code defect is the naming and the missing guard, which slices 1-5 above cover.
ADD ONE SLICE (6): make a stale image pin impossible on the dev and uat lanes the same way prod already does it - deployments/tier/docker-compose.yml must require the tag (${APP_IMAGE_TAG:?APP_IMAGE_TAG is required}) exactly as deployments/tier/docker-compose.prod.yml does, so a hand restart cannot silently roll a lane back to an old build; and either stop shipping APP_IMAGE_TAG in the lane .env or have rollout.sh write the deployed tag into it after a successful rollout, so the file and the running container agree. Add a test or a lane assertion that the running image tag matches the tag the lane just deployed. Commit: build(deploy): lane compose requires an explicit image tag.
CONTRACT NOTE for whoever writes the guest e2e: POST /api/v1/usage/events requires correlation_id, attempt_id, tenant_id, customer_id and a positive total_tokens (snake_case field names, Authorization header carries the Lindy webhook secret); principal_id, channel and source_event_id are what make it a guest-channel event.
