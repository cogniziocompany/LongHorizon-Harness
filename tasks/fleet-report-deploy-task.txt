BUILD + TEST (code; the lane deploys to CT202): the deploy + ingress side of the "Harness Fleet Window" plan (planning session c84a6035; full plan in /home/harness/work/LongHorizon-Harness/tasks/harness-fleet-window-2026-09-07.md - READ IT FIRST, never write there). Restore fleet.easybutt0n.ai as the fleet-admin report view (this SUPERSEDES the earlier idea of pointing the hostname at Hydra; Hydra's fleet MCP stays the control surface). Work ONLY in /home/harness/work/cognizioware-mcp-tools on branch fix/fleet-admin-redeploy-harness-report (run `git fetch origin && git checkout -B fix/fleet-admin-redeploy-harness-report origin/main` first); plain git; commit by EXPLICIT PATH; no push; no live calls to CT202; docker-compose.yml and Caddyfile are CRLF - intended lines only, verify with `git diff --stat`; do NOT change the LiteLLM image digest; secrets names only (FLEET_ADMIN_KEY, FLEET_READ_KEY, agent-db POSTGRES_* names, YOUTRACK_URL, YOUTRACK_TOKEN).
READ FIRST: infrastructure/docker-compose.yml (service layout, journald tags, healthchecks, env_file mcp-tools.env), infrastructure/Caddyfile (fleet.easybutt0n.ai block -> fleet-admin:8787), .github/workflows/deploy-mcp-tools.yml (build contexts tarred per infrastructure/docker/<name> ~L690-700; health checks ~L771; the hooks/*.py copy loop; never add a bare docker compose up), e2e/suites/29-*.test.js (bootstrap-grace soft-skip pattern), e2e/run-all.sh, CLAUDE.md. The fleet-admin source you vendor comes from /home/harness/work/ptait09-easybutt0n-ai/fleet-admin on branch feat/harness-fleet-report if present, else easybutt0n-runner-v2 (READ only).
SLICES (one per round; code + tests + explicit-path commit each):
 (1) vendor: infrastructure/docker/fleet-admin/ as a pinned copy of fleet-admin/ (src, schema.sql, web/, Dockerfile, package*.json; no node_modules) with SOURCE_REF.md recording repo, branch, commit; a script scripts/vendor-fleet-admin.sh that refreshes the copy from a given ref (used by future updates). Commit: build(fleet-admin): vendor pinned copy.
 (2) compose + Caddy: add/restore service fleet-admin (build.context ./docker/fleet-admin, expose 8787, env_file mcp-tools.env, journald tag fleet-admin, healthcheck wget -q -O /dev/null http://127.0.0.1:8787/health, restart unless-stopped, depends on nothing in-stack - agent-db is external at 192.168.21.154); Caddyfile keeps fleet.easybutt0n.ai -> fleet-admin:8787 with websocket passthrough; infrastructure/.env.example gains the env NAMES. Commit: build(compose): fleet-admin service + ingress.
 (3) lane + e2e: deploy-mcp-tools.yml builds the new context like the others and adds https://fleet.easybutt0n.ai/health to the health-check step (readiness-style, never a full sweep); e2e/suites/31-fleet-report.test.js: health, GET /api/fleet/overview shape, socket.io connect receives overview (socket.io-client dev dep), bootstrap-grace soft-skips; "test:fleet" in e2e/package.json. Commit: ci(fleet-admin): lane build + e2e.
 (4) docs: S14 in design docs/mcp-placeholder-services-audit-and-plan.md (operator sequence: backup docker-compose.yml + Caddyfile as .bak-YYYYMMDD, env names into mcp-tools.env, first deploy via the lane, `caddy reload` only - never restart LiteLLM, enrol ct110 via POST /enroll with a bootstrap token, rollback = remove service + reload); CLAUDE.md line. Commit: docs(fleet-admin): runbook.
HARD RULES: CRLF preserved; no image digest or unrelated workflow changes; no secrets; explicit-path commits; no push. Completion = commits, diff stat, e2e dry summary, the operator env NAMES and the enrol sequence. Auditor: no git fetch; compose diff touches only the intended lines; Caddyfile still routes every existing host; health-check addition is a single URL; no bare compose up added.
