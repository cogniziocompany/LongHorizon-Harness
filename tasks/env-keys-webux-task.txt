BUILD + TEST (code; the overseer deploys via the Hydra lane on corsairai300): give the fleet web UX (Hydra fleet admin, the surface fleet.easybutt0n.ai will point at) an "Env & keys" page so Paxton can set our provider keys and per-repo environment variables from the browser instead of over chat, for every related repo that does not already have a way to set them. Work ONLY in /home/harness/work/cognizioware-hydra-rsi on branch feat/env-keys-webux (run `git fetch origin && git checkout -B feat/env-keys-webux origin/main` first); plain git; commit by EXPLICIT PATH; no push; no live calls (fixtures); NEVER log, echo, or persist secret VALUES anywhere except the write targets below; env NAMES only in docs and tests.
READ FIRST: the Hydra fleet admin UI and API (apps/*, packages/*, the /fleet/* routes and the control-plane doctrine from tasks/expert-handoff-program-2026-09-07.md in LongHorizon-Harness: doctrine invariants live in the API, the UI only renders and calls); scripts/hivemind-ingest.mjs for the SSH/pct access pattern to boxes; the related repos' env contracts which you may READ under /home/harness/work: cognizioware-mcp-tools (infrastructure/docker-compose.yml + mcp-tools.env names, e.g. OLLAMA_CLOUD_KEY_1..4, CLOUDFLARE_API_TOKEN, CLOUDFLARE_DNS_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_MCP_TOKEN, REDIS_PASSWORD, LITELLM_MASTER_KEY, *_MCP_TOKEN/KEY), mcp-cognizioware (billing: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_*_METER_ID, ADMIN_SPA_CLIENT_ID; deployments/instances.json for dev/uat/prod), cognizioware-powerplatform (/opt/powerplatform-dev/.env names such as BILLING_API_URL/KEY, STRIPE_TEST_CUSTOMER_ID, GUEST_TEST_PRINCIPAL_EMAIL), cognizioware-qa (repo secrets ADMIN_CENTER_AI_USERNAME/PASSWORD, LITELLM_MASTER_KEY_UAT/PROD, variables like ADMIN_GATE_NOTIFY_TO), ptait09-easybutt0n-ai (runner env), LongHorizon-Harness (tasks/.env: LH_HARNESS_WEB_TOKEN, LH_HARNESS_MCP_GATEWAY_KEY). Never write into those workspaces.
SLICES (one per round; code + tests + explicit-path commit each):
 (1) Inventory + contract: packages/env-catalog (or the repo's equivalent module) with a static catalog: for each repo x environment (dev/uat/prod where applicable) the required env var NAMES, where each lives (GitHub repo/environment secret or variable; box env file path + CT + PVE host; both), which service reads it, whether the repo ALREADY has a way to set it (GitHub environment secrets UI = yes; a box env file that deploys overwrite = no; note mcp-tools.env survives mcp-tools deploys but mcp-cognizioware's lane rewrites its env), and a "sensitivity" flag. Derive names by parsing compose/.env.example files at build time where possible, hand-list the rest, with a test that fails when a compose file names a variable missing from the catalog. Output: docs/env-catalog.md generated from the same data. Commit: feat(env-catalog): per-repo env contract.
 (2) API: GET /fleet/env (catalog + STATUS only: set/unset/last-changed per target, computed by reading GitHub via the existing token - secret presence, never values - and by `grep -c '^NAME=.'` on the box file over the pct pattern) and PUT /fleet/env/{repo}/{env}/{name} with the value in the request body only: writes to the GitHub secret/variable via the API and/or appends-or-replaces the line in the box env file (backup file, mode 600, atomic rename), records an audit row (who/when/name/targets, NO value) in the fleet plane store, and returns the new status. Refuse names not in the catalog. Optional "verify" hook per name (e.g. Stripe: GET /v1/balance with the key; Cloudflare: GET /user/tokens/verify; Ollama: a 1-token completion) run server-side, result stored as status, value never stored. Tests with fake GitHub/pct/verify clients. Commit: feat(fleet-api): env status + set + verify.
 (3) UI: "Env & keys" page in the fleet admin: table per repo, columns name / service / dev / uat / prod status chips / has-own-way link (deep link to the GitHub environment settings when the repo already has one) / Set button opening a masked input that PUTs and shows the verify result; sensitive values never rendered; a "what the overseer is waiting on" filter that lists unset required names. Playwright/component tests with fixtures. Commit: feat(fleet-ui): env & keys page.
 (4) docs + rollout: docs/env-keys.md (threat model: token with least scope for GitHub, pct access only from the Hydra host, audit rows, no value logging; operator runbook; how the overseer consumes status via GET /fleet/env instead of asking in chat), CHANGELOG. Commit: docs(env-keys): runbook.
HARD RULES: values only ever travel request-body -> write target; never in logs, audit rows, fixtures, screenshots or docs; refuse catalog-unknown names; explicit-path commits; no push. Completion = commits, diff stat, tests, the deploy steps for corsairai300 and the list of repos that already had a way (so the overseer records them as "no change needed"). Auditor: no git fetch; grep the diff for anything that looks like a real key; verify the audit row schema has no value column; verify status computation never reads values into responses.

STANDING RULES (every harness task; the harness enforces the first two, state them so the run does not fight them): the AUDITOR runs on the read-only `audit` MCP profile and is blocked from network git operations (no git fetch/pull/push/clone, no gh) - it audits by reading the working tree, the diff and the round reports, and it must NOT run anything that writes into the workspace (no builds that emit artifacts, no test runs that regenerate fixtures, screenshots or baselines; use the hermetic unit target). MANAGER and EXECUTOR run on the `default` MCP profile (kb, gateway guides/skills, hivemind memory, langfuse, github, youtrack, ssh). Every episode carries a session id `<run_id>.<round_tag>.<role>` on the LLM path (x-litellm-tags lh-session/...) and the MCP path (X-LH-Session), so quote the run id in the completion report and reference it when citing evidence. Before declaring completion, `git fetch origin && git merge origin/<base>` so the branch is not stale, and report the merge result.
