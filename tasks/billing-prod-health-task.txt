INVESTIGATE + FIX (repo only; the overseer deploys through the lanes): the PROD billing service (mcp-billing-service on CT100 ptait01, port 20251, public https://mcp-cognizioware.easybutt0n.ai) answers 301 on / and 404 on /health but GET /api/health HANGS (no response within 15 s, from LAN and via the tunnel) while dev (:20252) and uat (:20253) answer 200 immediately. Requests are otherwise being served (logs show requests finishing). Work ONLY in /home/harness/work/mcp-cognizioware on branch fix/health-endpoint-timeout (run `git fetch origin && git checkout -B fix/health-endpoint-timeout origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live calls (reason from code + fixtures). Read the health endpoint implementation and every dependency it awaits (DB, Stripe, YouTrack, Redis, external HTTP). DELIVER: (1) a diagnosis doc docs/ops/health-endpoint-hang-2026-09-07.md naming the awaited dependency that can block forever in prod configuration (no timeout / no cancellation token / synchronous over async); (2) the fix: every dependency check in /api/health gets a bounded timeout (e.g. 2 s) and reports degraded instead of hanging; a separate /api/health/live (process up, no dependencies) and /api/health/ready (dependencies, bounded); (3) tests (hermetic) for the timeout path and the live/ready split; (4) the docker HEALTHCHECK and the lanes' health probes updated to /api/health/live for liveness and /api/health/ready for readiness (deployments/instances.json healthUrl fields, rollout.sh, promote-billing.yml). Commit each. Completion = commits, diff stat, test summary, the named root cause. Auditor: no git fetch; verify no health path can await an unbounded call.

ADDED SCOPE (overseer 2026-09-08 01:40 PT, lane gap found on promotion 34195647515): the develop (dev) lane in ci.yml deploys but never dispatches the cognizioware-qa `billing` gate for the deployed sha, so promote-billing.yml's "Require billing-dev QA gate" step (which polls the cognizioware-qa/billing-dev commit status for 5 minutes) fails for every fresh sha until someone dispatches qa-gate.yml by hand (target=billing env=dev caller_repo caller_sha). Fix in the same way the powerplatform dev lane does it: after a successful dev deploy, dispatch qa-gate.yml on cognizioware-qa with the caller inputs (workflow_dispatch via GH_TOKEN/PAT name only), and make the promote step's poll window configurable (default 5 min -> 30 min) with a clear error naming the missing status. Same for the uat deploy -> billing-uat gate. Tests: a dry-run of the dispatch step with a fake gh. Also note in docs/deploy/billing-lanes.md that promote-billing.yml must live on main (registered 2026-09-08 via PR #77, legacy push-to-main deploy disabled) and that the `prod` branch was created from release.

ADDED SCOPE 2 (overseer 2026-09-08 02:45 PT, found on the first prod rollout attempt, run 34197279905): the prod lane instance /opt/mcp-cognizioware-prod does NOT exist on CT100 (.153), and deployments/tier/docker-compose.prod.yml defines a NEW stack (app on 20251 + its own postgres:16 with a fresh pgdata volume, env_file .env) while the live prod service `mcp-billing-service` already runs from /opt/mcp-cognizioware/docker-compose.prod.yml (project mcp-cognizioware-prod) on 20251. Rolling the lane as-is would collide on the port and point prod at an empty database. Deliver: (a) a first-run provisioning script scripts/deploy/provision-prod-lane.sh that creates /opt/mcp-cognizioware-prod, copies the live prod env (names only in docs; the operator runs it on the box), and writes a docker-compose.override.yml that makes the lane ADOPT the existing project/container (same project name, same DB connection as today, no second postgres, no new volume); (b) a dry-run mode that diffs the lane compose against the live stack and refuses to roll on port or database mismatches; (c) docs/deploy/billing-lanes.md "first prod rollout" section; (d) tests with a fake docker. The overseer runs the provisioning during a quiet window, then re-dispatches promote-billing from main. Until then the prod promotion stops at "Roll out prod lane" by design.
