# Admin Config Parity QA Gate (UAT -> PROD) — 2026-09-07

**Source:** Paxton's planning session e426d3d2 (`C:\Users\PaxtonTait\.claude\plans\create-a-qa-plan-cryptic-owl.md`), repo cognizioware-qa. Snapshot what the LIVE system serves through admin APIs/UIs (LiteLLM admin, Ops command center, Billing admin SPA), compare UAT vs PROD live and PROD vs committed golden; UAT gate blocks on mismatch, post-prod advisory. ai-dev03 non-MFA account drives the UIs; Graph sendMail report.
**Harness task:** `C:\tmp\admin-config-gate-task.txt` = build brief (Phase 1 + 2, six slices) + the plan verbatim. Queue entry 11-admin-config-parity-gate (kimi, 10 rounds, workspace /home/harness/work/cognizioware-qa). No live calls in the run; endpoints marked UNVERIFIED.
**Overseer after the run:** review + PR; Phase 0 spike on qa-host per docs/admin-config-gate-SPIKE.md (needs the secrets Paxton adds on cognizioware-qa: ADMIN_CENTER_AI_USERNAME/PASSWORD, LITELLM_MASTER_KEY_UAT/PROD, ADMIN_GATE_NOTIFY_TO); manual dispatch `qa-gate.yml -f target=admin-litellm -f env=uat -f compare_to=prod`; first goldens via PR; then caller wiring tasks (mcp-tools qa-gate-uat + qa-verify-prod; mcp-cognizioware ci.yml + build-and-deploy.yml, needs QA_GATE_DISPATCH_TOKEN). Power Platform admin center deferred.
**Prerequisite (Paxton):** ai-dev03 added to Entra group "Ai Agents-Dev" (Global.Admin on the ops enterprise app).
- 17:30 PT (Paxton): the litellm surface also snapshots both skill channels - Skill Hub (/claude-code/marketplace.json, /claude-code/plugins) and the MCP-prompt catalog (prompts/list with x-mcp-servers: skills) - with parity rules (names/descriptions/versions equal, source url may differ per env) and a UI screenshot of /ui/skills/; drift check prompts vs hub. Added to task 11's text; task 15 keeps the Hub payload deterministic.
- 18:10 PT (Paxton): credential env NAMES in the qa repo env files (prod.env on the qa-host runner): CE_USERNAME=ai-dev01@cognizio.company / CE_PASSWORD for CE + web-ux flows; admin_center_ai_username=Ai-dev03@cognizio.company / admin_center_ai_password for admin surfaces. Written into tasks 06e and 11 (names only).
- 20:40 PT (Paxton): billing.easybutt0n.ai is live (tunnel published route + Entra redirect URI added; 200 on /admin/ and auth-config). Task 11 now maps it as the billing surface (with the old name as alias, equal auth-config) and generalises to one gate target per admin surface across the infra apps via a data-driven surfaces list: litellm, ops, billing, hydra, pihole (mirror drift gate), portainer, pgadmin, fleet chat, harness. Power Platform admin center still deferred.
