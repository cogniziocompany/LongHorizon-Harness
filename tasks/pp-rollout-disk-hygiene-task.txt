FIX + TEST (repo only; the lane deploys): the powerplatform LAN rollout has no disk hygiene - the dev/uat CT (pp-dev-uat, 40 GB) filled to 100% with 27 sha-tagged images (35 GB unused) and the develop deploy died with "failed to extract layer ... no space left on device" (CI 34165084379, 2026-09-07). Work ONLY in /home/harness/work/cognizioware-powerplatform-evalfix on branch fix/rollout-disk-hygiene (run `git fetch origin && git checkout -B fix/rollout-disk-hygiene origin/develop` first); plain git; commit by EXPLICIT PATH; no push; no live calls.
DELIVER in scripts/deploy/rollout.sh (read it fully first; keep its rollback contract "previous version sha-... (rollback target)"): (1) BEFORE pulling, on the target host: log `df -h <dir>` and `docker system df`; if free space < 6 GB or use > 85 percent, remove images of THIS instance's repository except the currently running tag and the recorded rollback tag, oldest first, until free >= 6 GB (`docker image prune -a -f --filter until=48h` as a first pass, then targeted `docker rmi` by created date); never touch containers or volumes; abort with a clear error if still short. (2) AFTER a successful rollout: keep only current + previous images for the instance (configurable KEEP_IMAGES=2). (3) A `--dry-run` that prints what would be removed. (4) Unit-style tests with a fake `docker` shim in PATH (bats or bash test harness already in repo; else a small scripts/deploy/test-rollout.sh) covering: below threshold -> no removal; above threshold -> removes oldest not-current-not-rollback; rollback tag preserved. (5) deployments/README.md note on disk hygiene and the thresholds. Commit each. Completion = commits, diff stat, test output. Auditor: no git fetch; verify the running and rollback tags can never be removed and no `docker system prune`/volume prune appears.
