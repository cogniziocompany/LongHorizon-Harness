# LiteLLM response caching on the CT202 gateway — 2026-09-07

**Source:** Paxton's planning session 3fbf60f8 (`C:\Users\PaxtonTait\.claude\plans\fancy-wondering-codd.md`). Symptom: admin UI Response Cache → Cache Health "unhealthy", `cache_params: {}`; no Redis anywhere in the stack. Decisions: Redis container in the mcp-tools stack (redis-py already in the pinned image), 300 s TTL for retry-storm/duplicate de-dup, and PER-TENANT namespacing (shared router across 20+ keys/teams - a flat cache would leak completions between tenants).
**Harness task:** 16-litellm-response-cache (mcp-tools workspace, kimi, 7 rounds): compose redis service + router wiring, config cache block, hooks/cache_namespacer.py (team_id or hashed key; fail-open; fallback flag), e2e suite 38 incl. the cross-tenant isolation test, S12 runbook + CLAUDE.md. No live calls in the run; the namespacing mechanism is marked UNVERIFIED until the CT204 probe.
**Operator sequence (overseer, gated):** CT204 first - add REDIS_PASSWORD to mcp-tools.env (backup first), `docker compose --env-file mcp-tools.env up -d --no-deps redis` (the UAT lane never creates new services), safe-recreate the router, run the cross-tenant isolation probe with two throwaway keys - it must pass before anything ships; then merge -> lane -> after qa-gate-uat, CT202 the same steps, then confirm the admin panel reads healthy. Rollback: cache: false + safe-recreate. These are in-CT container steps, not host-level, done with Paxton's standing go for lane deploys; the CT204 pre-merge step is announced before I run it.
**Session for deployment coordination:** the plan's author session 3fbf60f8 can be resumed from the mcp-tools repo for the probe/verification steps (`claude --resume 3fbf60f8-149a-43e7-a36c-56a7184bc5dc -p "<step>"`), like the Skill Hub task.
**Out of scope noted by the plan:** audioqa-mcp restart loop (already task 09; container stopped).
