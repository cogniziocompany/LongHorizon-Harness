BUILD + TEST (code; the overseer merges and re-dispatches the gate): fix the web-ux runner flow sign-in step. After PRs #8-#11 the cognizioware-qa `web-ux` gate now runs end to end and produces a report, but the single scenario fails at sign-in: `page.waitForSelector: Timeout 30000ms exceeded` waiting for `input[name="loginfmt"], input[type="email"], input[name="passwd"]` (QA run 169cb5c6, gate run 34193516784, base_url https://powerplatform-dev.easybutt0n.ai, env_id from QA_ENV_ID_DEV). The dev app root returns 200 with the SPA (title "Cognizioware - Power Platform Builder"); the Microsoft login inputs never appear, so either the SPA needs a "Sign in" click before the MSAL redirect, MSAL opens a popup rather than a redirect, an existing session skips the login page, or the page lands on a different route after the Env->Session v2 redesign (pp #85, deployed to dev 2026-09-08). Work ONLY in /home/harness/work/cognizioware-qa on branch fix/web-ux-signin (run `git fetch origin && git checkout -B fix/web-ux-signin origin/master` first); plain git; commit by EXPLICIT PATH; no push; no live calls (fixtures; the overseer dispatches the gate); env NAMES only (CE_USERNAME, CE_PASSWORD, CE_URL, QA_ENV_ID_DEV).
READ FIRST: packages/runner/src/flows/web-ux-nav-archive.ts (sign-in via ce-auth loginPassword()), the ce-auth helper and how the passing `ce` target signs in (compare selectors, popup handling, storage-state reuse), packages/runner/playwright.config.ts, docs/web-ux-gate.md, the gate artifact from run 34193516784 (screenshots under /tmp/web-ux-results were uploaded as qa-gate-report-web-ux; describe what the overseer should look for), and the powerplatform frontend routes/login entry you may READ at /home/harness/work/cognizioware-powerplatform-evalfix/packages/frontend/src (App.tsx, auth/MSAL setup, the sign-in button) - never write there.
SLICES: (1) make the sign-in step robust: navigate to base_url, if the app shell shows a sign-in control click it, handle MSAL redirect OR popup (context.waitForEvent('page')), reuse a cached storage state when present (path under the gate artifact dir, never committed), fall through to the loginfmt/passwd form with the existing credentials, and on failure capture a screenshot + the current URL + page title into the step notes so the gate report says WHY (tag drift vs real); unit tests with a fake page for each branch. (2) docs/web-ux-gate.md "Sign-in paths" section + CHANGELOG. Explicit-path commits. Completion = commits, diff stat, tests, and the dispatch command. Auditor: no git fetch; verify the three sign-in branches are tested and failures record URL/title/screenshot.

ROOT CAUSE CONFIRMED (Paxton, 2026-09-08 00:40 PT): the Microsoft sign-in for the QA account shows a PASSKEY prompt ("Sign in with a passkey" / passkey registration nudge) instead of the password form, so the loginfmt/passwd selectors never match. Handle it explicitly in the sign-in step: after entering the username, if the page offers a passkey (text like "passkey", "Use your face, fingerprint, PIN, or security key", "Sign in with a passkey"), click "Other ways to sign in" / "Use your password" / "Skip for now" / "No thanks" as applicable, then continue with the password form; after password, dismiss "Stay signed in?" (No) and any "Sign in faster with a passkey" nudge (Skip). Add fixture HTML for each interstitial and tests. Also document in docs/web-ux-gate.md that the tenant-side alternative is to exclude the QA accounts from the passkey registration campaign in Entra (Authentication methods > Registration campaign), which the overseer will ask Paxton to do.

PROBE RESULT (overseer, Playwright from CT210, 2026-09-08 00:50 PT): GET base_url redirects to /login showing controls [INPUT, BUTTON "Sign in with Microsoft", A "Sign up for a free trial"]; the flow never clicked the button, so the loginfmt form (which DOES appear after the click, at login.microsoftonline.com/<tenant>/oauth2/v2.0/authorize, client_id ac953095..., redirect_uri /auth, MSAL redirect interaction) was never reached. PAXTON'S RULE (00:55 PT): put this logic in the SHARED ce-auth loginPassword() helper used by every gate that signs in with a username + password, not only in the web-ux flow: (a) if the app shows a "Sign in with Microsoft" (or similar) button before redirecting, click it; (b) handle MSAL redirect and popup; (c) after the username, bypass the passkey interstitial ("Other ways to sign in" -> "Use your password" / "Skip for now"); (d) after the password, answer "Stay signed in?" with No and skip any "Sign in faster with a passkey" nudge; (e) on any failure record URL + title + screenshot in the step notes. Keep the helper's existing behaviour for apps that already redirect straight to Microsoft. Unit-test each branch with fixture pages; the ce, chat and billing gates must keep passing with the new helper (their flows import it).

SIGN-IN TIMING (Paxton 2026-09-08 01:05 PT): the shared helper must TIME every sign-in (first navigation -> app authenticated) against an expected window (default 20 s, env SIGNIN_EXPECTED_SECONDS, per-gate override). When the first attempt exceeds the window, or any interstitial appears (sign-in button, passkey prompt, stay-signed-in, passkey nudge, MFA/consent), write a diagnostic entry to the QA run's step notes AND to the gate report (a "signin" row: elapsedMs, expectedMs, interstitials[], finalUrl, title, screenshot path, tag infra - never counted as a test failure) so the e2e testing log keeps a history of when Microsoft sign-in slowed down or changed shape. Persist the timing on the QA backend run record (additive field) so it can be charted later. Tests: fast path writes only the timing; slow path writes the full diagnostic.

PROBE RESULT 2 (overseer, 2026-09-08 01:15 PT, QA account against the dev app): /login -> click "Sign in with Microsoft" -> Microsoft username form -> password -> "Stay signed in?" prompt -> NO passkey prompt for the QA account (the Entra registration-campaign exclusion for Ai Agents-Dev is in effect). So the REQUIRED fix is: click the app's sign-in button before waiting for the Microsoft form, and answer "Stay signed in?" (No). Keep the passkey / MFA / consent handling as defensive branches with fixtures, and keep the timing + diagnostic logging.
