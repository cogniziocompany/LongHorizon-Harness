BUILD + TEST (code; the overseer merges and deploys through the billing dev lane): add an idempotent customer-ensure endpoint to the billing service so the powerplatform guest onboarding (Slice 1, branch feat/guest-slices-1-4 in cognizioware-powerplatform) can bind a first-time guest to a billing row without touching the billing DB directly. Work ONLY in /home/harness/work/mcp-cognizioware on branch feat/billing-ensure-customer (run `git fetch origin && git checkout -B feat/billing-ensure-customer origin/develop` first); plain git; commit by EXPLICIT PATH; no push; never touch /home/harness/venv; no live Stripe calls (test-mode client is mocked in tests).
CONTRACT (agreed 2026-09-07 with the pp side): POST /api/v1/customers/ensure  body {email, tenantId, stripeCustomerId?}  -> 200 {userOrganizationId, stripeCustomerId, created: boolean}. Idempotent by (email, tenantId): returns the existing user_organizations row when present (created=false); creates one otherwise. stripeCustomerId: if supplied it MUST already exist in Stripe test mode (verify via the existing Stripe client abstraction; reject 422 if not); if absent, create a Stripe customer through the existing single-writer path used by signup (never a second writer). Auth: service key (the same scheme the pp backend uses for the other billing calls; env name BILLING_API_KEY). Errors: 400 invalid body, 401/403 auth, 409 if the email is bound to a different tenant, 422 unknown stripeCustomerId, 503 Stripe unavailable (with retry-after). Log a ledger/audit entry for a create.
DELIVER: (1) controller + service + repository changes following the existing SignupController/UserOrganizationService patterns; (2) unit + integration tests (existing row -> created=false; new row; unknown customer id -> 422; different tenant -> 409; production refuses env-named test customer mapping if such a mapping exists here - state if not applicable); (3) OpenAPI/contract doc update where the service documents endpoints; (4) a short note in the README on how the pp backend calls it. One commit per item. HARD RULES: no secrets; keep the single Stripe writer; existing tests green (`dotnet test`). Completion = commit hashes, diff stat, test summary. Auditor: no git fetch; verify idempotency and the 409/422 paths are tested.

OVERSEER DECISIONS (final, 2026-09-07 23:05 PT - do NOT ask about these again; the previous run 4f5bb97c stalled twice on them): (1) Idempotency key: add a NULLABLE tenant_id column to user_organizations via an additive EF migration and KEEP the existing UNIQUE(UserEmail). Ensure logic: look up by email; if the row exists with tenant_id null, set it to the supplied tenantId; if it exists with the same tenantId return it (created=false); if it exists with a DIFFERENT tenantId return 409 with a clear message. No separate mapping table, no data loss. (2) Route: implement POST /api/customers/ensure following the existing partner-route convention as canonical AND register /api/v1/customers/ensure on the SAME handler as an alias (both 200, no redirect) because the powerplatform caller was contracted on the v1 path. Document both in the OpenAPI/contract doc. Branch feat/billing-ensure-customer already exists on origin? No - it exists only locally in the workspace with no code commits; re-create it from origin/develop as instructed above.
