INVESTIGATE + FIX (code/eval/workflow changes in this repo only; the n8n instance is read-only). cognizioware-powerplatform: the eval case `multistep/multi-webhook-crud` fails in PROD (and intermittently in dev) and is the only red case blocking the promotion pipeline's prod eval gate (run 34025759906 re-run on the new Linux runner scored 15/16 with `eval:multi-webhook-crud@prod fail`). Work ONLY in this workspace /home/harness/work/cognizioware-powerplatform-evalfix (branch fix/eval-multi-webhook-crud from origin/develop); plain git only; commit by EXPLICIT PATH (never git add -A; never commit packages/backend/dist/**); no push. Node is /usr/bin/node (v22).

Verified facts (overseer, 2026-09-06): the eval runner is e2e/evals/run.mjs (undici cap fix #62/#63 already in develop: long builds no longer become NO_RESPONSE at 300 s); the case's build goes through the n8n workflow bcfo8MKr7IcXXZmu ("Webhook: Eval Micro Run" → Agent: Developer → LLM → Agent: QA → Parse QA Verdict → IF QA Passed? → IF Attempts Left? → Agent: Developer Rework → … → Respond Fail) on n8n 2.37.10 at http://192.168.21.154:5678 (API key in the untracked file .evalfix.env at the worktree root: N8N_BASE_URL, N8N_API_KEY — read-only; never print or commit it). Evidence: exec 150601 (prod, 2026-09-06 08:06–08:23Z) ended in "Respond Fail" with qaVerdict FAIL after 3 attempts; exec 150113 (dev, 2026-09-06 03:10Z) failed the same way in 0.1 s because every LLM node returned "Connection error." (the LiteLLM router was being restarted — environmental, not the case); in the 15/16 prod run the other multistep cases (multi-mini-app, multi-table-and-data-import) PASSED, so the QA agent and the build path work; the ask text of the case is "Stand up an inbound-lead webhook that creates a Lead record in Dataverse when it fires." — and the powerplatform eval orgs are bare Dataverse environments WITHOUT the Sales app: the cognizioware-qa README records that org9130dfc5 has no `lead` table ("the factory declares dataverse_lead_id but org9130dfc5 has no lead table"). Hypothesis to test first: the case asks for a Lead record in an org that has no Lead entity, so the QA verdict fails on a missing-table condition rather than on the webhook logic.

Slices, one per round:
(1) Diagnose with evidence: read e2e/evals/cases/**/multi-webhook-crud* (ask, checks, expected artifacts), the QA verdict parsing (Parse QA Verdict node output shape as consumed by run.mjs / the eval schema), and pull the three most recent prod executions of the case from the n8n API (`GET /api/v1/executions?workflowId=bcfo8MKr7IcXXZmu&status=success&limit=20` then `GET /api/v1/executions/{id}?includeData=true`; match by the ask text; redact any customer data). Write design/evals/multi-webhook-crud-diagnosis-2026-09-06.md: the exact QA failure reason per attempt (quote qaOutput), whether the Lead entity exists in org937bd6b5 (prod eval org) and org9130dfc5 (dev) per the case's own discovery/whoami calls or the QA logs, and the root cause statement with citations (file:line, execution ids). Commit: docs(evals): multi-webhook-crud diagnosis.
(2) Fix in the repo, chosen by the evidence: (a) if the case's target entity is missing in the eval orgs, make the case self-contained — either target a custom-Dataverse table the case creates itself (consistent with "custom Dataverse only in sample copy") or gate the case on discovered entity availability with a typed `skipped:missing-entity` outcome that the gate treats as not-a-regression and reports honestly (not as a pass); (b) if the failure is in the build/QA prompt contract (e.g., webhook URL/response shape the QA agent cannot verify), fix the case's checks or the runner's verification rather than the n8n workflow (n8n changes are out of scope — write them up as a follow-up if genuinely needed); (c) if it is a real product defect in the webhook provisioning path (packages/backend webhook/lead handlers), fix it with a regression test. Keep changes minimal and cite the diagnosis. Commit: fix(evals): multi-webhook-crud — <cause>.
(3) Verification: `npm run build` in packages/backend; the eval runner's unit tests if present; run the single case against DEV only if `EVAL_TIER=dev` credentials exist here (they do not; report NOT RUN honestly) — the overseer runs the prod gate on the CT210 runner after merge. Update e2e/evals/README.md with the new outcome semantics if you added one, and add the case to design/evals/foresight-gap-analysis.md as the first "foresight" example (a case that assumed an out-of-the-box entity that never existed in the target org — exactly the TRMS auto-case-creation miss Paxton described in orchestrator decision D4). Commit: docs(evals): outcome semantics + foresight example.

HARD RULES: no n8n edits/activations/imports; no .github/** changes; no secrets in files or output (read .evalfix.env only into env); no changes outside e2e/evals/**, packages/backend/src/** (only if (c)), design/evals/**; commit by explicit path; no push. Completion = commit hashes, the root-cause statement with execution ids, and what the overseer must run to prove it (exact gate dispatch). Auditor: no git fetch; confirm the diagnosis quotes real execution data (ids, timestamps) rather than assumptions, that a skipped outcome is never rendered as a pass, and no secrets in the diff.
