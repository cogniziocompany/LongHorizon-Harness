{
  "name": "007-116-hydra-reviewer-flow-bmad",
  "task_file": "C:/tmp/hydra-reviewer-flow-bmad-task.txt",
  "workspace": "/home/harness/work/BMAD_Cognizioware",
  "max_rounds": 10,
  "trio": "kimi",
  "note": "*** OVERSEER 2026-09-10 12:45 PT: the WORKSPACE blocker is gone - BMAD_Cognizioware is now cloned on CT110 (cc0ec3b) and both toolchain scripts are proven runnable under plain python3 (no uv; lint_spine.py lives at .claude/skills/bmad-architecture/scripts/, not _bmad/scripts/). This task REMAINS BLOCKED for its own stated reason only: it is gated behind task 111 proving the BMAD toolchain actually produces a spine. It is NOT held by any Paxton instruction - that was my conflation with a different, deleted task. Unblock this the moment 111 lands a real ARCHITECTURE-SPINE.md. ***  || PRIOR: BLOCKED behind task 111 (BMAD must be proven runnable) AND needs the BMAD_Cognizioware workspace cloned on CT110. Paxton 2026-09-09 22:40: 'run this also through a new queue lh-harness task, bmad_cognizioware process to finish the flow before build and specs' re: hydra #26 (add reviewer) + the PRs it unblocks (#5 device-token, #23 ingest, #24 env/keys page). Spec-first: produce a BMAD spec of the Hydra control-plane flow the open PRs touch BEFORE merging/building, so #24's prod-write-to-dev-box, #5's revocation-reverses-on-restart, #24's key-verification-never-tests-the-key, and #23's ingest-false-green are each judged against intended design. #26 itself is a trivial CI add (reviewer workflow) but per Paxton goes through the flow not a raw merge; the run RECOMMENDS whether #26 merges, the overseer does it. Merging #26 also deploys corsairai300. SPEC ONLY, no code, no merges, RULE 2 (hydra writes/fleet reads/ops infra-only). Do NOT invent BMAD output; if the tool cannot run, stop and say so."
}