{
 "name": "006-113-golden-runs-operator-surfaces",
 "task_file": "C:/tmp/golden-runs-operator-surfaces-task.txt",
 "workspace": "/home/harness/work/cognizioware-mcp-tools-b",
 "max_rounds": 12,
 "trio": "kimi",
 "note": "*** ADDED 2026-09-14 (Paxton): golden scenario for per-user gateway key in Fleet Chat + status + gate resolution; see end of task text. *** || BLOCKED behind task 111 (BMAD flow specs). Golden QA runs for EVERY operator surface, per Paxton 2026-09-09: 'golden QA runs being deployed and passing on each repo's admin center or any and all web ux.' Task 112/109 covers the powerplatform product UX; this covers ops.easybutt0n.ai (7 routes, NO /doctor - it renders on /overview), fleet.easybutt0n.ai (wallboard, read-only), the Hydra web UX (the ONLY write surface) and the billing admin on CT100. Copy e2e/golden-path/run.mjs from powerplatform - do NOT build a new harness per surface. The NETWORK LEDGER is non-negotiable: on an operator surface it is what separates 'the page rendered' from 'the page rendered AND its data loaded', which is exactly today's failure where tiles show green having made no call. TWO ASSERTIONS MUST BE ALLOWED TO FAIL AND MUST NOT BE SOFTENED: fleet currently shows ZERO registered nodes - and /api/meta now says why, fleet_configured=true, fleet_ever_succeeded=false, fleet_last_error=HTTP 413 - so a fleet golden run SHOULD fail today; and ops /certs is reported EMPTY, so a run that screenshots an empty page and passes is worthless. Commit the captures (.gitignore is _out/**/*.webm only - PNGs and report.md are TRACKED, that diff IS the review), maintain a run-history table per suite as the approval step, wire into each repo's DEPLOY LANE not ci.yml/PRs/cron, upload artifacts, and write down the promotion criterion. RULE 2: add no view, route or dashboard - if a test wants a page that does not exist, the finding is 'untestable as built'. Never the bare gateway /health. Do not create data in production. Do NOT push or open PRs; note that mcp-tools suites merge into a lane that recreates the CT202 prod router so the overseer picks the window."
}