{
  "name": "9999h-168-harness-queue-cutover",
  "task_file": "C:/tmp/harness-queue-cutover-task.txt",
  "task_source": "task_file",
  "workspace": "LongHorizon-Harness",
  "max_rounds": 8,
  "trio": "kimi",
  "note": "*** (d) NOW PROVEN ON REAL TRAFFIC, SAME TICK #1558, 17:55 PDT - this is the end-to-end proof the entry above said was still missing, and it arrived because the task-204 requeue needed the launcher restarted anyway. Launcher pid 281472 launched 9999zh-204 (run 20260923T005453Z_c1724ef3) at 17:54 and the log shows `shadow-filed q-5573d2ee7cb64715` WITHOUT an adjacent `shadow-retired`; the retire appears after the RAMP break line instead, i.e. from the new deferred path. READ BACK FROM THE API: the real PC launch generated **3** shadow records (not the zero #1555 measured), and /api/queue returned to pending=0 - so no stale pending was left and the reconcile-then-flip promotion hazard is NOT worsened. THE EVIDENCE GATE IS NOW ACCRUABLE; it demonstrably was not before. STILL NOT DONE, and 168 STAYS HELD: nobody has yet run compare_shadow.py over these records to see whether they classify `agree`, and the six-item promotion remains PAXTON'S CALL. NEXT TICK: run compare_shadow.py against the shadow store. *** *** BLOCKER (d) CLOSED AS AN IMPLEMENTATION 2026-09-22 17:57 PDT, SCHEDULED TICK #1558 - OPTION (1) TAKEN DELIBERATELY, and taken as a BOUNDED DELAY rather than a plain deferral. #1555 left three options unchosen; (1) is the only one that needs no CT110 code and no deploy (option (2) is a webapi change, and a CT110 deploy kills in-flight runs - run 204 is live), and (3) is the do-nothing that accrues nothing. PATCH (C:/tmp/launch_queue.py, backup .bak-tick1558, AST-checked): the launch site no longer calls dualfile_retire() inline; it appends to _RETIRE_AFTER, and the retire runs _RETIRE_DELAY=45 s later, immediately before the 900 s idle sleep (so it costs no cycle time), with the existing loop-head sweep left in place as the safety net. 45 s is chosen against BOTH failure modes: retiring at once is blocker (d) (zero records); never retiring is blocker (c) (the unbounded re-decide log). *** MEASURED, NOT ASSUMED: a probe entry filed against a deliberately nonexistent workspace (__tick1558-workspace-does-not-exist__, so it could not launch even if observe were off) was held 48 s and the shadow store went 3 -> 6 records, exactly one per ~15 s poll; DELETE returned the queue to pending=0, read back. So ~3 records per launched entry: non-zero, and too few for duplicates to inflate the evidence count. *** WHAT IS STILL NOT PROVEN, AND 168 STAYS HELD FOR IT: this is NOT A DEPLOY - the launcher is not running (queue empty, clean self-exit 17:41), so the patch is inert until the next enqueue starts it. The probe records are probe-shaped (nonexistent workspace); they are NOT agreement rows. The end-to-end claim - a real PC launch producing records that compare_shadow.py classifies `agree` - needs one real queued task and has not been observed even once. DEFAULT IF NOBODY ACTS: the next task queued will exercise it automatically and the next tick should read /api/queue/shadow for real launch records. Promotion remains PAXTON'S CALL on the six items, and the reconcile-then-flip-then-retire hazard above is unaffected. *** *** BLOCKER (a) IS LIVE AND IT STILL PRODUCES NO EVIDENCE - MEASURED 2026-09-22 17:26 PDT, SCHEDULED TICK #1555, ON THE FIRST REAL TRAFFIC SINCE #1554 PATCHED IT. Tick #1555 released task 204 to the queue and started the launcher (pid 141096). The dual-file FIRED, exactly as written: the log reads `17:26 shadow-filed 9999zh-204-... q-e26dfee32205461c` then `17:26 shadow-retired ... q-e26dfee32205461c`, and the run LAUNCHED as 20260923T002658Z_ef0cfa47. So #1554's code works on real work, not just on its own probe - that part is settled and (a) is CLOSED as an implementation. *** BUT THE FILE AND THE RETIRE LANDED IN THE SAME SECOND, AND THAT IS A FOURTH BLOCKER, (d), STRUCTURAL AND NOT A DEFECT IN THE PATCH: read back immediately after, GET /api/queue = 0 entries and GET /api/queue/shadow count = 3, STILL the same three 16:55:29 records. The shadow decides pending entries on its ~15 s poll; an entry that is filed at task-text resolve and deleted at launch inside ONE launcher iteration never survives a poll boundary, so it generates ZERO shadow records and therefore ZERO agreement rows for compare_shadow.py. *** WHAT THIS MEANS FOR THE EVIDENCE GATE: dual-filing can only ever yield records for entries that WAIT - workspace already busy, the per-cycle CAP break, or synthetic down - because only those stay pending across a poll. An idle queue with a free workspace, which is the normal state here, produces nothing. 168's six items cannot be accrued by simply queueing work and waiting; that expectation, carried since #1554, is now measured false. *** OPTIONS, NOT YET CHOSEN (whoever takes this, pick one deliberately): (1) delay the retire by one poll interval so every filed entry is decided once - smallest change, but it deliberately leaves a launched entry pending in the API queue for ~15 s, which is precisely the stale-pending state the promotion hazard warns about; (2) have the shadow decide on the POST itself rather than only on the poll; (3) accept that agreement evidence comes only from contended cycles and gather it over time. DEFAULT IF NOBODY ACTS: 168 stays HELD and accrues nothing - unchanged in substance from #1554, but now for a reason that is proven rather than assumed. *** PRIOR NOTE FOLLOWS: *** BLOCKER (a) IMPLEMENTED 2026-09-22 17:17 PDT, SCHEDULED TICK #1554 - THE LAST OF THE THREE. launch_queue.py now DUAL-FILES: new helpers `dualfile` / `dualfile_retire` (backup C:/tmp/launch_queue.py.bak-tick1554, AST-checked). It POSTs /api/queue the moment an entry's task text resolves (i.e. exactly when this launcher first treats the entry as launchable), and DELETEs /api/queue/{queue_id} both at launch and whenever an entry leaves the queue dir by some other hand. THE DELETE IS NOT OPTIONAL: observe mode never marks an entry launched, so a filed entry stays pending and the shadow re-decides it every poll - dual-filing WITHOUT the DELETE reinstates blocker (c)'s unbounded log. PROVED END TO END AGAINST THE LIVE STORE, not assumed: probe filed as q-01e9800b12f64e62 and read back (status pending, trio kimi, workspace LongHorizon-Harness, requested_by launch_queue.py, dedup_key pc:<basename>); a re-POST from a FRESH process returned the SAME queue_id with pending still 1, so the enqueue is idempotent across launcher restarts; DELETE returned pending to 0. The queue was left at 0 and GET /api/queue/shadow stayed count=3 - the probe lived under one poll interval, so it produced ZERO shadow records and (c) stays closed. Every call is wrapped and fails open: CT110 being unreachable can never block a launch. LH_DUALFILE=0 in the launcher's environment disables the whole mechanism. *** A FILE EDIT IS NOT A DEPLOY: the PC launcher is NOT running (queue empty, clean 13:11 self-exit at launch_queue.py:630), so this code takes effect at the NEXT launcher start. Whoever queues the next LongHorizon-Harness task gets the dual-filing automatically. *** CORRECTION TO THIS NOTE'S OWN #1553 TEXT: it said the launcher sees entries 'in C:/tmp/queue/active'. IT DOES NOT - it globs C:/tmp/queue/*.json, the ROOT dir (launch_queue.py, `q=sorted(glob.glob(...))`); C:/tmp/queue/active currently holds 0 .json files and is never read. The dual-filing is keyed off the root glob. Unacted on elsewhere. TRIO MAPPING, stated so nobody reads agreement into it: CT110 accepts only {kimi,qwen}; this launcher also uses 'degraded' and 'default', both mapped to kimi. That is a lossy mapping and is safe ONLY because compare_shadow.py never classifies trio-differ and calls the PC trio family 'not determinable' by design (L150-159). KNOWN SCOPE LIMIT, deliberately not papered over: entries the launcher skips BEFORE task text resolves (workspace already busy, or the per-cycle CAP break) are not filed that cycle, so the two pending sets can differ at cap. Honest, and visible in the log. WHAT IS LEFT FOR 168: (1) real traffic - queue a LongHorizon-Harness task through C:/tmp/queue as normal and start the launcher; both streams now cover it; (2) run scripts/compare_shadow.py --pc-log C:/tmp/launch_queue.log --shadow <runs_root>/queue/shadow.jsonl; (3) PAXTON promotes on the six evidence items. DEFAULT IF NOBODY ACTS: 168 stays HELD - nothing accrues and nothing grows, because the queue is empty and no entry is filed. THE PROMOTION HAZARD RECORDED BY [5321285c] 16:57 IS UNCHANGED AND NOW MATTERS MORE: once dual-filing is live, promotion must be reconcile-then-flip-observe-then-retire-launch_queue.py in ONE window, never a bare config toggle. The DELETE above is what keeps the API queue reconciled while the shadow runs. *** || *** BLOCKER SET CUT 3 -> 1 BY SCHEDULED TICK #1553, 2026-09-22 17:0x PDT - TWO OF #1552's THREE GAPS ARE DEAD, BOTH READ FROM SOURCE NOT INFERRED. (c) CLOSED: the smoke-test probe q-ddf7f30f53ef4ba2 was DEQUEUED by [5321285c] at 16:57 PDT. Measured 17:03: GET /api/queue = 0 entries, GET /api/queue/shadow count=3 STATIC (the three 16:55:29 records), i.e. the log stopped growing 8 min ago. The '~5.4 records/min, unbounded' hazard is historical, not current. (b) DISSOLVED, AND ITS PRESCRIBED FIX WAS DANGEROUS. #1552's next-action (1) 'mirror the PC _TRIOS into [queue.trios]' IS IMPOSSIBLE AND WOULD HAVE KILLED THE SERVICE. config.py:52 `_QUEUE_TRIOS = {\"kimi\",\"qwen\"}` and :221 RAISES ProjectConfigError on any other trio name; :228 allows only {agent,model,mcp_profile} per trio, i.e. ONE model applied uniformly to all three roles (confirmed live in the shadow payload: manager/executor/auditor identical). The PC _TRIOS (launch_queue.py:180-192) are SIX rows each seating THREE DIFFERENT models per role. The schema cannot express them; writing them would have raised on restart and left CT110 down. IT IS ALSO UNNECESSARY: scripts/compare_shadow.py @origin/main NEVER ASSIGNS `trio-differ` - classify() returns `agree` on decision match at L497/502/505/512 and puts the trio family in `detail` via _trio_note(); the literal 'trio-differ' occurs only in the docstring and summarize()'s ordering tuple, and L150-159 says outright that the PC trio family is 'not determinable' without an operator-supplied index->name map and is 'reported honestly'. So trio mismatch does not block a single `agree`. (a) STANDS - THE ONE REAL BLOCKER, and now specified concretely. Confirmed: launch_queue.py has ZERO /api/queue calls; its only write is line 838 POST /api/runs. The two decision streams therefore cover disjoint entry sets and every row classifies pc-only/shadow-only. THE IMPLEMENTABLE SHAPE, bounded by what the API actually offers (routes enumerated on origin/main server.py: POST /api/queue :1077, GET :1087, DELETE /api/queue/{id} :1117, POST priority :1159, GET config :1176, GET shadow :1191) - THERE IS NO 'mark launched' ROUTE, so DELETE is the only retirement mechanism: (1) PC launcher POSTs each pending entry to /api/queue when it first sees it in C:/tmp/queue/active, so both streams see the same pending entry; (2) PC launcher DELETEs /api/queue/{queue_id} as soon as it launches or drops that entry. That bounds shadow.jsonl to one-or-two poll cycles per entry (~5/min for seconds, not forever) and is what stops (c) from coming back the moment dual-filing starts - dual-filing WITHOUT the DELETE reintroduces the unbounded log per entry. (3) then run scripts/compare_shadow.py --pc-log C:/tmp/launch_queue.log --shadow <runs_root>/queue/shadow.jsonl; (4) Paxton promotes on the six items. PROMOTION HAZARD, recorded by [5321285c] 16:57 and still true: observe mode never marks entries launched, so flipping observe=false would launch every stale pending entry at once - promotion must be reconcile-then-flip-then-retire-launch_queue.py in ONE window, never a bare config toggle. NOTE scripts/compare_shadow.py EXISTS on origin/main (merged with PR #35 / daa9161); it is absent from the feat/spec-staging checkout on this box, which is stale - not a missing file. DEFAULT IF NOBODY ACTS: 168 stays HELD; nothing grows and nothing accrues, because the queue is empty and the probe is gone. *** || *** SHADOW PHASE IS LIVE AS OF 2026-09-22 16:54 PDT - refreshed by scheduled tick #1552 [ead105]. `[queue] observe = true` was written to /home/harness/work/.lh-harness/config.toml and the service restarted at 23:54:25Z (MainPID 961248) by the interactive session [5321285c]; tick #1552 verified it live (observe=True, backend=file, read with the VENV python) and confirmed shadow.jsonl now EXISTS (1463 B, 3 records) where #1551 measured it absent. #1551's RECOMMENDATION IS RETRACTED: it said to enqueue the next real LHH task through the CT110 API queue *instead of* C:/tmp/launch_queue.py. Wrong twice - (1) while observe was false the CT110 launcher was AUTHORITATIVE-ARMED, so that would have launched a second real run alongside the PC launcher, the exact double-launch the shadow phase exists to rule out; (2) 'instead of' removes the PC-side stream, and scripts/compare_shadow.py compares launch_queue.log AGAINST shadow.jsonl - drop either side and every row classifies pc-only/shadow-only, never `agree`. THE SIX EVIDENCE ITEMS STILL CANNOT ACCRUE. Three gaps, each measured this tick: (a) NO DUAL-FILING - launch_queue.py posts only to /api/runs (line 838) and never POST /api/queue, so the CT110 store never sees the entries the PC launcher works; the two decision streams cover disjoint entry sets. (b) TRIO MISMATCH - the live [queue] section sets only `observe`, so config.py backfills trios kimi/qwen with model=None (kimi->claude_code, else codex). The PC launcher resolves the synthetic _TRIOS rows instead, so even a matched entry classifies `trio-differ`, never `agree`. Mirror the PC _TRIOS into [queue.trios] before counting agreement. (c) THE ONLY ENTRY IS A SMOKE-TEST PROBE - queue_id q-ddf7f30f53ef4ba2, workspace `/home/harness/work/__shadow-probe-does-not-exist__`. Observe mode leaves entries pending, so the launcher re-decides and re-logs it EVERY poll (~16 s, ~5.4 records/min, ~7.8k/day): shadow.jsonl grows without bound and carries zero real evidence. Dequeue the probe before the evidence window starts. NEXT ACTIONS, in order: (1) mirror _TRIOS into [queue.trios]; (2) dequeue the probe; (3) dual-file each queued task to POST /api/queue as well as C:/tmp/queue so both streams cover it; (4) run scripts/compare_shadow.py --pc-log launch_queue.log --shadow <runs_root>; (5) Paxton promotes on the six items. DEFAULT IF NOBODY ACTS: 168 stays HELD with a growing log of one non-existent probe workspace. *** || *** BLOCKER STATUS REFRESHED TICK #1551 2026-09-22 16:46 PDT [9b1024] - THE 09-21 LIST BELOW IS STALE ON THREE ITEMS, each re-read live this tick. CLEARED: (i) 'LHH #20 merged' - #20 IS MERGED, 2026-09-22T00:21:15Z (feat/task-172-retry-requeue -> main); the 09-21 17:40 note predates the merge. (ii) 'shadow phase (173)' - 173 reached VERIFIED at #1550 and its wiring is proven live at #1551 (see 219's note). (iii) 'zero-run window' - OPEN RIGHT NOW: 0 active, queue empty, PC launcher self-exited 13:11, 0 non-terminal runs but the 2 known stale rows. STILL OUTSTANDING: [queue] backend=postgres + database_url in the live config, migrations 001/002 applied, service restart so workers inherit LH_HARNESS_DB_PASSWORD. *** *** NEW FINDING, AND IT CHANGES THE SHAPE OF THE HOLD: THE PROMOTION GATE CANNOT BE SATISFIED BY WAITING. The six evidence items (HANDOFF-overseer-audit-2026-09-11.md:265) are ALL agreement measurements between the CT110 shadow and the PC launcher - zero unexplained divergence, zero phantom launches, zero missed launches, liveness across one reboot + one blip, zero double-launch, time-to-launch <= PC baseline. Measured live: GET /api/queue/shadow = count 0, and shadow.jsonl is ABSENT from the live runs_root /home/harness/work/.lh-harness/runs/queue/ (which holds only .lease + contention.json, both written 23:44 UTC, so the dir is live and current - reader verified: no `since` filter was passed, and I first mis-read one level up at .lh-harness/queue/ before correcting). ZERO shadow records exist because nothing flows through the CT110 API queue: /api/queue is empty and the PC launcher has exited. The shadow is deployed and has observed NOTHING. So the hold will NOT self-resolve; promotion needs DELIBERATE traffic through POST /api/queue, not more waiting. RECOMMENDED: enqueue the next real LongHorizon-Harness task through the CT110 API queue instead of C:/tmp/launch_queue.py, so the six items accrue against real launches. DEFAULT IF NOBODY ACTS: 168 stays HELD indefinitely with an empty shadow log, which is where the last ~8 ticks were heading. *** || *** BLOCKER STATUS 2026-09-21 17:40 PT by the interactive session, on Paxton's instruction ('move everything forward that I unblocked'): (a) 134 LANDED - LHH #19 merged 18:04Z; (b) lh_harness DB/ROLE EXIST on CT103 and the END-TO-END LOGIN FROM CT110 IS PROVEN (psycopg 3.3.6 installed in the venv as root; harness user connected to 192.168.21.154/lh_harness as lh_harness, current_schema=harness, DDL over the network OK; 0 tables until migrations run); (c) zero-run window - STILL NEEDED. ALSO STILL NEEDED before cutover: LHH #20 merged (MERGEABLE now), `queue_backend=\"postgres\"` + `database_url` in config, migrations 001/002 applied, a harness service restart so workers inherit LH_HARNESS_DB_PASSWORD (EnvironmentFile), and the shadow phase (173, releases when #20 merges). *** || *** AMENDED 2026-09-14 [fe2679]: shadow phase per task 55 added (task 173 observe mode first; NO fixed length - promote on the six evidence items; Paxton). Step A of the 09-14 handoff is DONE (55's three commits on main). *** || BLOCKED. Queued by the overseer 2026-09-11 from audit session [fe2679] section A. This is the unowned Step 0: move the queue off C:/tmp onto the CT110 API and retire launch_queue.py. VERIFIED UNOWNED: explicitly out of task 134's run scope by design, and absent from 150, 155 and 104. BLOCKED ON THREE THINGS: (a) task 134 landing - 134 died at round 0 on a provider 429 and was re-requeued by hand twice, the second time by the overseer this tick as the 15th done-at-launch stranding; (b) the lh_harness database/role on CT103, which is PAXTON'S to provision because a run never gets credentials; (c) a counted zero-run window. Rationale that makes it worth the priority: filing entries to done/ AT LAUNCH has stranded SIXTEEN tasks with failures invisible up to EIGHT HOURS, and it is unfixable in the PC script's model. Explicitly forbids zero-pad renames and launcher refactors - that code is being retired. || CUTOVER PRECONDITIONS 2026-09-22 00:36 PT [5321285c], zero-run window: (1) CT110 now runs main 8b349bc (PR #20), PgQueueStore importable, psycopg 3.3.6, service restarted 07:30:47Z and its process holds LH_HARNESS_DB_PASSWORD, SEQ_READ_KEY_PP_PROD, PP_DEV_E2E_USER/PASSWORD (names read from /proc). (2) CT103 lh_harness DB: migrations 001+002 applied; 001 FAILED on a fresh DB at ALTER COLUMN status TYPE (default cannot be cast) - fixed by hand with DROP DEFAULT / TYPE / SET DEFAULT and the dedup index created; schema now queue, queue_events, enum queue_status, 4 indexes, FK. (3) BLOCKER FOUND: on main, webapi/server.py:881 still constructs QueueStore(runs_root, queue_config) unconditionally; _select_queue_store (queue.py:87) has no caller outside tests. So [queue] backend=postgres + database_url in .lh-harness/config.toml would be READ but IGNORED - the cutover needs the wiring change first (shadow-mode PR #35 from task 173 may carry it; check before setting config). Config NOT written. || 2026-09-22 09:05 PT SCHEDULED TICK #1479: the (3) blocker LANDED - LHH PR #39 (task 219) merged at 25787ff wires _select_queue_store into webapi/server.py (selector at server.py:885, read by tick #1478); CT110 now runs main 7944511 (service active 16:03:27Z). 168 is no longer BLOCKED on wiring; it is HELD this tick because the interactive session [5321285c] owns the current zero-run deploy window on CT110. Next actor: move to root queue, restart the launcher (it exited 05:34 on empty queue), bind an all-Synthetic trio, and run the cutover ([queue] backend=postgres + database_url) with read-back that the selector chose PgQueueStore. || CORRECTION 2026-09-22 12:56 PT [5321285c]: I said last pass that \"cutover 168 can now proceed by configuration\". That was WRONG and is retracted. The wiring (LH #39) is live and the config location is confirmed (service WorkingDirectory=/home/harness/work, so PROJECT_CONFIG_PATH resolves to /home/harness/work/.lh-harness/config.toml - the runs_root probe path does not exist), but THIS ENTRY'S OWN NOTE requires the SHADOW PHASE FIRST (task 55 / task 173 observe mode, no fixed length, promote on six evidence items, PAXTON'S CALL). Flipping [queue] backend now would skip a phase he asked for. Nothing was written to config.toml. Remaining preconditions: PR #35 merged (CONFLICTING - task 220 queued to rebase it), shadow window run, Paxton promotes on the evidence. Ready when those land: backend = \"postgres\", database_url = postgresql://lh_harness@192.168.21.154:5432/lh_harness (no password in the file; LH_HARNESS_DB_PASSWORD is already in the service process), tables harness.queue / harness.queue_events exist and the API queue is EMPTY so the cutover has nothing to migrate. || PROMOTION HAZARD, found 2026-09-22 16:57 PT [5321285c] while arming the shadow phase, and it bears directly on Paxton's promotion decision: in observe mode an entry is NEVER marked launched, so it stays pending and is re-decided every 15 s poll. The moment observe flips to false, EVERY entry still pending in the CT110 API queue launches at once - including anything the PC launcher already ran days earlier. Therefore promotion is not just \"flip observe\": it must be (1) reconcile the API queue against the PC queue and delete every entry the PC launcher already completed, (2) flip observe=false, (3) retire launch_queue.py in the same window so the two launchers never both act. The API queue is EMPTY today (verified), so this hazard only bites once the shadow phase starts mirroring real entries. Whoever mirrors entries must also delete them as the PC launcher finishes them. || BLOCKER (e), MEASURED 2026-09-22 18:2x PT SCHEDULED TICK #1562, AND IT CORRECTS MY OWN #1558. #1558 claimed the real launch q-5573d2ee7cb64715 'made 3 shadow records' and declared blocker (d) closed on that. The count was right; the TYPE was not. Enumerated by type this tick: the 9 shadow records are 3x queue.shadow_launch (probe q-ddf7f30f53ef4ba2), 3x queue.shadow_launch (probe q-43f624e5258f468d), and the one REAL entry q-5573d2ee7cb64715 is 3x **queue.shadow_skip**, reason verbatim: 'workspace LongHorizon-Harness occupied: dirty tree (git status --porcelain non-empty)'. A skip is not an agreement row, so the gate accrued ZERO agreement evidence from real work - only the two nonexistent-workspace probes ever produced launch decisions. WHY IT IS STRUCTURAL, read from launcher.py@release-src: _check_eligibility:531 calls _dirty_workspace_occupied (:170), which is `git status --porcelain` non-empty - THAT COUNTS UNTRACKED FILES. The CT110 workspace /home/harness/work/LongHorizon-Harness carries 8 permanent untracked litter files (.claude/, debug_test.py, debug_test2.py, e2e/review_run.py, scripts/compare_shadow.py.bak, src/lh_harness/fleet/reporter.py.backup{,2,3}) that predate every current run. So EVERY entry targeting that workspace skips forever, not occasionally. :536 _unpushed_branch_occupied is a SECOND standing reason - feat/task-204-memory-death-reason is unpushed right now. The PC launcher has NO occupancy check at all and launched into that same dirty tree successfully, so this is a REAL divergence, not a shadow bug: the new launcher is enforcing the 'never two runs on one working tree' doctrine the PC launcher violates. TWO CONSEQUENCES FOR THE PROMOTION DECISION. (1) The six-item evidence gate cannot be satisfied for LongHorizon-Harness while the litter sits there - waiting accrues nothing. (2) If observe flips to false with the tree dirty, every LHH entry skips and the queue STALLS silently; this stacks on top of the pending-entry stampede hazard above. RECOMMENDED NEXT ACTION, needs an ACTIVE=0 window and is NOT safe now (run c1724ef3 is working in that exact tree): delete the 8 untracked litter files, push or set upstream on the task-204 branch, then queue one real LHH entry and confirm it records queue.shadow_launch instead of queue.shadow_skip. Only then is an agreement row possible. 168 stays HELD. || STARTUP BLOCKER, REPRODUCED 2026-09-22 20:56 PT [5321285c] on origin/main 2679f63 in a clean worktree: writing the documented cutover config ([queue] backend=\"postgres\" + database_url) would CRASH THE SERVICE AT BOOT. The loader ACCEPTS database_url as a valid key but _flatten_queue_table does not carry it (its return dict is trios/capacity/backend/observe/occupancy_ignore_dirty), so _select_queue_store raises ValueError \"queue_backend=postgres requires queue.database_url\" and create_app re-raises it. Task 221 queued to fix it. Task 219 is NOT at fault - it wired the selector correctly; the DSN simply never arrives. Also note the shipped [queue.trios] defaults carry model = null, so shadow decisions resolve no models until trios are configured - see ask 168-trio-agreement-scoring. || *** TWO OF 168's BLOCKERS CLEARED 2026-09-22 22:15 PDT, SCHEDULED TICK #1597 [4d0117], BOTH MEASURED ON CT110, NEITHER INFERRED. (1) THE STARTUP BLOCKER IS GONE: task 221 merged (767f856) and DEPLOYED to CT110 at 22:07:59 UTC-7 (MainPID 995927 active); tick #1596 ran the INSTALLED config._flatten_queue_table under CT110's venv and it returns database_url, so [queue] backend=postgres + database_url no longer crashes create_app. The 20:56 PT reproduction by [5321285c] is answered. (2) THE OCCUPANCY BLOCKER IS GONE, AND THE 8 LITTER FILES NO LONGER EXIST: `git status --porcelain` in /home/harness/work/LongHorizon-Harness is EMPTY, and the checked-out branch fix/task-221-queue-database-url HAS an upstream, so the second rule cannot fire either. Called the installed probes directly: _dirty_workspace_occupied=False, _unpushed_branch_occupied=False. So an LHH entry would now record queue.shadow_launch, NOT queue.shadow_skip - the prescription in the entry above (delete the litter, push the 204 branch) is SUPERSEDED: it was already satisfied by events, nothing was deleted by this tick. *** WHAT 168 STILL WAITS ON, and why it stays HELD: (a) OPEN-ASKS row `168-trio-agreement-scoring` - Paxton's call on how a static-trio harness launcher can agree with the PC script's rotating trio, evidence item 1 of the six; (b) the agreement rows themselves, which need ONE REAL LongHorizon-Harness entry to be launched by the PC launcher while the shadow launcher observes - no LHH task is queued right now, so this accrues at the next genuine LHH task rather than from a synthetic probe. Do NOT queue a task purely to manufacture evidence. *** || SHADOW WINDOW STARTED 2026-09-23 00:19 PT (07:19:54Z) [5321285c], per Paxton handoff PAX-DECISIONS-20260923-LAST3 (\"starts as soon as one real LHH entry queues normally\"). The first LongHorizon-Harness entry after his last gate cleared (22:15 PDT) is 9999zs-218-queue-task-validator: shadow-filed q-342db80ee6554646 at 00:19 and launched by the PC as run 20260923T071954Z_24b4a1ad. Proposed 7-day window -> ends ~2026-09-30 00:19 PT. SCORING (Paxton, 2026-09-23): evidence item 1 on ENTRY SELECTION and LAUNCH/SKIP only; trio agreement OUT OF SCOPE; doc section 5 to be amended so it no longer contradicts section 7. Items 2-6 unchanged (zero phantom launches, zero missed launches, liveness across a CT110 reboot and a network blip, no double-launch, queue latency <= PC). Shadow log at start of window: 18 records (8 launch, 10 skip) - the earlier ones predate the window and include my 09-22 probe. STILL OPEN before promotion regardless of evidence: task 221 (database_url dropped by the loader - the cutover config would crash the service at boot) and the reconcile-then-flip-then-retire promotion order. || EVIDENCE TOOL CHECKED EARLY, 2026-09-23 00:29 PT [5321285c] - day 0 of the window, deliberately, so a tooling fault is not discovered at day 7. Ran scripts/compare_shadow.py (origin/main) on the real data: naive run 0/139; correctly joined and dated (name map from the PC log, anchored slice) 0/5 with missed=3. Hand-checked the raw records: for 221 and 218 the shadow decided LAUNCH seconds BEFORE the PC launched, then every later pass skipped because the workspace was occupied by the PC's run of that same entry - the tool scores the latest record, so it calls agreement a miss. 217-clawdtalk is a genuine unobserved race (PC launched before the shadow's first pass). TRUE PICTURE SO FAR: 2 of 2 observable entries AGREE, no genuine disagreement, 1 race, 1 probe. Task 222 queued to fix the tool (judge at-or-before PC launch, self-occupied and unobserved-race buckets, derive the name map from the PC log, dated slicing, probe bucket). Until 222 lands, do NOT read this tool's raw verdicts as evidence. || CORRECTION to my 2026-09-22 PROMOTION HAZARD note on this entry: I overstated it. The PC launcher retires each mirrored API entry AT LAUNCH (log: \"shadow-retired\" in the same minute as LAUNCHED), and it only shadow-files entries it is about to launch, so stale pending entries do NOT accumulate in the CT110 queue and flipping observe off would not mass-launch old work. The reconcile-then-flip-then-retire order is still right, but the reconcile step should find an empty or near-empty queue."
}
