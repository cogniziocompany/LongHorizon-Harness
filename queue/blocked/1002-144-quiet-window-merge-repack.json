{
 "name": "1002-144-quiet-window-merge-repack",
 "task_file": "C:/tmp/quiet-window-merge-repack-task.txt",
 "workspace": "cognizioware-mcp-tools",
 "max_rounds": 1,
 "trio": "kimi",
 "note": "*** BUNDLED 2026-09-16 15:58 PT (Paxton): THIS is the one window - 136 fallback apply, 170 retention settings, CT105 stop (backup + connector read first), then the 25 GB reclaim only if dead tuples > 0 and margin >= 8 G, then impact:deploy merges (#111, hydra reviewer, 178 stubs). Ordered steps at the END of the task text. 147 arms DRAIN. *** || *** OVERSEER-EXECUTED. DO NOT LAUNCH A HARNESS RUN FOR THIS. *** Scheduled by Paxton 2026-09-10 18:35 PT. Placed in queue/blocked/ DELIBERATELY so the launcher cannot pick it up and burn one of only two capacity slots on work a run is structurally incapable of doing: both steps need the LiteLLM master key and prod credentials, and a harness run is NEVER given credentials. *** THE PRECONDITION IS ZERO ACTIVE HARNESS RUNS - not one, not 'only the BMAD one', ZERO. Both steps disturb CT202, and EVERY run routes its model traffic through the CT202 router regardless of which repo it works on. The overseer got this wrong once already, at 18:31, offering to take the window 'as soon as task 125 dies' while task 111 was still running - that would have left ACTIVE=1 and killed a productive run. READ GET /api/runs AND COUNT; do not reason from whichever task you were watching. Also confirm no deploy lane, promotion or QA gate is mid-flight, and check the launcher log mtime - it polls every 15 MINUTES AND WILL REFILL THE SLOT UNDER YOU (it did exactly that at 18:33:04, taking ACTIVE from 1 to 3). *** STEP 1 MERGE PR #140: head 44ab80c, 4 files +779/-0, litellm-config.yaml is NOT in the diff (0079e7d reverted by 5938a1e) - re-confirm that before merging, because if the YAML returned, merging force-recreates the prod router. Reviewed AND re-reviewed by claude[bot], verdict 'item 3 is fixed, all four compounding defects are actually resolved'. Author-side: py_compile x3 OK, YAML parses, guard exits clean 0 tracebacks, fixtures 6/6 on the final tree. The merge FIRES THE DEPLOY LANE - `Build local images` and `docker compose up -d --remove-orphans` both run UNCONDITIONALLY against CT202 and the workflow sleeps 40s 'waiting for LiteLLM startup'. The config-application steps skip on LITELLM_CONFIG_CHANGED, but that is NOT the same claim as 'the router will not restart' - do not conflate them. A GREEN ROLLUP CAN CONTAIN A HARD-FAILING TEST: read the jobs, not the summary. *** STEP 2 pg_repack (task 141 track D): builder prune ALREADY DONE (10.43 GB, 72%->59%) so that pre-check is satisfied; the 219,367-row backlog was ALREADY exported, checksum-verified at the destination and deleted, and the file did NOT shrink (total_relation 26 GB, toast 25 GB) because DELETE returns nothing to the filesystem - that dead space is exactly what this reclaims. Free space was 34G vs a 26G table: TIGHT, so state the measured margin and REFUSE if insufficient. pg_repack -t '\\\"LiteLLM_SpendLogs\\\"' --no-kill-backend --wait-timeout=60, quoted and CASE-SENSITIVE, run in the BACKGROUND with a log and polled (ssh MCP has a 180 s cap; pkill -f self-kills the call). VACUUM FULL is NOT an alternative - ACCESS EXCLUSIVE on a table the gateway writes to every request. Rollback: DROP EXTENSION pg_repack CASCADE. *** ORDER: MERGE FIRST, THEN REPACK - the merge restarts CT202 containers, and doing that DURING a repack interrupts it mid-swap, the one moment it is unsafe to interrupt. If the window closes before step 2, STOP AFTER STEP 1: a partial merge is fine, a partial repack is not. *** Advance 136 to MERGED/LANE/LIVE/VERIFIED only on live probes, and 141 track D only on the MEASURED table-size drop - not on pg_repack exiting 0."
}