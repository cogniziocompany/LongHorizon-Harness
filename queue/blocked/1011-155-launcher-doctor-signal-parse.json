{
  "name": "1011-155-launcher-doctor-signal-parse",
  "task_file": "C:/tmp/launcher-doctor-signal-parse-task.txt",
  "workspace": "LongHorizon-Harness",
  "max_rounds": 1,
  "trio": "kimi",
  "note": "WORKSPACE NOTE (was a sentence, 2026-09-14): n/a - edits C:/tmp/launch_queue.py, which is not in any repo\nOVERSEER-EXECUTED, in blocked/: it edits C:/tmp/launch_queue.py, the LIVE launcher. A run must never edit the thing that launches it, and the file is in no repo. Paxton 2026-09-10, plan from session [4aa9cb]. *** THIS TASK EXISTS BECAUSE TASK 146 FORBIDS IT: 146 line 76 says 'Do NOT re-enable any launcher gate ... the launcher is not in this repo', so the launcher half of that plan had no owner. *** GATED ON 146: it parses the new machine-readable detail line 146 emits (live=... cause=... req_5h=.../2500 credit=$.../$120). There is NOTHING TO PARSE until 146 lands - if it has not, STOP; do not write a parser against an unseen format and do not fall back to the old '0 tokens remaining' string, which 146 deletes. FOUR CHANGES: (1) parse live=/cause= in synthetic_quota_exhausted() ~L497 - IT STAYS ADVISORY and must NOT gate launches (Paxton 18:55: do NOT flip to fail-closed). (2) replace the doctor_quota= status field with doctor=<live>/<cause> req_5h=... credit=... on the SAME line. (3) *** THE REAL BUG: tighten synthetic_failing_real_work() ~L553 to count ONLY runs with abort_reason == provider_rate_limit. Today ANY failed run mentioning :synthetic counts, so a timeout or bad path can trip a 2-in-30min threshold that is supposed to mean 429s. *** (4) on cause=credit, skip Synthetic trios for the whole cycle instead of re-probing every 900s - credit exhaustion is multi-day and re-probing burns the exhausted meter. EDIT DISCIPLINE: THREE other tasks touch this file (147 DRAIN applied, 148 push_trios patch-only, 150 spec gate). Confirm SPEC_PENDING and DRAIN are still present BEFORE editing (both were at 21:45: 2 and 4 hits); if either is gone, an edit was lost - reconcile against .bak-* first. CRLF only (CR CR LF bug made twice today; ast.parse does NOT catch it). A file edit is INERT until restart - proven at 19:09. Restart with the DRAIN-proof method (arm, restart, confirm the DRAIN line, CLEAR the flag, exactly one process) because neither change emits output otherwise. Check how many entries carry spec fields first - 0 of 21 at 20:30; if that has changed, a restart GATES them and the queue stops."
}
