{
  "name": "1007-150-spec-staging-land",
  "task_file": "C:/tmp/spec-staging-land-task.txt",
  "workspace": "LongHorizon-Harness",
  "max_rounds": 1,
  "trio": "kimi",
  "note": "*** PAXTON DECIDED 2026-09-21 (OPEN-ASKS 150-spec-staging-decisions, both recommendations accepted): SS8 = option 1 MIRROR (file-queue mirrors into QueueStore; option 3 stays ruled out). SS9 = read each vendored copy's OWN semantics. The architecture questions are closed. STILL BLOCKED for the original reason only: overseer-executed, deploy restarts CT110 so it needs a counted zero-active window - and the work exists ONLY in Paxton's local tree on PTAIT09 (feat/spec-staging, never pushed, 22 uncommitted files as of 2026-09-21). *** || *** BLOCKER RESTATED 2026-09-14 21:08 PT: the 'CT110 has no gh' reason below is SUPERSEDED (runs hold GH_TOKEN now). THE REAL BLOCKER: its deploy RESTARTS the CT110 service and kills every in-flight run, so it needs a counted zero-active window the overseer makes. Stays overseer-executed for that reason alone. ***\nOVERSEER-EXECUTED, NOT FOR A HARNESS RUN - in blocked/ deliberately: it needs git push + a PR (CT110 has NO gh and NO GitHub token) and its deploy RESTARTS the CT110 service, killing every in-flight run. A run must never deploy the harness it is running inside. Paxton asked 20:30 PT whether the spec-staging work was 'tasked or queued'. IT WAS NEITHER - this task exists so it is not lost. *** THE WORK IS ALREADY BUILT. DO NOT REBUILD IT. *** MEASURED 20:32 PT: branch feat/spec-staging is 18 commits ahead of main with ZERO remote refs (never pushed), plus 17 files still UNCOMMITTED on top (+848/-24) and 5 untracked paths (templates/, spec_stats.py, two test files, the handoff doc). Open LHH PRs are #10/#8/#7 - none is this. State is BRANCHED at best, partly not even committed. OVERLAP WITH TASK 94 (done/): 94 owned the role_history_chars double-spend and deferred the VALUE change to a separate evidenced PR. Verified: main role_prompts.py:58,60 still spend max_history_chars TWICE; the branch fixes it at 72,74 via auditor_share/feedback_share. So this is 94's follow-through, not a duplicate - reference 94 in the PR or a reviewer reads it as unowned scope creep. THREE TASKS NOW EDIT C:/tmp/launch_queue.py: 147 (DRAIN flag, APPLIED 20:30), 148 (push_trios, patch only), and this one (the spec gate). Check the live file before editing; reconcile against the .bak-* backups if edits collided. A file edit is INERT until restart - the building session did not restart it. TWO OPEN DECISIONS ARE PAXTON'S: SS8 file-queue->QueueStore bridge (their rec: option 1 Mirror; option 3 porting probe/rotation/ramp into launcher.py is HIGH RISK and must not be chosen unilaterally) and SS9 the same history fix in three vendored eval harnesses (whether 0 means unlimited or none DIFFERS PER COPY and must be read). SS6 and SS9-opt-1 are ONE story across four files. Reported 585 passed / 2 failed / 1 skipped, failures claimed pre-existing on a clean tree - VERIFY that claim before merging. UI unverified: CT110 returns 405 for the new routes until deployed. Do NOT post @claude (bills Paxton). Deploy only in the zero-active window shared with 144/147."
}