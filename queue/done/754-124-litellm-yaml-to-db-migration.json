{
  "name": "754-124-litellm-yaml-to-db-migration",
  "task_file": "C:/tmp/litellm-yaml-to-db-migration-task.txt",
  "workspace": "cognizioware-mcp-tools",
  "max_rounds": 8,
  "trio": "kimi",
  "note": "THE UPGRADE PAXTON IDENTIFIED 2026-09-10 06:50 PT: 'Once they identify that we have to start putting everything in the database because we're trying to move away from the Yamel that's part of the upgrade right?' - yes. PLAN AND TOOLING ONLY, NO LIVE MIGRATION, and DO NOT edit litellm-config.yaml (editing it is the problem being solved). Evidence the hybrid is costing us, all measured: (1) POST /model/update on a config-sourced alias is REFUSED - \"Can't edit model. Model in config. Store model in db via /model/new to edit\" - measured 06:4x on qwen3.8-nothink; ornith-1.5:pool has a UUID id and IS editable. (2) every YAML change force-recreates the CT202 PROD router and kills in-flight runs, so it needs a zero-active-runs window - and measured tonight that window never arrives on its own because the launcher relaunches to keep its cap. (3) the DB already WINS over the file for five sections (general_settings, guardrails, litellm_settings, mcp_servers, router_settings) so the file is already not the truth; live drift right now is router_settings.allowed_fails file=1 db=50. (4) NINE merged alias edits were DEAD on the live router on 2026-09-09 - reviewed, merged, changed nothing, because the DB row won. That is the worst property of the hybrid: work that looks shipped and is not. SLICES: (1) inventory script classifying every deployment DB (UUID id) vs CONFIG (64-hex id) plus a file-vs-live diff - nobody currently knows the real split, it was 13 CONFIG vs 1 DB among qwen aliases alone; (2) idempotent migration tool using /model/new that verifies by reading /model/info back field-by-field and REFUSES if a DB deployment with that model_name already exists, or the alias silently load-balances across a stale config copy and a new DB copy; (3) a written migration ORDER by blast radius, naming which aliases are load-bearing for harness runs (see the trio table in C:/tmp/launch_queue.py); (4) THE PART NOT TO GET WRONG - the answer is NOT 'delete the YAML': it must stay a viable SEED for rebuild-from-scratch, AND an export-to-git path must be proposed so config changes stay reviewable after they stop being file edits, otherwise we trade one real problem for another; (5) extend the EXISTING config_drift check in ops_doctor.py to surface the DB-vs-CONFIG split - do not add a view or route, the doctor renders on /overview. A 200 is never proof - POST /config/update returned 200 twice and silently discarded the key. Never put LITELLM_MASTER_KEY in a workflow or a file; env var names only. Do not push, do not merge. *** SLICE 6 ADDED 08:1x PT, PAXTON: 'add doc so we don't mess with ymal it's deprecated unless required'. TWO placements, because a doc nobody opens changes nothing: (A) a BANNER AT THE TOP OF infrastructure/litellm-config.yaml itself - deprecated as the way to change models, use /model/new |/model/update|/model/delete then READ BACK via /model/info and diff; WHY: editing the file force-recreates the CT202 prod router and interrupts every in-flight run, whereas a DB change is live with no restart (proven 2026-09-10: /model/new took deployments 213->214 and /model/delete took 2->1, both read back, router never restarted); AND the file may not even win - five sections are DB-shadowed and the DB row WINS, which is why nine merged alias edits were DEAD on 2026-09-09. State explicitly WHEN the file is still legitimate (bootstrap/seed, rebuild after DB loss, entries with no DB equivalent) or people will either ignore the banner or fear touching anything. Also note that container-level settings - OLLAMA_NUM_PARALLEL, the pinned LiteLLM image digest, env vars - are NOT in this file at all; they live in docker-compose.yml and still need a container recreate, so nobody wastes time looking for them here. (B) docs/litellm-config-is-deprecated.md with the same policy at length, the DB-vs-CONFIG migration status from Slice 1's inventory, and the runbook for changing a model the DB way including read-back-and-diff. The banner is itself a comment-only edit that force-recreates the router - that is NOT a reason to skip it, it is a reason to land it in the SAME config change as the migration. Do not open a separate PR for the banner. *** PAXTON 08:2x PT CHOSE OPTION (B), COMBINED: tasks 123 and 124 LAND TOGETHER, one recreate, the last one. PHASE 1 live/no-recreate: /model/new the keepers (proven live tonight, 213->214 and 2->1, no restart) - EXPECT a transient double-serve where a config entry and a new DB entry share a model_name and the router load-balances across BOTH; harmless where both point at the same backend but for qwen3.8 and qwen3.8-nothink the config copy is the span and the DB copy is ornith, so traffic splits ~50/50 until Phase 2 - keep it short and expect it. PHASE 2 the ONE recreate: a single litellm-config.yaml change removing the migrated entries (the API refuses to delete config models, the file is the only way), purging qwen3.8--128k/--256k, removing the :11442 span deployment, cleaning context_window_fallbacks of the purged aliases (if those outlive the aliases a qwen3.8 context overflow falls back to a model that no longer exists), and adding the Slice 6 deprecation banner. PHASE 3 live again: span now unreferenced -> unload -> returns 4.3 GB private host RAM (measured RssAnon, not shared cache) + 18.6 GB VRAM, giving ~8.1 GB against the ~7.0 GB a second ornith needs - fits with ~1 GB margin, which is exactly why the span must go first; then load ornith on :11438 (CUDA 0) and /model/new its deployment so ornith-1.5:pool spans both GPUs, one instance per card, both carrying supports_vision AND supports_function_calling. VERIFY by driving real traffic to both api_bases, not by a 200 on the alias - a pool of one passes that. || *** STATE PR 2026-09-16 20:55 PT, SCHEDULED TICK #358: run 20260917T033356Z_dfffdc55 pushed and opened cognizioware-mcp-tools #159 ITSELF (branch task-124-litellm-model-inventory, base main, MERGEABLE, +646/-0, 2 files, slice 1 = model-list inventory / DB-vs-CONFIG split + live-vs-file diff). Unreviewed, NOT merged, no @claude. The run is still live at round 2 auditor - do not requeue. *** | TICK #370 2026-09-16 22:17 PT: GATE RESOLVED -> stop (appr 38cb4bd1bfcf, read back resolved/stop; run cancelled exit 0, r7/8). WORK DONE: mcp-tools PR #159 OPEN/MERGEABLE, base main, head 4503ddeb258851ec09339aa4d807362d03ca605e, 11 files +2106/-35. OVERSEER VERIFIED INDEPENDENTLY: litellm-config.yaml diff (+43/-35) is COMMENT-ONLY - zero non-comment changed lines. STATE = PR (unreviewed, no @claude, NOT merged). DO-NOT-REQUEUE. Remaining is overseer's: review+merge #159, then the combined 123/124 single-recreate in a zero-active-runs window.",
  "task_source": "task_file",
  "run_id": "20260917T033356Z_dfffdc55",
  "roles_bound": {
    "manager": "kimi-k3:synthetic-anthropic",
    "executor": "nemotron-3-super:synthetic-anthropic",
    "auditor": "syn:large:vision:synthetic-anthropic"
  },
  "launched_at": "2026-09-17T03:33:56.491171+00:00"
}