{
  "name": "reviewer-hydra",
  "task_file": "C:/tmp/reviewer-hydra-task.txt",
  "workspace": "cognizioware-hydra",
  "max_rounds": 4,
  "trio": "kimi",
  "note": "DISPOSITIONED 2026-09-15 17:52 PT, SCHEDULED TICK #180 [a11d57] - NOT A STRANDING, THE WORK LANDED. Run 20260911T024108Z_69d1e6b9 failed in round 3 on abort_reason=provider_rate_limit, but the thing this task existed to verify is now TRUE: hydra PR #26 (ci/add-claude-reviewer) MERGED 2026-09-11T06:21:15Z and .github/workflows/claude-code.yml IS PRESENT on hydra main (sha 71abcfef5cbc), read from the GitHub API this tick - it is no longer the 404 the task was written against. *** CONSEQUENCE THAT MATTERS MORE THAN THE TASK: an @claude comment on a hydra PR is NO LONGER INERT. *** It would now fire the reviewer on Paxton's personal subscription. CORRECTED WITHIN THE SAME TICK: Paxton ALREADY ruled on this - OPEN-ASKS.md row `hydra-26` (Answered, 20:25 PT) says \"Billing @claude to Paxton's subscription on hydra is ACCEPTED\". So hydra is the one repo where the spend is AUTHORISED. It is still not free and still not a tick's call to make unilaterally - route any invocation through task 117's triage. Do NOT requeue. || PRIOR NOTE: VERY LOW PRIORITY - deliberately last in the 99x band, behind the 14 active entries; must NOT jump task 126 or 107, and the local span is single-slot so this starts only as capacity frees. Full brief on disk: cognizioware-hydra/tasks/handoff-reviewer-hydra-2026-09-10.md - read it before acting, do not re-derive its facts. THIS IS VERIFICATION, NOT NEW CODE. Measured live against the GitHub API 2026-09-10, not inherited from notes: claude-code.yml is 404 on hydra main, and PR #26 (ci/add-claude-reviewer, one file, +65/-0) is ALREADY OPEN and MERGEABLE with the fix. HARD RULE 1 - DO NOT OPEN A SECOND PR; verify #26, do not replace it. HARD RULE 2 - DO NOT MERGE; hydra-ci.yml triggers on ANY push to main with NO path filter, so a workflow-only merge runs the full deploy against cognizio03-corsairai300, the same host CT110 runs on - Paxton decides merge timing in a quiet window. Open/verify/report/stop. HARD RULE 3 - COPY THE REFERENCE FILE (cognizioware-mcp-tools/.github/workflows/claude-code.yml, 2,491 B) BYTE-FOR-BYTE; never retype the write-access gate from prose, because author_association == 'OWNER' || 'MEMBER' || 'COLLABORATOR' is ALWAYS TRUE in Actions expression syntax and silently disables the gate. #26 already carries the three corrections the other repos learned the hard way: claude_code_oauth_token not anthropic_api_key (the API key is org-scoped, not workspace-scoped; the action sends no workspace header, so every request is rejected before inference - is_error at 271 ms, nothing billed), fetch-depth: 0 (a depth-1 checkout made the reviewer report a REAL commit as nonexistent and accuse correct work of being fabricated on 2026-09-09), and a JOB-LEVEL timeout-minutes: 20 (the action's own timeout_minutes INPUT is rejected as unknown, so the cap it appeared to set never existed; with cancel-in-progress: false a hang would block every later @claude on that PR for six hours). CREDENTIAL IS ALREADY SOLVED - orgs/cogniziocompany/actions/secrets/CLAUDE_CODE_OAUTH_TOKEN, visibility=all, already visible to this repo: no per-repo secret, no token minting, no org change, and adding the file costs nothing. Each @claude invocation spends a PERSONAL SUBSCRIPTION QUOTA, not a metered key - that is why nobody chose review-on-open, so also recommend whether hydra should carry the event-driven no-@claude pattern (prior art: mcp-cognizioware/.github/workflows/pr-failure-investigator.yml, on workflow_run completed + conclusion == 'failure', which reviews only failures). FALSE GREEN - on 2026-09-09 a review run reported completed/success with every step green and posted NO review: the credential resolved empty and the action's internal steps were outcome=skipped/conclusion=skipped, so a green 'PR Review' run is NOT evidence anything was reviewed. The precondition guard that exits non-zero on an absent credential is being PROVEN ON cognizioware-qa FIRST; hydra INHERITS the proven version - do not design a competing one, just say where in #26's file it goes. Whatever lands must also be back-ported to the two already-correct repos (mcp-tools, LongHorizon-Harness). CORRECTION TO THE RECORD: the stated hold reason for #26 - the 2026-09-08 CT110 .env wipe - is STALE; that wipe is now guarded ([ -f /opt/.../.env ], bootstrap only when absent, documented inline in the deploy job). It is no longer a reason to hold. Re-check corsairai300 disk before anyone merges (10% used / 99 G free on 2026-09-10, but the box has hit 0 bytes before on the Chromium terminal-host rebuild). VERIFICATION THAT COUNTS, only after a human has merged #26: comment @claude on a live PR - PR #25 (feat/task-92-session-bridge) is green and completely unreviewed, the natural target - from an OWNER/MEMBER/COLLABORATOR account, then confirm a REVIEW COMMENT WAS POSTED BACK, not merely that the run went green; record repo, PR number, run id, and what the reviewer said. If #26 is still open when this runs, SKIP that step and say so - do not merge to unblock yourself. Regression check: a run with no credential must FAIL, not skip-and-pass. Authored by session cognizioware-hydra-e9 [ea6bb0]. NOTE: this is hydra's slice only - the qa, powerplatform and mcp-cognizioware briefs and their queue entries (992/993/994) were NOT written in that session and do not exist yet; mcp-cognizioware is DRIFTED not covered (1,521 B, fetch-depth: 1 at line 46 and timeout_minutes: 15 at line 52 still in it) and powerplatform is NOT a safe one-file add (LAN_DEPLOY_ENABLED=1 confirmed live, so merging any PR to its main runs scripts/deploy/rollout.sh on the [self-hosted, lan-deploy] runner against LAN prod). *** OVERSEER 2026-09-10 12:24 PT: THIS ENTRY WAS BROKEN WHEN WRITTEN. Its task_file pointed at cognizioware-hydra/tasks/reviewer-hydra.task.txt, which does NOT exist on disk - session [ea6bb0] committed it to branch docs/reviewer-handoff-hydra (3aed398) and then switched the working tree back, so the brief lived only inside an unchecked-out commit and this entry would have launched a run with NO TASK TEXT. Recovered via `git show 3aed398:...` and repointed at C:/tmp/reviewer-hydra-task.txt (4,404 bytes), the house convention every other entry uses. The handoff doc is at C:/tmp/reviewer-hydra-handoff.md (7,976 bytes). FACT CORRECTIONS measured today: it is THREE repos missing the reviewer, not four - LongHorizon-Harness got it at 11:12 PT (PR #15, 7b27032, read back at 2,491 bytes on main, and it FIRED for real: run 34512622903, completed/success in 2m45s, posting a review that found a genuine security defect). Missing now: hydra, powerplatform, qa. The false-green risk is therefore specifically the EMPTY-CREDENTIAL case, not the reference file being wrong. DISPUTED CLAIM - DO NOT ASSERT IT, VERIFY IT: the brief says the 2026-09-08 CT110 .env-wipe hold reason is 'stale' because the wipe is now guarded. The overseer read hydra-ci.yml line 143 today and it still records '2026-09-08: two deploys wiped the CT110 node seeding'. Both can be true (a guard added after the incident), but CT110 runs EVERY harness job, so treat the hold as LIVE until you have read the guard yourself and quoted it. Do not merge PR #26 on the strength of that claim. *** HOW TO GET A CREDENTIAL-LESS RUN (Paxton, 2026-09-10 12:35 PT) - THIS IS NOT OBVIOUS. The org secret CLAUDE_CODE_OAUTH_TOKEN has visibility: all, so EVERY run in EVERY repo now resolves it. YOU CANNOT PROVE THE FAILURE CASE JUST BY RUNNING THE WORKFLOW. Pick one and SAY WHICH YOU USED: (a) PREFERRED, touches nothing shared - in the same PR have the guard read its input from an intermediate env var, and add a temporary matrix/second job (or a workflow_dispatch input) that runs ONLY the guard step with that var forced empty; proves the step's own logic and deletes cleanly before merge. (b) FORK PR - secrets are not passed to workflows triggered from a fork, so a fork PR gives a genuinely empty credential; costs nothing but needs a fork. (c) DO NOT delete, unset or re-scope the org secret to test this - it is shared by six repos and visibility: all was set 2026-09-10T02:04:01Z; breaking it to test a guard would take the reviewer DOWN FLEET-WIDE. ***",
  "run_id": "20260911T024108Z_69d1e6b9",
  "roles_bound": {
    "manager": "nemotron-3-super:synthetic-anthropic",
    "executor": "kimi-k3:synthetic-anthropic",
    "auditor": "syn:large:vision:synthetic-anthropic"
  },
  "launched_at": "2026-09-11T02:41:08.656230+00:00"
}