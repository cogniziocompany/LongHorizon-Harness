{
  "name": "9999z8-194-runs-list-degradation",
  "task_file": "C:/tmp/runs-list-degradation-task.txt",
  "workspace": "/home/harness/work/LongHorizon-Harness",
  "max_rounds": 6,
  "trio": "kimi",
  "task_source": "task_file",
  "note": "Queued 2026-09-17 06:31 PT by SCHEDULED TICK #420, appended to the END of the active band (band position only; priority is Paxton's). CAUSE, measured this tick, not inferred: GET /api/runs TIMED OUT at 60 s (HTTP 000, 0 bytes, socket open and silent) and then returned 200 in 168.371 s / 4,868,247 bytes for 461 runs. Same tick, same host, same token: /api/runs/<id>/status 200 in 0.31 s and /api/runs/<id>/snapshot 200 in ~1 s, so the SERVICE IS HEALTHY and ONLY THE LIST ROUTE IS SLOW - do not chase a dead-API theory. The list embeds each run's full task text (~10.5 KB/run) and runs are append-only: 4,846,147 b at tick #411 -> 4,862,695 b at #418 -> 4,868,247 b at #420. THIS IS THE GATE-DETECTION PATH (doctrine: gate detection reads GET /api/runs, never a scratch file or a directory listing), so a tick that cannot afford the call cannot see a gate. CONTRACT THAT MUST NOT CHANGE: the body stays {\"runs\":[...]} and each row keys the id as `id`, NOT `run_id` - a run_id reader prints Nones and reads exactly like a corrupt API. DO NOT prune or archive runs (retention is not this task's call) and DO NOT change stranding semantics. MERGE HAZARD: landing this restarts lh-harness.service on CT110 and kills every in-flight run, so it needs a zero-active window the OVERSEER picks - the run opens the PR and STOPS. No .github/workflows/ pushes: CT110's GH_TOKEN refuses them by permission. No @claude invocations. || TICK #421 (2026-09-17 06:36 PT) - NOT REPRODUCED: the SAME call, GET /api/runs, returned 200 in 2.5 s for 461 runs (same host, same token, same reader) only ~7 minutes after #420 measured 168.371 s. So the degradation is INTERMITTENT, not a steady state, and #420's row should be read as one sample, not a characterisation. THIS CHANGES THE TASK: do not 'fix a slow route' that is fast most of the time - first INSTRUMENT it (log per-request duration + concurrent-run count + response bytes on the list route) and establish what correlates with the 168 s tail. Payload growth alone cannot explain a 67x swing at a constant 4.87 MB. Prime suspect to test FIRST: contention with in-flight runs writing run state while the list route serialises every run - #420 and #421 both had 3 runs live, so round/role phase may matter more than count. Keep the payload-slimming idea as a SECOND slice, not the first. | TICK #499 2026-09-17 17:10 PT: STATE=PR. Run 20260917T214346Z_5334959a pushed feat/task-194-cheap-runs and opened LHH PR #24 ITSELF (base main, MERGEABLE, +379/-2, 3 files: webapi/server.py, supervisor/service.py, tests/webapi/test_runs_cheap_listing.py). NOT MERGED and not to be merged on sight: (a) unreviewed - report, never buy a review; (b) the LHH lane is PyPI+CT110 deploy which RESTARTS lh-harness.service and kills every in-flight run, and 3 runs were live. Merge it in the same zero-run window task 170 is owed, and run the build/tests first. || TICK #501, 2026-09-17 17:33 PT - RUN TERMINATED, TASK STAYS AT **PR**. The run's end-of-round approval f8fdd97e1bac (outcome=completed, round 6/6) was resolved with action=stop: ACCEPT COMPLETION, no extra rounds. Verified before accepting rather than on the run's word: PR #24 OPEN, base main, MERGEABLE, +379/-2 / 3 files, head fccbba6cfdb33bb80d0f49f578d51ac7a4a444d8 = exactly the sha final_response names. Run 5334959a now reads status=cancelled (the terminal status a stop-resolve produces here) with the approval resolved - that is an ACCEPTED COMPLETION, NOT a failure and NOT a stranding; do not requeue it on the status alone. PREMISE CORRECTED BY THE RUN, recorded so no future tick re-derives it: the 168 s figure above DID NOT REPRODUCE. Measured baseline this run was ~2.83 s for 5,031,369 bytes / 479 rows, and the cost was NOT JSON serialization (~9 ms) but filesystem churn in the handler - ~15,000+ stat/resolve/relative_to boundary checks plus per-row owner.json/report.json re-reads. The fix is opt-in and the default contract is preserved: GET /api/runs?fields=summary = ~0.031 s / 89,885 b / 480 rows, and ?fields=summary&status=running,waiting_approval,starting = ~0.032 s / 572 b / 3 rows - the call a gate tick should make ONCE THIS MERGES. Until then keep using the plain list. STILL NOT MERGED, and the reason is unchanged since #499: unreviewed (reported, never bought - no @claude), AND the LHH lane restarts lh-harness.service, which kills every in-flight run - 2 were live at this tick (ca0cb519 r2/8, 3cbedb2f r6/14). Queued behind task 170's owed zero-active-runs window, BUILD-FIRST before merging.",
  "run_id": "20260917T214346Z_5334959a",
  "roles_bound": {
    "manager": "nemotron-3-super:synthetic-anthropic",
    "executor": "kimi-k3:synthetic-anthropic",
    "auditor": "syn:large:vision:synthetic-anthropic"
  },
  "launched_at": "2026-09-17T21:43:46.870816+00:00"
}