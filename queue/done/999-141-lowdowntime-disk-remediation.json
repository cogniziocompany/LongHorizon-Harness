{
  "name": "999-141-lowdowntime-disk-remediation",
  "task_file": "C:/tmp/lowdowntime-disk-remediation-task.txt",
  "workspace": "cognizioware-mcp-tools",
  "max_rounds": 8,
  "trio": "kimi",
  "note": "*** NOTE 2026-09-14 (overseer, audit [fe2679] K): Track C 'CT202 SpendLogs reclaim' is meant to be satisfied by app-side RETENTION, not by this task - BUT retention is NOT LIVE on the prod router (measured 2026-09-11: retention period unset, store_prompts_in_spend_logs=True, DB-shadowed). Task 170 owns making it live. Do not duplicate 170 here; treat Track C as 'verify 170 landed', not as work. ***\nPaxton 2026-09-10, source session [092659]. FOUR TRACKS in order A -> C -> D -> B: ptait07 OS disk, CT100 thin volume, CT202 SpendLogs reclaim, and moving Docker onto the idle thin pool. *** SCOPE SPLIT - THIS IS ALMOST ENTIRELY HOST-LEVEL MUTATION ON PRODUCTION PROXMOX HOSTS AND IS NOT YOURS TO EXECUTE. A harness run is never given credentials, and the standing rule is absolute: NO host-level change on any PVE host without Paxton's EXPLICIT GO naming the change, the rollback and a quiet window. YOU PREPARE AND PROVE; the overseer and Paxton execute. Deliver four idempotent scripts plus a runbook, each destructive step GATED on a verification that must pass in the SAME invocation. Do NOT ssh into a PVE host and mutate it, do NOT delete archives, do NOT create/remove LVs, do NOT run pg_repack, do NOT restart docker/containerd/any container. *** MEASURED 2026-09-11 00:23 UTC, do not re-derive: ptait07 root 66G/94G = 74%; /var/lib/vz/dump holds 18G (CT201 3,904,268,284 b + CT204 14,688,865,867 b); thin pool `data` 794.30g at Data% 14.14 so ~682G IDLE; vm-100-disk-0 Data% 99.87 (the trim problem) while CT100 internally is ~23%. *** CORRECTION TO THE SOURCE PLAN: it says the CT204 archive 'is still copying'. IT IS NOT - there is no rsync process, and BOTH archives are already on terranas01 at /Volume2/usbdrive/ptait07-vzdump/ with BYTE-EXACT matching sizes, and vzdump.sha256 (438 b) is present AT THE DESTINATION. So track A is gated purely on running `sha256sum -c vzdump.sha256` ON TERRANAS01 - equal byte sizes are necessary but NOT sufficient, and a successful rsync exit code is not proof the bytes are readable at the far end. ONLY IF EVERY LINE READS OK may the originals be deleted. *** ALSO RECONCILE BEFORE TRUSTING TRACK B's ARITHMETIC: the plan says CT disks total 234G; lvs shows FOUR vm disks totalling 154G (40+10+24+80). An overcommitted thin pool that fills is worse than the problem being fixed. *** TRACK D - ALREADY PARTLY DONE TODAY, do not repeat blindly: `docker builder prune -f` on CT202 already freed 10.43 GB and took the disk 72% -> 59%, so the plan's pre-check is SATISFIED; and the entire pre-cutoff SpendLogs backlog (219,367 rows, ~5.5 GB) was already exported to terranas01, checksum-verified at the destination and deleted - and the file did NOT shrink (total_relation stayed 26 GB, toast 25 GB) because a DELETE returns nothing to the filesystem. THAT dead space is exactly what pg_repack reclaims. Autovacuum was verified keeping up (n_dead_tup 122,525 -> 0). Free space was 34G against a 26G table - TIGHT, so state the margin measured and refuse if insufficient. pg_repack must run in the BACKGROUND with a log and be polled (the ssh MCP has a 180 s hard cap and pkill -f self-kills the call); table name is quoted and CASE-SENSITIVE. *** NO OVERLAP WITH TASK 125: 003-125 owns the RECURRING 2-hourly export and explicitly defers this one-time reclaim to a separate attended window. Do not re-implement 125, and never let its schedule call pg_repack or VACUUM FULL. *** OUT OF SCOPE: ptait01 ZFS snapshots (a ptait01 host change); and shrinking the thin pool to grow root (LVM cannot shrink a thin pool - REJECTED for downtime, do not revisit). HARD RULES: never `docker compose down -v`; mcp-tools compose ALWAYS takes --env-file mcp-tools.env; never the bare gateway /health (use /health/liveliness); do NOT edit litellm-config.yaml (force-recreates the CT202 prod router); no secrets anywhere, env var NAMES only; never git add -A; do NOT merge - open the PR and stop. || TICK #421 (2026-09-17 06:36 PT): TASK 141 RUNNING -> PR. The run opened its OWN PR (GH_TOKEN as designed): cognizioware-mcp-tools #163 'TASK-141: disk-reclaim tooling - 4 per-track scripts + runbook (A/C/D/B)', head task-141-disk-reclaim-tooling 2653466, created 2026-09-17T13:31:21Z, 9 files +1255/-34, OPEN/MERGEABLE, reviewDecision EMPTY = UNREVIEWED (reported, NOT bought - no @claude). Read from gh pr view, not from the run's claim. Touches NO infrastructure/litellm-config.yaml and NO .github/workflows/, so it needs neither a quiet window nor a workflow-scoped PAT. Run 20260917T125750Z_0150c87a is STILL ALIVE at active_round 1/8 (auditor), so the PR head may advance; re-read the head sha before any merge. NOT MERGED - merging is the overseer's and this is unreviewed. | TICK 428 2026-09-17 07:29 PT: run 0150c87a gated at its round ceiling with the work DONE; resolved STOP (approval f4524220b5f0, read back resolved/stop, run cancelled, alive=false). STATE = PR, not MERGED. Deliverable is mcp-tools PR #163, base main, head 5da3a82, OPEN/MERGEABLE, reviewDecision EMPTY (unreviewed - reported, not bought). 5 files: tracks A/B/C/D scripts + runbook; no workflows, no infrastructure/litellm-config.yaml, so NO quiet window needed to merge. NOTHING WAS EXECUTED ON ANY HOST - this run only prepared and proved the tooling. Track B (ptait07 docker thin-pool move, ~5 min ollama downtime) needs Paxton's explicit go; A and C are reversible; D needs a zero-active-run window but keeps the router up. Order A->C->D->B.",
  "task_source": "task_file",
  "run_id": "20260917T125750Z_0150c87a",
  "roles_bound": {
    "manager": "syn:large:vision:synthetic-anthropic",
    "executor": "nemotron-3-super:synthetic-anthropic",
    "auditor": "kimi-k3:synthetic-anthropic"
  },
  "launched_at": "2026-09-17T12:57:50.974252+00:00"
}