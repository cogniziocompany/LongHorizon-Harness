{
  "name": "1005-148-fleet-trios-table",
  "task_file": "C:/tmp/fleet-trios-table-task.txt",
  "workspace": "/home/harness/work/cognizioware-mcp-tools",
  "max_rounds": 8,
  "trio": "kimi",
  "note": "*** SUPERSEDED 2026-09-14 12:40 PT by slices 148a (classifier), 148b (backend), 148e (launcher patch) in active, and 148c (UI) and 148d (re-pin + deploy, overseer) in blocked. This parent text stays as the spec the slices reference. ***\n*** BLOCKED 2026-09-14 12:15 PT - SCOPE MUST BE CUT BEFORE RELAUNCH. Run 20260914T162539Z_049291cc used all 8 rounds and reported 'Completed: None' for every component (fleet-admin backend, provider classifier, TriosPage UI, tests, push_trios() patch). Eight rounds with zero completed components is a LOOP, which per doctrine means the scope is too big: slice it (e.g. 148a backend + classifier, 148b TriosPage UI, 148c push_trios patch + the 2026-09-11 measured columns) and queue the slices, rather than granting rounds. ***\nPaxton 2026-09-10, spec from session [949d74]. Fleet Window v1: show the launcher's trio table (manager/executor/auditor per trio) on fleet.easybutt0n.ai. DECIDED: the launcher PUSHES; cost columns are OUT of v1 because per-role tokens cannot be measured on one shared LiteLLM key. *** HARD DEPENDENCY, MEASURED BY THE OVERSEER 20:20 PT: /api/fleet/nodes returns [] - fleet.hosts is EMPTY because the harness heartbeat has never succeeded (fleet_ever_succeeded=false, HTTP 413). The spec needs that host for a FOREIGN KEY, so POST /harness/trios CANNOT land until TASK 114 does. Build the backend+UI+tests (they need no live host), open the PR, and report the live steps BLOCKED ON 114 BY NAME. DO NOT insert a fake host row to satisfy the FK. *** 114's run 4fd07c9c FAILED at 19:25 and its entry sits in queue/done/ - the launcher files on LAUNCH not SUCCESS, so it will not retry; the overseer owns that requeue. SPEC LINE NUMBERS ARE STALE (launcher edited 19:12): _TRIOS=175, _SYN_DOWN=188, _SEQ=198, SYN_ONLY=214, DEGRADED_TRIOS=237, DEGRADED_CAP=286, check_pools() call=569, _SYN_DOWN set=574. THREE CAP= assignments exist (25 is a superseded CAP=4; 62 and 100 are CAP=6) - line 100 is live. Line 6 is BASE=, NOT a token; the auth header dict is H= at line 349. The overseer verified the trio tuples match the spec exactly. LAUNCHER EDIT IS NOT THE RUN'S - deliver push_trios() as a reviewable patch and STOP; task 147 also edits that file. fleet-admin is a PINNED COPY (SOURCE_REF.md -> ptait09-easybutt0n-ai, branch feat/harness-fleet-report, commit 804a4f07) - change AT SOURCE then re-pin. table.tsx confirmed absent. DEDUPED: 145 owns run-liveness metrics on this surface, 77b owns fleet registration e2e, 113 owns golden runs - this task owns the TRIO CONFIG table only. DO NOT post @claude (bills Paxton). Do not merge.",
  "task_source": "task_file",
  "roles_bound": {
    "manager": "nemotron-3-super:synthetic-anthropic",
    "executor": "kimi-k3:synthetic-anthropic",
    "auditor": "syn:large:vision:synthetic-anthropic"
  }
}
